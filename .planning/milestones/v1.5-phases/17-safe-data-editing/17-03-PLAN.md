---
phase: 17-safe-data-editing
plan: 03
type: execute
wave: 3
depends_on:
  - "17-01"
  - "17-02"
files_modified:
  - client/src/components/extensions/db-workbench/ResultGridPane.tsx
  - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  - client/src/components/extensions/db-workbench/GridEditCommitDialog.tsx
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
must_haves:
  truths:
    - "Edit mode appears only when active batch is explicitly eligible and connection is not readonly"
    - "Primary-key columns are non-editable and no-op edits are removed from pending patch set"
    - "Commit flow is two-step (prepare preview -> explicit confirm commit) with user-visible affected-row and SQL preview summary"
  artifacts:
    - path: "client/src/components/extensions/db-workbench/ResultGridPane.tsx"
      provides: "Editable-cell UI, pending patch indicators, and commit/discard controls"
      contains: "pendingEditCount"
    - path: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      provides: "Prepare/commit/discard orchestration and post-commit table refresh"
      contains: "handlePrepareGridCommit"
    - path: "client/src/components/extensions/db-workbench/GridEditCommitDialog.tsx"
      provides: "Preview confirmation dialog for SQL lines and affected-row summary"
      contains: "SQL Preview"
  key_links:
    - from: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      to: "client/src/extensions/host-api.ts"
      via: "Workbench calls capability-gated prepare/commit APIs through hostApi.connections"
      pattern: "prepareGridCommit"
    - from: "client/src/components/extensions/db-workbench/ResultGridPane.tsx"
      to: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      via: "Grid emits patch edits and commit/discard actions to workbench controller"
      pattern: "onPrepareCommit"
---

<objective>
Integrate safe row editing into the existing workbench result surface without breaking read-only browsing behavior.

Purpose: DATA-01/02/03 must be visible and trustworthy to operators in the UI, including explicit eligibility reasons and a confirm-before-commit mutation flow.
Output: Result grid editing with patch tracking, prepare-preview dialog, and commit/discard actions wired to backend commands.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-safe-data-editing/17-CONTEXT.md
@client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
@client/src/components/extensions/db-workbench/ResultGridPane.tsx
@client/src/components/extensions/db-workbench/DangerousSqlDialog.tsx
@client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
@client/src/extensions/host-api.ts

<interfaces>
UI flow constraints:
- editable only for safe single-table results
- count-only result sets remain read-only
- preview must come from backend `prepareGridCommit`, never from frontend SQL string assembly
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ResultGridPane with cell patch editing model and safety locks</name>
  <files>client/src/components/extensions/db-workbench/ResultGridPane.tsx</files>
  <read_first>
    - .planning/phases/17-safe-data-editing/17-CONTEXT.md (patch model and PK lock decisions)
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx (current read-only row rendering and inspector flow)
    - shared/schema.ts (DbQueryBatchResult and edit eligibility metadata added by Plan 17-01)
  </read_first>
  <action>
Add in-grid editing primitives that remain fail-closed by default.

1. Add props required for edit orchestration:
   - `editEligibility`
   - `primaryKeyColumns`
   - `pendingEditCount`
   - `onEditCell`
   - `onPrepareCommit`
   - `onDiscardEdits`
2. Render cells in editable mode only when:
   - `editEligibility.eligible === true`
   - active column is not in `primaryKeyColumns`
   - batch has rows and no error.
3. Keep PK columns visually locked with explicit style/tooltip (`Primary key column (read-only)`).
4. On cell value change:
   - emit `(rowPkTuple, columnName, oldValue, newValue)` payload through `onEditCell`.
   - auto-remove no-op edits where `newValue` equals original value.
5. Add action strip near status footer with buttons:
   - `Prepare commit` (disabled when `pendingEditCount === 0`)
   - `Discard edits`
   - pending counter label (`Pending edits: N`).
6. Preserve existing browsing behaviors (filter, copy, load more, row inspector, stop-on-error toggle).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx contains `editEligibility`
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx contains `primaryKeyColumns`
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx contains `pendingEditCount`
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx contains `onPrepareCommit`
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx contains `onDiscardEdits`
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx contains `Primary key column (read-only)`
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx contains `Pending edits:`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Grid supports local patch editing while enforcing PK lock and safe edit entry constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Wire prepare/commit/discard flow in WorkbenchLayout with strict eligibility messaging</name>
  <files>client/src/components/extensions/db-workbench/WorkbenchLayout.tsx</files>
  <read_first>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx (query execution lifecycle and active batch handling)
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx (starter-query source paths)
    - client/src/extensions/host-api.ts (prepare/commit API contracts)
  </read_first>
  <action>
Implement workbench-level state and handlers for safe edit operations.

1. Add state for patch and commit lifecycle:
   - `pendingEditCells` keyed by `rowPkTuple + columnName`
   - `preparedGridPlan` (planId/planHash/preview summary)
   - `isPreparingGridCommit` and `isCommittingGridEdit`
2. Derive active batch editability from backend metadata (`activeBatch.editEligibility`) and show blocking reason text when not eligible.
3. Implement handlers:
   - `handleEditCell` (upsert/remove no-op patch entries)
   - `handleDiscardGridEdits` (clear local patch state only, no backend mutation call)
   - `handlePrepareGridCommit` -> call `hostApi.connections.prepareGridCommit(...)`
   - `handleCommitGridEdits` -> call `hostApi.connections.commitGridEdits(...)`
4. On commit success:
   - clear pending patches and prepared plan.
   - re-run originating table query with `handleRunStarterQuery(selectedTableName, "select")`.
5. Keep count-query results read-only:
   - do not allow prepare/commit actions when source indicates `count` mode.
6. Pass new editing props into `<ResultGridPane ... />`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `pendingEditCells`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `handlePrepareGridCommit`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `handleCommitGridEdits`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `handleDiscardGridEdits`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `hostApi.connections.prepareGridCommit`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `hostApi.connections.commitGridEdits`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `handleRunStarterQuery(selectedTableName, \"select\")`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `Count rows` and `read-only`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Workbench controls safe edit state, preview preparation, commit/discard actions, and post-commit refresh behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add backend-preview dialog component and connect explicit confirm UX</name>
  <files>client/src/components/extensions/db-workbench/GridEditCommitDialog.tsx, client/src/components/extensions/db-workbench/WorkbenchLayout.tsx</files>
  <read_first>
    - client/src/components/extensions/db-workbench/DangerousSqlDialog.tsx (existing confirm/cancel interaction pattern)
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx (dialog state management conventions)
  </read_first>
  <action>
Create a dedicated preview dialog for grid-edit commit confirmation.

1. Create `GridEditCommitDialog.tsx` with props:
   - `open`
   - `affectedRows`
   - `changedColumnsSummary`
   - `sqlPreviewLines`
   - `previewTruncated`
   - `onConfirm`
   - `onCancel`
2. Render required sections:
   - title: `Apply pending row edits`
   - summary label: `Affected rows`
   - section header: `SQL Preview`
   - truncation note when preview is capped.
3. In `WorkbenchLayout.tsx`, open dialog only after successful prepare response and call `handleCommitGridEdits` only from dialog confirm action.
4. Keep destructive styling aligned with current workbench safety dialogs (bordered panel + explicit confirm button text).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/extensions/db-workbench/GridEditCommitDialog.tsx exists
    - client/src/components/extensions/db-workbench/GridEditCommitDialog.tsx contains `Apply pending row edits`
    - client/src/components/extensions/db-workbench/GridEditCommitDialog.tsx contains `Affected rows`
    - client/src/components/extensions/db-workbench/GridEditCommitDialog.tsx contains `SQL Preview`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `GridEditCommitDialog`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `onConfirm={handleCommitGridEdits}`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>UI displays backend-generated preview summary and requires explicit user confirmation before commit.</done>
</task>

</tasks>

<verification>
- `npm run check`
- `rg -n "prepareGridCommit|commitGridEdits|pendingEditCount|GridEditCommitDialog|Pending edits:" client/src/components/extensions/db-workbench/WorkbenchLayout.tsx client/src/components/extensions/db-workbench/ResultGridPane.tsx client/src/components/extensions/db-workbench/GridEditCommitDialog.tsx`
</verification>

<success_criteria>
- DATA-01: user can only edit safe, eligible single-table batches and sees explicit block reasons otherwise
- DATA-02: user reviews backend-generated SQL preview and affected-row summary before commit
- DATA-03: user can discard local patches or run explicit commit path that routes through backend transaction command
</success_criteria>

<output>
After completion, create `.planning/phases/17-safe-data-editing/17-03-SUMMARY.md`
</output>
