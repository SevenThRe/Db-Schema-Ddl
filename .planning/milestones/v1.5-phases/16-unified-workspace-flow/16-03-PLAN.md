---
phase: 16-unified-workspace-flow
plan: 03
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
  - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  - shared/schema.ts
  - src-tauri/src/db_connector/introspect.rs
autonomous: true
requirements:
  - NAV-01
  - NAV-02
must_haves:
  truths:
    - "Object explorer exposes schema/table/view/column/index/foreign-key structure from active snapshot"
    - "Users can launch starter queries directly from object explorer actions"
    - "Explorer data and actions are scoped to active connection + active schema"
  artifacts:
    - path: "client/src/components/extensions/db-workbench/ConnectionSidebar.tsx"
      provides: "Explorer tree with schema/table/view nodes and starter query actions"
      contains: "Starter Queries"
    - path: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      provides: "Handlers to inject generated SQL templates from explorer actions"
      contains: "handleRunStarterQuery"
    - path: "shared/schema.ts"
      provides: "Snapshot typing for views/indexes/foreign keys used by explorer"
      contains: "DbSchemaSnapshot"
    - path: "src-tauri/src/db_connector/introspect.rs"
      provides: "Snapshot population for views/indexes/foreign keys"
      contains: "foreign_keys"
  key_links:
    - from: "src-tauri/src/db_connector/introspect.rs"
      to: "shared/schema.ts"
      via: "snapshot response fields"
      pattern: "views"
    - from: "client/src/components/extensions/db-workbench/ConnectionSidebar.tsx"
      to: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      via: "onRunStarterQuery callback"
      pattern: "onRunStarterQuery"
---

<objective>
Complete object explorer depth and starter query launching for Phase 16.

Purpose: Navigation is only useful if users can inspect full object metadata and immediately act. This plan deepens explorer nodes and adds one-click starter query actions tied to current schema context.
Output: Users can browse schema objects and launch standard SQL templates directly from explorer.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
@client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
@shared/schema.ts
@src-tauri/src/db_connector/introspect.rs

<interfaces>
Starter query templates to support from explorer:
- `SELECT * FROM <table> LIMIT 100;`
- `SELECT COUNT(*) AS total_count FROM <table>;`
- `SELECT <column list> FROM <table> LIMIT 100;`

All templates must honor driver quoting:
- MySQL: backticks
- PostgreSQL: double quotes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand explorer model to include richer object categories</name>
  <files>shared/schema.ts, src-tauri/src/db_connector/introspect.rs, client/src/components/extensions/db-workbench/ConnectionSidebar.tsx</files>
  <read_first>
    - shared/schema.ts (DbSchemaSnapshot/table/index/foreign key types)
    - src-tauri/src/db_connector/introspect.rs (snapshot assembly for mysql/postgres)
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx (current tables rendering)
  </read_first>
  <action>
Align snapshot shape and explorer rendering so users can inspect full object structure.

1. In `shared/schema.ts`, ensure `DbSchemaSnapshot` explicitly carries `schema`, `tables`, and `views` collections with index/foreign-key metadata.
2. In `src-tauri/src/db_connector/introspect.rs`, populate snapshot `views`, `indexes`, and `foreign_keys` for both mysql/postgres paths where metadata exists.
3. In `ConnectionSidebar.tsx`, render explorer sections with stable labels:
   - `Schemas`
   - `Tables`
   - `Views`
   - per-table groups for `Columns`, `Indexes`, `Foreign Keys`
4. Keep existing compact operator styling (bordered list blocks, no card-heavy redesign).
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml introspect -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - shared/schema.ts contains `views:` in `DbSchemaSnapshot`
    - src-tauri/src/db_connector/introspect.rs contains `foreign_keys`
    - src-tauri/src/db_connector/introspect.rs contains `indexes`
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx contains `Schemas`
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx contains `Views`
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx contains `Foreign Keys`
    - `npm run check` exits 0
    - `cargo test --manifest-path src-tauri/Cargo.toml introspect -- --nocapture` exits 0
  </acceptance_criteria>
  <done>Explorer exposes schema/table/view/index/foreign-key navigation depth from runtime snapshot data.</done>
</task>

<task type="auto">
  <name>Task 2: Add explorer starter query actions and wire execution callbacks</name>
  <files>client/src/components/extensions/db-workbench/ConnectionSidebar.tsx, client/src/components/extensions/db-workbench/WorkbenchLayout.tsx</files>
  <read_first>
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx (table selection/open actions)
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx (handleOpenTable and execute flow)
  </read_first>
  <action>
Implement concrete starter query actions from explorer nodes.

1. Add an `onRunStarterQuery(tableName: string, mode: "select" | "count" | "columns")` callback prop in `ConnectionSidebar.tsx`.
2. Add `Starter Queries` action menu/button near selected table details with three concrete actions:
   - `Select top 100`
   - `Count rows`
   - `Select explicit columns`
3. In `WorkbenchLayout.tsx`, implement `handleRunStarterQuery` that builds SQL templates with driver-specific quoting and injects into active tab.
4. For `select` and `count`, execute immediately via existing `handleExecute`; for `columns`, only insert SQL and focus editor.
5. Keep behavior scoped to active schema/connection context (`runtimeSchema`).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx contains `onRunStarterQuery`
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx contains `Starter Queries`
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx contains `Select top 100`
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx contains `Count rows`
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx contains `Select explicit columns`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `handleRunStarterQuery`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `runtimeSchema`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Users can launch starter SQL directly from explorer without leaving the primary workspace.</done>
</task>

</tasks>

<verification>
- `npm run check`
- `cargo test --manifest-path src-tauri/Cargo.toml introspect -- --nocapture`
- Manual smoke: pick table from explorer, run `Select top 100` and `Count rows`, verify SQL and execution context
</verification>

<success_criteria>
- NAV-01 and NAV-02 are satisfied with explorer depth plus direct action affordances
- Explorer and starter-query flow stays inside the primary workbench path
</success_criteria>

<output>
After completion, create `.planning/phases/16-unified-workspace-flow/16-03-SUMMARY.md`
</output>
