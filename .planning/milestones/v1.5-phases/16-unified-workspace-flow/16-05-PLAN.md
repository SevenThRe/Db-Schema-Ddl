---
phase: 16-unified-workspace-flow
plan: 05
type: execute
wave: 1
depends_on:
  - "16-04"
files_modified:
  - client/src/components/extensions/db-workbench/workbench-session.ts
  - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  - test/client/db-workbench-session-phase16.test.ts
  - test/client/db-workbench-flow-phase16.test.tsx
autonomous: true
requirements:
  - FLOW-02
  - FLOW-03
gap_closure: true
must_haves:
  truths:
    - "Selected object focus (`selectedTableName`) persists per connection and restores on connection switch/reopen."
    - "Connection-scoped session persistence still isolates tabs/drafts/recent/snippets without cross-connection leakage."
    - "All `.tsx` node-test verification commands in this plan explicitly include `--import tsx` for reliable cross-shell execution."
  artifacts:
    - path: "client/src/components/extensions/db-workbench/workbench-session.ts"
      provides: "Session contract/storage that includes selected object state in `db-workbench:session:v2:{connectionId}`"
      contains: "selectedTableName"
    - path: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      provides: "Connection switch restore + persistence wiring for selected object state"
      contains: "setSelectedTableName"
    - path: "test/client/db-workbench-session-phase16.test.ts"
      provides: "Contract-level regression coverage for selected object persistence per connection"
      contains: "selected table"
    - path: "test/client/db-workbench-flow-phase16.test.tsx"
      provides: "Flow-level regression coverage that selected object focus remains isolated across connections"
      contains: "selectedTableName"
  key_links:
    - from: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      to: "client/src/components/extensions/db-workbench/workbench-session.ts"
      via: "session restore/persist path includes selected object payload"
      pattern: "loadSessionForConnection(connection.id)"
    - from: "test/client/db-workbench-flow-phase16.test.tsx"
      to: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      via: "connection-switch flow assertions for selected object restore"
      pattern: "switching connection"
---

<objective>
Close the remaining functional FLOW-02 gap by persisting selected object state per connection.

Purpose: Phase 16 currently restores tabs/drafts/recent/snippets but not selected object focus, so connection context restoration is incomplete.
Output: `selectedTableName` is stored in the v2 per-connection session contract, restored on connection switch/reopen, and covered by regression tests.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-unified-workspace-flow/16-VERIFICATION.md
@.planning/phases/16-unified-workspace-flow/16-02-PLAN.md
@client/src/components/extensions/db-workbench/workbench-session.ts
@client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
@test/client/db-workbench-session-phase16.test.ts
@test/client/db-workbench-flow-phase16.test.tsx

<interfaces>
Gap contract from `16-VERIFICATION.md`:
- FLOW-02 currently partial because selected object state (for example `selectedTableName`) is local runtime-only.
- Missing persistence targets:
  - `WorkbenchSessionState` payload shape
  - save/restore wiring in `WorkbenchLayout`
  - regression tests proving per-connection restore behavior
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend per-connection session contract to persist selected object state</name>
  <files>client/src/components/extensions/db-workbench/workbench-session.ts, client/src/components/extensions/db-workbench/WorkbenchLayout.tsx</files>
  <read_first>
    - .planning/phases/16-unified-workspace-flow/16-VERIFICATION.md
    - client/src/components/extensions/db-workbench/workbench-session.ts
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  </read_first>
  <action>
Implement selected object persistence with concrete field names and storage wiring.

1. In `workbench-session.ts`, add `selectedTableName: string | null` to `WorkbenchSessionState`.
2. Update `EMPTY_WORKBENCH_SESSION`/default state and `normalizeSessionState(...)` so `selectedTableName` is always present (`string | null`) when loading malformed/legacy payloads.
3. In `WorkbenchLayout.tsx`, include `selectedTableName` in the payload passed to `saveSessionForConnection(connection.id, {...})`.
4. In the connection-switch restore effect (`loadSessionForConnection(connection.id)`), call `setSelectedTableName(restored.selectedTableName)` after session hydrate.
5. Keep existing session boundaries untouched: tabs/drafts/recent/snippets must remain connection-scoped and unchanged in behavior.
  </action>
  <acceptance_criteria>
    - client/src/components/extensions/db-workbench/workbench-session.ts contains `selectedTableName: string | null` in `WorkbenchSessionState`
    - client/src/components/extensions/db-workbench/workbench-session.ts contains `selectedTableName: null` in default empty session
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `setSelectedTableName(restored.selectedTableName)`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx includes `selectedTableName` in the `saveSessionForConnection(connection.id, { ... })` payload
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Selected object state is now part of the per-connection session contract and is restored deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Lock selected-object persistence with regression coverage and loader-safe verification commands</name>
  <files>test/client/db-workbench-session-phase16.test.ts, test/client/db-workbench-flow-phase16.test.tsx</files>
  <read_first>
    - .planning/phases/16-unified-workspace-flow/16-VERIFICATION.md
    - test/client/db-workbench-session-phase16.test.ts
    - test/client/db-workbench-flow-phase16.test.tsx
    - client/src/components/extensions/db-workbench/workbench-session.ts
  </read_first>
  <action>
Add targeted tests that fail if selected object persistence regresses.

1. In `test/client/db-workbench-session-phase16.test.ts`, add a test that saves distinct `selectedTableName` values for two connection IDs and verifies each connection restores only its own value.
2. In `test/client/db-workbench-flow-phase16.test.tsx`, add/extend a flow test asserting connection switch restores the previous connection's selected table focus.
3. Keep existing FLOW-02/FLOW-03 assertions (`tabs`, `recent sql`, `snippet`) in place; do not replace them.
4. Verify `.tsx` tests with the loader caveat explicitly in command form: `node --import tsx --test --experimental-strip-types ...`.
  </action>
  <acceptance_criteria>
    - test/client/db-workbench-session-phase16.test.ts contains `selectedTableName` assertions for at least two connection IDs
    - test/client/db-workbench-flow-phase16.test.tsx contains a selected-object restore assertion for connection switching
    - `node --test --experimental-strip-types test/client/db-workbench-session-phase16.test.ts` exits 0
    - `node --import tsx --test --experimental-strip-types test/client/db-workbench-flow-phase16.test.tsx` exits 0
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --experimental-strip-types test/client/db-workbench-session-phase16.test.ts</automated>
    <automated>node --import tsx --test --experimental-strip-types test/client/db-workbench-flow-phase16.test.tsx</automated>
    <automated>npm run check</automated>
  </verify>
  <done>FLOW-02 selected-object persistence is regression-locked with reliable `.tsx` verification commands.</done>
</task>

</tasks>

<verification>
- `node --test --experimental-strip-types test/client/db-workbench-session-phase16.test.ts`
- `node --import tsx --test --experimental-strip-types test/client/db-workbench-flow-phase16.test.tsx`
- `npm run check`
</verification>

<success_criteria>
- FLOW-02 is fully satisfied: tabs, drafts, selected object focus, recent SQL, and snippets are all restored per connection without leakage.
- FLOW-03 behavior remains intact while selected-object persistence is added.
- Phase-16 `.tsx` verification command reliability is explicit in this gap-closure plan.
</success_criteria>

<output>
After completion, create `.planning/phases/16-unified-workspace-flow/16-05-SUMMARY.md`
</output>
