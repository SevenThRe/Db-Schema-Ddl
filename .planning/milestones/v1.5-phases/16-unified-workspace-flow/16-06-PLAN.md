---
phase: 16-unified-workspace-flow
plan: 06
type: execute
wave: 2
depends_on:
  - "16-05"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/16-unified-workspace-flow/16-04-PLAN.md
  - .planning/phases/16-unified-workspace-flow/16-VERIFICATION.md
autonomous: true
requirements:
  - NAV-03
  - FLOW-02
gap_closure: true
must_haves:
  truths:
    - "NAV-03 is marked complete in planning traceability docs once implementation/tests are already proven in Phase 16 evidence."
    - "Phase-16 plans that specify `.tsx` node-test verification commands explicitly use `node --import tsx --test --experimental-strip-types`."
    - "No stale phase-16 `.tsx` verification command omits `--import tsx`."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Canonical requirement checklist + traceability row aligned to verified NAV-03 implementation"
      contains: "[x] **NAV-03**"
    - path: ".planning/ROADMAP.md"
      provides: "Milestone coverage table aligned to the same NAV-03 completion status"
      contains: "| NAV-03 | Phase 16 | Complete |"
    - path: ".planning/phases/16-unified-workspace-flow/16-04-PLAN.md"
      provides: "`.tsx` node-test verification commands documented with required loader import"
      contains: "node --import tsx --test --experimental-strip-types"
    - path: ".planning/phases/16-unified-workspace-flow/16-VERIFICATION.md"
      provides: "Phase verification command docs aligned to cross-shell command shape with explicit historical syntax note"
      contains: "node --import tsx --test --experimental-strip-types"
  key_links:
    - from: ".planning/phases/16-unified-workspace-flow/16-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "verified NAV-03 implementation drives requirements status update"
      pattern: "NAV-03"
    - from: ".planning/phases/16-unified-workspace-flow/16-04-PLAN.md"
      to: ".planning/phases/16-unified-workspace-flow/16-VERIFICATION.md"
      via: "verification command shape includes `.tsx` loader caveat proven during phase verification"
      pattern: "--import tsx"
---

<objective>
Close planning/documentation traceability gaps after Phase 16 verification.

Purpose: Runtime behavior and tests already satisfy NAV-03, but requirement status and `.tsx` verification command documentation are stale/incomplete.
Output: NAV-03 status is synchronized in canonical planning docs, and phase-16 plan verification commands encode the `node --import tsx --test --experimental-strip-types` reliability caveat.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-unified-workspace-flow/16-VERIFICATION.md
@.planning/phases/16-unified-workspace-flow/16-04-PLAN.md
@.planning/phases/16-unified-workspace-flow/16-05-PLAN.md

<interfaces>
Gap sources to close:
- `16-VERIFICATION.md`: NAV-03 is complete in code/tests, but REQUIREMENTS status is stale.
- `16-VERIFICATION.md`: `.tsx` node tests require `--import tsx` for reliable execution.
- Planning traceability must stay deterministic for downstream `$gsd-execute-phase`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synchronize NAV-03 completion status in requirement traceability docs</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/16-unified-workspace-flow/16-VERIFICATION.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
Update status fields with exact value changes so traceability matches verified implementation.

1. In `.planning/REQUIREMENTS.md`, change `- [ ] **NAV-03**` to `- [x] **NAV-03**`.
2. In `.planning/REQUIREMENTS.md` Traceability table, change NAV-03 status cell from `Pending` to `Complete`.
3. In `.planning/ROADMAP.md` Coverage table, change NAV-03 status cell from `Pending` to `Complete` so roadmap and requirements stay aligned.
4. Update the footer `Last updated` lines in modified docs to include this gap-closure sync date.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `- [x] **NAV-03**`
    - `.planning/REQUIREMENTS.md` contains `| NAV-03 | Phase 16 | Complete |`
    - `.planning/ROADMAP.md` contains `| NAV-03 | Phase 16 | Complete |`
    - `rg -n \"NAV-03\" .planning/REQUIREMENTS.md .planning/ROADMAP.md` shows no NAV-03 row with `Pending`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "NAV-03" .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <done>NAV-03 traceability status is synchronized across canonical milestone planning docs.</done>
</task>

<task type="auto">
  <name>Task 2: Encode `.tsx` node-test loader caveat in Phase-16 verification commands</name>
  <files>.planning/phases/16-unified-workspace-flow/16-04-PLAN.md, .planning/phases/16-unified-workspace-flow/16-05-PLAN.md, .planning/phases/16-unified-workspace-flow/16-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/16-unified-workspace-flow/16-VERIFICATION.md
    - .planning/phases/16-unified-workspace-flow/16-04-PLAN.md
    - .planning/phases/16-unified-workspace-flow/16-05-PLAN.md
  </read_first>
  <action>
Document the exact verification command shape required for `.tsx` tests.

1. In `16-04-PLAN.md`, replace every legacy `.tsx` command in `<verify>`, `<acceptance_criteria>`, and `<verification>` that omits `--import tsx` with:
   `node --import tsx --test --experimental-strip-types test/client/db-workbench-autocomplete-phase16.test.tsx test/client/db-workbench-flow-phase16.test.tsx`
2. Add one explicit note in `16-04-PLAN.md` verification section:
   `Note: .tsx node tests require node --import tsx --test --experimental-strip-types.`
3. Confirm `16-05-PLAN.md` keeps the same loader-aware command style for its `.tsx` verification steps.
4. In `16-VERIFICATION.md`, align the `.tsx` PASS command bullet to cross-shell form:
   `node --import tsx --test --experimental-strip-types test/client/db-workbench-autocomplete-phase16.test.tsx test/client/db-workbench-flow-phase16.test.tsx`
   and add a short historical note that `NODE_OPTIONS=--import tsx node ...` was the original shell syntax used during that verification run.
  </action>
  <acceptance_criteria>
    - `.planning/phases/16-unified-workspace-flow/16-04-PLAN.md` contains `node --import tsx --test --experimental-strip-types test/client/db-workbench-autocomplete-phase16.test.tsx test/client/db-workbench-flow-phase16.test.tsx`
    - `.planning/phases/16-unified-workspace-flow/16-04-PLAN.md` contains `Note: .tsx node tests require node --import tsx --test --experimental-strip-types.`
    - `.planning/phases/16-unified-workspace-flow/16-VERIFICATION.md` contains `node --import tsx --test --experimental-strip-types test/client/db-workbench-autocomplete-phase16.test.tsx test/client/db-workbench-flow-phase16.test.tsx`
    - `.planning/phases/16-unified-workspace-flow/16-VERIFICATION.md` contains `historical` and `NODE_OPTIONS=--import tsx node`
    - `rg -n "node\\s+--test\\s+--experimental-strip-types\\s+[^\\n]*\\.tsx" .planning/phases/16-unified-workspace-flow -g "16-0*-PLAN.md"` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>rg -n "node --import tsx --test --experimental-strip-types" .planning/phases/16-unified-workspace-flow/16-04-PLAN.md .planning/phases/16-unified-workspace-flow/16-05-PLAN.md</automated>
    <automated>rg -n "node --import tsx --test --experimental-strip-types" .planning/phases/16-unified-workspace-flow/16-VERIFICATION.md</automated>
    <automated>rg -n "historical.*NODE_OPTIONS=--import tsx node|NODE_OPTIONS=--import tsx node.*historical" .planning/phases/16-unified-workspace-flow/16-VERIFICATION.md</automated>
    <automated>rg -n "node\\s+--test\\s+--experimental-strip-types\\s+[^\\n]*\\.tsx" .planning/phases/16-unified-workspace-flow -g "16-0*-PLAN.md"</automated>
  </verify>
  <done>Phase-16 plan verification commands consistently document the `.tsx` loader prerequisite.</done>
</task>

</tasks>

<verification>
- `rg -n "NAV-03" .planning/REQUIREMENTS.md .planning/ROADMAP.md`
- `rg -n "node --import tsx --test --experimental-strip-types" .planning/phases/16-unified-workspace-flow/16-04-PLAN.md .planning/phases/16-unified-workspace-flow/16-05-PLAN.md`
- `rg -n "node --import tsx --test --experimental-strip-types" .planning/phases/16-unified-workspace-flow/16-VERIFICATION.md`
- `rg -n "historical.*NODE_OPTIONS=--import tsx node|NODE_OPTIONS=--import tsx node.*historical" .planning/phases/16-unified-workspace-flow/16-VERIFICATION.md`
- `rg -n "node\\s+--test\\s+--experimental-strip-types\\s+[^\\n]*\\.tsx" .planning/phases/16-unified-workspace-flow -g "16-0*-PLAN.md"`
</verification>

<success_criteria>
- NAV-03 traceability state is complete and no longer stale in canonical planning docs.
- `.tsx` node-test reliability caveat is explicitly documented in phase-16 plan verification commands.
- Gap-closure documentation now matches runtime evidence and supports deterministic downstream execution.
</success_criteria>

<output>
After completion, create `.planning/phases/16-unified-workspace-flow/16-06-SUMMARY.md`
</output>
