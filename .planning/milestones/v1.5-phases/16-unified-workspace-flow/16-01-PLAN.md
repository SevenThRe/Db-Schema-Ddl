---
phase: 16-unified-workspace-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/extensions/DbConnectorWorkspace.tsx
  - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
autonomous: true
requirements:
  - FLOW-01
  - NAV-01
must_haves:
  truths:
    - "DB operations open through one primary workbench route; legacy tabs no longer compete as first-choice entry points"
    - "When an active connection exists, workspace entry lands in SQL workbench context by default"
    - "Object explorer remains visible and reachable from the primary workbench shell"
  artifacts:
    - path: "client/src/components/extensions/DbConnectorWorkspace.tsx"
      provides: "Single primary entry flow with explicit fallback to legacy-only operations"
      contains: "const PRIMARY_WORKSPACE_VIEW"
    - path: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      provides: "Unified operator shell remains the primary execution surface"
      contains: "onSwitchToLegacy"
    - path: "client/src/components/extensions/db-workbench/ConnectionSidebar.tsx"
      provides: "Object explorer header and table navigation in the primary pane"
      contains: "Tables"
  key_links:
    - from: "client/src/components/extensions/DbConnectorWorkspace.tsx"
      to: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      via: "Primary entry route hands active connection into workbench shell"
      pattern: "<WorkbenchLayout"
---

<objective>
Make the DB workbench the single primary route for daily DB operations in Phase 16.

Purpose: Users should no longer choose between parallel legacy vs operator surfaces when starting database work. The SQL workbench path must be the default route while legacy views stay as explicit fallback tools.
Output: One primary route opens DB work; legacy sections are demoted to secondary paths without breaking existing schema/diff capabilities.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/components/extensions/DbConnectorWorkspace.tsx
@client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
@client/src/components/extensions/db-workbench/ConnectionSidebar.tsx

<interfaces>
Phase-16 route expectations:
- `FLOW-01`: one primary DB workbench entry path
- legacy `connections/schema/diff` remains available but not the default entry when connection context exists
- object explorer must still be visible in the primary shell (`ConnectionSidebar`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote SQL workbench as the default route and demote legacy tabs to explicit fallback</name>
  <files>client/src/components/extensions/DbConnectorWorkspace.tsx</files>
  <read_first>
    - client/src/components/extensions/DbConnectorWorkspace.tsx (workspaceView initialization, route persistence, tab triggers)
    - .planning/ROADMAP.md (Phase 16 success criteria 1)
  </read_first>
  <action>
In `DbConnectorWorkspace.tsx`, make the primary route deterministic.

1. Add `const PRIMARY_WORKSPACE_VIEW: WorkspaceView = "sql";` near workspace constants.
2. Update initial view selection logic so:
   - if there is a selected/active connection, default view resolves to `PRIMARY_WORKSPACE_VIEW`
   - if no connection exists, fallback remains `connections`
3. Keep `connections/schema/diff` tabs, but rename the section label copy to explicitly indicate "legacy/fallback" intent (for example: "Legacy tools").
4. Ensure route persistence still writes both `db-workbench-view` and `db-workbench-connection` query params.
5. Do not remove existing schema/diff functionality.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/extensions/DbConnectorWorkspace.tsx contains `const PRIMARY_WORKSPACE_VIEW: WorkspaceView = "sql"`
    - client/src/components/extensions/DbConnectorWorkspace.tsx contains a branch that routes to `PRIMARY_WORKSPACE_VIEW` when `selectedConnId` exists
    - client/src/components/extensions/DbConnectorWorkspace.tsx contains the phrase `Legacy tools`
    - client/src/components/extensions/DbConnectorWorkspace.tsx still contains `TabsTrigger value="schema"`
    - client/src/components/extensions/DbConnectorWorkspace.tsx still contains `TabsTrigger value="diff"`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>User enters DB work through one primary SQL workbench route without removing legacy tools.</done>
</task>

<task type="auto">
  <name>Task 2: Keep primary shell context clear and object explorer always present</name>
  <files>client/src/components/extensions/db-workbench/WorkbenchLayout.tsx, client/src/components/extensions/db-workbench/ConnectionSidebar.tsx</files>
  <read_first>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx (header, active context, sidebar placement)
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx (tables/object area)
  </read_first>
  <action>
Tighten UX wording and visibility cues so the primary shell is clearly the default work surface.

1. In `WorkbenchLayout.tsx`, add a short primary-path hint near top-level shell text (for example: "Primary DB workspace").
2. In `ConnectionSidebar.tsx`, ensure object explorer section title is explicit and stable (`Object Explorer`).
3. Keep table list, key/index metadata, and open-table action in the same sidebar; do not move them back to legacy panels.
4. Ensure the sidebar still renders with fixed operator width (`w-[240px]`) and no card-style wrappers.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `Primary DB workspace`
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx contains `Object Explorer`
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx contains `w-[240px]`
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx contains `onOpenTable`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Primary workbench shell has clear operator cues and persistent object explorer visibility.</done>
</task>

</tasks>

<verification>
- `npm run check`
- Confirm DB entry with existing connection lands in SQL workbench path by default
- Confirm schema/diff legacy capabilities remain reachable but are not positioned as primary start path
</verification>

<success_criteria>
- FLOW-01 is enforceable from code paths and user-visible entry behavior
- NAV-01 prerequisites remain in the primary shell through persistent object explorer placement
</success_criteria>

<output>
After completion, create `.planning/phases/16-unified-workspace-flow/16-01-SUMMARY.md`
</output>
