---
phase: 15-query-runtime-hardening-v1_5
plan: 04
type: execute
wave: 4
depends_on:
  - "15-02"
  - "15-03"
files_modified:
  - src-tauri/src/db_connector/query.rs
  - src-tauri/src/db_connector/introspect.rs
  - test/client/db-workbench-runtime-phase15.test.tsx
  - test/client/db-connection-config.test.ts
autonomous: true
requirements:
  - RUN-01
  - RUN-02
  - RUN-03
  - RUN-04
  - RUN-05
must_haves:
  truths:
    - "Phase 15 runtime behavior is covered by automated tests instead of only manual smoke checks"
    - "Regression tests lock the new paging/export/schema semantics so later workspace phases do not reintroduce demo-only behavior"
    - "Client tests verify operator-visible states for unsupported paging, backend export scopes, and schema persistence"
  artifacts:
    - path: "src-tauri/src/db_connector/query.rs"
      provides: "Rust unit tests for page-first execution and cancellation cleanup"
      contains: "#[test]"
    - path: "src-tauri/src/db_connector/introspect.rs"
      provides: "Rust coverage for schema-aware PostgreSQL introspection helpers"
      contains: "#[cfg(test)]"
    - path: "test/client/db-workbench-runtime-phase15.test.tsx"
      provides: "UI regression coverage for load-more, export scopes, and unsupported paging copy"
      contains: "Full result"
    - path: "test/client/db-connection-config.test.ts"
      provides: "Persistence coverage for defaultSchema on saved connections"
      contains: "defaultSchema"
  key_links:
    - from: "test/client/db-workbench-runtime-phase15.test.tsx"
      to: "client/src/components/extensions/db-workbench/ResultGridPane.tsx"
      via: "operator-visible paging and export labels"
      pattern: "Load"
    - from: "test/client/db-connection-config.test.ts"
      to: "shared/schema.ts"
      via: "serialized connection config"
      pattern: "defaultSchema"
---

<objective>
Lock Phase 15 behavior with automated regression coverage.

Purpose: Query runtime hardening is the kind of work that quietly regresses when later phases chase workspace flow, editing, and sync features. This plan adds the tests that prove the new paging, export, cancellation, and schema-context semantics continue to hold.
Output: Rust and client tests cover the Phase 15 runtime contract and the operator-visible workbench states.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/15-query-runtime-hardening-v1_5/15-02-PLAN.md
@.planning/phases/15-query-runtime-hardening-v1_5/15-03-PLAN.md
@src-tauri/src/db_connector/query.rs
@src-tauri/src/db_connector/introspect.rs
@test/client/db-connection-config.test.ts

<interfaces>
Preferred verification commands already used in this repo:

```bash
npm run check
node --test --experimental-strip-types test/client/db-connection-config.test.ts
node --test --experimental-strip-types test/client/extension-boundaries.test.ts
cargo test --manifest-path src-tauri/Cargo.toml query -- --nocapture
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Rust coverage for page-first query behavior and schema-aware helpers</name>
  <files>src-tauri/src/db_connector/query.rs, src-tauri/src/db_connector/introspect.rs</files>
  <read_first>
    - src-tauri/src/db_connector/query.rs (existing unit-test style near the bottom of the file)
    - src-tauri/src/db_connector/introspect.rs (helper structure and any existing tests)
    - .planning/phases/15-query-runtime-hardening-v1_5/15-02-PLAN.md (required backend semantics)
  </read_first>
  <action>
Add Rust unit coverage for the new backend rules introduced by Plan 02.

1. In `src-tauri/src/db_connector/query.rs`, add tests for:
   - safe paged-query SQL construction uses the wrapper form and a bounded extra-row fetch
   - unsupported paging returns `paging_mode = unsupported`
   - cancellation cleanup removes tokens for both query and export request IDs

2. In `src-tauri/src/db_connector/introspect.rs`, add tests for:
   - schema-name selection defaults to `public` only when no explicit schema exists
   - helper SQL or helper functions bind a variable schema instead of embedding `'public'`
   - PostgreSQL identifier quoting for `search_path` escapes embedded quotes correctly

3. Keep tests close to the runtime helpers so future refactors trip fast when semantics drift.
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/db_connector/query.rs contains tests for `paging_mode`
    - src-tauri/src/db_connector/query.rs contains tests for export-token cleanup
    - src-tauri/src/db_connector/introspect.rs contains `#[cfg(test)]`
    - src-tauri/src/db_connector/introspect.rs contains a test that references a non-public schema value
    - `cargo test --manifest-path src-tauri/Cargo.toml -- --nocapture` exits 0
  </acceptance_criteria>
  <done>Rust tests prove the page-first, cancellation, and schema-aware backend behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add client regression coverage for paging states, export scopes, and schema persistence</name>
  <files>test/client/db-workbench-runtime-phase15.test.tsx, test/client/db-connection-config.test.ts</files>
  <read_first>
    - test/client/db-connection-config.test.ts (existing node --test style)
    - test/client/db-live-export-phase2-ui.test.tsx (UI test style for export-related behavior)
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx
    - client/src/components/extensions/db-workbench/ResultExportMenu.tsx
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
  </read_first>
  <action>
Add client-side regression tests that pin the operator-visible Phase 15 behavior.

1. Create `test/client/db-workbench-runtime-phase15.test.tsx` and cover:
   - supported paging shows a load-more action
   - unsupported paging shows the backend reason instead of a fake load-more action
   - export menu renders `Current page`, `Loaded rows`, and `Full result`
   - full-result export warning copy is present

2. Extend `test/client/db-connection-config.test.ts` with a case that proves `defaultSchema` survives schema parsing/serialization for saved connection configs.

3. Use the same `node --test --experimental-strip-types` test style already used by the repo. Do not introduce a second client test runner just for this phase.
  </action>
  <verify>
    <automated>node --test --experimental-strip-types test/client/db-connection-config.test.ts test/client/db-workbench-runtime-phase15.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - test/client/db-workbench-runtime-phase15.test.tsx exists
    - test/client/db-workbench-runtime-phase15.test.tsx contains `Current page`
    - test/client/db-workbench-runtime-phase15.test.tsx contains `Loaded rows`
    - test/client/db-workbench-runtime-phase15.test.tsx contains `Full result`
    - test/client/db-connection-config.test.ts contains `defaultSchema`
    - `node --test --experimental-strip-types test/client/db-connection-config.test.ts test/client/db-workbench-runtime-phase15.test.tsx` exits 0
  </acceptance_criteria>
  <done>Client regression tests lock the Phase 15 operator-facing semantics.</done>
</task>

</tasks>

<verification>
- `npm run check`
- `cargo test --manifest-path src-tauri/Cargo.toml -- --nocapture`
- `node --test --experimental-strip-types test/client/db-connection-config.test.ts test/client/db-workbench-runtime-phase15.test.tsx`
- Test coverage includes unsupported paging, backend export scopes, and default schema persistence
</verification>

<success_criteria>
- Phase 15 runtime hardening is protected by both Rust and client regression tests
- Later phases can build on the workbench without reintroducing full-prefetch or public-only schema assumptions
</success_criteria>

<output>
After completion, create `.planning/phases/15-query-runtime-hardening-v1_5/15-04-SUMMARY.md`
</output>
