---
phase: 15-query-runtime-hardening-v1_5
plan: 03
type: execute
wave: 3
depends_on:
  - "15-01"
  - "15-02"
files_modified:
  - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  - client/src/components/extensions/db-workbench/ResultGridPane.tsx
  - client/src/components/extensions/db-workbench/ResultExportMenu.tsx
  - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
autonomous: true
requirements:
  - RUN-02
  - RUN-03
  - RUN-04
  - RUN-05
must_haves:
  truths:
    - "Result browsing uses backend paging metadata instead of guessing from totalRows versus loaded row count"
    - "The UI tells operators when load more is unsupported rather than silently failing or issuing unsafe paging"
    - "Export actions map to runtime-backed scopes: current page, loaded rows, and full result"
    - "PostgreSQL users can see and change active schema from the workbench and that choice persists"
  artifacts:
    - path: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      provides: "Schema-aware execute/load-more/export/cancel orchestration"
      contains: "listSchemas"
    - path: "client/src/components/extensions/db-workbench/ResultGridPane.tsx"
      provides: "Load-more and status rendering driven by paging metadata"
      contains: "pagingMode"
    - path: "client/src/components/extensions/db-workbench/ResultExportMenu.tsx"
      provides: "Three explicit export scopes with warnings for full-result export"
      contains: "Full result"
    - path: "client/src/components/extensions/db-workbench/ConnectionSidebar.tsx"
      provides: "PostgreSQL schema selector and snapshot context"
      contains: "Schema"
  key_links:
    - from: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      to: "client/src/lib/desktop-bridge.ts"
      via: "typed export and schema-list calls"
      pattern: "exportRows"
    - from: "client/src/components/extensions/db-workbench/ConnectionSidebar.tsx"
      to: "src-tauri/src/db_connector/commands.rs"
      via: "listSchemas + saveConnection persistence"
      pattern: "defaultSchema"
---

<objective>
Integrate the hardened runtime semantics into the DB Workbench operator surface.

Purpose: Phase 15 is not complete if the backend is fixed but the UI still assumes old `totalRows` behavior, re-executes client-side exports, or hides schema context. This plan makes the workbench reflect the real runtime capabilities and safety boundaries.
Output: The workbench displays honest paging state, backend export scopes, query/export cancellation state, and active PostgreSQL schema selection with persistence.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-query-runtime-hardening-v1_5/15-01-PLAN.md
@.planning/phases/15-query-runtime-hardening-v1_5/15-02-PLAN.md
@client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
@client/src/components/extensions/db-workbench/ResultGridPane.tsx
@client/src/components/extensions/db-workbench/ResultExportMenu.tsx
@client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
@client/src/extensions/host-api.ts

<interfaces>
Current frontend mismatches confirmed from code:

```tsx
const moreBatch = await hostApi.connections.fetchMore({
  requestId: results.requestId,
  batchIndex,
  sql: batch.sql,
  connectionId: connection.id,
  offset: batch.rows.length,
  limit: 1000,
});
```

```tsx
// ResultExportMenu.tsx (current)
// Current page: client-side Blob generation
// All rows (re-execute): frontend reruns executeQuery and downloads Blob
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive result browsing from explicit paging metadata and fail-closed load-more behavior</name>
  <files>client/src/components/extensions/db-workbench/WorkbenchLayout.tsx, client/src/components/extensions/db-workbench/ResultGridPane.tsx</files>
  <read_first>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx (current executeImmediate, handleLoadMore, cancel flow, schemaSnapshot query)
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx (current totalRows and hasMore assumptions)
    - client/src/extensions/host-api.ts (updated query/fetchMore batch metadata)
  </read_first>
  <action>
Refactor result browsing to use the backend metadata from Plan 02 instead of inferring paging from `totalRows`.

1. In `WorkbenchLayout.tsx`, pass the active schema on every runtime request:
   - `executeQuery`
   - `fetchMore`
   - `explainQuery`
   - `exportRows`

2. Update `handleLoadMore` to use backend metadata:
   - only call `fetchMore` when `batch.pagingMode === "offset"` and `batch.hasMore === true`
   - send `offset: batch.nextOffset`
   - if paging is unsupported, show a destructive or warning notification containing `batch.pagingReason`
   - do not silently swallow fetch-more failures; show operator-visible feedback and preserve current rows

3. In `ResultGridPane.tsx`, replace the old footer and status-bar assumptions with:
   - loaded count from `batch.returnedRows` / `batch.rows.length`
   - total text that handles `totalRows === null` as the exact string `Unknown total`
   - load-more button visibility from `batch.hasMore`
   - unsupported paging copy when `batch.pagingMode === "unsupported"` using the exact string `Load more unavailable for this result.`

4. Keep the UI dense and operator-focused. Do not convert this surface into card-heavy marketing UI. Preserve pane layout, borders, and monospace data display.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `schema:`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `pagingMode`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `nextOffset`
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx contains `pagingMode`
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx contains `hasMore`
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx contains `Unknown total`
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx contains `Load more unavailable for this result.`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx does NOT contain a fetch-more offset derived only from `batch.rows.length`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>The grid tells the truth about paging support and uses backend pagination metadata instead of guessing.</done>
</task>

<task type="auto">
  <name>Task 2: Replace client-side export behavior with backend export scopes and shared cancel handling</name>
  <files>client/src/components/extensions/db-workbench/WorkbenchLayout.tsx, client/src/components/extensions/db-workbench/ResultExportMenu.tsx</files>
  <read_first>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx (current handleExportCurrentPage, handleExportFull, cancel flow)
    - client/src/components/extensions/db-workbench/ResultExportMenu.tsx (current client-side serializeBatch usage)
    - client/src/lib/desktop-bridge.ts (updated BinaryCommandResult export response)
  </read_first>
  <action>
Move export behavior onto the registered runtime command and make the operator-facing scope explicit.

1. In `ResultExportMenu.tsx`, keep format selection but change scope handling to three backend-backed entries:
   - `Current page`
   - `Loaded rows`
   - `Full result`

2. Remove direct Blob creation and direct `serializeBatch(...); triggerDownload(...)` execution from the menu component. The menu should only dispatch scope + format to `WorkbenchLayout`.

3. In `WorkbenchLayout.tsx`, implement a single export handler that:
   - creates a dedicated export request ID
   - calls `hostApi.connections.exportRows(...)`
   - downloads the returned `BinaryCommandResult`
   - shows a warning notification using the exact string `Full result export may be truncated at 100000 rows.` when the backend reports truncation or a capped export

4. Unify cancel semantics:
   - while a query is running, Stop cancels the query request ID
   - while an export is running, Stop cancels the export request ID
   - UI state clears correctly on success, error, and cancel for both job types

5. Preserve the existing small-button operator feel. Do not add modal-heavy flows unless the backend explicitly reports a warning that must be acknowledged.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/extensions/db-workbench/ResultExportMenu.tsx contains `Current page`
    - client/src/components/extensions/db-workbench/ResultExportMenu.tsx contains `Loaded rows`
    - client/src/components/extensions/db-workbench/ResultExportMenu.tsx contains `Full result`
    - client/src/components/extensions/db-workbench/ResultExportMenu.tsx does NOT contain `new Blob`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `hostApi.connections.exportRows`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `BinaryCommandResult`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains export-specific request ID state
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `Full result export may be truncated at 100000 rows.`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Export scopes are runtime-backed, cancellable, and honest about full-result limits.</done>
</task>

<task type="auto">
  <name>Task 3: Surface PostgreSQL schema selection and persist the active schema</name>
  <files>client/src/components/extensions/db-workbench/WorkbenchLayout.tsx, client/src/components/extensions/db-workbench/ConnectionSidebar.tsx</files>
  <read_first>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx (schema snapshot loading and connection switching)
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx (current connection summary and object tree)
    - client/src/extensions/host-api.ts (save/listSchemas/introspect signatures)
  </read_first>
  <action>
Add visible schema context for PostgreSQL operators and persist their choice through the existing connection model.

1. In `WorkbenchLayout.tsx`, load schema options with `hostApi.connections.listSchemas(connection.id)` when the active connection driver is `postgres`.

2. Determine the UI-active schema as:
   - `connection.defaultSchema` if present
   - otherwise `"public"`

3. In `ConnectionSidebar.tsx`, add a compact schema selector row above the object tree when `connection.driver === "postgres"`:
   - label text: `Schema`
   - current value bound to the active schema
   - options from `listSchemas`
   - when changed, call a parent callback that saves `{ ...connection, defaultSchema: nextSchema }` through `hostApi.connections.save(...)` and then refreshes introspection

4. Ensure introspection/query/result paths refresh against the new schema. The sidebar should make it obvious which schema produced the current object tree and result context.

5. Do not show the selector for MySQL connections.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `listSchemas(connection.id)`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `defaultSchema`
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx contains `Schema`
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx contains a driver guard for `postgres`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `saveConnection` or `hostApi.connections.save`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>PostgreSQL users can see, change, and persist the active schema directly from the workbench.</done>
</task>

</tasks>

<verification>
- `npm run check`
- Result grid renders supported, unsupported, and unknown-total paging states correctly
- Export menu no longer performs client-side Blob generation
- PostgreSQL schema selector is visible only for PostgreSQL and persists through connection save
</verification>

<success_criteria>
- The DB Workbench surface matches the real runtime semantics delivered by the backend
- Operators can trust paging, export, cancel, and schema context without hidden demo-only assumptions
</success_criteria>

<output>
After completion, create `.planning/phases/15-query-runtime-hardening-v1_5/15-03-SUMMARY.md`
</output>
