---
phase: 46-extract-db-workbench-into-an-on-demand-installable-extension-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/extension-schema.ts
  - src-tauri/src/extensions/manifest.rs
  - src-tauri/src/extensions/lifecycle.rs
  - src-tauri/src/extensions/registry.rs
  - src-tauri/src/extensions/process.rs
  - src-tauri/src/extensions/commands.rs
  - client/src/hooks/use-extensions.ts
  - client/src/components/ExtensionPanel.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "External extensions can install as UI-only packages when they provide `uiBundle` without a sidecar binary."
    - "Installed-extension metadata exposes whether a sidecar exists so the UI does not offer fake start/stop actions."
    - "The official extension marketplace exposes `db-connector` as an installable external tool."
  artifacts:
    - path: "shared/extension-schema.ts"
      provides: "UI-only external manifest contract and canonical DB capability list"
      contains: "entry: z.record"
    - path: "src-tauri/src/extensions/manifest.rs"
      provides: "Rust validation parity for optional sidecar entry"
      contains: "at least one of"
    - path: "src-tauri/src/extensions/process.rs"
      provides: "Explicit UI-only start failure semantics"
      contains: "sidecar"
    - path: "client/src/components/ExtensionPanel.tsx"
      provides: "Official marketplace exposure for db-connector without fake sidecar controls"
      contains: "db-connector"
  key_links:
    - from: "shared/extension-schema.ts"
      to: "src-tauri/src/extensions/manifest.rs"
      via: "optional external `entry` only when `uiBundle` exists"
      pattern: "uiBundle"
    - from: "src-tauri/src/extensions/registry.rs"
      to: "client/src/hooks/use-extensions.ts"
      via: "nullable entry path flows to UI start/stop visibility"
      pattern: "entry_path"
---

<objective>
Let `db-connector` install as a UI-only external extension package and make the extension-management surfaces truthful about whether an installed package actually has a sidecar process.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-extract-db-workbench-into-an-on-demand-installable-extension-package/46-CONTEXT.md
@shared/extension-schema.ts
@src-tauri/src/extensions/manifest.rs
@src-tauri/src/extensions/lifecycle.rs
@src-tauri/src/extensions/registry.rs
@src-tauri/src/extensions/process.rs
@src-tauri/src/extensions/commands.rs
@client/src/hooks/use-extensions.ts
@client/src/components/ExtensionPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relax the external manifest contract for UI-only packages</name>
  <read_first>
    - shared/extension-schema.ts
    - src-tauri/src/extensions/manifest.rs
    - .planning/phases/46-extract-db-workbench-into-an-on-demand-installable-extension-package/46-CONTEXT.md
  </read_first>
  <files>
    - shared/extension-schema.ts
    - src-tauri/src/extensions/manifest.rs
  </files>
  <action>
Make external `entry` optional when `uiBundle` exists, require at least one runtime artifact (`entry` or `uiBundle`), and align the shared capability enum with the DB workbench APIs already exposed through the host runtime.
  </action>
  <acceptance_criteria>
    - `shared/extension-schema.ts` no longer requires `entry` for every external manifest
    - `shared/extension-schema.ts` includes `db.plan.read`, `db.result.export`, `db.data.edit`, and `db.data.sync`
    - `src-tauri/src/extensions/manifest.rs` allows missing `entry` when `ui_bundle` is present
    - Rust validation fails clearly when both `entry` and `ui_bundle` are absent
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -j 1</automated>
  </verify>
  <done>External manifests can represent the extracted DB workbench package truthfully.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve nullable sidecar metadata across install, registry, process, and list flows</name>
  <read_first>
    - src-tauri/src/extensions/lifecycle.rs
    - src-tauri/src/extensions/registry.rs
    - src-tauri/src/extensions/process.rs
    - src-tauri/src/extensions/commands.rs
  </read_first>
  <files>
    - src-tauri/src/extensions/lifecycle.rs
    - src-tauri/src/extensions/registry.rs
    - src-tauri/src/extensions/process.rs
    - src-tauri/src/extensions/commands.rs
  </files>
  <action>
Carry optional sidecar-entry metadata all the way through installation and process management so UI-only packages register cleanly and process commands fail with explicit operator-readable errors instead of invalid-manifest noise.
  </action>
  <acceptance_criteria>
    - installed-extension metadata stores `entry_path` as optional
    - install validation only checks binary entry existence when one is declared
    - `ext_start` rejects UI-only packages with an explicit sidecar-missing error
    - `ext_list_all` keeps runtime bundle resolution working for UI-only packages
  </acceptance_criteria>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -j 1</automated>
  </verify>
  <done>UI-only external packages behave as first-class installed extensions.</done>
</task>

<task type="auto">
  <name>Task 3: Make extension-management surfaces truthful about db-connector installation</name>
  <read_first>
    - client/src/hooks/use-extensions.ts
    - client/src/components/ExtensionPanel.tsx
    - client/src/i18n/locales/zh.json
    - client/src/i18n/locales/ja.json
  </read_first>
  <files>
    - client/src/hooks/use-extensions.ts
    - client/src/components/ExtensionPanel.tsx
    - client/src/i18n/locales/zh.json
    - client/src/i18n/locales/ja.json
  </files>
  <action>
Expose `db-connector` as the official installable extension, invalidate extension-shell caches after install/uninstall, and suppress start/stop controls for installed packages that do not include a sidecar binary.
  </action>
  <acceptance_criteria>
    - `ExtensionPanel.tsx` lists `db-connector` as an official extension
    - install/uninstall invalidates both installed-list and resolved-shell extension queries
    - start/stop buttons are not rendered for UI-only packages
    - i18n labels describe the extracted DB workbench package truthfully
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
    <automated>npm run check:i18n</automated>
  </verify>
  <done>The host UI can install and manage the extracted DB workbench package without pretending it is a sidecar service.</done>
</task>

</tasks>

<verification>
- npm run check
- npm run check:i18n
- cargo check --manifest-path src-tauri/Cargo.toml -j 1
</verification>

<success_criteria>
- Phase 46 truth 1: the base app can rely on UI-only installable packages instead of builtin DB workbench registration
- Phase 46 truth 2: installing db-connector becomes a real marketplace/install action
- Phase 46 truth 3: optional sidecar metadata stays truthful across host and backend layers
</success_criteria>

<output>
After completion, create `.planning/phases/46-extract-db-workbench-into-an-on-demand-installable-extension-package/46-01-SUMMARY.md`
</output>
