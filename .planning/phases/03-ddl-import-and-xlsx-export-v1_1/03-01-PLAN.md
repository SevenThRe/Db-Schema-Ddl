---
phase: 03-ddl-import-and-xlsx-export-v1_1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - shared/routes.ts
  - client/src/hooks/use-ddl.ts
  - test/server/ddl-import-phase3.test.ts
  - test/server/ddl-export-phase3.test.ts
  - test/client/ddl-import-phase3-ui.test.tsx
autonomous: true
requirements:
  - DDLX-01
  - DDLX-02
  - DDLX-03
  - DDLX-04
user_setup: []
must_haves:
  truths:
    - "Phase 3 has one explicit typed contract for DDL preview, issue reporting, selected-table export, and template choice."
    - "MySQL-first reverse authoring is modeled explicitly and Oracle import remains deferred by contract."
    - "Blocking versus confirmable lossy issues are represented distinctly rather than buried in toast text."
  artifacts:
    - path: "shared/schema.ts"
      provides: "Typed DDL import catalog, issue taxonomy, export request/response, and remembered template choice"
      contains: "ddl"
    - path: "shared/routes.ts"
      provides: "Typed API routes for DDL preview and workbook export"
      contains: "ddl"
    - path: "client/src/hooks/use-ddl.ts"
      provides: "Typed client seams for DDL import preview and export actions"
      contains: "use"
  key_links:
    - from: "shared/routes.ts"
      to: "shared/schema.ts"
      via: "DDL import preview and export contracts"
      pattern: "ddl|import|export"
---

<objective>
Define the shared contract and Wave 0 validation seam for DDL reverse authoring before parser, export, and UI implementation begin.

Purpose: Phase 3 needs one typed language for imported SQL source, canonical review output, fidelity issues, selected-table export, and template preference. This plan creates that language first so backend and UI work do not invent incompatible local states.
Output: Shared schemas, route contracts, hook seams, and focused Wave 0 tests for DDL import/export.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-CONTEXT.md
@.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-RESEARCH.md
@.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-VALIDATION.md
@shared/schema.ts
@shared/routes.ts
@client/src/hooks/use-ddl.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed DDL import/export contracts and Wave 0 test scaffolds</name>
  <files>shared/schema.ts, test/server/ddl-import-phase3.test.ts, test/server/ddl-export-phase3.test.ts, test/client/ddl-import-phase3-ui.test.tsx</files>
  <behavior>
    - Test 1: DDL preview contracts can represent pasted SQL and uploaded DDL source modes
    - Test 2: issue taxonomy distinguishes blocking and confirmable lossy cases
    - Test 3: export contracts remain MySQL-first and do not imply Oracle import support
  </behavior>
  <action>Add Phase 3 schemas for DDL source input, normalized review catalog, issue reporting, selected-table export, template preference, and parser-backed export results. Create focused server/client tests that lock the contract before implementation spreads.</action>
  <verify>
    <automated>node --test --import tsx test/server/ddl-import-phase3.test.ts</automated>
  </verify>
  <done>Phase 3 starts from a stable typed contract for reverse authoring and fidelity reporting.</done>
</task>

<task type="auto">
  <name>Task 2: Extend typed routes and hooks for DDL preview and workbook export</name>
  <files>shared/routes.ts, client/src/hooks/use-ddl.ts</files>
  <action>Expose typed routes and hooks for DDL preview, export planning, workbook export, and remembered template choice through the existing DDL/file namespace rather than a parallel API family.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Later parser, export, and UI work can rely on typed seams instead of manual fetch logic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/server/ddl-import-phase3.test.ts`
- [ ] `node --test --import tsx test/server/ddl-export-phase3.test.ts`
- [ ] `node --test --import tsx test/client/ddl-import-phase3-ui.test.tsx`
- [ ] `npm run check`
- [ ] Oracle import remains explicitly deferred in contracts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 starts from one shared reverse-authoring contract instead of multiple local shapes
</success_criteria>

<output>
After completion, create `.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-01-SUMMARY.md`
</output>

