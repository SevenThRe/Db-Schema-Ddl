---
phase: 03-ddl-import-and-xlsx-export-v1_1
plan: 04
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - client/src/components/DdlGenerator.tsx
  - client/src/components/ddl-import/DdlImportWorkspace.tsx
  - client/src/hooks/use-ddl.ts
  - client/src/pages/Dashboard.tsx
  - test/client/ddl-import-phase3-ui.test.tsx
autonomous: true
requirements:
  - DDLX-01
  - DDLX-02
  - DDLX-03
  - DDLX-04
user_setup: []
must_haves:
  truths:
    - "Pasted SQL remains the primary Phase 3 entry path while `.sql/.ddl` upload remains available."
    - "The DDL import workflow is review-first and three-column rather than blind export."
    - "Blocking and confirmable lossy issues are visible before export and remembered template choice reduces repeated friction."
  artifacts:
    - path: "client/src/components/ddl-import/DdlImportWorkspace.tsx"
      provides: "Three-column DDL import review and export workspace"
      contains: "workspace"
    - path: "client/src/components/DdlGenerator.tsx"
      provides: "SQL-oriented viewer patterns and DDL entry integration"
      contains: "sql"
    - path: "client/src/pages/Dashboard.tsx"
      provides: "Activation path into the new DDL import workflow"
      contains: "ddl"
  key_links:
    - from: "client/src/components/ddl-import/DdlImportWorkspace.tsx"
      to: "client/src/hooks/use-ddl.ts"
      via: "preview, export, and remembered template choice hooks"
      pattern: "ddl|template"
---

<objective>
Deliver the user-facing three-column DDL import and export workflow.

Purpose: The user explicitly asked for a visible review flow where SQL source, parsed schema, and warnings/export controls are all visible together. This plan implements that workspace and keeps pasted SQL as the first-class entry path while preserving upload and subset export.
Output: Three-column DDL import workspace, source-mode controls, warning UX, and export completion flow.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-CONTEXT.md
@.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-RESEARCH.md
@.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-VALIDATION.md
@client/src/components/DdlGenerator.tsx
@client/src/hooks/use-ddl.ts
@client/src/pages/Dashboard.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the three-column DDL import review workspace</name>
  <files>client/src/components/ddl-import/DdlImportWorkspace.tsx, client/src/components/DdlGenerator.tsx, test/client/ddl-import-phase3-ui.test.tsx</files>
  <behavior>
    - Test 1: the workspace supports pasted SQL as the primary source mode and uploaded DDL as a secondary source mode
    - Test 2: left, center, and right review panes remain visible and coherent through preview and export states
    - Test 3: blocking versus confirmable lossy issues are surfaced distinctly before export
  </behavior>
  <action>Implement a dedicated DDL import workspace with source input, canonical review grid, issue presentation, selected-table export controls, and template selection that matches the agreed three-column flow.</action>
  <verify>
    <automated>node --test --import tsx test/client/ddl-import-phase3-ui.test.tsx</automated>
  </verify>
  <done>The UI shows users the original SQL, parsed schema, and export risks/settings together before any workbook is produced.</done>
</task>

<task type="auto">
  <name>Task 2: Finish export UX with remembered template choice and post-create activation</name>
  <files>client/src/hooks/use-ddl.ts, client/src/pages/Dashboard.tsx, client/src/components/ddl-import/DdlImportWorkspace.tsx</files>
  <action>Wire export completion so successful workbooks enter the normal file list and can become the active file immediately, while remembered template choice reduces repeated friction and blocked failures remain explicit and actionable.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The DDL import workflow feels native to the product rather than an isolated converter.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/client/ddl-import-phase3-ui.test.tsx`
- [ ] `npm run check`
- [ ] pasted SQL remains the primary entry path
- [ ] blocking and confirmable issues are visually distinct before export
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 ships as a review-first DDL import workflow rather than a blind converter
</success_criteria>

<output>
After completion, create `.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-04-SUMMARY.md`
</output>

