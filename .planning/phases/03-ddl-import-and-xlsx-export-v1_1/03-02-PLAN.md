---
phase: 03-ddl-import-and-xlsx-export-v1_1
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - server/lib/ddl-import/parser-adapter.ts
  - server/lib/ddl-import/normalize.ts
  - server/lib/ddl-import/issues.ts
  - server/routes/ddl-routes.ts
  - test/server/ddl-import-phase3.test.ts
autonomous: true
requirements:
  - DDLX-01
  - DDLX-02
  - DDLX-04
user_setup: []
must_haves:
  truths:
    - "Supported MySQL `CREATE TABLE` DDL is parsed through a dedicated adapter instead of ad hoc text splitting."
    - "Normalization produces a reviewable canonical catalog richer than legacy `TableInfo`."
    - "Unsupported, inexpressible, and confirmable lossy constructs are surfaced explicitly."
  artifacts:
    - path: "server/lib/ddl-import/parser-adapter.ts"
      provides: "MySQL-first DDL parsing boundary"
      contains: "parse"
    - path: "server/lib/ddl-import/normalize.ts"
      provides: "Normalization from parser output into canonical review catalog"
      contains: "normalize"
    - path: "server/routes/ddl-routes.ts"
      provides: "Preview route for parsed DDL review and issue reporting"
      contains: "preview"
  key_links:
    - from: "server/routes/ddl-routes.ts"
      to: "server/lib/ddl-import/parser-adapter.ts"
      via: "preview route delegates parsing through adapter seam"
      pattern: "ddl|preview"
---

<objective>
Implement the DDL ingestion and normalization pipeline for supported MySQL `CREATE TABLE` input.

Purpose: The phase only works if pasted or uploaded MySQL DDL can be parsed into a trustworthy review model with explicit fidelity reporting. This plan builds the parser adapter, normalization path, and issue classifier while keeping Oracle import deferred.
Output: MySQL DDL preview service, canonical review catalog, and explicit issue classification.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-CONTEXT.md
@.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-RESEARCH.md
@.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-VALIDATION.md
@server/routes/ddl-routes.ts
@server/lib/extensions/db-management/schema-normalizer.ts
@shared/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a MySQL-first parser adapter and normalization seam</name>
  <files>server/lib/ddl-import/parser-adapter.ts, server/lib/ddl-import/normalize.ts, test/server/ddl-import-phase3.test.ts</files>
  <behavior>
    - Test 1: supported MySQL `CREATE TABLE` input becomes a canonical review catalog with tables, columns, keys, indexes, comments, and defaults
    - Test 2: unsupported grammar is surfaced as explicit issues instead of silent drops
    - Test 3: Oracle-specific or non-MySQL paths remain explicitly deferred
  </behavior>
  <action>Implement a parser adapter around the chosen MySQL DDL ingestion library, normalize the result into the internal review catalog, and keep parser-specific details isolated behind the adapter boundary.</action>
  <verify>
    <automated>node --test --import tsx test/server/ddl-import-phase3.test.ts</automated>
  </verify>
  <done>Phase 3 can transform supported MySQL DDL into one stable review model without leaking parser internals.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit fidelity issue classification to preview responses</name>
  <files>server/lib/ddl-import/issues.ts, server/routes/ddl-routes.ts</files>
  <action>Classify parsed constructs into blocking, confirm-required, and informational issues, and return them from preview routes so the UI can present unsupported or lossy semantics before export.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The preview response tells users what was parsed faithfully, what needs confirmation, and what blocks export.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/server/ddl-import-phase3.test.ts`
- [ ] `npm run check`
- [ ] parser output is normalized into one canonical review shape
- [ ] unsupported constructs are explicit, not silent
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Supported MySQL DDL can be previewed in a canonical review model with explicit fidelity reporting
</success_criteria>

<output>
After completion, create `.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-02-SUMMARY.md`
</output>

