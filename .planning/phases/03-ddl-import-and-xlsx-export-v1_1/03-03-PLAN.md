---
phase: 03-ddl-import-and-xlsx-export-v1_1
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - server/lib/ddl-import/export-service.ts
  - server/lib/workbook-templates.ts
  - server/routes/ddl-routes.ts
  - server/routes/files-routes.ts
  - test/server/ddl-export-phase3.test.ts
autonomous: true
requirements:
  - DDLX-02
  - DDLX-03
  - DDLX-04
user_setup: []
must_haves:
  truths:
    - "Workbook export only targets the two official template families from Phase 2."
    - "Export can operate on all parsed tables or a selected subset."
    - "Generated workbooks are parser-validated before entering the normal file list."
  artifacts:
    - path: "server/lib/ddl-import/export-service.ts"
      provides: "Workbook export pipeline from review catalog into official templates"
      contains: "export"
    - path: "server/routes/ddl-routes.ts"
      provides: "Export route and result payload for DDL-to-workbook generation"
      contains: "export"
    - path: "server/routes/files-routes.ts"
      provides: "File registration path reused by exported workbooks"
      contains: "file"
  key_links:
    - from: "server/lib/ddl-import/export-service.ts"
      to: "server/lib/workbook-templates.ts"
      via: "official template family reuse"
      pattern: "template|workbook"
---

<objective>
Implement official-template workbook export, subset selection, and parser-backed registration for reviewed DDL imports.

Purpose: Parsing alone does not complete the reverse-authoring promise. This plan converts reviewed DDL into parser-compatible workbooks, validates those workbooks through the existing Excel parser, and only then registers them into the normal file flow.
Output: Workbook export service, export routes, subset export behavior, and parser-backed file registration.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-CONTEXT.md
@.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-RESEARCH.md
@.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-VALIDATION.md
@server/lib/workbook-templates.ts
@server/lib/excel.ts
@server/routes/files-routes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build official-template export and parser-backed round-trip validation</name>
  <files>server/lib/ddl-import/export-service.ts, server/routes/ddl-routes.ts, test/server/ddl-export-phase3.test.ts</files>
  <behavior>
    - Test 1: reviewed DDL can export to either official workbook template family
    - Test 2: selected-table subset export only includes the requested parsed tables
    - Test 3: parser-backed round-trip failure blocks file registration and success response
  </behavior>
  <action>Implement workbook rendering from the reverse-authoring catalog into the official template families, including selected-table export, remembered template choice support, and parser-backed validation before success is returned.</action>
  <verify>
    <automated>node --test --import tsx test/server/ddl-export-phase3.test.ts</automated>
  </verify>
  <done>Phase 3 can produce parser-compatible workbooks from reviewed DDL without bypassing trust validation.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse the normal file lifecycle for successful exported workbooks</name>
  <files>server/routes/files-routes.ts, server/routes/ddl-routes.ts, server/lib/workbook-templates.ts</files>
  <action>Register successful exported workbooks through the same file inventory path used by uploads and template creation so exported files appear immediately in the normal file list without a manual import loop.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>A successful DDL export yields a trusted workbook that already belongs to the product's normal file workflow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/server/ddl-export-phase3.test.ts`
- [ ] `npm run check`
- [ ] export only targets official template families
- [ ] parser-backed validation blocks registration on mismatch
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Reviewed DDL exports into parser-compatible `.xlsx` workbooks that immediately enter the normal file lifecycle
</success_criteria>

<output>
After completion, create `.planning/phases/03-ddl-import-and-xlsx-export-v1_1/03-03-SUMMARY.md`
</output>

