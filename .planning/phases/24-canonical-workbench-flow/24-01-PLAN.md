---
phase: 24-canonical-workbench-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/extensions/DbConnectorWorkspace.tsx
  - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
  - test/client/db-workbench-flow-phase24.test.ts
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
must_haves:
  truths:
    - "The DB product teaches one canonical operator route centered on Database Workspace instead of keeping legacy Schema and Diff as peer navigation."
    - "Connection context, environment cues, readonly posture, and active schema remain visible while the operator moves through the workbench."
    - "Connection management is reachable without silently discarding current workbench state."
  artifacts:
    - path: "client/src/components/extensions/DbConnectorWorkspace.tsx"
      provides: "Canonical shell routing and secondary legacy-tool access"
      contains: "Legacy tools"
    - path: "client/src/components/extensions/db-workbench/ConnectionSidebar.tsx"
      provides: "Persistent connection/schema/environment visibility"
      contains: "Switch connection"
    - path: "test/client/db-workbench-flow-phase24.test.ts"
      provides: "Static guardrails for canonical-vs-legacy route semantics"
      contains: "Database Workspace"
  key_links:
    - from: "client/src/components/extensions/DbConnectorWorkspace.tsx"
      to: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      via: "connection-management entrypoints re-open the same canonical workbench shell"
      pattern: "onManageConnections"
---

<objective>
Make the DB shell teach one canonical workflow. Operators should land in `Database Workspace` as the primary route, treat connection management as a supporting surface, and only reach legacy DB Schema/Diff tools through a clearly secondary affordance.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-canonical-workbench-flow/24-CONTEXT.md
@client/src/components/extensions/DbConnectorWorkspace.tsx
@client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
@client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse DB shell navigation onto one primary route</name>
  <read_first>
    - client/src/components/extensions/DbConnectorWorkspace.tsx
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
    - .planning/phases/24-canonical-workbench-flow/24-CONTEXT.md
  </read_first>
  <files>
    - client/src/components/extensions/DbConnectorWorkspace.tsx
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  </files>
  <action>
Replace the top-level peer navigation model in `DbConnectorWorkspace.tsx` so `Database Workspace` is the only first-class DB route. Keep `Connection Center` reachable as a supporting management surface, but stop presenting legacy DB `Schema` and `Diff` as equal navigation beside the canonical workspace. If legacy DB tools remain for migration, expose them behind an explicit secondary affordance labeled `Legacy tools` and keep the primary shell copy aligned with the unified workbench route.
  </action>
  <acceptance_criteria>
    - `client/src/components/extensions/DbConnectorWorkspace.tsx` no longer renders top-level peer buttons for legacy DB `Schema` and `Diff`
    - `client/src/components/extensions/DbConnectorWorkspace.tsx` contains a secondary `Legacy tools` affordance rather than peer primary navigation
    - the shell copy in `DbConnectorWorkspace.tsx` describes `Database Workspace` as the unified DB route
    - `WorkbenchLayout` can request connection management without forcing a route reset that discards current workbench state
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The DB shell now teaches one primary route instead of prototype-era split surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Keep active connection context visible and stable across workbench modes</name>
  <read_first>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
    - client/src/components/extensions/DbConnectorWorkspace.tsx
  </read_first>
  <files>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
    - client/src/components/extensions/DbConnectorWorkspace.tsx
  </files>
  <action>
Ensure the canonical workbench surface keeps connection identity, driver, database, environment, readonly state, and active schema visible in stable UI positions while the operator moves through results, explain, schema diff, sync, and inspection panes. Add or refine explicit callbacks and shell wiring so opening connection management or switching internal panes does not silently clear the active workbench context.
  </action>
  <acceptance_criteria>
    - `ConnectionSidebar.tsx` still exposes connection, environment, readonly, and schema cues for the active connection
    - `DbConnectorWorkspace.tsx` shows active-context metadata while the canonical workbench is open
    - `WorkbenchLayout.tsx` includes a dedicated callback or equivalent path for opening connection management from inside the workbench
    - workbench pane switching remains internal to `WorkbenchLayout.tsx` rather than routing through legacy shell states
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Operators keep one stable connection context while moving through the canonical workbench.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the canonical shell semantics with focused frontend guard tests</name>
  <read_first>
    - test/client/db-workbench-flow-phase16.test.tsx
    - client/src/components/extensions/DbConnectorWorkspace.tsx
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  </read_first>
  <files>
    - test/client/db-workbench-flow-phase24.test.ts
  </files>
  <action>
Add a focused Phase 24 static regression test file that asserts the DB shell now exposes one primary `Database Workspace` route, relegates legacy DB tools behind explicit secondary copy, and wires a workbench-to-connection-management callback instead of forcing route-level resets. Keep the assertions grep-verifiable against the actual source files so the flow contract remains easy to guard in CI.
  </action>
  <acceptance_criteria>
    - `test/client/db-workbench-flow-phase24.test.ts` exists
    - the test asserts `DbConnectorWorkspace.tsx` contains `Database Workspace`
    - the test asserts legacy DB tools are described via `Legacy tools` secondary copy rather than peer primary buttons
    - the test asserts `WorkbenchLayout.tsx` exposes a connection-management callback or equivalent canonical-shell hook
  </acceptance_criteria>
  <verify>
    <automated>NODE_OPTIONS=--import=tsx node --test --experimental-strip-types test/client/db-workbench-flow-phase24.test.ts</automated>
  </verify>
  <done>Canonical route semantics are guarded against regression.</done>
</task>

</tasks>

<verification>
- npm run check
- NODE_OPTIONS=--import=tsx node --test --experimental-strip-types test/client/db-workbench-flow-phase24.test.ts
</verification>

<success_criteria>
- FLOW-01: primary DB tasks route through one canonical `Database Workspace`
- FLOW-02: active connection, schema, environment, and readonly cues stay visible from one coherent shell
- FLOW-03: moving through workbench views no longer depends on legacy route switching
</success_criteria>

<output>
After completion, create `.planning/phases/24-canonical-workbench-flow/24-01-SUMMARY.md`
</output>
