---
phase: 24-canonical-workbench-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - client/src/components/extensions/DbConnectorWorkspace.tsx
  - client/src/components/extensions/db-workbench/workbench-session.ts
  - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  - test/client/db-workbench-session-phase24.test.ts
autonomous: true
requirements:
  - FLOW-03
  - FLOW-04
must_haves:
  truths:
    - "Per-connection workbench state restores enough context that reopen does not feel like a hidden reset."
    - "The DB shell explicitly tells the operator when the remembered connection cannot be recovered."
    - "Unavailable remembered connections fall back to Connection Center with clear recovery messaging rather than silent state loss."
  artifacts:
    - path: "client/src/components/extensions/db-workbench/workbench-session.ts"
      provides: "Extended persisted workbench session shape"
      contains: "lastResultTab"
    - path: "client/src/components/extensions/DbConnectorWorkspace.tsx"
      provides: "Last-connection resume and explicit recovery notice"
      contains: "recovery"
    - path: "test/client/db-workbench-session-phase24.test.ts"
      provides: "Session and recovery guard coverage"
      contains: "lastResultTab"
  key_links:
    - from: "client/src/components/extensions/db-workbench/workbench-session.ts"
      to: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      via: "extended per-connection session restore"
      pattern: "lastResultTab"
    - from: "client/src/components/extensions/DbConnectorWorkspace.tsx"
      to: "client/src/components/extensions/db-workbench/workbench-session.ts"
      via: "last active connection recovery and fallback notice"
      pattern: "WORKSPACE_CONNECTION_STORAGE_KEY"
---

<objective>
Make reopening the DB workbench truthful. Operators should get their last active connection and meaningful in-workbench context back when possible, and see an explicit recovery/fallback notice when the saved connection is gone or cannot be restored.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-canonical-workbench-flow/24-CONTEXT.md
@client/src/components/extensions/DbConnectorWorkspace.tsx
@client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
@client/src/components/extensions/db-workbench/workbench-session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend per-connection workbench session persistence beyond SQL tabs</name>
  <read_first>
    - client/src/components/extensions/db-workbench/workbench-session.ts
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
    - test/client/db-workbench-session-phase16.test.ts
  </read_first>
  <files>
    - client/src/components/extensions/db-workbench/workbench-session.ts
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  </files>
  <action>
Extend the persisted `WorkbenchSessionState` so reopening a connection restores not only SQL tabs and selected table, but also the last active pane, inspection target, schema diff target connection id, and sync source/target connection ids. Wire `WorkbenchLayout.tsx` to save and hydrate these fields while keeping reset behavior explicit when a restored target connection is missing from the current connection list.
  </action>
  <acceptance_criteria>
    - `client/src/components/extensions/db-workbench/workbench-session.ts` persists `lastResultTab`
    - `client/src/components/extensions/db-workbench/workbench-session.ts` persists inspection and diff/sync target metadata
    - `WorkbenchLayout.tsx` hydrates restored pane/session metadata on connection load
    - `WorkbenchLayout.tsx` explicitly normalizes missing restored connection ids instead of blindly reusing invalid values
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Reopening a connection restores the real workbench context rather than only the SQL tab text.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit last-connection recovery behavior at the DB shell level</name>
  <read_first>
    - client/src/components/extensions/DbConnectorWorkspace.tsx
    - client/src/components/extensions/db-workbench/workbench-session.ts
    - .planning/phases/24-canonical-workbench-flow/24-CONTEXT.md
  </read_first>
  <files>
    - client/src/components/extensions/DbConnectorWorkspace.tsx
  </files>
  <action>
Add explicit shell-level resume and recovery behavior for the last active DB connection. When the remembered connection still exists, reopen the canonical `Database Workspace`. When it no longer exists, fall back to `Connection Center` and surface a clear recovery notice explaining that the previous connection could not be restored. Keep this behavior local-first via existing browser storage keys rather than adding a new backend persistence layer.
  </action>
  <acceptance_criteria>
    - `DbConnectorWorkspace.tsx` attempts to restore the last active connection into `Database Workspace` when it still exists
    - `DbConnectorWorkspace.tsx` displays an explicit recovery notice when the remembered connection id no longer exists
    - the fallback route for a missing remembered connection is `Connection Center`
    - the shell no longer silently clears remembered connection state without user-visible explanation
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>App reopen behavior is explicit and trustworthy when the previous connection is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Guard restore and recovery semantics with focused session tests</name>
  <read_first>
    - test/client/db-workbench-session-phase16.test.ts
    - client/src/components/extensions/db-workbench/workbench-session.ts
    - client/src/components/extensions/DbConnectorWorkspace.tsx
  </read_first>
  <files>
    - test/client/db-workbench-session-phase24.test.ts
  </files>
  <action>
Add a focused Phase 24 session test file that locks the extended workbench session shape and the explicit recovery semantics. Cover persisted `lastResultTab`, persisted schema diff/sync target ids, and shell-level recovery copy for a missing remembered connection through static assertions against the relevant source files and storage helpers.
  </action>
  <acceptance_criteria>
    - `test/client/db-workbench-session-phase24.test.ts` exists
    - the test asserts `workbench-session.ts` contains `lastResultTab`
    - the test asserts `workbench-session.ts` contains persisted schema diff and sync target fields
    - the test asserts `DbConnectorWorkspace.tsx` contains explicit recovery copy for a missing remembered connection
  </acceptance_criteria>
  <verify>
    <automated>NODE_OPTIONS=--import=tsx node --test --experimental-strip-types test/client/db-workbench-session-phase24.test.ts</automated>
  </verify>
  <done>Restore and fallback behavior is guarded against regression.</done>
</task>

</tasks>

<verification>
- npm run check
- NODE_OPTIONS=--import=tsx node --test --experimental-strip-types test/client/db-workbench-session-phase24.test.ts
</verification>

<success_criteria>
- FLOW-03: workbench mode switching and restored state stay explicit rather than silently resetting
- FLOW-04: last active connection/workspace state reopens with clear fallback behavior when the connection is unavailable
</success_criteria>

<output>
After completion, create `.planning/phases/24-canonical-workbench-flow/24-02-SUMMARY.md`
</output>
