---
phase: 01-extension-host-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - client/src/components/Sidebar.tsx
  - client/src/pages/Dashboard.tsx
  - client/src/components/extensions/ExtensionInstallDialog.tsx
  - client/src/components/extensions/ExtensionStatusDialog.tsx
autonomous: true
requirements:
  - HOST-01
  - HOST-02
  - HOST-04
user_setup: []
must_haves:
  truths:
    - "Users always see a DB management entry in the left sidebar"
    - "When the extension is absent, the entry is greyed out with an install marker and opens a concise install prompt"
    - "Disabled and incompatible extension states are visually distinct and explainable"
  artifacts:
    - path: "client/src/components/Sidebar.tsx"
      provides: "Persistent DB management sidebar entry"
      contains: "DB"
    - path: "client/src/components/extensions/ExtensionInstallDialog.tsx"
      provides: "Concise install prompt for the official extension"
      min_lines: 30
    - path: "client/src/components/extensions/ExtensionStatusDialog.tsx"
      provides: "Disabled/incompatible state explanation and actions"
      min_lines: 30
    - path: "client/src/pages/Dashboard.tsx"
      provides: "Host integration for sidebar and extension state"
      contains: "extension"
  key_links:
    - from: "client/src/components/Sidebar.tsx"
      to: "client/src/hooks/use-extensions.ts"
      via: "sidebar status query"
      pattern: "useExtension"
    - from: "client/src/components/extensions/ExtensionInstallDialog.tsx"
      to: "electron/preload.ts"
      via: "host lifecycle action"
      pattern: "extension"
---

<objective>
Integrate the extension host UX into the dashboard sidebar and status dialogs.

Purpose: The host foundation is only real if users can discover the DB management module, understand its state, and trigger the correct host-side lifecycle actions without disturbing existing workflows.
Output: Sidebar integration, install/status dialogs, and host-state-driven UI behavior for absent, disabled, incompatible, and enabled states.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-extension-host-foundation/01-CONTEXT.md
@.planning/phases/01-extension-host-foundation/01-RESEARCH.md
@client/src/components/Sidebar.tsx
@client/src/pages/Dashboard.tsx
@client/src/hooks/use-extensions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persistent sidebar entry and state rendering</name>
  <files>client/src/components/Sidebar.tsx, client/src/pages/Dashboard.tsx</files>
  <action>Add the always-visible `DB 管理` sidebar entry and drive its presentation from extension host state. Follow the locked decisions exactly: module-level placement in the sidebar, greyed installable state when absent, and visible disabled/incompatible markers rather than hiding the entry.</action>
  <verify>Run `npm run check` and inspect the sidebar rendering logic for all four states.</verify>
  <done>The sidebar always exposes DB management with state-specific affordances.</done>
</task>

<task type="auto">
  <name>Task 2: Build concise install and status dialogs</name>
  <files>client/src/components/extensions/ExtensionInstallDialog.tsx, client/src/components/extensions/ExtensionStatusDialog.tsx</files>
  <action>Create a concise install-confirmation dialog for the absent state and a lightweight status dialog for disabled/incompatible states. Keep the copy concise, brand the module as an official extension, and wire the primary actions to host lifecycle APIs created in earlier plans.</action>
  <verify>Inspect dialog props and state transitions; run `npm run check`.</verify>
  <done>Absent, disabled, and incompatible states each have an explicit user-facing explanation and next action.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve existing dashboard behavior with no extension installed</name>
  <files>client/src/pages/Dashboard.tsx, client/src/components/Sidebar.tsx</files>
  <action>Ensure the new sidebar module entry does not change file selection, sheet workflows, DDL generation, or diff flows when the extension is absent. Keep the extension host UI additive and avoid making current users traverse extension logic to continue their existing work.</action>
  <verify>Check that no existing dashboard modes are blocked and run `npm run check`.</verify>
  <done>The extension host UX is additive; the base product still behaves normally without any extension installed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] Sidebar exposes DB management in absent, enabled, disabled, and incompatible states
- [ ] Existing dashboard workflows still render with no extension installed
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can discover extension state from the sidebar without losing access to existing workflows
</success_criteria>

<output>
After completion, create `.planning/phases/01-extension-host-foundation/01-04-SUMMARY.md`
</output>

