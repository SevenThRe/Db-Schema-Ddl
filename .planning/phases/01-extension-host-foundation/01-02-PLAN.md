---
phase: 01-extension-host-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - electron/main.ts
  - electron/preload.ts
  - electron/extensions.ts
autonomous: true
requirements:
  - HOST-03
  - HOST-04
user_setup: []
must_haves:
  truths:
    - "The Electron host has a dedicated extension lifecycle service instead of ad hoc updater reuse"
    - "The renderer only receives narrow extension lifecycle APIs through preload"
    - "The host can trigger a controlled relaunch/reload path for extension activation"
  artifacts:
    - path: "electron/extensions.ts"
      provides: "Extension lifecycle orchestration in the main process"
      min_lines: 40
    - path: "electron/preload.ts"
      provides: "Narrow extension host bridge"
      contains: "extension"
    - path: "electron/main.ts"
      provides: "Electron-side registration of extension lifecycle handlers"
      contains: "extension"
  key_links:
    - from: "electron/preload.ts"
      to: "electron/main.ts"
      via: "ipcRenderer.invoke to ipcMain.handle"
      pattern: "extensions:"
    - from: "electron/main.ts"
      to: "electron/extensions.ts"
      via: "main-process lifecycle orchestration"
      pattern: "extension"
---

<objective>
Add the Electron-side extension host service and lifecycle bridge.

Purpose: The extension host needs a desktop-native place to own install roots, activation/relaunch behavior, and privileged lifecycle actions without exposing unsafe renderer access.
Output: A new Electron extension service module plus preload APIs and main-process handler wiring.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-extension-host-foundation/01-CONTEXT.md
@.planning/phases/01-extension-host-foundation/01-RESEARCH.md
@electron/main.ts
@electron/preload.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Electron extension host service</name>
  <files>electron/extensions.ts, electron/main.ts</files>
  <action>Create a dedicated Electron-side module for extension install roots, status lookup, relaunch/reload activation, and v1-safe lifecycle commands. Keep actual GitHub download logic out of this phase, but define the host API surface that later phases will call.</action>
  <verify>Review the new service exports and run `npm run check`.</verify>
  <done>Main-process extension lifecycle logic lives in a focused module instead of being scattered across updater code.</done>
</task>

<task type="auto">
  <name>Task 2: Expose narrow preload APIs for extension lifecycle</name>
  <files>electron/preload.ts, electron/main.ts</files>
  <action>Add purpose-built preload methods for reading extension status, opening the install prompt flow, enabling/disabling an installed extension, and triggering the relaunch/reload activation path. Do not expose raw IPC primitives; follow the existing contextBridge style.</action>
  <verify>Confirm preload API shape is narrow and `npm run check` passes.</verify>
  <done>The renderer has a safe and typed extension lifecycle bridge.</done>
</task>

<task type="auto">
  <name>Task 3: Separate extension lifecycle from app updater flow</name>
  <files>electron/extensions.ts, electron/main.ts, electron/updater.ts</files>
  <action>Keep the extension lifecycle adjacent to but separate from the app updater. Reuse the interaction pattern conceptually, but do not overload app-update channels for extension actions. Leave clear boundaries so Phase 2 can add GitHub delivery without entangling app updates and extension installs.</action>
  <verify>Inspect the handler names and module responsibilities for a clean separation of concerns.</verify>
  <done>Extension host lifecycle has its own orchestration path and does not piggyback on the updater implementation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] Extension lifecycle IPC handlers are action-specific
- [ ] Relaunch/reload path is reachable from the host API
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Extension lifecycle is owned by Electron without weakening preload security
</success_criteria>

<output>
After completion, create `.planning/phases/01-extension-host-foundation/01-02-SUMMARY.md`
</output>

