---
phase: 01-extension-host-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - server/routes.ts
  - server/routes/extensions-routes.ts
  - server/lib/extensions/registry.ts
  - client/src/hooks/use-extensions.ts
autonomous: true
requirements:
  - HOST-01
  - HOST-03
  - HOST-04
user_setup: []
must_haves:
  truths:
    - "The app can query extension host status through typed server endpoints"
    - "The DB management extension appears as a known first-party module even before installation"
    - "Core app flows remain available regardless of extension state"
  artifacts:
    - path: "server/routes/extensions-routes.ts"
      provides: "Extension host status and lifecycle endpoints"
      min_lines: 40
    - path: "server/lib/extensions/registry.ts"
      provides: "Official extension registry and state resolution"
      min_lines: 30
    - path: "client/src/hooks/use-extensions.ts"
      provides: "Client data hooks for extension host state"
      min_lines: 30
  key_links:
    - from: "server/routes.ts"
      to: "server/routes/extensions-routes.ts"
      via: "route registration"
      pattern: "extensions"
    - from: "client/src/hooks/use-extensions.ts"
      to: "server/routes/extensions-routes.ts"
      via: "typed fetch flow"
      pattern: "extensions"
---

<objective>
Add server-side extension registry and typed host APIs.

Purpose: The sidebar and Electron lifecycle bridge need a single source of truth for extension states, known-extension metadata, and user-visible lifecycle actions.
Output: Extension routes, a registry/state service, and client hooks for consuming extension host status.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-extension-host-foundation/01-CONTEXT.md
@.planning/phases/01-extension-host-foundation/01-RESEARCH.md
@server/routes.ts
@client/src/hooks/use-ddl.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build an official extension registry service</name>
  <files>server/lib/extensions/registry.ts, server/storage.ts</files>
  <action>Create a focused service that resolves the DB management extension slot into user-visible host states: not installed, enabled, disabled, incompatible. Keep the registry first-party and local for Phase 1; do not fetch GitHub catalog data yet.</action>
  <verify>Inspect status resolution logic and run `npm run check`.</verify>
  <done>A server-side registry can answer the current extension state without any phase-2 network work.</done>
</task>

<task type="auto">
  <name>Task 2: Expose typed extension host endpoints</name>
  <files>server/routes/extensions-routes.ts, server/routes.ts, shared/routes.ts</files>
  <action>Register extension host endpoints for reading known extensions, reading the DB management extension status, and invoking local lifecycle actions that belong to Phase 1. Keep response shapes aligned with the new shared route contract so client hooks can remain strongly typed.</action>
  <verify>Confirm route registration and `npm run check` success.</verify>
  <done>Extension host status is available through the same typed API pattern as the rest of the app.</done>
</task>

<task type="auto">
  <name>Task 3: Add client-side hooks for host status</name>
  <files>client/src/hooks/use-extensions.ts, client/src/hooks/use-ddl.ts</files>
  <action>Create dedicated hooks for extension host state instead of overloading unrelated file/DDL hooks. Reuse the current fetch + Zod parse pattern so the sidebar can consume install-state, disable-state, and compatibility-state cleanly.</action>
  <verify>Inspect hook usage and run `npm run check`.</verify>
  <done>The client has a focused extension-host data layer ready for UI integration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] Extension routes register cleanly with the existing server
- [ ] Client hooks parse extension responses through typed contracts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Host status is available to the UI without coupling it to future download logic
</success_criteria>

<output>
After completion, create `.planning/phases/01-extension-host-foundation/01-03-SUMMARY.md`
</output>

