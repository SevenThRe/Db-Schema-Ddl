---
phase: 01-extension-host-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - shared/routes.ts
  - server/storage.ts
  - server/init-db.ts
autonomous: true
requirements:
  - HOST-01
  - HOST-03
user_setup: []
must_haves:
  truths:
    - "The core app has a typed first-party extension state model persisted in desktop storage"
    - "Extension status can represent not installed, enabled, disabled, and incompatible"
    - "The base app can reason about a known DB management extension slot without the extension being installed"
  artifacts:
    - path: "shared/schema.ts"
      provides: "Extension host data model and validation schema"
      contains: "extension"
    - path: "shared/routes.ts"
      provides: "Typed extension host API contract"
      contains: "extensions"
    - path: "server/storage.ts"
      provides: "Persistence methods for extension host state"
      contains: "extension"
    - path: "server/init-db.ts"
      provides: "SQLite initialization support for extension host state"
  key_links:
    - from: "shared/routes.ts"
      to: "shared/schema.ts"
      via: "route input/output schemas"
      pattern: "extension"
    - from: "server/storage.ts"
      to: "shared/schema.ts"
      via: "storage table and type usage"
      pattern: "extension"
---

<objective>
Create the shared extension-host contract and persistence foundation.

Purpose: Every later host capability depends on a stable status model, typed API surface, and persistent state for official extensions.
Output: Extension-related schemas, route contracts, and storage methods that describe the official DB management extension even when it is absent.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-extension-host-foundation/01-CONTEXT.md
@.planning/phases/01-extension-host-foundation/01-RESEARCH.md
@shared/schema.ts
@shared/routes.ts
@server/storage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model the official extension host domain</name>
  <files>shared/schema.ts, shared/routes.ts</files>
  <action>Add extension-host types and storage schemas for the first-party DB management extension. Include status enums, manifest/catalog summaries, host actions, and typed route contracts for listing known extensions and reading current status. Keep the design official-only for v1, but avoid naming that would block future extension IDs.</action>
  <verify>Confirm new schemas and routes are imported cleanly and `npm run check` passes.</verify>
  <done>Shared schemas and route contracts exist for extension host status and lifecycle actions.</done>
</task>

<task type="auto">
  <name>Task 2: Add extension persistence to the storage layer</name>
  <files>server/storage.ts, server/init-db.ts</files>
  <action>Extend the storage abstraction with CRUD/read methods for installed extension state, active version, compatibility status, and known-extension metadata. Wire SQLite initialization so the desktop app can persist extension records without requiring the extension package itself to exist.</action>
  <verify>Inspect storage interfaces/implementations for matching methods and run `npm run check`.</verify>
  <done>Storage layer can persist and retrieve extension host records for the DB management extension slot.</done>
</task>

<task type="auto">
  <name>Task 3: Seed a first-party extension slot contract</name>
  <files>shared/schema.ts, shared/routes.ts, server/storage.ts</files>
  <action>Define the canonical first-party extension identity for the DB management module and ensure the host can report its state as "not installed" before any download flow exists. Avoid mixing actual GitHub download details into this phase; keep the output focused on local host readiness.</action>
  <verify>Trace the code path for a not-installed extension state and confirm no phase-2 download assumptions are required.</verify>
  <done>The host can represent an official extension slot even when no extension asset has been installed yet.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] Shared route contracts compile with new extension schemas
- [ ] Storage layer exposes typed extension-host methods
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Host state model is available before any GitHub delivery logic exists
</success_criteria>

<output>
After completion, create `.planning/phases/01-extension-host-foundation/01-01-SUMMARY.md`
</output>

