---
phase: 01-electron-stability-and-real-env-smoke-v1_3
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - electron/main.ts
  - electron/preload.ts
  - electron/extensions.ts
  - electron/github-release.ts
  - client/src/hooks/use-extensions.ts
  - test/electron/extensions-delivery.test.ts
  - test/electron/electron-runtime-phase1.test.ts
autonomous: true
requirements:
  - STAB-01
  - STAB-02
user_setup: []
must_haves:
  truths:
    - "Electron main owns fatal-path reporting and duplicate-dialog suppression."
    - "User-facing extension/runtime messages are translated at the boundary."
    - "Persistent logs do not depend on renderer health."
---

<objective>
Harden the Electron runtime path so startup, shutdown, and extension failures stop leaking raw JS noise and reliably write diagnostics.

Purpose: This plan converts the current ad-hoc runtime handling into one consistent operational layer without changing product scope.
Output: Hardened Electron main/runtime error handling, translated boundary messages, and focused runtime tests.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-electron-stability-and-real-env-smoke-v1_3/01-CONTEXT.md
@.planning/phases/01-electron-stability-and-real-env-smoke-v1_3/01-RESEARCH.md
@electron/main.ts
@electron/extensions.ts
@electron/github-release.ts
@client/src/hooks/use-extensions.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unify fatal-path reporting in Electron main</name>
  <read_first>
    - electron/main.ts
    - .planning/phases/01-electron-stability-and-real-env-smoke-v1_3/01-RESEARCH.md
  </read_first>
  <files>electron/main.ts, test/electron/electron-runtime-phase1.test.ts</files>
  <behavior>
    - Test 1: startup/shutdown helpers log deterministic checkpoints
    - Test 2: duplicate user dialogs are suppressed during shutdown
    - Test 3: unhandled errors still reach persistent logs
  </behavior>
  <action>Refactor `electron/main.ts` around one shared fatal-path reporter plus checkpoint logging for startup/shutdown. Ensure shutdown-path errors do not spam dialogs while still being logged immediately.</action>
  <acceptance_criteria>
    - `electron/main.ts` contains a shared fatal-path reporting helper
    - shutdown-path duplicate dialog suppression is explicit
    - `node --test --import tsx test/electron/electron-runtime-phase1.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/electron/electron-runtime-phase1.test.ts</automated>
  </verify>
  <done>Electron startup and shutdown are governed by one consistent reporting path.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize user-facing extension/runtime boundary messages</name>
  <read_first>
    - electron/extensions.ts
    - electron/github-release.ts
    - client/src/hooks/use-extensions.ts
  </read_first>
  <files>electron/extensions.ts, electron/github-release.ts, client/src/hooks/use-extensions.ts, test/electron/extensions-delivery.test.ts</files>
  <action>Ensure extension/runtime errors crossing the Electron boundary are translated into user-friendly product messages while logs retain technical detail. Extend tests so known catalog/release failure modes stay normalized.</action>
  <acceptance_criteria>
    - raw `Error invoking remote method` text is not the intended user-facing message path
    - extension catalog fallback messaging remains covered by focused tests
    - `node --test --import tsx test/electron/extensions-delivery.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/electron/extensions-delivery.test.ts</automated>
  </verify>
  <done>Runtime/extension failures are understandable to users without losing technical diagnostics.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/electron/electron-runtime-phase1.test.ts`
- [ ] `node --test --import tsx test/electron/extensions-delivery.test.ts`
- [ ] `npm run check`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Startup/shutdown and extension failures are calmer for users and easier to debug locally
</success_criteria>

<output>
After completion, create `.planning/phases/01-electron-stability-and-real-env-smoke-v1_3/01-02-SUMMARY.md`
</output>
