---
phase: 01-electron-stability-and-real-env-smoke-v1_3
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - package.json
  - script/build.ts
  - server/constants/db-init.ts
  - script/desktop-preflight.ts
  - test/electron/electron-preflight-phase1.test.ts
autonomous: true
requirements:
  - STAB-03
user_setup: []
must_haves:
  truths:
    - "Known fragile seams fail early and clearly instead of surfacing late as opaque runtime crashes."
    - "Preflight/release guards stay narrow and desktop-specific."
---

<objective>
Add targeted release guards for the exact desktop seams already proven fragile: native modules, migration compatibility, and extension-catalog fallback.

Purpose: This plan prevents recurrence of known startup/runtime failures without expanding into broad CI or infrastructure work.
Output: Targeted build/start/preflight guards with focused tests.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-electron-stability-and-real-env-smoke-v1_3/01-CONTEXT.md
@.planning/phases/01-electron-stability-and-real-env-smoke-v1_3/01-RESEARCH.md
@package.json
@script/build.ts
@server/constants/db-init.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add narrow desktop preflight checks for native modules and migration compatibility</name>
  <read_first>
    - package.json
    - script/build.ts
    - server/constants/db-init.ts
  </read_first>
  <files>package.json, script/build.ts, server/constants/db-init.ts, script/desktop-preflight.ts, test/electron/electron-preflight-phase1.test.ts</files>
  <behavior>
    - Test 1: missing native-module expectations fail with a clear actionable message
    - Test 2: migration compatibility assumptions are checked explicitly
    - Test 3: preflight output is deterministic enough for release usage
  </behavior>
  <action>Add a narrow desktop preflight script and wire it into the build/start path only where it adds value. The checks should stay focused on native-module readiness, migration compatibility, and similarly proven fragile seams.</action>
  <acceptance_criteria>
    - a dedicated desktop preflight script exists
    - the script checks native-module and migration assumptions
    - `node --test --import tsx test/electron/electron-preflight-phase1.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/electron/electron-preflight-phase1.test.ts</automated>
  </verify>
  <done>The desktop runtime gets a small but meaningful release guard instead of failing late.</done>
</task>

<task type="auto">
  <name>Task 2: Keep catalog fallback and runtime guard behavior aligned with release scripts</name>
  <read_first>
    - electron/github-release.ts
    - package.json
  </read_first>
  <files>package.json, script/build.ts, script/desktop-preflight.ts</files>
  <action>Ensure the new guard path and current build/start scripts stay aligned so developers hit actionable failures during release and local runtime preparation rather than opaque crashes after launch.</action>
  <acceptance_criteria>
    - build/start flow documentation in scripts is consistent with new guard behavior
    - `npm run check` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Desktop guardrails are wired into the existing release path without broadening scope.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/electron/electron-preflight-phase1.test.ts`
- [ ] `npm run check`
- [ ] `npm run build`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Known fragile desktop seams fail early and clearly
</success_criteria>

<output>
After completion, create `.planning/phases/01-electron-stability-and-real-env-smoke-v1_3/01-03-SUMMARY.md`
</output>
