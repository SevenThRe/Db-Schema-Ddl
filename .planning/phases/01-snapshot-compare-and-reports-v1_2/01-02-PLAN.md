---
phase: 01-snapshot-compare-and-reports-v1_2
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - server/lib/extensions/db-management/history-service.ts
  - server/lib/extensions/db-management/graph-service.ts
  - server/routes/db-management-routes.ts
  - test/server/db-snapshot-phase1.test.ts
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
user_setup: []
must_haves:
  truths:
    - "Cross-connection snapshot compare is supported through a dual-source compare service."
    - "Live freshness behavior is explicit and preserved in compare context."
    - "Markdown and JSON reports are projections of the same compare artifact."
  artifacts:
    - path: "server/lib/extensions/db-management/history-service.ts"
      provides: "arbitrary source resolution and snapshot compare artifact construction"
      contains: "history"
    - path: "server/routes/db-management-routes.ts"
      provides: "route handlers for snapshot compare and report export"
      contains: "history"
    - path: "server/lib/extensions/db-management/graph-service.ts"
      provides: "graph/report reuse from the stable compare artifact where needed"
      contains: "graph"
---

<objective>
Implement the backend compare and report pipeline on top of the new snapshot-compare artifact.

Purpose: Phase 1's real value is arbitrary history/source comparison plus review/handoff exports. This plan makes the service layer support cross-history source resolution, explicit live freshness choices, and report generation without forking away from existing compare logic.
Output: Backend snapshot-compare service, new route handlers, and report export pipeline.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-snapshot-compare-and-reports-v1_2/01-CONTEXT.md
@.planning/phases/01-snapshot-compare-and-reports-v1_2/01-RESEARCH.md
@.planning/phases/01-snapshot-compare-and-reports-v1_2/01-VALIDATION.md
@server/lib/extensions/db-management/history-service.ts
@server/lib/extensions/db-management/graph-service.ts
@server/routes/db-management-routes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize history compare into dual-source snapshot compare</name>
  <files>server/lib/extensions/db-management/history-service.ts, test/server/db-snapshot-phase1.test.ts</files>
  <behavior>
    - Test 1: cross-connection `snapshot vs snapshot` comparisons are allowed
    - Test 2: `live vs snapshot` preserves explicit freshness context in the compare artifact
    - Test 3: existing single-connection timeline listing/detail behavior remains intact
  </behavior>
  <action>Extract and generalize the current history compare logic so arbitrary left/right source pairs can be resolved into one stable compare artifact. Remove the old cross-connection blocker for snapshot compare while keeping timeline/detail flows backward compatible.</action>
  <verify>
    <automated>node --test --import tsx test/server/db-snapshot-phase1.test.ts</automated>
  </verify>
  <done>Snapshot compare works across saved histories without creating a second compare engine.</done>
</task>

<task type="auto">
  <name>Task 2: Add report export handlers that project from the compare artifact</name>
  <files>server/lib/extensions/db-management/history-service.ts, server/routes/db-management-routes.ts, server/lib/extensions/db-management/graph-service.ts</files>
  <action>Create report export handlers for Markdown and JSON that project from the same compare artifact used by the UI. Preserve enough source/target/version context for handoff and future MCP consumers, and avoid report generation that depends on current client tree state.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The backend can serve both human-facing and machine-facing report outputs from one compare result.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/server/db-snapshot-phase1.test.ts`
- [ ] `npm run check`
- [ ] cross-connection snapshot compare works
- [ ] report outputs project from the compare artifact rather than UI state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Compare/report service reuse is preserved instead of branching into a second subsystem
</success_criteria>

<output>
After completion, create `.planning/phases/01-snapshot-compare-and-reports-v1_2/01-02-SUMMARY.md`
</output>
