---
phase: 01-snapshot-compare-and-reports-v1_2
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - client/src/components/db-management/DbManagementWorkspace.tsx
  - client/src/components/db-management/DbSnapshotCompareWorkspace.tsx
  - client/src/hooks/use-db-management.ts
  - test/client/db-snapshot-phase1-ui.test.tsx
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
user_setup: []
must_haves:
  truths:
    - "Snapshot Compare is a new main view inside DB 管理."
    - "The compare workspace is dual-source and supports explicit live freshness choices."
    - "Report export is discoverable from the compare workspace, not hidden in timeline details."
  artifacts:
    - path: "client/src/components/db-management/DbSnapshotCompareWorkspace.tsx"
      provides: "dedicated snapshot compare workspace"
      contains: "snapshot"
    - path: "client/src/components/db-management/DbManagementWorkspace.tsx"
      provides: "main view integration and navigation"
      contains: "history"
    - path: "test/client/db-snapshot-phase1-ui.test.tsx"
      provides: "structural coverage for new workspace and view model"
      contains: "snapshot"
---

<objective>
Add the new Snapshot Compare main view and its dual-source compare workflow to `DB 管理`.

Purpose: Users need a workspace for arbitrary snapshot comparison that does not overload the existing timeline view. This plan introduces the dedicated compare workspace with source selectors, freshness controls, diff summary/detail, and report export entry points.
Output: New snapshot compare view integrated into DB management.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-snapshot-compare-and-reports-v1_2/01-CONTEXT.md
@.planning/phases/01-snapshot-compare-and-reports-v1_2/01-RESEARCH.md
@.planning/phases/01-snapshot-compare-and-reports-v1_2/01-VALIDATION.md
@client/src/components/db-management/DbManagementWorkspace.tsx
@client/src/components/db-management/DbHistoryPanel.tsx
@client/src/components/db-management/DbVsDbWorkspace.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Snapshot Compare as a new DB-management main view</name>
  <files>client/src/components/db-management/DbManagementWorkspace.tsx, client/src/components/db-management/DbSnapshotCompareWorkspace.tsx, test/client/db-snapshot-phase1-ui.test.tsx</files>
  <behavior>
    - Test 1: `Snapshot Compare` appears as a new main view alongside existing DB-management views
    - Test 2: the new workspace has independent left/right source selectors
    - Test 3: live freshness controls are explicit and visible in the workspace
  </behavior>
  <action>Create a dedicated `DbSnapshotCompareWorkspace` and wire it into the DB-management main view set. Reuse the project's three-column workspace style and keep source selection, summary/tree, and detail/export concerns clearly separated.</action>
  <verify>
    <automated>node --test --import tsx test/client/db-snapshot-phase1-ui.test.tsx</automated>
  </verify>
  <done>The product gains a dedicated snapshot compare surface without collapsing it into the History tab.</done>
</task>

<task type="auto">
  <name>Task 2: Wire compare execution and report export into the new workspace</name>
  <files>client/src/components/db-management/DbSnapshotCompareWorkspace.tsx, client/src/hooks/use-db-management.ts</files>
  <action>Bind the workspace to the new compare/report APIs, including source swapping, explicit live freshness mode, compare execution, result detail rendering, and Markdown/JSON export actions.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Users can run arbitrary snapshot comparisons and export the result from one coherent workspace.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/client/db-snapshot-phase1-ui.test.tsx`
- [ ] `npm run check`
- [ ] snapshot compare is a new main view, not just more logic inside History
- [ ] report export is reachable from the compare workspace
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Snapshot compare has a dedicated workspace consistent with prior DB-management UX decisions
</success_criteria>

<output>
After completion, create `.planning/phases/01-snapshot-compare-and-reports-v1_2/01-03-SUMMARY.md`
</output>
