---
phase: 01-snapshot-compare-and-reports-v1_2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - shared/routes.ts
  - client/src/hooks/use-db-management.ts
  - test/server/db-snapshot-phase1.test.ts
  - test/client/db-snapshot-phase1-ui.test.tsx
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
user_setup: []
must_haves:
  truths:
    - "Phase 1 starts from one stable snapshot-compare artifact rather than UI-only trees."
    - "Compare/report outputs are schema-backed and AI/MCP-friendly."
    - "Single-connection history routes remain stable while new snapshot-compare/report seams are introduced."
  artifacts:
    - path: "shared/schema.ts"
      provides: "snapshot compare artifact, report export, and stable machine-friendly schemas"
      contains: "history"
    - path: "shared/routes.ts"
      provides: "typed snapshot-compare and report-export route contracts"
      contains: "dbManagement"
    - path: "client/src/hooks/use-db-management.ts"
      provides: "typed hook seams for snapshot compare and report export"
      contains: "useDb"
  key_links:
    - from: "shared/routes.ts"
      to: "shared/schema.ts"
      via: "snapshot compare and report contracts"
      pattern: "history|snapshot|report"
---

<objective>
Define the shared language for arbitrary snapshot comparison and report export before backend/UI implementation spreads.

Purpose: Phase 1 needs a stable compare artifact that can drive UI, Markdown export, JSON export, and future MCP behaviors. This plan creates that contract first so later service and workspace work do not invent incompatible local shapes.
Output: Shared schemas, route contracts, hook seams, and Wave 0 tests for snapshot compare and reports.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-snapshot-compare-and-reports-v1_2/01-CONTEXT.md
@.planning/phases/01-snapshot-compare-and-reports-v1_2/01-RESEARCH.md
@.planning/phases/01-snapshot-compare-and-reports-v1_2/01-VALIDATION.md
@shared/schema.ts
@shared/routes.ts
@client/src/hooks/use-db-management.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add snapshot-compare artifact and report-export contracts</name>
  <files>shared/schema.ts, test/server/db-snapshot-phase1.test.ts, test/client/db-snapshot-phase1-ui.test.tsx</files>
  <behavior>
    - Test 1: snapshot compare supports arbitrary `snapshot vs snapshot` and `live vs snapshot` source pairs
    - Test 2: compare artifact includes stable source descriptors, summary, table changes, column changes, and stable keys
    - Test 3: report export contracts support Markdown and task-friendly JSON outputs
  </behavior>
  <action>Add Phase 1 schemas for snapshot-compare artifact, explicit freshness context, report export requests/responses, and any lightweight workspace state needed by the client. Create focused phase tests that encode AI/MCP-friendly expectations without depending on a full UI implementation.</action>
  <verify>
    <automated>node --test --import tsx test/server/db-snapshot-phase1.test.ts</automated>
  </verify>
  <done>Phase 1 has one stable typed artifact for comparison and reports before service work begins.</done>
</task>

<task type="auto">
  <name>Task 2: Extend DB-management route and hook seams for snapshot compare and report export</name>
  <files>shared/routes.ts, client/src/hooks/use-db-management.ts</files>
  <action>Expose typed DB-management routes and hooks for snapshot compare execution and report export. Keep single-connection history routes intact, and add a dedicated route family for arbitrary compare/report operations that are not awkwardly keyed by one connection id in the URL.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Backend and UI implementation can build on typed compare/report seams instead of manual fetch logic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/server/db-snapshot-phase1.test.ts`
- [ ] `node --test --import tsx test/client/db-snapshot-phase1-ui.test.tsx`
- [ ] `npm run check`
- [ ] compare/report contracts remain schema-backed and task-friendly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Snapshot compare starts from one shared artifact instead of multiple local shapes
</success_criteria>

<output>
After completion, create `.planning/phases/01-snapshot-compare-and-reports-v1_2/01-01-SUMMARY.md`
</output>
