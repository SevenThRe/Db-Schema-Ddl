---
phase: 44-build-vs-code-style-extension-activity-bar-and-secondary-sidebar-host
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src-tauri/src/builtin_extensions/mod.rs
  - client/src/extensions/builtin/register-all.tsx
  - client/src/extensions/sidebar-view-registry.ts
  - client/src/components/extensions/DbConnectorWorkspace.tsx
  - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
  - client/src/components/extensions/db-workbench/sidebar/DbConnectionsSidebarView.tsx
  - client/src/components/extensions/db-workbench/sidebar/DbExplorerSidebarView.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The `db-connector` extension becomes the first adopter of host-managed activity and secondary sidebar contributions."
    - "The new extension shell does not render a duplicate left sidebar inside `WorkbenchLayout`."
    - "DB connector sidebar tabs map to concrete contribution ids and do not depend on footer buttons or one monolithic panel assumption."
  artifacts:
    - path: "src-tauri/src/builtin_extensions/mod.rs"
      provides: "Builtin shell contribution ids for the DB connector"
      contains: "db-connector-sidebar-connections"
    - path: "client/src/extensions/sidebar-view-registry.ts"
      provides: "Registry for extension-contributed sidebar view components"
      contains: "registerSidebarView"
    - path: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      provides: "Workbench host mode that suppresses duplicate internal sidebar chrome"
      contains: "sidebarMode"
    - path: "client/src/components/extensions/db-workbench/sidebar/DbConnectionsSidebarView.tsx"
      provides: "DB connector secondary sidebar tab for connection-centric controls"
      contains: "function DbConnectionsSidebarView"
  key_links:
    - from: "src-tauri/src/builtin_extensions/mod.rs"
      to: "client/src/extensions/sidebar-view-registry.ts"
      via: "sidebar view ids must resolve to registered React components"
      pattern: "db-connector-sidebar-connections"
    - from: "client/src/components/extensions/DbConnectorWorkspace.tsx"
      to: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      via: "host-managed sidebar mode suppresses duplicate internal sidebar"
      pattern: "sidebarMode=\"host\""
---

<objective>
Make `db-connector` the first real extension on the new shell. The Database activity should own secondary sidebar tabs and a host-managed workbench layout instead of behaving like one footer-launched panel.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-build-vs-code-style-extension-activity-bar-and-secondary-sidebar-host/44-CONTEXT.md
@src-tauri/src/builtin_extensions/mod.rs
@client/src/extensions/builtin/register-all.tsx
@client/src/components/extensions/DbConnectorWorkspace.tsx
@client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
@client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give the DB connector explicit host-shell contribution ids</name>
  <read_first>
    - src-tauri/src/builtin_extensions/mod.rs
    - .planning/phases/44-build-vs-code-style-extension-activity-bar-and-secondary-sidebar-host/44-CONTEXT.md
  </read_first>
  <files>
    - src-tauri/src/builtin_extensions/mod.rs
  </files>
  <action>
Update the builtin `db-connector` manifest in `src-tauri/src/builtin_extensions/mod.rs` so it contributes explicit shell ids:
`db-connector-activity`,
`db-connector-sidebar-connections`,
`db-connector-sidebar-explorer`,
and `db-connector-workbench`.
Make the activity item label `Database`, set the default sidebar view id to `db-connector-sidebar-connections`, and set the default workbench view id to `db-connector-workbench`.
  </action>
  <acceptance_criteria>
    - `src-tauri/src/builtin_extensions/mod.rs` contains `db-connector-activity`
    - `src-tauri/src/builtin_extensions/mod.rs` contains `db-connector-sidebar-connections`
    - `src-tauri/src/builtin_extensions/mod.rs` contains `db-connector-sidebar-explorer`
    - `src-tauri/src/builtin_extensions/mod.rs` contains `db-connector-workbench`
    - `src-tauri/src/builtin_extensions/mod.rs` contains `Database`
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The DB connector now declares explicit shell contribution ids for the new host.</done>
</task>

<task type="auto">
  <name>Task 2: Register DB connector sidebar views and mount them through a sidebar-view registry</name>
  <read_first>
    - client/src/extensions/builtin/register-all.tsx
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
    - client/src/components/extensions/DbConnectorWorkspace.tsx
  </read_first>
  <files>
    - client/src/extensions/sidebar-view-registry.ts
    - client/src/extensions/builtin/register-all.tsx
    - client/src/components/extensions/db-workbench/sidebar/DbConnectionsSidebarView.tsx
    - client/src/components/extensions/db-workbench/sidebar/DbExplorerSidebarView.tsx
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
  </files>
  <action>
Create `client/src/extensions/sidebar-view-registry.ts` with `registerSidebarView` and `getSidebarView` helpers. Add DB connector sidebar view components at `client/src/components/extensions/db-workbench/sidebar/DbConnectionsSidebarView.tsx` and `client/src/components/extensions/db-workbench/sidebar/DbExplorerSidebarView.tsx`, reusing and extracting the relevant connection and explorer content from `ConnectionSidebar.tsx`. Register those sidebar views from `client/src/extensions/builtin/register-all.tsx` so the host shell can mount them by contribution id.
  </action>
  <acceptance_criteria>
    - `client/src/extensions/sidebar-view-registry.ts` exists
    - `client/src/extensions/sidebar-view-registry.ts` contains `registerSidebarView`
    - `client/src/components/extensions/db-workbench/sidebar/DbConnectionsSidebarView.tsx` exists
    - `client/src/components/extensions/db-workbench/sidebar/DbConnectionsSidebarView.tsx` contains `function DbConnectionsSidebarView`
    - `client/src/components/extensions/db-workbench/sidebar/DbExplorerSidebarView.tsx` exists
    - `client/src/components/extensions/db-workbench/sidebar/DbExplorerSidebarView.tsx` contains `function DbExplorerSidebarView`
    - `client/src/extensions/builtin/register-all.tsx` registers the DB connector sidebar views
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The host can mount DB connector secondary sidebar tabs through an explicit registry path.</done>
</task>

<task type="auto">
  <name>Task 3: Add a host-managed sidebar mode to the DB workbench so the new shell does not duplicate left chrome</name>
  <read_first>
    - client/src/components/extensions/DbConnectorWorkspace.tsx
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
  </read_first>
  <files>
    - client/src/components/extensions/DbConnectorWorkspace.tsx
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  </files>
  <action>
Add a prop named `sidebarMode?: "host" | "embedded"` to `client/src/components/extensions/db-workbench/WorkbenchLayout.tsx` with default `embedded`. When `sidebarMode === "host"`, suppress the internal `ConnectionSidebar` rendering and rely on the host-managed secondary sidebar instead. Update `client/src/components/extensions/DbConnectorWorkspace.tsx` so the extension-shell path passes `sidebarMode="host"` while any legacy fallback path can continue using the embedded sidebar during the transition.
  </action>
  <acceptance_criteria>
    - `client/src/components/extensions/db-workbench/WorkbenchLayout.tsx` contains `sidebarMode?: "host" | "embedded"`
    - `client/src/components/extensions/db-workbench/WorkbenchLayout.tsx` contains `sidebarMode === "host"`
    - `client/src/components/extensions/db-workbench/WorkbenchLayout.tsx` suppresses `ConnectionSidebar` when `sidebarMode === "host"`
    - `client/src/components/extensions/DbConnectorWorkspace.tsx` contains `sidebarMode="host"`
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The Database activity can use host-managed secondary sidebar tabs without rendering duplicate left panes.</done>
</task>

</tasks>

<verification>
- npm run check
</verification>

<success_criteria>
- Phase 44 truth 1: `db-connector` contributes explicit shell activity and sidebar ids
- Phase 44 truth 2: host-managed sidebar views mount through an explicit registry
- Phase 44 truth 3: the DB workbench can run without duplicate internal sidebar chrome
</success_criteria>

<output>
After completion, create `.planning/phases/44-build-vs-code-style-extension-activity-bar-and-secondary-sidebar-host/44-02-SUMMARY.md`
</output>
