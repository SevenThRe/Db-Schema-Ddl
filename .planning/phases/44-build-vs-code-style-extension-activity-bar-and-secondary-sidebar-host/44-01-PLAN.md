---
phase: 44-build-vs-code-style-extension-activity-bar-and-secondary-sidebar-host
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/pages/Dashboard.tsx
  - client/src/components/Sidebar.tsx
  - client/src/extensions/host-api.ts
  - client/src/extensions/ExtensionWorkspaceHost.tsx
  - client/src/extensions/shell/ExtensionActivityBar.tsx
  - client/src/extensions/shell/ExtensionSecondarySidebar.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The visible shell for extensions has a distinct activity bar and a contextual secondary sidebar instead of footer shortcuts."
    - "Core Excel and DDL workspace navigation still works when no extension activity is selected."
    - "The main workbench area can open an extension workbench view without overloading the core file sidebar."
  artifacts:
    - path: "client/src/extensions/shell/ExtensionActivityBar.tsx"
      provides: "Primary extension activity bar component"
      contains: "function ExtensionActivityBar"
    - path: "client/src/extensions/shell/ExtensionSecondarySidebar.tsx"
      provides: "Contextual sidebar host for selected extension views"
      contains: "function ExtensionSecondarySidebar"
    - path: "client/src/pages/Dashboard.tsx"
      provides: "Top-level shell routing between core workspace and extension shell"
      contains: "ExtensionActivityBar"
    - path: "client/src/components/Sidebar.tsx"
      provides: "Core workspace sidebar without extension-footer navigation leakage"
      contains: "sidebar.definitionFiles"
  key_links:
    - from: "client/src/pages/Dashboard.tsx"
      to: "client/src/extensions/shell/ExtensionActivityBar.tsx"
      via: "selected activity drives extension shell state"
      pattern: "activeActivityItemId"
    - from: "client/src/pages/Dashboard.tsx"
      to: "client/src/extensions/shell/ExtensionSecondarySidebar.tsx"
      via: "selected extension activity drives visible sidebar tabs"
      pattern: "activeSidebarViewId"
---

<objective>
Build the host shell chrome for extensions. The app should render an activity bar and a secondary sidebar for extension surfaces while keeping the core Excel and DDL workspace intact.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-build-vs-code-style-extension-activity-bar-and-secondary-sidebar-host/44-CONTEXT.md
@client/src/pages/Dashboard.tsx
@client/src/components/Sidebar.tsx
@client/src/extensions/host-api.ts
@client/src/extensions/host-context.tsx
@client/src/extensions/ExtensionWorkspaceHost.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dedicated extension shell chrome components</name>
  <read_first>
    - client/src/pages/Dashboard.tsx
    - client/src/components/Sidebar.tsx
    - .planning/phases/44-build-vs-code-style-extension-activity-bar-and-secondary-sidebar-host/44-CONTEXT.md
  </read_first>
  <files>
    - client/src/extensions/shell/ExtensionActivityBar.tsx
    - client/src/extensions/shell/ExtensionSecondarySidebar.tsx
  </files>
  <action>
Create `client/src/extensions/shell/ExtensionActivityBar.tsx` and `client/src/extensions/shell/ExtensionSecondarySidebar.tsx`. `ExtensionActivityBar` should render the selected activity plus the available extension activities from host state. `ExtensionSecondarySidebar` should render the selected extension's contributed sidebar views and expose callbacks for selecting a sidebar view and opening the default workbench view for that extension.
  </action>
  <acceptance_criteria>
    - `client/src/extensions/shell/ExtensionActivityBar.tsx` exists
    - `client/src/extensions/shell/ExtensionActivityBar.tsx` contains `function ExtensionActivityBar`
    - `client/src/extensions/shell/ExtensionSecondarySidebar.tsx` exists
    - `client/src/extensions/shell/ExtensionSecondarySidebar.tsx` contains `function ExtensionSecondarySidebar`
    - `client/src/extensions/shell/ExtensionSecondarySidebar.tsx` contains `activeSidebarViewId`
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The extension shell has real host chrome components instead of relying on sidebar footer buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Dashboard routing so extension surfaces use the new shell chrome</name>
  <read_first>
    - client/src/pages/Dashboard.tsx
    - client/src/extensions/host-api.ts
    - client/src/extensions/ExtensionWorkspaceHost.tsx
    - client/src/extensions/shell/ExtensionActivityBar.tsx
    - client/src/extensions/shell/ExtensionSecondarySidebar.tsx
  </read_first>
  <files>
    - client/src/pages/Dashboard.tsx
    - client/src/extensions/host-api.ts
    - client/src/extensions/ExtensionWorkspaceHost.tsx
  </files>
  <action>
Refactor `client/src/pages/Dashboard.tsx` so core workspace surfaces still use `Sidebar.tsx`, but extension surfaces render through `ExtensionActivityBar`, `ExtensionSecondarySidebar`, and the main workbench area. Persist selected extension activity and sidebar view state with explicit keys `dashboard:lastExtensionActivity` and `dashboard:lastSidebarViewByExtension`. Update `client/src/extensions/host-api.ts` and `client/src/extensions/ExtensionWorkspaceHost.tsx` as needed so the visible shell opens an extension `workbenchViewId` rather than only a legacy `panelId`.
  </action>
  <acceptance_criteria>
    - `client/src/pages/Dashboard.tsx` contains `ExtensionActivityBar`
    - `client/src/pages/Dashboard.tsx` contains `ExtensionSecondarySidebar`
    - `client/src/pages/Dashboard.tsx` contains `dashboard:lastExtensionActivity`
    - `client/src/pages/Dashboard.tsx` contains `dashboard:lastSidebarViewByExtension`
    - `client/src/extensions/host-api.ts` contains `workbenchViewId`
    - `client/src/extensions/ExtensionWorkspaceHost.tsx` opens extension content from `workbenchViewId`
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Extension surfaces now route through dedicated shell chrome with persisted selection state.</done>
</task>

<task type="auto">
  <name>Task 3: Remove extension-navigation leakage from the core file sidebar</name>
  <read_first>
    - client/src/components/Sidebar.tsx
    - client/src/pages/Dashboard.tsx
    - client/src/extensions/host-context.tsx
  </read_first>
  <files>
    - client/src/components/Sidebar.tsx
  </files>
  <action>
Remove extension launch buttons from the footer of `client/src/components/Sidebar.tsx` so the core file sidebar returns to host-native workspace concerns. Keep file upload, file selection, docs, settings, and language controls truthful, but stop using the footer to render the extension activity model that now belongs to `ExtensionActivityBar`.
  </action>
  <acceptance_criteria>
    - `client/src/components/Sidebar.tsx` no longer maps extension activity items in the footer action section
    - `client/src/components/Sidebar.tsx` still contains `sidebar.definitionFiles`
    - `client/src/components/Sidebar.tsx` still contains docs and settings controls
    - no extension activity launch path in `client/src/components/Sidebar.tsx` depends on `extNavItems`
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The core file sidebar no longer leaks extension navigation concerns.</done>
</task>

</tasks>

<verification>
- npm run check
</verification>

<success_criteria>
- Phase 44 truth 1: extension shell chrome exists as activity bar plus secondary sidebar
- Phase 44 truth 2: core workspace sidebar remains separate from extension navigation
- Phase 44 truth 3: extension workbench routing no longer depends on footer shortcuts
</success_criteria>

<output>
After completion, create `.planning/phases/44-build-vs-code-style-extension-activity-bar-and-secondary-sidebar-host/44-01-SUMMARY.md`
</output>
