## 28-02 Plan

Objective: add inserted-row drafting and commit support after delete staging is stable.

Required work:

1. Extend shared schema with inserted-row draft contracts.
2. Extend Rust grid-edit planner with `INSERT` mutation support.
3. Decide null/default/autoincrement handling for drafted rows.
4. Add row creation UI in the grid and review dialog support.
5. Keep insert/update/delete review coherent in one commit plan.

Deferred until 28-01 validation passes.
