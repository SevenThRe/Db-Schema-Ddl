---
phase: 17-safe-data-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - client/src/extensions/host-api.ts
  - client/src/extensions/host-api-runtime.ts
  - client/src/lib/desktop-bridge.ts
  - src-tauri/src/builtin_extensions/mod.rs
  - src-tauri/src/db_connector/mod.rs
  - src-tauri/src/db_connector/grid_edit.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "Grid-edit data contracts are explicit in shared schema and Rust request/response types before mutation logic is implemented"
    - "Edit APIs are capability-gated with a dedicated `db.data.edit` permission across host runtime and builtin extension manifest"
    - "Tauri command surface exposes `db_grid_prepare_commit` and `db_grid_commit` endpoints for downstream UI flow wiring"
  artifacts:
    - path: "shared/schema.ts"
      provides: "Phase-17 grid edit request/response contracts and batch eligibility metadata"
      contains: "export interface DbGridPrepareCommitRequest"
    - path: "src-tauri/src/db_connector/mod.rs"
      provides: "Rust mirror structs for prepare/commit requests and responses"
      contains: "pub struct DbGridCommitRequest"
    - path: "client/src/extensions/host-api-runtime.ts"
      provides: "Capability gate for edit mutations"
      contains: "requireCap(granted, \"db.data.edit\")"
    - path: "src-tauri/src/builtin_extensions/mod.rs"
      provides: "Builtin db-connector capability declaration for edit flow"
      contains: "\"db.data.edit\""
    - path: "src-tauri/src/lib.rs"
      provides: "Tauri invoke registration for grid edit commands"
      contains: "db_connector::grid_edit::db_grid_prepare_commit"
  key_links:
    - from: "client/src/extensions/host-api.ts"
      to: "client/src/lib/desktop-bridge.ts"
      via: "Host API method signatures map to desktop bridge invoke methods"
      pattern: "prepareGridCommit"
    - from: "client/src/lib/desktop-bridge.ts"
      to: "src-tauri/src/lib.rs"
      via: "Bridge command names match registered Tauri handlers"
      pattern: "db_grid_prepare_commit"
    - from: "src-tauri/src/builtin_extensions/mod.rs"
      to: "client/src/extensions/host-api-runtime.ts"
      via: "Capability strings must match runtime gate names"
      pattern: "db.data.edit"
---

<objective>
Define Phase-17 safe-editing contracts and capability boundaries before backend mutation execution begins.

Purpose: DATA-01 and DATA-02 require strict, typed contracts so eligibility, preview, and commit integrity are not inferred from ad-hoc payloads.
Output: Shared + Rust contract alignment, capability-gated host APIs, and registered Tauri grid-edit command endpoints.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-safe-data-editing/17-CONTEXT.md
@shared/schema.ts
@client/src/extensions/host-api.ts
@client/src/extensions/host-api-runtime.ts
@client/src/lib/desktop-bridge.ts
@src-tauri/src/db_connector/mod.rs
@src-tauri/src/builtin_extensions/mod.rs
@src-tauri/src/lib.rs

<interfaces>
Phase-17 contract anchors:
- new commands: `db_grid_prepare_commit`, `db_grid_commit`
- integrity handshake: `planId` + `planHash`
- eligibility is fail-closed and user-visible through explicit reason objects
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared and Rust type contracts for eligibility, patches, prepare/commit</name>
  <files>shared/schema.ts, src-tauri/src/db_connector/mod.rs</files>
  <read_first>
    - .planning/phases/17-safe-data-editing/17-CONTEXT.md (decisions 1-3 define eligibility, patch model, and commit semantics)
    - shared/schema.ts (existing query batch/column contracts to extend without breaking current paths)
    - src-tauri/src/db_connector/mod.rs (existing request/response struct patterns and serde annotations)
  </read_first>
  <action>
Extend both TS and Rust contracts with explicit Phase-17 structures.

1. In `shared/schema.ts`:
   - add optional source metadata to query columns (`sourceTable`, `sourceSchema`, `sourceColumn`, `isPrimaryKey`).
   - add `DbGridEditEligibility`, `DbGridEditEligibilityReason`, and `DbGridEditSource` interfaces.
   - extend `DbQueryBatchResult` with optional `editEligibility`, `editSource`, and `primaryKeyColumns`.
   - add request/response contracts:
     - `DbGridEditPatchCell`
     - `DbGridPrepareCommitRequest`
     - `DbGridPrepareCommitResponse`
     - `DbGridCommitRequest`
     - `DbGridCommitResponse`
   - include `planId: string`, `planHash: string`, preview line list, and truncation flag in prepare response.
2. In `src-tauri/src/db_connector/mod.rs`:
   - add serde-annotated mirrors for the same request/response payloads and eligibility structures.
   - keep optional fields backward compatible using `Option<T>` + `#[serde(skip_serializing_if = "Option::is_none")]` where applicable.
3. Keep existing query/explain/export contracts unchanged except for additive optional fields required by Phase 17.
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - shared/schema.ts contains `export interface DbGridPrepareCommitRequest`
    - shared/schema.ts contains `export interface DbGridCommitRequest`
    - shared/schema.ts contains `planHash: string`
    - shared/schema.ts contains `editEligibility?: DbGridEditEligibility`
    - src-tauri/src/db_connector/mod.rs contains `pub struct DbGridPrepareCommitRequest`
    - src-tauri/src/db_connector/mod.rs contains `pub struct DbGridCommitRequest`
    - `npm run check` exits 0
    - `cargo check --manifest-path src-tauri/Cargo.toml` exits 0
  </acceptance_criteria>
  <done>Typed payload contracts for safe editing are available in both shared TS and Rust connector modules.</done>
</task>

<task type="auto">
  <name>Task 2: Wire capability-gated host APIs and register grid-edit command endpoints</name>
  <files>client/src/extensions/host-api.ts, client/src/extensions/host-api-runtime.ts, client/src/lib/desktop-bridge.ts, src-tauri/src/builtin_extensions/mod.rs, src-tauri/src/db_connector/grid_edit.rs, src-tauri/src/lib.rs</files>
  <read_first>
    - client/src/extensions/host-api.ts (ConnectionsApi method contract shape)
    - client/src/extensions/host-api-runtime.ts (capability gate patterns for db.query/db.plan.read)
    - client/src/lib/desktop-bridge.ts (db invoke naming conventions)
    - src-tauri/src/builtin_extensions/mod.rs (db-connector capability declaration)
    - src-tauri/src/lib.rs (tauri::generate_handler registration list)
  </read_first>
  <action>
Add end-to-end API wiring and capability gates for Phase-17 mutation flow.

1. In `client/src/extensions/host-api.ts`, add to `ConnectionsApi`:
   - `prepareGridCommit(request: DbGridPrepareCommitRequest): Promise<DbGridPrepareCommitResponse>`
   - `commitGridEdits(request: DbGridCommitRequest): Promise<DbGridCommitResponse>`
2. In `client/src/extensions/host-api-runtime.ts`:
   - add `"db.data.edit"` to `ALL_CAPABILITIES`.
   - gate both new methods with `requireCap(granted, "db.data.edit")`.
3. In `client/src/lib/desktop-bridge.ts`, add db bridge methods invoking:
   - `db_grid_prepare_commit`
   - `db_grid_commit`
4. In `src-tauri/src/builtin_extensions/mod.rs`, add `"db.data.edit"` to db-connector manifest capabilities.
5. Create `src-tauri/src/db_connector/grid_edit.rs` with command function stubs:
   - `pub async fn db_grid_prepare_commit(...) -> Result<... , String>`
   - `pub async fn db_grid_commit(...) -> Result<... , String>`
   Return an explicit placeholder error until Plan 17-02 implements full logic.
6. In `src-tauri/src/lib.rs`, register both grid-edit commands in `tauri::generate_handler!`.
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - client/src/extensions/host-api.ts contains `prepareGridCommit(`
    - client/src/extensions/host-api.ts contains `commitGridEdits(`
    - client/src/extensions/host-api-runtime.ts contains `\"db.data.edit\"`
    - client/src/extensions/host-api-runtime.ts contains `requireCap(granted, \"db.data.edit\")`
    - client/src/lib/desktop-bridge.ts contains `invoke<DbGridPrepareCommitResponse>(\"db_grid_prepare_commit\"`
    - client/src/lib/desktop-bridge.ts contains `invoke<DbGridCommitResponse>(\"db_grid_commit\"`
    - src-tauri/src/builtin_extensions/mod.rs contains `\"db.data.edit\"`
    - src-tauri/src/db_connector/grid_edit.rs contains `pub async fn db_grid_prepare_commit`
    - src-tauri/src/lib.rs contains `db_connector::grid_edit::db_grid_prepare_commit`
    - src-tauri/src/lib.rs contains `db_connector::grid_edit::db_grid_commit`
    - `npm run check` exits 0
    - `cargo check --manifest-path src-tauri/Cargo.toml` exits 0
  </acceptance_criteria>
  <done>Capability-gated host and desktop wiring exists for the grid edit prepare/commit command path.</done>
</task>

</tasks>

<verification>
- `npm run check`
- `cargo check --manifest-path src-tauri/Cargo.toml`
- `rg -n "db_grid_prepare_commit|db_grid_commit|db.data.edit" client/src/extensions/host-api.ts client/src/extensions/host-api-runtime.ts client/src/lib/desktop-bridge.ts src-tauri/src/builtin_extensions/mod.rs src-tauri/src/lib.rs src-tauri/src/db_connector/grid_edit.rs`
</verification>

<success_criteria>
- DATA-01 readiness: edit eligibility has explicit, typed structures and cannot be silently inferred
- DATA-02 readiness: prepare response contract exposes preview/summary + integrity tokens (`planId`, `planHash`)
- Phase-17 execution plans can build on a stable API surface without cross-layer contract drift
</success_criteria>

<output>
After completion, create `.planning/phases/17-safe-data-editing/17-01-SUMMARY.md`
</output>
