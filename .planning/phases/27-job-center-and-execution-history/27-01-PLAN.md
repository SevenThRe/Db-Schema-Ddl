---
phase: 27-job-center-and-execution-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - client/src/extensions/host-api.ts
  - client/src/extensions/host-api-runtime.ts
  - client/src/lib/desktop-bridge.ts
  - src-tauri/src/db_connector/mod.rs
  - src-tauri/src/db_connector/commands.rs
  - src-tauri/src/db_connector/data_apply.rs
  - src-tauri/src/storage.rs
  - test/server/job-center-phase27.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The runtime exposes a durable recent-jobs API instead of forcing the UI to know individual job ids in advance."
    - "Job summaries already contain enough operational context to make a persistent Job Center useful before opening full detail."
    - "The job contract is extensible for future job families without pretending unsupported families already exist."
  artifacts:
    - path: "shared/schema.ts"
      provides: "background job list/summary contracts"
      contains: "DbBackgroundJobSummary"
    - path: "src-tauri/src/storage.rs"
      provides: "recent persisted apply-job history query"
      contains: "list_db_data_apply_jobs"
    - path: "src-tauri/src/db_connector/commands.rs"
      provides: "tauri command for background job listing"
      contains: "db_background_job_list"
  key_links:
    - from: "src-tauri/src/storage.rs"
      to: "client/src/components/extensions/db-workbench/JobCenterPane.tsx"
      via: "DbBackgroundJobSummary[]"
      pattern: "jobKind"
---

<objective>
Create the backend and shared-contract foundation for a persistent Job Center by exposing recent background DB job history from the already persisted apply-job records.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-job-center-and-execution-history/27-CONTEXT.md
@shared/schema.ts
@src-tauri/src/storage.rs
@src-tauri/src/db_connector/data_apply.rs
@src-tauri/src/db_connector/commands.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define background-job list contracts and runtime bridge wiring</name>
  <read_first>
    - shared/schema.ts
    - client/src/extensions/host-api.ts
    - client/src/lib/desktop-bridge.ts
    - src-tauri/src/db_connector/mod.rs
  </read_first>
  <files>
    - shared/schema.ts
    - client/src/extensions/host-api.ts
    - client/src/extensions/host-api-runtime.ts
    - client/src/lib/desktop-bridge.ts
    - src-tauri/src/db_connector/mod.rs
  </files>
  <action>
  Add a generic background-job summary/list contract with a current `data-apply` implementation and thread it through the host API and desktop bridge.
  </action>
  <acceptance_criteria>
    - a typed list request/response exists in shared and Rust contracts
    - the host API exposes a recent-job listing entrypoint
    - the summary model contains job kind, status, timestamps, counts, related connection ids, and a review-friendly preview
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Frontend and backend can speak about recent jobs without bespoke apply-only wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Query persisted apply-job history from storage and expose it as recent background jobs</name>
  <read_first>
    - src-tauri/src/storage.rs
    - src-tauri/src/db_connector/data_apply.rs
  </read_first>
  <files>
    - src-tauri/src/storage.rs
    - src-tauri/src/db_connector/data_apply.rs
    - src-tauri/src/db_connector/commands.rs
  </files>
  <action>
  Add a storage query for recent apply jobs, map them to reusable background-job summaries, and register a Tauri command that the workbench can call.
  </action>
  <acceptance_criteria>
    - recent jobs are ordered newest-first and limited explicitly
    - summaries expose failure hints and blocker status without loading full detail first
    - the list command remains honest that `data-apply` is the only currently wired job family
  </acceptance_criteria>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -j 1</automated>
  </verify>
  <done>The backend can return durable recent-job history for the Job Center.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the recent-job backend with focused regression tests</name>
  <read_first>
    - shared/schema.ts
    - src-tauri/src/storage.rs
    - src-tauri/src/db_connector/data_apply.rs
  </read_first>
  <files>
    - test/server/job-center-phase27.test.ts
  </files>
  <action>
  Add focused source-level tests that assert the new job-list contract and runtime entrypoint exist and stay wired.
  </action>
  <acceptance_criteria>
    - a Phase 27 test file exists
    - it asserts shared/runtime wiring for recent background jobs
    - it asserts the Job Center contract is generic rather than detail-only
  </acceptance_criteria>
  <verify>
    <automated>$env:NODE_OPTIONS='--import=tsx'; node --test --experimental-strip-types test/server/job-center-phase27.test.ts</automated>
  </verify>
  <done>The backend slice is guarded against regression.</done>
</task>

</tasks>

<verification>
- npm run check
- $env:NODE_OPTIONS='--import=tsx'; node --test --experimental-strip-types test/server/job-center-phase27.test.ts
- cargo check --manifest-path src-tauri/Cargo.toml -j 1
</verification>

<success_criteria>
- Phase 27 has a durable runtime entrypoint for recent background job history
</success_criteria>

<output>
After completion, create `.planning/phases/27-job-center-and-execution-history/27-01-SUMMARY.md`
</output>
