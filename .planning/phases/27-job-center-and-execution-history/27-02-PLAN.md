---
phase: 27-job-center-and-execution-history
plan: 02
type: execute
wave: 1
depends_on:
  - 27-01
files_modified:
  - client/src/components/extensions/db-workbench/workbench-session.ts
  - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  - client/src/components/extensions/db-workbench/JobCenterPane.tsx
  - test/client/job-center-phase27.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Operators can reopen recent background jobs from a persistent pane even after leaving the sync surface."
    - "Running jobs stay observable from one central history surface rather than only from transient toast state."
    - "Historical job review can restore relevant sync context back into the canonical workbench flow."
  artifacts:
    - path: "client/src/components/extensions/db-workbench/JobCenterPane.tsx"
      provides: "persistent list/detail history surface for background jobs"
      contains: "Reopen sync context"
    - path: "client/src/components/extensions/db-workbench/workbench-session.ts"
      provides: "persisted last active job-center selection"
      contains: "selectedJobId"
    - path: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      provides: "job-center tab, polling, and reopen wiring"
      contains: "backgroundJobs"
  key_links:
    - from: "client/src/components/extensions/db-workbench/JobCenterPane.tsx"
      to: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      via: "selectedJobId"
      pattern: "setResultTab(\"sync\")"
---

<objective>
Land the persistent Job Center UI inside the canonical workbench shell so recent background jobs can be reopened and monitored without relying on the sync pane or transient notifications.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-job-center-and-execution-history/27-CONTEXT.md
@client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
@client/src/components/extensions/db-workbench/workbench-session.ts
@shared/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist job-center view state in the per-connection workbench session</name>
  <read_first>
    - client/src/components/extensions/db-workbench/workbench-session.ts
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  </read_first>
  <files>
    - client/src/components/extensions/db-workbench/workbench-session.ts
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  </files>
  <action>
  Extend the workbench session model so `jobs` is a first-class result pane and the last selected job can restore on reopen.
  </action>
  <acceptance_criteria>
    - `jobs` is a valid workbench result tab
    - the workbench session can remember the selected job id safely
    - restore logic falls back cleanly when a remembered job no longer exists
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Job Center state survives ordinary workbench reopen flows.</done>
</task>

<task type="auto">
  <name>Task 2: Add a persistent Job Center pane with list/detail review and reopen actions</name>
  <read_first>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  </read_first>
  <files>
    - client/src/components/extensions/db-workbench/JobCenterPane.tsx
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  </files>
  <action>
  Add a dense list/detail Job Center pane, wire it into the canonical result tabs, and make it able to refresh, select recent jobs, and reopen sync context from historical runs.
  </action>
  <acceptance_criteria>
    - a dedicated Job Center tab exists beside the other canonical workbench panes
    - the pane shows active/recent jobs, key status metadata, and detailed persisted audit information for the selected job
    - operators can reopen a selected sync job back into the sync pane without guessing its source/target context
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The canonical workbench now has a persistent job-review surface.</done>
</task>

<task type="auto">
  <name>Task 3: Guard the Job Center UI wiring with focused regression tests</name>
  <read_first>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
    - client/src/components/extensions/db-workbench/workbench-session.ts
  </read_first>
  <files>
    - test/client/job-center-phase27.test.ts
  </files>
  <action>
  Add focused assertions that the workbench recognizes the new `jobs` result tab, persists the selected job id, and exposes reopen wiring to the sync pane.
  </action>
  <acceptance_criteria>
    - a Phase 27 client test file exists
    - it asserts `jobs` participates in session restore and workbench tab wiring
    - it asserts reopen actions are wired from job history back into sync context
  </acceptance_criteria>
  <verify>
    <automated>$env:NODE_OPTIONS='--import=tsx'; node --test --experimental-strip-types test/client/job-center-phase27.test.ts</automated>
  </verify>
  <done>The Job Center UI slice is guarded against regression.</done>
</task>

</tasks>

<verification>
- npm run check
- $env:NODE_OPTIONS='--import=tsx'; node --test --experimental-strip-types test/client/job-center-phase27.test.ts
- cargo check --manifest-path src-tauri/Cargo.toml -j 1
</verification>

<success_criteria>
- Phase 27 exposes one persistent Job Center surface inside the canonical workbench
</success_criteria>

<output>
After completion, create `.planning/phases/27-job-center-and-execution-history/27-02-SUMMARY.md`
</output>
