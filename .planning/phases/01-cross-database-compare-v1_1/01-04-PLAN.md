---
phase: 01-cross-database-compare-v1_1
plan: 04
type: execute
wave: 4
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - shared/schema.ts
  - shared/routes.ts
  - server/storage.ts
  - client/src/pages/Settings.tsx
  - client/src/components/settings/DbComparePolicySection.tsx
  - client/src/hooks/use-db-management.ts
  - test/client/db-vs-db-ui-phase1.test.tsx
autonomous: true
requirements:
  - DBDB-03
user_setup: []
must_haves:
  truths:
    - "Rename/equivalence handling remains conservative by default."
    - "Users can configure low-learning-cost auto-accept thresholds by object type."
    - "DB-vs-DB compare sessions can honor that policy without introducing a complex scope matrix."
  artifacts:
    - path: "client/src/components/settings/DbComparePolicySection.tsx"
      provides: "rename/equivalence policy settings UI"
      contains: "threshold"
    - path: "server/storage.ts"
      provides: "persistent DB-compare settings storage"
      contains: "settings"
    - path: "shared/schema.ts"
      provides: "typed DB-compare policy contract"
      contains: "policy"
---

<objective>
Add the settings-backed rename/equivalence policy for DB-vs-DB compare.

Purpose: The user explicitly wants a CLI-permission-like model that stays conservative by default but can auto-accept specific rename scopes with low learning cost. This plan adds that policy in the simplest viable form.
Output: Persistent DB-compare policy settings and UI, plus compare-session integration.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cross-database-compare-v1_1/01-CONTEXT.md
@.planning/phases/01-cross-database-compare-v1_1/01-RESEARCH.md
@shared/schema.ts
@shared/routes.ts
@server/storage.ts
@client/src/pages/Settings.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed and persistent DB-compare policy settings</name>
  <files>shared/schema.ts, shared/routes.ts, server/storage.ts</files>
  <behavior>
    - Test 1: default policy keeps all rename/equivalence cases manual
    - Test 2: table and column thresholds can be stored independently
    - Test 3: compare sessions can consume that policy without changing apply semantics
  </behavior>
  <action>Add DB-management-specific policy schemas, route contracts, and storage for low-complexity rename thresholds. Do not extend the global DDL settings blob with compare-policy concerns unless planner proves that is the cleanest existing seam.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Phase 1 has a persistent, typed, low-complexity policy model for rename/equivalence handling.</done>
</task>

<task type="auto">
  <name>Task 2: Surface policy settings and bind them to compare workflow</name>
  <files>client/src/pages/Settings.tsx, client/src/components/settings/DbComparePolicySection.tsx, client/src/hooks/use-db-management.ts, test/client/db-vs-db-ui-phase1.test.tsx</files>
  <action>Add a settings section that exposes table and column auto-accept thresholds and wire those values into DB-vs-DB compare sessions. Keep the UI understandable in one glance and preserve manual confirmation when thresholds are unset.</action>
  <verify>
    <automated>node --test --import tsx test/client/db-vs-db-ui-phase1.test.tsx</automated>
  </verify>
  <done>The compare-policy model behaves like a low-friction permission system instead of a high-overhead rules engine.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] `node --test --import tsx test/client/db-vs-db-ui-phase1.test.tsx`
- [ ] manual remains the default
- [ ] only table/column thresholds are exposed in Phase 1
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Rename/equivalence policy is configurable without becoming a complex approval framework
</success_criteria>

<output>
After completion, create `.planning/phases/01-cross-database-compare-v1_1/01-04-SUMMARY.md`
</output>
