---
phase: 01-cross-database-compare-v1_1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - shared/routes.ts
  - client/src/hooks/use-db-management.ts
  - test/server/db-db-phase1.test.ts
  - test/client/db-vs-db-ui-phase1.test.tsx
autonomous: true
requirements:
  - DBDB-01
  - DBDB-02
  - DBDB-03
  - DBDB-04
user_setup: []
must_haves:
  truths:
    - "Phase 1 has a typed `DB vs DB` contract that clearly distinguishes source and target live schemas."
    - "Live-to-live compare is enabled only for compare and graph flows, not for apply."
    - "Directional preview and rename-policy settings are modeled explicitly rather than hidden in ad hoc state."
  artifacts:
    - path: "shared/schema.ts"
      provides: "Typed live-vs-live compare, directional preview, and rename-policy contracts"
      contains: "db"
    - path: "shared/routes.ts"
      provides: "DB management route contracts for cross-database compare and preview"
      contains: "dbManagement"
    - path: "client/src/hooks/use-db-management.ts"
      provides: "Typed hook seams for DB-vs-DB compare workflows"
      contains: "useDb"
  key_links:
    - from: "shared/routes.ts"
      to: "shared/schema.ts"
      via: "DB-vs-DB request/response contracts"
      pattern: "compare|preview"
---

<objective>
Define the shared contract and validation seam for `DB vs DB` compare before backend and UI implementation begin.

Purpose: The phase needs one typed language for source/target live compare, directional preview, rename policy, and graph linkage. This plan creates that language first so the later backend and renderer work do not invent incompatible local shapes.
Output: Shared schemas, route contracts, hook seams, and Wave 0 tests for Phase 1.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cross-database-compare-v1_1/01-CONTEXT.md
@.planning/phases/01-cross-database-compare-v1_1/01-RESEARCH.md
@.planning/phases/01-cross-database-compare-v1_1/01-VALIDATION.md
@shared/schema.ts
@shared/routes.ts
@client/src/hooks/use-db-management.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed DB-vs-DB compare contracts and Wave 0 test scaffolds</name>
  <files>shared/schema.ts, test/server/db-db-phase1.test.ts, test/client/db-vs-db-ui-phase1.test.tsx</files>
  <behavior>
    - Test 1: compare and graph contracts can represent `live vs live` source/target requests
    - Test 2: apply contracts remain blocked for `live vs live`
    - Test 3: rename policy settings stay low-complexity and object-type based
  </behavior>
  <action>Add Phase 1 schemas for source/target live compare, directional preview, dedicated `db-vs-db` workspace state, and rename-policy thresholds. Create focused Wave 0 server/client tests that encode the new contract expectations without requiring full UI markup snapshots or a real DB.</action>
  <verify>
    <automated>node --test --import tsx test/server/db-db-phase1.test.ts</automated>
  </verify>
  <done>Phase 1 has a stable typed contract and executable safety tests before service work starts.</done>
</task>

<task type="auto">
  <name>Task 2: Extend DB management route and hook seams for cross-database compare</name>
  <files>shared/routes.ts, client/src/hooks/use-db-management.ts</files>
  <action>Expose typed DB-management routes and hooks for source/target live compare, directional preview, graph compare, and rename-policy retrieval/update. Keep these inside the existing DB-management namespace and do not create a separate API family.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The rest of Phase 1 can build on typed compare and preview hooks instead of manual fetch logic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/server/db-db-phase1.test.ts`
- [ ] `node --test --import tsx test/client/db-vs-db-ui-phase1.test.tsx`
- [ ] `npm run check`
- [ ] `live vs live` remains blocked for apply
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 starts from one shared contract instead of multiple local compare shapes
</success_criteria>

<output>
After completion, create `.planning/phases/01-cross-database-compare-v1_1/01-01-SUMMARY.md`
</output>
