---
phase: 01-cross-database-compare-v1_1
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - client/src/components/db-management/DbManagementWorkspace.tsx
  - client/src/components/db-management/DbVsDbWorkspace.tsx
  - client/src/hooks/use-db-management.ts
  - test/client/db-vs-db-ui-phase1.test.tsx
autonomous: true
requirements:
  - DBDB-01
  - DBDB-02
  - DBDB-04
user_setup: []
must_haves:
  truths:
    - "DB-vs-DB compare is a distinct main view inside `DB 管理`."
    - "Users can select any source/target pair, swap them, run whole-database compare, and then filter results."
    - "The dedicated workspace shows diff tree, directional preview, and graph linkage coherently."
  artifacts:
    - path: "client/src/components/db-management/DbManagementWorkspace.tsx"
      provides: "top-level DB-vs-DB view integration"
      contains: "db-vs-db"
    - path: "client/src/components/db-management/DbVsDbWorkspace.tsx"
      provides: "dedicated source/target compare workspace"
      contains: "source"
    - path: "test/client/db-vs-db-ui-phase1.test.tsx"
      provides: "structural coverage for source/target, swap, and dedicated view behavior"
      contains: "db-vs-db"
---

<objective>
Build the dedicated `DB vs DB` workspace inside `DB 管理`.

Purpose: The user explicitly wants this to feel operationally complete, not like a hidden alternate compare mode. This plan creates the new top-level view and the source/target compare workspace that unifies tree, directional preview, and graph linkage.
Output: Dedicated DB-vs-DB view and workspace UI with source/target swap, whole-db compare, and post-compare filtering.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cross-database-compare-v1_1/01-CONTEXT.md
@.planning/phases/01-cross-database-compare-v1_1/01-RESEARCH.md
@client/src/components/db-management/DbManagementWorkspace.tsx
@client/src/components/db-management/DbDiffWorkspace.tsx
@client/src/components/db-management/DbSchemaGraph.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a new DB-vs-DB main view and dedicated workspace shell</name>
  <files>client/src/components/db-management/DbManagementWorkspace.tsx, client/src/components/db-management/DbVsDbWorkspace.tsx, test/client/db-vs-db-ui-phase1.test.tsx</files>
  <behavior>
    - Test 1: `DB 管理` exposes a distinct `db-vs-db` main view
    - Test 2: users can pick source and target, including same-connection different-database combinations
    - Test 3: source/target swap is first-class and preserves compare intent
  </behavior>
  <action>Create a dedicated `DbVsDbWorkspace` and integrate it as a new main view inside `DB 管理`. The workspace should make source/target explicit, support swap, and keep the compare action whole-database first.</action>
  <verify>
    <automated>node --test --import tsx test/client/db-vs-db-ui-phase1.test.tsx</automated>
  </verify>
  <done>The user can enter a purpose-built DB-vs-DB workspace instead of overloading the file-vs-DB experience.</done>
</task>

<task type="auto">
  <name>Task 2: Present tree, directional preview, and graph linkage from one workspace</name>
  <files>client/src/components/db-management/DbVsDbWorkspace.tsx, client/src/hooks/use-db-management.ts</files>
  <action>Wire the dedicated workspace so compare results show a post-compare filterable object tree, directional preview artifacts, and graph linkage. Keep whole-database compare as the default, then let users focus down after the result arrives.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The DB-vs-DB workspace feels like one operational surface rather than a loose collection of tabs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/client/db-vs-db-ui-phase1.test.tsx`
- [ ] `npm run check`
- [ ] source/target swap exists
- [ ] compare defaults to whole database and filters afterward
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The user gets a dedicated, source/target-explicit DB-vs-DB workspace
</success_criteria>

<output>
After completion, create `.planning/phases/01-cross-database-compare-v1_1/01-03-SUMMARY.md`
</output>
