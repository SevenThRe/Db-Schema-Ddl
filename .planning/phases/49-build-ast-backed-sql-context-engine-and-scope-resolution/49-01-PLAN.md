---
phase: 49-build-ast-backed-sql-context-engine-and-scope-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/extensions/db-workbench/sql-semantic-context.ts
  - client/src/components/extensions/db-workbench/sql-autocomplete.ts
  - client/src/components/extensions/db-workbench/SqlEditorPane.tsx
  - test/client/db-workbench-autocomplete-phase16.test.tsx
  - test/client/db-workbench-sql-semantic-context-phase49.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Completion and hover both consume one shared SQL semantic analysis path."
    - "The semantic engine resolves common `SELECT`, `INSERT`, `UPDATE`, `DELETE`, and `WITH` statement contexts."
    - "Alias, CTE, and subquery scope resolution no longer depends on autocomplete-only heuristics."
---

<objective>
Land the Phase 49 SQL semantic foundation: extract a shared statement-analysis module, wire Monaco completion and hover to it, and add focused regression tests for statement kind, clause resolution, and nested scope handling.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-build-ast-backed-sql-context-engine-and-scope-resolution/49-CONTEXT.md
@client/src/components/extensions/db-workbench/sql-autocomplete.ts
@client/src/components/extensions/db-workbench/SqlEditorPane.tsx
@test/client/db-workbench-autocomplete-phase16.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the shared SQL semantic-context engine</name>
  <read_first>
    - client/src/components/extensions/db-workbench/sql-autocomplete.ts
  </read_first>
  <files>
    - client/src/components/extensions/db-workbench/sql-semantic-context.ts
    - client/src/components/extensions/db-workbench/sql-autocomplete.ts
  </files>
  <action>
Move the reusable statement-window, clause, alias, CTE, subquery, and member-access analysis into a dedicated semantic-context module. Keep the output structured enough that editor consumers can read statement kind, clause spans, visible bindings, and active alias/member-access state without reparsing SQL themselves.
  </action>
  <acceptance_criteria>
    - a dedicated semantic analysis module exists under `db-workbench/`
    - completion no longer derives clause/scope from autocomplete-local parsing alone
    - `SELECT`, `INSERT`, `UPDATE`, `DELETE`, and `WITH` all resolve statement kind and clause context through the shared analyzer
  </acceptance_criteria>
  <verify>
    <automated>node --import=tsx --test test/client/db-workbench-sql-semantic-context-phase49.test.ts</automated>
  </verify>
  <done>The workbench has one reusable SQL semantic contract instead of autocomplete-only parsing helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Monaco hover and completion to the shared semantic contract</name>
  <read_first>
    - client/src/components/extensions/db-workbench/SqlEditorPane.tsx
    - client/src/components/extensions/db-workbench/sql-autocomplete.ts
  </read_first>
  <files>
    - client/src/components/extensions/db-workbench/SqlEditorPane.tsx
    - client/src/components/extensions/db-workbench/sql-autocomplete.ts
  </files>
  <action>
Use the shared semantic analyzer as the source of truth for completion scope and alias/member-access resolution, then add a Monaco hover provider that surfaces relation or column metadata from the same analysis path.
  </action>
  <acceptance_criteria>
    - completion item generation consumes the shared analyzer result
    - a SQL hover provider exists in `SqlEditorPane.tsx`
    - hover can resolve at least bound relations and bound columns from the current statement
  </acceptance_criteria>
  <verify>
    <automated>node --import=tsx --test test/client/db-workbench-autocomplete-phase16.test.tsx test/client/db-workbench-sql-library-phase16.test.ts</automated>
  </verify>
  <done>Completion and hover both prove the semantic contract is real and reusable.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused regression evidence for Phase 49 semantics</name>
  <read_first>
    - test/client/db-workbench-autocomplete-phase16.test.tsx
  </read_first>
  <files>
    - test/client/db-workbench-sql-semantic-context-phase49.test.ts
    - test/client/db-workbench-autocomplete-phase16.test.tsx
  </files>
  <action>
Add direct tests that assert statement kind and clause resolution for common DML forms, CTE/subquery scope lifting, and hover symbol resolution. Preserve existing autocomplete regressions to ensure the extraction does not break current editor behavior.
  </action>
  <acceptance_criteria>
    - a dedicated Phase 49 semantic test file exists
    - semantic tests cover `SELECT`, `INSERT`, `UPDATE`, `DELETE`, and `WITH`
    - existing autocomplete regressions remain green after the extraction
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Phase 49 has direct regression evidence instead of relying only on older autocomplete tests.</done>
</task>

</tasks>

<verification>
- node --import=tsx --test test/client/db-workbench-sql-semantic-context-phase49.test.ts test/client/db-workbench-autocomplete-phase16.test.tsx test/client/db-workbench-sql-library-phase16.test.ts
- npm run check
</verification>

<output>
After completion, create `.planning/phases/49-build-ast-backed-sql-context-engine-and-scope-resolution/49-01-SUMMARY.md`
</output>
