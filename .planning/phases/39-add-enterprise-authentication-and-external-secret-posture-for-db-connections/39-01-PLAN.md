---
phase: 39-add-enterprise-authentication-and-external-secret-posture-for-db-connections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - src-tauri/src/storage.rs
  - src-tauri/src/db_connector/mod.rs
  - src-tauri/src/db_connector/introspect.rs
  - src-tauri/src/db_connector/query.rs
  - client/src/components/extensions/DbConnectorWorkspace.tsx
  - docs/db-connection-auth.md
  - test/client/db-connection-auth-phase39.test.ts
  - test/server/db-connection-auth-phase39.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Saved connections can express non-inline credential modes instead of assuming one stored password is always available."
    - "Resolved secrets are loaded only at runtime and never written back into saved config or artifacts."
    - "Operators can tell which credential mode a connection uses without seeing the secret value."
  artifacts:
    - path: "shared/schema.ts"
      provides: "enterprise credential mode contract"
      contains: "credentialMode"
    - path: "src-tauri/src/storage.rs"
      provides: "runtime secret resolution without persistence leakage"
      contains: "credential_mode"
    - path: "docs/db-connection-auth.md"
      provides: "maintainer and operator auth posture guidance"
      contains: "runtime_prompt"
  key_links:
    - from: "client/src/components/extensions/DbConnectorWorkspace.tsx"
      to: "src-tauri/src/storage.rs"
      via: "credentialMode"
      pattern: "env_var"
---

<objective>
Support enterprise-friendly credential modes and external secret references so DB connections no longer depend only on locally saved static passwords.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/db-workbench-feature-checklist.md
@shared/schema.ts
@src-tauri/src/storage.rs
@src-tauri/src/db_connector/mod.rs
@src-tauri/src/db_connector/introspect.rs
@src-tauri/src/db_connector/query.rs
@client/src/components/extensions/DbConnectorWorkspace.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit credential modes and secret references to connection config</name>
  <read_first>
    - shared/schema.ts
    - src-tauri/src/db_connector/mod.rs
    - client/src/components/extensions/DbConnectorWorkspace.tsx
  </read_first>
  <files>
    - shared/schema.ts
    - src-tauri/src/db_connector/mod.rs
    - client/src/components/extensions/DbConnectorWorkspace.tsx
  </files>
  <action>
Extend saved connections with `credentialMode: "secure_store_password" | "env_var" | "runtime_prompt" | "token_command"` plus `credentialRef` and `tokenTtlSeconds`, and expose those choices in the connection editor without showing resolved secret values.
  </action>
  <acceptance_criteria>
    - `shared/schema.ts` contains `credentialMode`
    - `shared/schema.ts` contains `credentialRef`
    - `shared/schema.ts` contains `tokenTtlSeconds`
    - `DbConnectorWorkspace.tsx` contains `runtime_prompt`
    - `DbConnectorWorkspace.tsx` contains `env_var`
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Connection config can now describe enterprise-oriented credential posture explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve credentials at runtime without serializing resolved secrets back to disk</name>
  <read_first>
    - src-tauri/src/storage.rs
    - src-tauri/src/db_connector/introspect.rs
    - src-tauri/src/db_connector/query.rs
  </read_first>
  <files>
    - src-tauri/src/storage.rs
    - src-tauri/src/db_connector/introspect.rs
    - src-tauri/src/db_connector/query.rs
  </files>
  <action>
Implement runtime resolution for `secure_store_password`, `env_var`, `runtime_prompt`, and `token_command` so resolved credential material is only held in-memory long enough for connection use and is never written back into saved config, logs, or artifact output.
  </action>
  <acceptance_criteria>
    - `storage.rs` contains `credential_mode`
    - `storage.rs` does not serialize resolved env-var or token-command output back into saved connection JSON
    - `introspect.rs` and `query.rs` can consume runtime-resolved credentials
    - runtime errors identify the failing credential mode without printing the secret value
  </acceptance_criteria>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -j 1</automated>
  </verify>
  <done>Enterprise credential modes resolve at runtime without weakening the no-secret-export posture.</done>
</task>

<task type="auto">
  <name>Task 3: Document auth posture and add focused client/server coverage</name>
  <read_first>
    - shared/schema.ts
    - src-tauri/src/storage.rs
    - client/src/components/extensions/DbConnectorWorkspace.tsx
  </read_first>
  <files>
    - docs/db-connection-auth.md
    - test/client/db-connection-auth-phase39.test.ts
    - test/server/db-connection-auth-phase39.test.ts
  </files>
  <action>
Write a dedicated DB connection auth doc covering the supported credential modes, then add client and server tests that assert the new config fields exist and resolved secrets do not leak into persisted config or UI labels.
  </action>
  <acceptance_criteria>
    - `docs/db-connection-auth.md` exists
    - `docs/db-connection-auth.md` contains `secure_store_password`
    - `docs/db-connection-auth.md` contains `token_command`
    - `test/client/db-connection-auth-phase39.test.ts` exists
    - `test/server/db-connection-auth-phase39.test.ts` exists
  </acceptance_criteria>
  <verify>
    <automated>node --import=tsx --test test/client/db-connection-auth-phase39.test.ts test/server/db-connection-auth-phase39.test.ts</automated>
  </verify>
  <done>Credential-mode posture is documented and regression-covered across UI and runtime storage behavior.</done>
</task>

</tasks>

<verification>
- npm run check
- cargo check --manifest-path src-tauri/Cargo.toml -j 1
- node --import=tsx --test test/client/db-connection-auth-phase39.test.ts test/server/db-connection-auth-phase39.test.ts
</verification>

<success_criteria>
- DB connections can use explicit enterprise credential modes without leaking resolved secrets into saved config or artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/39-add-enterprise-authentication-and-external-secret-posture-for-db-connections/39-01-SUMMARY.md`
</output>
