---
phase: 03-reverse-import-expansion-v1_2
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - server/routes/ddl-routes.ts
  - server/lib/ddl-import/export-service.ts
  - client/src/hooks/use-ddl.ts
  - test/server/reverse-import-phase3.test.ts
autonomous: true
requirements:
  - REV-01
  - REV-02
  - REV-03
  - REV-04
user_setup: []
must_haves:
  truths:
    - "MySQL, bundle, and Oracle subset imports converge on one preview/export route family."
    - "Export continues to use only the official workbook templates and parser-backed round-trip validation."
    - "New source modes do not bypass the existing file registration and remembered-template behavior."
  artifacts:
    - path: "server/routes/ddl-routes.ts"
      provides: "expanded DDL import route handlers for preview and workbook export"
      contains: "previewImport"
    - path: "server/lib/ddl-import/export-service.ts"
      provides: "official-template workbook export and validation gate"
      contains: "export"
  key_links:
    - from: "server/routes/ddl-routes.ts"
      to: "server/lib/ddl-import/export-service.ts"
      via: "previewed reverse-import artifact exported through the official template path"
      pattern: "template|round-trip|file"
---

<objective>
Integrate bundle and Oracle subset inputs into the shipped preview/export HTTP flow and trust gate.

Purpose: Phase 3 is not complete until the new source modes can move through preview, issue review, export, round-trip validation, and file registration exactly like the current MySQL-first flow.
Output: Expanded DDL route handling, export integration, and end-to-end server behavior for new source modes.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reverse-import-expansion-v1_2/03-CONTEXT.md
@.planning/phases/03-reverse-import-expansion-v1_2/03-RESEARCH.md
@.planning/phases/03-reverse-import-expansion-v1_2/03-VALIDATION.md
@server/routes/ddl-routes.ts
@server/lib/ddl-import/export-service.ts
@server/lib/workbook-templates.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand preview and export route flow for bundle and Oracle subset sources</name>
  <read_first>
    - server/routes/ddl-routes.ts
    - server/lib/ddl-import/export-service.ts
    - shared/routes.ts
    - .planning/phases/03-reverse-import-expansion-v1_2/03-CONTEXT.md
  </read_first>
  <files>server/routes/ddl-routes.ts, client/src/hooks/use-ddl.ts, test/server/reverse-import-phase3.test.ts</files>
  <behavior>
    - Test 1: preview accepts the new source modes and returns one converged review payload
    - Test 2: export honors the same blocker/lossy rules regardless of source mode
    - Test 3: exported workbooks still register only after official-template round-trip validation succeeds
  </behavior>
  <action>Update `server/routes/ddl-routes.ts` so the existing preview and export endpoints accept the new source-mode values and route them through the correct parser adapter before normalization, issue classification, and export. Preserve the same file registration path, remembered-template persistence, and `DdlWorkbookExportError` behavior already used for MySQL. Update hook typings only as needed to carry the new source-mode values and review payload fields. Add focused server tests that cover one SQL bundle preview/export path and one Oracle subset preview path.</action>
  <acceptance_criteria>
    - `server/routes/ddl-routes.ts` contains `sourceMode`
    - `server/routes/ddl-routes.ts` contains `oracle`
    - `server/routes/ddl-routes.ts` contains `bundle`
    - `server/routes/ddl-routes.ts` still contains `DdlWorkbookExportError`
    - `node --test --import tsx test/server/reverse-import-phase3.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/server/reverse-import-phase3.test.ts</automated>
  </verify>
  <done>New reverse-import inputs now reach the same trusted preview/export pipeline as the shipped MySQL path.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve export trust gates and template behavior across all new source modes</name>
  <read_first>
    - server/lib/ddl-import/export-service.ts
    - server/lib/workbook-templates.ts
    - server/routes/ddl-routes.ts
    - .planning/phases/03-reverse-import-expansion-v1_2/03-RESEARCH.md
  </read_first>
  <files>server/lib/ddl-import/export-service.ts, test/server/reverse-import-phase3.test.ts</files>
  <action>Keep `server/lib/ddl-import/export-service.ts` as the only workbook export implementation, and make sure new source modes feed it through the same official template ids, selected-table filtering, round-trip validation, and file registration flow. Add tests that prove Oracle subset and bundle-origin artifacts cannot bypass blocking issues or round-trip validation and that successful exports still produce normal file records.</action>
  <acceptance_criteria>
    - `server/lib/ddl-import/export-service.ts` still contains `round_trip_failed`
    - `server/lib/ddl-import/export-service.ts` still contains `duplicate_hash`
    - `test/server/reverse-import-phase3.test.ts` contains `round-trip`
    - `test/server/reverse-import-phase3.test.ts` contains `official template`
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Phase 3 broadens inputs without broadening the trust boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/server/reverse-import-phase3.test.ts`
- [ ] `npm run check`
- [ ] official template export is still the only workbook path
- [ ] round-trip validation still blocks bad outputs
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- New reverse-import sources converge on the shipped review/export flow
</success_criteria>

<output>
After completion, create `.planning/phases/03-reverse-import-expansion-v1_2/03-03-SUMMARY.md`
</output>
