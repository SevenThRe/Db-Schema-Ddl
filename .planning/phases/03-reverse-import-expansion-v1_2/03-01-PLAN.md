---
phase: 03-reverse-import-expansion-v1_2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - shared/routes.ts
  - client/src/hooks/use-ddl.ts
  - test/server/reverse-import-phase3.test.ts
  - test/client/reverse-import-phase3-ui.test.tsx
autonomous: true
requirements:
  - REV-01
  - REV-02
  - REV-03
  - REV-04
user_setup: []
must_haves:
  truths:
    - "Every new reverse-import source mode produces one stable machine-usable artifact instead of source-specific UI state."
    - "Bundle and Oracle subset support remain explicit in contracts rather than hidden inside implementation-only switches."
    - "The existing DDL import workflow remains the single home for reverse authoring."
  artifacts:
    - path: "shared/schema.ts"
      provides: "typed reverse-import source modes, preview responses, issue payloads, and export requests"
      contains: "ddlImport"
    - path: "shared/routes.ts"
      provides: "typed preview/export seams for the expanded reverse-import workflow"
      contains: "ddl"
    - path: "client/src/hooks/use-ddl.ts"
      provides: "React Query hooks for previewing and exporting new reverse-import modes"
      contains: "usePreviewDdlImport"
  key_links:
    - from: "shared/routes.ts"
      to: "shared/schema.ts"
      via: "preview/export request and response contracts"
      pattern: "sourceMode|dialect|issue|template"
---

<objective>
Define the shared contract for Phase 3 before parser adapters, issue rules, and UI changes diverge.

Purpose: Reverse import expansion introduces new source modes and Oracle subset handling, but it must still look like one workflow to the rest of the app. This plan locks the contract and test seams first.
Output: Shared schemas, typed routes, hook seams, and Wave 0 tests for reverse-import expansion.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reverse-import-expansion-v1_2/03-CONTEXT.md
@.planning/phases/03-reverse-import-expansion-v1_2/03-RESEARCH.md
@.planning/phases/03-reverse-import-expansion-v1_2/03-UI-SPEC.md
@.planning/phases/03-reverse-import-expansion-v1_2/03-VALIDATION.md
@shared/schema.ts
@shared/routes.ts
@client/src/hooks/use-ddl.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed reverse-import source-mode and preview/export contracts</name>
  <read_first>
    - shared/schema.ts
    - shared/routes.ts
    - .planning/phases/03-reverse-import-expansion-v1_2/03-CONTEXT.md
    - .planning/phases/03-reverse-import-expansion-v1_2/03-RESEARCH.md
  </read_first>
  <files>shared/schema.ts, test/server/reverse-import-phase3.test.ts, test/client/reverse-import-phase3-ui.test.tsx</files>
  <behavior>
    - Test 1: contracts encode source modes for pasted SQL, uploaded SQL, SQL bundle, and Oracle subset
    - Test 2: preview artifact carries `sourceMode`, `dialect`, stable ids, issue summary, and selected/exportable tables
    - Test 3: export request still targets the official workbook template family and machine-usable artifact identity
  </behavior>
  <action>Extend `shared/schema.ts` to add a reverse-import source mode enum covering `mysql-paste`, `mysql-file`, `mysql-bundle`, `oracle-paste`, and `oracle-file`. Add or extend preview/export schemas so the response includes concrete fields for `sourceMode`, `dialect`, `catalog`, `issues`, `issueSummary`, `selectableTableNames`, `selectedTableNames`, and remembered template id. Keep the shapes under the existing DDL import namespace instead of creating a second route family. Add focused server/client tests that assert these exact fields and source-mode strings exist.</action>
  <acceptance_criteria>
    - `shared/schema.ts` contains `mysql-bundle`
    - `shared/schema.ts` contains `oracle-paste`
    - `shared/schema.ts` contains `oracle-file`
    - `shared/schema.ts` contains `sourceMode`
    - `shared/schema.ts` contains `dialect`
    - `node --test --import tsx test/server/reverse-import-phase3.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/server/reverse-import-phase3.test.ts</automated>
  </verify>
  <done>Phase 3 starts from one stable contract rather than source-specific implementation state.</done>
</task>

<task type="auto">
  <name>Task 2: Expose typed route and hook seams for expanded reverse import</name>
  <read_first>
    - shared/routes.ts
    - client/src/hooks/use-ddl.ts
    - shared/schema.ts
  </read_first>
  <files>shared/routes.ts, client/src/hooks/use-ddl.ts, test/client/reverse-import-phase3-ui.test.tsx</files>
  <action>Update `shared/routes.ts` and `client/src/hooks/use-ddl.ts` so the existing DDL import preview/export hooks accept the expanded source modes and surface the richer preview payload without manual fetch shape overrides. Preserve the current route names and mutation usage pattern so the workspace can extend in place rather than fork into a new hook surface.</action>
  <acceptance_criteria>
    - `shared/routes.ts` still contains `previewImport`
    - `shared/routes.ts` still contains `exportWorkbook`
    - `client/src/hooks/use-ddl.ts` contains `usePreviewDdlImport`
    - `client/src/hooks/use-ddl.ts` contains `useExportWorkbookFromDdl`
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Backend and UI can extend reverse import through one typed seam instead of ad hoc request bodies.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/server/reverse-import-phase3.test.ts`
- [ ] `npm run check`
- [ ] reverse-import source modes remain schema-backed and machine-usable
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 starts from one stable reverse-import contract
</success_criteria>

<output>
After completion, create `.planning/phases/03-reverse-import-expansion-v1_2/03-01-SUMMARY.md`
</output>
