---
phase: 03-reverse-import-expansion-v1_2
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - server/lib/ddl-import/parser-adapter.ts
  - server/lib/ddl-import/normalize.ts
  - server/lib/ddl-import/issues.ts
  - test/server/reverse-import-phase3.test.ts
autonomous: true
requirements:
  - REV-01
  - REV-02
  - REV-03
  - REV-04
user_setup: []
must_haves:
  truths:
    - "MySQL bundle import and Oracle subset import use local adapters but converge immediately into one normalized catalog."
    - "Unsupported and lossy semantics remain explicit for every new source mode."
    - "Phase 3 broadens import coverage without turning into arbitrary SQL support."
  artifacts:
    - path: "server/lib/ddl-import/parser-adapter.ts"
      provides: "source-specific parser adapters for MySQL bundles and Oracle subset input"
      contains: "parse"
    - path: "server/lib/ddl-import/normalize.ts"
      provides: "shared normalized reverse-import catalog"
      contains: "normalize"
    - path: "server/lib/ddl-import/issues.ts"
      provides: "blocking/confirm/info classification for bundle and Oracle subset inputs"
      contains: "issue"
  key_links:
    - from: "server/lib/ddl-import/parser-adapter.ts"
      to: "server/lib/ddl-import/normalize.ts"
      via: "raw parser output normalized into shared catalog"
      pattern: "parse|normalize|oracle|bundle"
---

<objective>
Implement the parser and issue-classification core for SQL bundles and Oracle first-cut subset support.

Purpose: Phase 3 only works if new source modes converge on the same review model with explicit trust reporting. This plan extends adapters, normalization, and issue rules while keeping scope conservative.
Output: MySQL bundle and Oracle subset parser adapters, normalized catalog support, and explicit unsupported/lossy classification.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reverse-import-expansion-v1_2/03-CONTEXT.md
@.planning/phases/03-reverse-import-expansion-v1_2/03-RESEARCH.md
@.planning/phases/03-reverse-import-expansion-v1_2/03-VALIDATION.md
@server/lib/ddl-import/parser-adapter.ts
@server/lib/ddl-import/normalize.ts
@server/lib/ddl-import/issues.ts
@node_modules/@dbml/core/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend parser adapters for MySQL bundle mode and Oracle subset mode</name>
  <read_first>
    - server/lib/ddl-import/parser-adapter.ts
    - server/lib/ddl-import/normalize.ts
    - .planning/phases/03-reverse-import-expansion-v1_2/03-RESEARCH.md
    - node_modules/@dbml/core/README.md
  </read_first>
  <files>server/lib/ddl-import/parser-adapter.ts, server/lib/ddl-import/normalize.ts, test/server/reverse-import-phase3.test.ts</files>
  <behavior>
    - Test 1: MySQL bundle input reaches the parser adapter through a dedicated source mode rather than a one-off special case
    - Test 2: Oracle subset input uses a dedicated adapter path and normalizes into the same catalog shape
    - Test 3: normalized artifacts preserve dialect and source-mode context
  </behavior>
  <action>Refactor `server/lib/ddl-import/parser-adapter.ts` so the current MySQL parser entry point becomes one branch of a source-aware adapter surface. Add a MySQL bundle parser function that still uses `@dbml/core` and a first-cut Oracle parser function based on `Parser.parseOracleToJSON`. Update `server/lib/ddl-import/normalize.ts` so both adapters produce the same normalized catalog structure, with explicit dialect tagging and no source-specific AST leakage beyond the adapter. Add focused server tests that cover one supported MySQL bundle example and one supported Oracle subset example.</action>
  <acceptance_criteria>
    - `server/lib/ddl-import/parser-adapter.ts` contains `parseOracle`
    - `server/lib/ddl-import/parser-adapter.ts` contains `bundle`
    - `server/lib/ddl-import/normalize.ts` contains `dialect`
    - `test/server/reverse-import-phase3.test.ts` contains `oracle`
    - `test/server/reverse-import-phase3.test.ts` contains `bundle`
    - `node --test --import tsx test/server/reverse-import-phase3.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/server/reverse-import-phase3.test.ts</automated>
  </verify>
  <done>Bundle and Oracle subset imports now feed one normalized review model instead of fragmenting the workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Expand issue classification for unsupported and lossy bundle/Oracle semantics</name>
  <read_first>
    - server/lib/ddl-import/issues.ts
    - server/lib/ddl-import/parser-adapter.ts
    - .planning/phases/03-reverse-import-expansion-v1_2/03-CONTEXT.md
    - .planning/phases/03-reverse-import-expansion-v1_2/03-RESEARCH.md
  </read_first>
  <files>server/lib/ddl-import/issues.ts, test/server/reverse-import-phase3.test.ts</files>
  <action>Extend `server/lib/ddl-import/issues.ts` so Phase 3 explicitly classifies unsupported bundle statements like `ALTER TABLE`, `INSERT`, `DROP`, `VIEW`, `TRIGGER`, and routine definitions, and also classifies Oracle first-cut exclusions such as virtual columns, tablespace/storage attributes, partitioning, and advanced identity/index variants. Keep the existing blocker/confirm/info vocabulary. Make truly inexpressible constructs blocking, reviewable metadata loss confirm/info, and add server tests asserting these exact issue families remain explicit.</action>
  <acceptance_criteria>
    - `server/lib/ddl-import/issues.ts` contains `ALTER TABLE`
    - `server/lib/ddl-import/issues.ts` contains `VIEW`
    - `server/lib/ddl-import/issues.ts` contains `tablespace`
    - `server/lib/ddl-import/issues.ts` contains `partition`
    - `server/lib/ddl-import/issues.ts` contains `blocking`
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Every new reverse-import input remains honest about what is unsupported or lossy.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/server/reverse-import-phase3.test.ts`
- [ ] `npm run check`
- [ ] unsupported bundle statements stay explicit
- [ ] Oracle support remains subset-based rather than accidental broad support
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 parser logic broadens input safely without breaking trust reporting
</success_criteria>

<output>
After completion, create `.planning/phases/03-reverse-import-expansion-v1_2/03-02-SUMMARY.md`
</output>
