---
phase: 03-reverse-import-expansion-v1_2
plan: 04
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - client/src/components/ddl-import/DdlImportWorkspace.tsx
  - client/src/components/DdlGenerator.tsx
  - client/src/hooks/use-ddl.ts
  - test/client/reverse-import-phase3-ui.test.tsx
autonomous: true
requirements:
  - REV-01
  - REV-02
  - REV-03
  - REV-04
user_setup: []
must_haves:
  truths:
    - "Phase 3 remains one DDL import workspace instead of fragmenting by source mode."
    - "Bundle and Oracle subset inputs stay explicit in the UI and do not masquerade as generic SQL support."
    - "Blocking and lossy trust states remain visible and actionable before export."
  artifacts:
    - path: "client/src/components/ddl-import/DdlImportWorkspace.tsx"
      provides: "expanded three-column reverse-import workspace"
      contains: "sourceMode"
    - path: "test/client/reverse-import-phase3-ui.test.tsx"
      provides: "UI seam and copy-contract coverage for the new reverse-import modes"
      contains: "oracle"
  key_links:
    - from: "client/src/components/ddl-import/DdlImportWorkspace.tsx"
      to: ".planning/phases/03-reverse-import-expansion-v1_2/03-UI-SPEC.md"
      via: "three-column layout, source-mode labels, and trust-first export rail"
      pattern: "导入来源|结构审阅|问题与导出"
---

<objective>
Extend the DDL import workspace so SQL bundles and Oracle subset input feel native to the existing review/export experience.

Purpose: Phase 3 must ship as one coherent reverse-import workflow, not as a pile of hidden switches. This plan adapts the existing UI to the new source modes while preserving the trust-first three-column model.
Output: Expanded DDL import workspace, source-mode controls, bundle/Oracle subset messaging, and focused client validation.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reverse-import-expansion-v1_2/03-CONTEXT.md
@.planning/phases/03-reverse-import-expansion-v1_2/03-RESEARCH.md
@.planning/phases/03-reverse-import-expansion-v1_2/03-UI-SPEC.md
@.planning/phases/03-reverse-import-expansion-v1_2/03-VALIDATION.md
@client/src/components/ddl-import/DdlImportWorkspace.tsx
@client/src/components/DdlGenerator.tsx
@client/src/hooks/use-ddl.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the DDL import workspace for bundle and Oracle subset source modes</name>
  <read_first>
    - client/src/components/ddl-import/DdlImportWorkspace.tsx
    - .planning/phases/03-reverse-import-expansion-v1_2/03-UI-SPEC.md
    - .planning/phases/03-reverse-import-expansion-v1_2/03-CONTEXT.md
    - client/src/components/DdlGenerator.tsx
  </read_first>
  <files>client/src/components/ddl-import/DdlImportWorkspace.tsx, test/client/reverse-import-phase3-ui.test.tsx</files>
  <behavior>
    - Test 1: workspace exposes source modes for pasted SQL, uploaded SQL, SQL bundle, and Oracle subset input
    - Test 2: the three-column structure remains intact with titles matching the UI contract
    - Test 3: unsupported and lossy states remain visible before export
  </behavior>
  <action>Update `client/src/components/ddl-import/DdlImportWorkspace.tsx` so the left panel explicitly supports the new source modes and explains their constraints. Keep the same three-column layout, but revise copy and controls to use the UI contract titles `导入来源`, `结构审阅`, and `问题与导出`. Add explicit wording that Oracle support is subset-based and SQL bundle import is structure-oriented rather than arbitrary SQL. Add focused client tests that assert these exact source-mode labels and trust-gate copy remain present.</action>
  <acceptance_criteria>
    - `client/src/components/ddl-import/DdlImportWorkspace.tsx` contains `导入来源`
    - `client/src/components/ddl-import/DdlImportWorkspace.tsx` contains `结构审阅`
    - `client/src/components/ddl-import/DdlImportWorkspace.tsx` contains `问题与导出`
    - `client/src/components/ddl-import/DdlImportWorkspace.tsx` contains `bundle`
    - `client/src/components/ddl-import/DdlImportWorkspace.tsx` contains `Oracle`
    - `node --test --import tsx test/client/reverse-import-phase3-ui.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/client/reverse-import-phase3-ui.test.tsx</automated>
  </verify>
  <done>The DDL import workspace now presents new reverse-import modes as one coherent workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve file-first handoff and final workflow validation</name>
  <read_first>
    - client/src/components/ddl-import/DdlImportWorkspace.tsx
    - client/src/hooks/use-ddl.ts
    - client/src/components/DdlGenerator.tsx
    - .planning/phases/03-reverse-import-expansion-v1_2/03-VALIDATION.md
  </read_first>
  <files>client/src/components/ddl-import/DdlImportWorkspace.tsx, client/src/hooks/use-ddl.ts, test/client/reverse-import-phase3-ui.test.tsx</files>
  <action>Preserve the existing behavior where successful export activates the generated workbook in the normal file flow, regardless of whether the source was pasted SQL, uploaded SQL, bundle, or Oracle subset. Expand client tests so they verify source-mode wiring, trust-state copy, and file-activation flow without introducing a new top-level module.</action>
  <acceptance_criteria>
    - `test/client/reverse-import-phase3-ui.test.tsx` contains `source mode`
    - `test/client/reverse-import-phase3-ui.test.tsx` contains `问题与导出`
    - `test/client/reverse-import-phase3-ui.test.tsx` contains `activate`
    - `npm run check` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
    <automated>npm test</automated>
  </verify>
  <done>Phase 3 closes with one coherent reverse-import UI that still returns users to the normal workbook workflow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/client/reverse-import-phase3-ui.test.tsx`
- [ ] `npm run check`
- [ ] `npm test`
- [ ] no separate Oracle-only or bundle-only review module is introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 ships as one expanded reverse-import workflow rather than fragmented mode-specific screens
</success_criteria>

<output>
After completion, create `.planning/phases/03-reverse-import-expansion-v1_2/03-04-SUMMARY.md`
</output>
