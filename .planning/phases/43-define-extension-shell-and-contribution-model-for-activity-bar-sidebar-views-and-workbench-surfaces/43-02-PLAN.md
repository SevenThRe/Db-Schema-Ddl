---
phase: 43-define-extension-shell-and-contribution-model-for-activity-bar-sidebar-views-and-workbench-surfaces
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - client/src/extensions/contribution-resolver.ts
  - client/src/extensions/host-context.tsx
  - client/src/extensions/host-api.ts
  - client/src/extensions/ExtensionWorkspaceHost.tsx
  - test/client/extension-contribution-model-phase43.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The frontend host resolves activity items, sidebar views, and workbench views as separate collections instead of one nav item mapped to one panel."
    - "Main-surface routing can point at an extension activity and workbench view without leaking `panelId` as the only contract."
    - "Legacy manifests still normalize into a usable extension shell shape during the migration window."
  artifacts:
    - path: "client/src/extensions/contribution-resolver.ts"
      provides: "Normalized extension-shell resolver output"
      contains: "resolveActivityBarItems"
    - path: "client/src/extensions/host-context.tsx"
      provides: "Frontend host state for activity/sidebar/workbench collections"
      contains: "activityBarItems"
    - path: "client/src/extensions/host-api.ts"
      provides: "Main surface route contract for the new extension shell"
      contains: "activityItemId"
    - path: "test/client/extension-contribution-model-phase43.test.ts"
      provides: "Regression guard for schema and compatibility mapping"
      contains: "workbenchViews"
  key_links:
    - from: "client/src/extensions/contribution-resolver.ts"
      to: "client/src/extensions/host-context.tsx"
      via: "normalized activity/sidebar/workbench collections flow into host state"
      pattern: "activityBarItems"
    - from: "client/src/extensions/host-api.ts"
      to: "client/src/extensions/ExtensionWorkspaceHost.tsx"
      via: "workbench view routing replaces panel-only assumptions"
      pattern: "workbenchViewId"
---

<objective>
Normalize the new shell contract on the frontend. The host should be able to resolve extension activities, sidebar tabs, and workbench surfaces separately while still tolerating old manifests until Phase 44 rebuilds the visible shell chrome.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-define-extension-shell-and-contribution-model-for-activity-bar-sidebar-views-and-workbench-surfaces/43-CONTEXT.md
@client/src/extensions/contribution-resolver.ts
@client/src/extensions/host-context.tsx
@client/src/extensions/host-api.ts
@client/src/extensions/ExtensionWorkspaceHost.tsx
@shared/extension-schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace nav-to-panel inference with normalized extension-shell resolvers</name>
  <read_first>
    - client/src/extensions/contribution-resolver.ts
    - shared/extension-schema.ts
    - .planning/phases/43-define-extension-shell-and-contribution-model-for-activity-bar-sidebar-views-and-workbench-surfaces/43-CONTEXT.md
  </read_first>
  <files>
    - client/src/extensions/contribution-resolver.ts
  </files>
  <action>
Add normalized resolver functions named `resolveActivityBarItems`, `resolveSidebarViews`, and `resolveWorkbenchViews` in `client/src/extensions/contribution-resolver.ts`. Make them prefer canonical `activityBar`, `sidebarViews`, and `workbenchViews` fields, but also provide compatibility mapping from legacy `navigation` and `workspacePanels` when the new fields are absent. Remove the current assumption that one nav item should automatically bind `panelId` from `workspacePanels[0]`.
  </action>
  <acceptance_criteria>
    - `client/src/extensions/contribution-resolver.ts` contains `resolveActivityBarItems`
    - `client/src/extensions/contribution-resolver.ts` contains `resolveSidebarViews`
    - `client/src/extensions/contribution-resolver.ts` contains `resolveWorkbenchViews`
    - `client/src/extensions/contribution-resolver.ts` contains `defaultSidebarViewId`
    - `client/src/extensions/contribution-resolver.ts` contains `defaultWorkbenchViewId`
    - no new resolver path in `client/src/extensions/contribution-resolver.ts` infers shell defaults from `workspacePanels[0]`
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The frontend can resolve a real extension shell model instead of flattening everything into nav items and panels.</done>
</task>

<task type="auto">
  <name>Task 2: Extend host state and workbench mounting to carry activity and workbench view identity</name>
  <read_first>
    - client/src/extensions/host-context.tsx
    - client/src/extensions/host-api.ts
    - client/src/extensions/ExtensionWorkspaceHost.tsx
    - client/src/extensions/contribution-resolver.ts
  </read_first>
  <files>
    - client/src/extensions/host-context.tsx
    - client/src/extensions/host-api.ts
    - client/src/extensions/ExtensionWorkspaceHost.tsx
  </files>
  <action>
Update `client/src/extensions/host-context.tsx` to expose `activityBarItems`, `sidebarViews`, and `workbenchViews` alongside any temporary compatibility outputs. Extend `MainSurface` in `client/src/extensions/host-api.ts` so extension navigation can carry `activityItemId` and `workbenchViewId`. Update `client/src/extensions/ExtensionWorkspaceHost.tsx` so the canonical path resolves a `workbenchViewId`, while a compatibility fallback can still open legacy `panelId` routes until Phase 44 finishes the shell migration.
  </action>
  <acceptance_criteria>
    - `client/src/extensions/host-context.tsx` contains `activityBarItems`
    - `client/src/extensions/host-context.tsx` contains `sidebarViews`
    - `client/src/extensions/host-context.tsx` contains `workbenchViews`
    - `client/src/extensions/host-api.ts` contains `activityItemId`
    - `client/src/extensions/host-api.ts` contains `workbenchViewId`
    - `client/src/extensions/ExtensionWorkspaceHost.tsx` contains `workbenchViewId`
    - `client/src/extensions/ExtensionWorkspaceHost.tsx` keeps a compatibility path for `panelId`
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The frontend host can route extension shell state without forcing everything through `panelId`.</done>
</task>

<task type="auto">
  <name>Task 3: Add a focused compatibility guard for the new contribution model</name>
  <read_first>
    - client/src/extensions/contribution-resolver.ts
    - client/src/extensions/host-context.tsx
    - client/src/extensions/host-api.ts
  </read_first>
  <files>
    - test/client/extension-contribution-model-phase43.test.ts
  </files>
  <action>
Add a static regression test file named `test/client/extension-contribution-model-phase43.test.ts` that asserts the new contribution model exists and that the migration path remains explicit. Cover the presence of `resolveActivityBarItems`, `activityBarItems`, `workbenchViewId`, and the canonical `activityBar` / `sidebarViews` / `workbenchViews` field names.
  </action>
  <acceptance_criteria>
    - `test/client/extension-contribution-model-phase43.test.ts` exists
    - the test asserts `client/src/extensions/contribution-resolver.ts` contains `resolveActivityBarItems`
    - the test asserts `client/src/extensions/host-context.tsx` contains `activityBarItems`
    - the test asserts `client/src/extensions/host-api.ts` contains `workbenchViewId`
    - the test asserts `shared/extension-schema.ts` contains `activityBar`, `sidebarViews`, and `workbenchViews`
  </acceptance_criteria>
  <verify>
    <automated>NODE_OPTIONS=--import=tsx node --test --experimental-strip-types test/client/extension-contribution-model-phase43.test.ts</automated>
  </verify>
  <done>The new shell contract and its compatibility bridge are guarded against regression.</done>
</task>

</tasks>

<verification>
- npm run check
- NODE_OPTIONS=--import=tsx node --test --experimental-strip-types test/client/extension-contribution-model-phase43.test.ts
</verification>

<success_criteria>
- Phase 43 truth 1: frontend resolver output separates activity, sidebar, and workbench concepts
- Phase 43 truth 2: host routing carries activity and workbench identity explicitly
- Phase 43 truth 3: compatibility mapping remains guarded while the shell migrates
</success_criteria>

<output>
After completion, create `.planning/phases/43-define-extension-shell-and-contribution-model-for-activity-bar-sidebar-views-and-workbench-surfaces/43-02-SUMMARY.md`
</output>
