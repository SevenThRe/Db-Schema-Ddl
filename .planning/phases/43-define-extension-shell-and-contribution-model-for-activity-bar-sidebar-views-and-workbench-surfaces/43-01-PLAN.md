---
phase: 43-define-extension-shell-and-contribution-model-for-activity-bar-sidebar-views-and-workbench-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/extension-schema.ts
  - src-tauri/src/builtin_extensions/mod.rs
  - src-tauri/src/extensions/manifest.rs
  - src-tauri/src/extensions/commands.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "The manifest contract names activity bar items, sidebar views, and workbench views explicitly instead of inferring one panel from one nav item."
    - "TypeScript and Rust manifest layers stay in lockstep so builtin and external extensions share one shell contribution model."
    - "Phase 44 can select default sidebar and workbench targets without relying on array-order guesses."
  artifacts:
    - path: "shared/extension-schema.ts"
      provides: "Canonical contribution schema for the new extension shell"
      contains: "activityBarItemSchema"
    - path: "src-tauri/src/builtin_extensions/mod.rs"
      provides: "Rust mirror of extension shell contribution structs"
      contains: "pub struct ActivityBarItem"
    - path: "src-tauri/src/extensions/manifest.rs"
      provides: "External manifest parsing for the new contribution model"
      contains: "pub contributes: Option<ExtensionContributes>"
    - path: "src-tauri/src/extensions/commands.rs"
      provides: "Serialized extension state that includes the new contribution fields"
      contains: "pub contributes: ExtensionContributes"
  key_links:
    - from: "shared/extension-schema.ts"
      to: "src-tauri/src/builtin_extensions/mod.rs"
      via: "matching activity/sidebar/workbench contribution names and defaults"
      pattern: "activityBar"
    - from: "src-tauri/src/extensions/manifest.rs"
      to: "src-tauri/src/extensions/commands.rs"
      via: "external manifest parsing flows into `ext_list_all` serialization"
      pattern: "contributes"
---

<objective>
Define one manifest contract for the extension shell. Builtin and external extensions should be able to declare activity bar entries, sidebar views, and workbench views without forcing the host to pretend one navigation item equals one panel.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-define-extension-shell-and-contribution-model-for-activity-bar-sidebar-views-and-workbench-surfaces/43-CONTEXT.md
@shared/extension-schema.ts
@src-tauri/src/builtin_extensions/mod.rs
@src-tauri/src/extensions/manifest.rs
@src-tauri/src/extensions/commands.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit activity, sidebar, and workbench contribution schemas in shared TypeScript</name>
  <read_first>
    - shared/extension-schema.ts
    - .planning/phases/43-define-extension-shell-and-contribution-model-for-activity-bar-sidebar-views-and-workbench-surfaces/43-CONTEXT.md
  </read_first>
  <files>
    - shared/extension-schema.ts
  </files>
  <action>
Add `activityBarItemSchema`, `sidebarViewSchema`, and `workbenchViewSchema` to `shared/extension-schema.ts`. Update `extensionContributesSchema` so the canonical contribution fields are `activityBar`, `sidebarViews`, and `workbenchViews`. Include explicit `defaultSidebarViewId` and `defaultWorkbenchViewId` on activity items. Keep `navigation` and `workspacePanels` parseable as deprecated compatibility inputs so existing manifests still validate during the migration window.
  </action>
  <acceptance_criteria>
    - `shared/extension-schema.ts` contains `activityBarItemSchema`
    - `shared/extension-schema.ts` contains `sidebarViewSchema`
    - `shared/extension-schema.ts` contains `workbenchViewSchema`
    - `shared/extension-schema.ts` contains `defaultSidebarViewId`
    - `shared/extension-schema.ts` contains `defaultWorkbenchViewId`
    - `shared/extension-schema.ts` contains `activityBar`, `sidebarViews`, and `workbenchViews` inside `extensionContributesSchema`
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The shared extension schema can express a real shell model instead of only nav items and panels.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the new contribution contract in Rust builtin and external manifest types</name>
  <read_first>
    - src-tauri/src/builtin_extensions/mod.rs
    - src-tauri/src/extensions/manifest.rs
    - src-tauri/src/extensions/commands.rs
    - shared/extension-schema.ts
  </read_first>
  <files>
    - src-tauri/src/builtin_extensions/mod.rs
    - src-tauri/src/extensions/manifest.rs
    - src-tauri/src/extensions/commands.rs
  </files>
  <action>
Add Rust structs named `ActivityBarItem`, `SidebarView`, and `WorkbenchView` in `src-tauri/src/builtin_extensions/mod.rs`, then extend `ExtensionContributes` so it serializes `activityBar`, `sidebarViews`, and `workbenchViews` with camelCase parity. Keep serde defaults so missing fields still deserialize. Update `src-tauri/src/extensions/manifest.rs` and `src-tauri/src/extensions/commands.rs` to parse and serialize the expanded `ExtensionContributes` without inventing a second external-extension schema.
  </action>
  <acceptance_criteria>
    - `src-tauri/src/builtin_extensions/mod.rs` contains `pub struct ActivityBarItem`
    - `src-tauri/src/builtin_extensions/mod.rs` contains `pub struct SidebarView`
    - `src-tauri/src/builtin_extensions/mod.rs` contains `pub struct WorkbenchView`
    - `src-tauri/src/builtin_extensions/mod.rs` contains `pub activity_bar: Vec<ActivityBarItem>`
    - `src-tauri/src/builtin_extensions/mod.rs` contains `pub sidebar_views: Vec<SidebarView>`
    - `src-tauri/src/builtin_extensions/mod.rs` contains `pub workbench_views: Vec<WorkbenchView>`
    - `src-tauri/src/extensions/manifest.rs` still deserializes `pub contributes: Option<ExtensionContributes>`
    - `src-tauri/src/extensions/commands.rs` still serializes `pub contributes: ExtensionContributes`
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Rust manifest parsing and serialization stay aligned with the new TypeScript shell contract.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the contract around stable defaults instead of array-order inference</name>
  <read_first>
    - shared/extension-schema.ts
    - src-tauri/src/builtin_extensions/mod.rs
    - .planning/phases/43-define-extension-shell-and-contribution-model-for-activity-bar-sidebar-views-and-workbench-surfaces/43-CONTEXT.md
  </read_first>
  <files>
    - shared/extension-schema.ts
    - src-tauri/src/builtin_extensions/mod.rs
  </files>
  <action>
Make the new contract explicit enough that Phase 44 can render stable defaults without guessing from array position. Activity items must provide `defaultSidebarViewId` and `defaultWorkbenchViewId`, and the Rust mirror must preserve those fields. Do not keep any new shell path dependent on `workspacePanels[0]` ordering.
  </action>
  <acceptance_criteria>
    - `shared/extension-schema.ts` contains `defaultSidebarViewId`
    - `shared/extension-schema.ts` contains `defaultWorkbenchViewId`
    - `src-tauri/src/builtin_extensions/mod.rs` contains `default_sidebar_view_id`
    - `src-tauri/src/builtin_extensions/mod.rs` contains `default_workbench_view_id`
    - no new contribution field in `shared/extension-schema.ts` relies on `workspacePanels[0]`
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The contribution contract provides stable defaults without array-order guessing.</done>
</task>

</tasks>

<verification>
- npm run check
</verification>

<success_criteria>
- Phase 43 truth 1: manifests express `activityBar`, `sidebarViews`, and `workbenchViews` explicitly
- Phase 43 truth 2: Rust and TypeScript contribution contracts stay in sync
- Phase 43 truth 3: default shell targets are explicit rather than inferred from list order
</success_criteria>

<output>
After completion, create `.planning/phases/43-define-extension-shell-and-contribution-model-for-activity-bar-sidebar-views-and-workbench-surfaces/43-01-SUMMARY.md`
</output>
