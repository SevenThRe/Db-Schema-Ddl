---
phase: 38-add-secure-ssh-and-tls-transport-connectivity-for-supported-drivers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - src-tauri/src/db_connector/mod.rs
  - src-tauri/src/db_connector/introspect.rs
  - src-tauri/src/db_connector/query.rs
  - src-tauri/src/db_connector/explain.rs
  - src-tauri/src/db_connector/transport.rs
  - client/src/components/extensions/DbConnectorWorkspace.tsx
  - test/client/db-transport-phase38.test.ts
  - test/server/db-transport-phase38.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Secure transport settings are typed connection config, not informal notes in the connection name or comment."
    - "TLS and SSH transport profiles are honored by connection test, introspection, query, and explain paths."
    - "Transport diagnostics fail clearly without leaking secret values or certificate contents."
  artifacts:
    - path: "shared/schema.ts"
      provides: "transport configuration contract"
      contains: "tlsMode"
    - path: "src-tauri/src/db_connector/transport.rs"
      provides: "shared transport resolution for DB runtime paths"
      contains: "ssh_tunnel"
    - path: "client/src/components/extensions/DbConnectorWorkspace.tsx"
      provides: "transport configuration UI"
      contains: "TLS mode"
  key_links:
    - from: "src-tauri/src/db_connector/transport.rs"
      to: "src-tauri/src/db_connector/query.rs"
      via: "resolved transport profile"
      pattern: "tlsMode"
---

<objective>
Add explicit TLS and SSH transport profiles for supported drivers so the workbench can operate through secure topologies expected from professional desktop DB tools.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/db-workbench-feature-checklist.md
@shared/schema.ts
@src-tauri/src/db_connector/mod.rs
@src-tauri/src/db_connector/introspect.rs
@src-tauri/src/db_connector/query.rs
@src-tauri/src/db_connector/explain.rs
@client/src/components/extensions/DbConnectorWorkspace.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add concrete TLS and SSH transport config to saved connections</name>
  <read_first>
    - shared/schema.ts
    - src-tauri/src/db_connector/mod.rs
    - client/src/components/extensions/DbConnectorWorkspace.tsx
  </read_first>
  <files>
    - shared/schema.ts
    - src-tauri/src/db_connector/mod.rs
    - client/src/components/extensions/DbConnectorWorkspace.tsx
  </files>
  <action>
Add transport fields `tlsMode`, `tlsCaPath`, `tlsCertPath`, `tlsKeyPath`, and `sshTunnel` with `host`, `port`, `username`, `authMode`, and `privateKeyPath`, then expose them in the connection editor with explicit driver-aware labels.
  </action>
  <acceptance_criteria>
    - `shared/schema.ts` contains `tlsMode`
    - `shared/schema.ts` contains `sshTunnel`
    - `DbConnectorWorkspace.tsx` contains `TLS mode`
    - `DbConnectorWorkspace.tsx` contains `SSH tunnel`
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Saved connections can now describe transport posture explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize secure transport resolution for introspection and runtime query paths</name>
  <read_first>
    - src-tauri/src/db_connector/introspect.rs
    - src-tauri/src/db_connector/query.rs
    - src-tauri/src/db_connector/explain.rs
  </read_first>
  <files>
    - src-tauri/src/db_connector/transport.rs
    - src-tauri/src/db_connector/introspect.rs
    - src-tauri/src/db_connector/query.rs
    - src-tauri/src/db_connector/explain.rs
  </files>
  <action>
Create `src-tauri/src/db_connector/transport.rs` to resolve TLS and SSH settings into runtime connection options, and route introspection, query, and explain through that shared transport layer so the same profile is honored everywhere.
  </action>
  <acceptance_criteria>
    - `src-tauri/src/db_connector/transport.rs` exists
    - `transport.rs` contains `tls_mode`
    - `transport.rs` contains `ssh_tunnel`
    - `introspect.rs`, `query.rs`, and `explain.rs` all consume the shared transport helper
  </acceptance_criteria>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -j 1</automated>
  </verify>
  <done>Runtime DB entrypoints no longer ignore the configured secure transport profile.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused transport tests for config, UI, and runtime diagnostics</name>
  <read_first>
    - shared/schema.ts
    - src-tauri/src/db_connector/transport.rs
    - client/src/components/extensions/DbConnectorWorkspace.tsx
  </read_first>
  <files>
    - test/client/db-transport-phase38.test.ts
    - test/server/db-transport-phase38.test.ts
  </files>
  <action>
Add client and server tests that assert TLS and SSH fields exist in the config contract, the connection editor renders transport controls, and runtime diagnostics fail without printing secret values or certificate contents.
  </action>
  <acceptance_criteria>
    - `test/client/db-transport-phase38.test.ts` exists
    - `test/server/db-transport-phase38.test.ts` exists
    - the tests assert `tlsMode`
    - the tests assert `sshTunnel`
  </acceptance_criteria>
  <verify>
    <automated>node --import=tsx --test test/client/db-transport-phase38.test.ts test/server/db-transport-phase38.test.ts</automated>
  </verify>
  <done>Transport profiles and diagnostics are guarded by focused regression tests.</done>
</task>

</tasks>

<verification>
- npm run check
- cargo check --manifest-path src-tauri/Cargo.toml -j 1
- node --import=tsx --test test/client/db-transport-phase38.test.ts test/server/db-transport-phase38.test.ts
</verification>

<success_criteria>
- Supported drivers can connect through explicit TLS and SSH transport profiles instead of direct plain-host assumptions
</success_criteria>

<output>
After completion, create `.planning/phases/38-add-secure-ssh-and-tls-transport-connectivity-for-supported-drivers/38-01-SUMMARY.md`
</output>
