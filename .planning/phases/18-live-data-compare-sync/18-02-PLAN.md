---
phase: 18-live-data-compare-sync
plan: 02
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - src-tauri/src/db_connector/data_diff.rs
  - src-tauri/src/db_connector/mod.rs
  - src-tauri/src/db_connector/commands.rs
  - src-tauri/src/db_connector/query.rs
  - src-tauri/src/storage.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
must_haves:
  truths:
    - "Compare direction is enforced as source->target and never inferred from UI state"
    - "Stable key precedence is deterministic: primary key, then unique key, then operator-selected business keys"
    - "Compare artifacts persist targetSnapshotHash with 15-minute TTL so apply-stage stale checks can be enforced server-side"
  artifacts:
    - path: "src-tauri/src/db_connector/data_diff.rs"
      provides: "Compare engine with key resolution and row status classification"
      contains: "fn resolve_stable_keys"
    - path: "src-tauri/src/db_connector/data_diff.rs"
      provides: "Snapshot hash generation and expiration validation"
      contains: "target_snapshot_hash"
    - path: "src-tauri/src/storage.rs"
      provides: "Local persistence schema for compare metadata and table-level results"
      contains: "db_data_compares"
    - path: "src-tauri/src/db_connector/commands.rs"
      provides: "Command handlers for diff preview and diff detail"
      contains: "db_data_diff_detail"
  key_links:
    - from: "src-tauri/src/db_connector/commands.rs"
      to: "src-tauri/src/db_connector/data_diff.rs"
      via: "Command handlers delegate to compare service functions"
      pattern: "db_data_diff_preview"
    - from: "src-tauri/src/db_connector/data_diff.rs"
      to: "src-tauri/src/storage.rs"
      via: "Compare artifact persistence writes to local sqlite tables"
      pattern: "db_data_compares"
---

<objective>
Implement compare runtime core with deterministic key policy, row delta classification, and snapshot guard artifact persistence.

Purpose: SYNC-01 requires reliable key-based classification; SYNC-02 requires server-side guard artifacts that block stale apply attempts.
Output: data-diff backend module, compare preview/detail commands, persisted compare artifacts with hash + TTL.
</objective>

<execution_context>
C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-live-data-compare-sync/18-CONTEXT.md
@docs/db-workbench-data-sync-design.md
@shared/schema.ts
@src-tauri/src/db_connector/mod.rs
@src-tauri/src/db_connector/commands.rs
@src-tauri/src/db_connector/query.rs
@src-tauri/src/storage.rs
@src-tauri/src/lib.rs

<interfaces>
Required backend behavior:
- compare request accepts explicit source_connection_id, target_connection_id, table scopes, and optional business key overrides
- output classifies rows as source_only, target_only, value_changed, unchanged
- unchanged rows are summarized but not included in default detail payload window
- compare artifact expires after 15 minutes and carries compareId + targetSnapshotHash
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build data-diff engine with deterministic key selection and row classification</name>
  <files>src-tauri/src/db_connector/data_diff.rs, src-tauri/src/db_connector/query.rs, src-tauri/src/db_connector/mod.rs</files>
  <read_first>
    - .planning/phases/18-live-data-compare-sync/18-CONTEXT.md (decisions 1 and 3 define key precedence and classification contract)
    - src-tauri/src/db_connector/query.rs (schema/table introspection and safe query helper reuse)
    - src-tauri/src/db_connector/mod.rs (new request/response structures from 18-01)
    - docs/db-workbench-data-sync-design.md (expected compare model and UI-facing shape)
  </read_first>
  <action>
Create src-tauri/src/db_connector/data_diff.rs and implement compare behavior with fixed key policy.

1. Implement resolve_stable_keys(table) with precedence:
   - primary key columns
   - unique index columns
   - request.business_key_columns when both prior options are unavailable
2. For tables without stable keys, mark table blocked with blocker code missing_stable_key and skip apply eligibility.
3. Load source and target rows with canonical key tuple extraction and classify each row into source_only, target_only, value_changed, unchanged.
4. Compare columns must be exactly the intersection of non-key columns that exist in both source and target snapshots.
5. Return summary counts by status and sampled row deltas for detail drilldown.
  </action>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml data_diff -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/db_connector/data_diff.rs exists
    - src-tauri/src/db_connector/data_diff.rs contains fn resolve_stable_keys
    - src-tauri/src/db_connector/data_diff.rs contains source_only
    - src-tauri/src/db_connector/data_diff.rs contains target_only
    - src-tauri/src/db_connector/data_diff.rs contains value_changed
    - src-tauri/src/db_connector/data_diff.rs contains missing_stable_key
    - cargo check --manifest-path src-tauri/Cargo.toml exits 0
    - cargo test --manifest-path src-tauri/Cargo.toml data_diff -- --nocapture exits 0
  </acceptance_criteria>
  <done>Compare engine deterministically resolves keys and produces explicit row status classification.</done>
</task>

<task type="auto">
  <name>Task 2: Persist compare artifacts with snapshot hash and 15-minute TTL</name>
  <files>src-tauri/src/storage.rs, src-tauri/src/db_connector/data_diff.rs, src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/storage.rs (sqlite schema creation and migration patterns)
    - src-tauri/src/lib.rs (storage initialization lifecycle)
    - .planning/phases/18-live-data-compare-sync/18-CONTEXT.md (snapshot guard and expiry requirements)
  </read_first>
  <action>
Add durable compare artifact storage and enforce expiration metadata.

1. In storage.rs, add sqlite tables:
   - db_data_compares(compare_id, source_connection_id, target_connection_id, target_snapshot_hash, created_at, expires_at, compare_scope_json)
   - db_data_compare_tables(compare_id, table_name, key_columns_json, status_counts_json, blocked, blocker_code)
2. In data_diff.rs preview flow:
   - compute deterministic target_snapshot_hash over target row canonical payload for selected tables and compare columns.
   - set expires_at = created_at + 15 minutes.
   - persist compare metadata and table summaries.
3. Reject detail requests when compare artifact is expired with blocker artifact_expired.
4. Keep hash generation canonical (sorted table names, sorted key tuples, stable JSON ordering) so repeat compare on unchanged data yields the same hash.
  </action>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
    <automated>rg -n "db_data_compares|db_data_compare_tables|target_snapshot_hash|artifact_expired|expires_at" src-tauri/src/storage.rs src-tauri/src/db_connector/data_diff.rs</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/storage.rs contains db_data_compares
    - src-tauri/src/storage.rs contains db_data_compare_tables
    - src-tauri/src/db_connector/data_diff.rs contains target_snapshot_hash
    - src-tauri/src/db_connector/data_diff.rs contains artifact_expired
    - src-tauri/src/db_connector/data_diff.rs contains expires_at
    - cargo check --manifest-path src-tauri/Cargo.toml exits 0
    - rg -n "db_data_compares|target_snapshot_hash" src-tauri/src/storage.rs src-tauri/src/db_connector/data_diff.rs returns matches in both files
  </acceptance_criteria>
  <done>Compare outputs now produce durable, expiring artifacts with deterministic target snapshot hash.</done>
</task>

<task type="auto">
  <name>Task 3: Expose compare preview/detail commands through command module and tests</name>
  <files>src-tauri/src/db_connector/commands.rs, src-tauri/src/db_connector/data_diff.rs, src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/db_connector/commands.rs (existing command wrapper style)
    - src-tauri/src/lib.rs (command registration pattern)
    - src-tauri/src/db_connector/data_diff.rs (service APIs from tasks 1-2)
  </read_first>
  <action>
Complete compare command path and regression lock-in.

1. Wire db_data_diff_preview and db_data_diff_detail handlers in commands.rs to call data_diff module functions and map service errors to String payloads consistent with existing command style.
2. Ensure lib.rs includes data_diff module registration and command handlers from commands.rs remain reachable.
3. Add focused Rust tests for:
   - key precedence selection order
   - status-count correctness for insert/update/delete/unchanged
   - expired artifact rejection in detail path
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml data_diff -- --nocapture</automated>
    <automated>rg -n "db_data_diff_preview|db_data_diff_detail|artifact_expired|resolve_stable_keys" src-tauri/src/db_connector/commands.rs src-tauri/src/db_connector/data_diff.rs</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/db_connector/commands.rs contains pub async fn db_data_diff_preview
    - src-tauri/src/db_connector/commands.rs contains pub async fn db_data_diff_detail
    - src-tauri/src/db_connector/data_diff.rs contains test name including key_precedence
    - src-tauri/src/db_connector/data_diff.rs contains test name including artifact_expired
    - cargo test --manifest-path src-tauri/Cargo.toml data_diff -- --nocapture exits 0
  </acceptance_criteria>
  <done>Compare command surface is executable and regression-covered for key precedence and TTL guard behavior.</done>
</task>

</tasks>

<verification>
- cargo check --manifest-path src-tauri/Cargo.toml
- cargo test --manifest-path src-tauri/Cargo.toml data_diff -- --nocapture
- rg -n "source_only|target_only|value_changed|target_snapshot_hash|artifact_expired|db_data_diff_preview" src-tauri/src/db_connector/data_diff.rs src-tauri/src/db_connector/commands.rs src-tauri/src/storage.rs
</verification>

<success_criteria>
- SYNC-01: compare API returns deterministic per-table insert/update/delete classifications based on stable key policy
- SYNC-02 foundation: compare artifacts capture target snapshot hash and hard-expire after 15 minutes
- Apply stage can now rely on persisted compare metadata rather than frontend-only state
</success_criteria>

<output>
After completion, create .planning/phases/18-live-data-compare-sync/18-02-SUMMARY.md
</output>
