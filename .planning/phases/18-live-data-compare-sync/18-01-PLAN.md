---
phase: 18-live-data-compare-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - client/src/extensions/host-api.ts
  - client/src/extensions/host-api-runtime.ts
  - client/src/lib/desktop-bridge.ts
  - src-tauri/src/builtin_extensions/mod.rs
  - src-tauri/src/db_connector/mod.rs
  - src-tauri/src/db_connector/commands.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03
must_haves:
  truths:
    - "Phase-18 data sync contracts are explicitly typed across shared/schema.ts, host API, desktop bridge, and Rust request/response structures before compare/apply logic lands"
    - "A dedicated capability gate db.data.sync is enforced in runtime API and declared in builtin extension metadata"
    - "Tauri command surface includes compare preview/detail and apply preview/execute/job detail entrypoints, not implicit internal calls"
  artifacts:
    - path: "shared/schema.ts"
      provides: "Typed request/response contracts for compare and apply workflows"
      contains: "export interface DbDataDiffPreviewRequest"
    - path: "src-tauri/src/db_connector/mod.rs"
      provides: "Rust mirror structs and blocker/status enums for compare/apply commands"
      contains: "pub struct DbDataApplyPreviewRequest"
    - path: "client/src/extensions/host-api-runtime.ts"
      provides: "Fail-closed capability checks for sync APIs"
      contains: "requireCap(granted, \"db.data.sync\")"
    - path: "src-tauri/src/lib.rs"
      provides: "Registered Tauri handlers for all sync command entrypoints"
      contains: "db_connector::commands::db_data_diff_preview"
  key_links:
    - from: "client/src/extensions/host-api.ts"
      to: "client/src/lib/desktop-bridge.ts"
      via: "Connections API signatures map one-to-one to bridge invocations"
      pattern: "previewDataDiff"
    - from: "client/src/lib/desktop-bridge.ts"
      to: "src-tauri/src/lib.rs"
      via: "Bridge command names must exist in tauri::generate_handler!"
      pattern: "db_data_apply_execute"
    - from: "src-tauri/src/builtin_extensions/mod.rs"
      to: "client/src/extensions/host-api-runtime.ts"
      via: "Capability declaration and runtime gate must use identical key"
      pattern: "db.data.sync"
---

<objective>
Establish Phase-18 command and type foundations before backend diff/apply behavior is implemented.

Purpose: SYNC-01, SYNC-02, and SYNC-03 all depend on a stable, explicit cross-layer contract and enforceable capability boundary.
Output: Shared+Rust types, host/bridge wiring, runtime gating, and registered Tauri command entrypoints for the data-sync workflow.
</objective>

<execution_context>
C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-live-data-compare-sync/18-CONTEXT.md
@docs/db-workbench-data-sync-design.md
@docs/db-workbench-extension-design.md
@docs/extension-boundary-spec.md
@shared/schema.ts
@client/src/extensions/host-api.ts
@client/src/extensions/host-api-runtime.ts
@client/src/lib/desktop-bridge.ts
@src-tauri/src/db_connector/mod.rs
@src-tauri/src/db_connector/commands.rs
@src-tauri/src/builtin_extensions/mod.rs
@src-tauri/src/lib.rs

<interfaces>
Phase-18 command surface to define in this plan:
- db_data_diff_preview
- db_data_diff_detail
- db_data_apply_preview
- db_data_apply_execute
- db_data_apply_job_detail

Required blocker/status vocabulary:
- missing_stable_key
- target_snapshot_changed
- unsafe_delete_threshold
- readonly_target
- artifact_expired
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared and Rust contracts for compare/apply preview and execution lifecycle</name>
  <files>shared/schema.ts, src-tauri/src/db_connector/mod.rs</files>
  <read_first>
    - .planning/phases/18-live-data-compare-sync/18-CONTEXT.md (locked decisions for direction, key precedence, guard hash, and auditability)
    - .planning/REQUIREMENTS.md (SYNC-01, SYNC-02, SYNC-03 acceptance language)
    - shared/schema.ts (existing db-workbench contracts to extend additively)
    - src-tauri/src/db_connector/mod.rs (serde modeling conventions for command payloads)
  </read_first>
  <action>
Define additive compare/apply contracts with concrete fields and enums.

1. In shared/schema.ts, add interfaces for:
   - DbDataDiffPreviewRequest/Response
   - DbDataDiffDetailRequest/Response
   - DbDataApplyPreviewRequest/Response
   - DbDataApplyExecuteRequest/Response
   - DbDataApplyJobDetailRequest/Response
2. Include explicit fields:
   - compareId, targetSnapshotHash, currentTargetSnapshotHash
   - table-level action counts for insert/update/delete
   - row-level statuses source_only, target_only, value_changed, unchanged
   - apply blockers list using fixed codes missing_stable_key, target_snapshot_changed, unsafe_delete_threshold, readonly_target, artifact_expired
3. Mirror these payloads in src-tauri/src/db_connector/mod.rs with serde rename_all camelCase and optional fields modeled via Option.
4. Keep all new fields additive so existing query/edit APIs remain backward-compatible.
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - shared/schema.ts contains export interface DbDataDiffPreviewRequest
    - shared/schema.ts contains export interface DbDataApplyExecuteResponse
    - shared/schema.ts contains targetSnapshotHash
    - shared/schema.ts contains missing_stable_key
    - src-tauri/src/db_connector/mod.rs contains pub struct DbDataDiffPreviewRequest
    - src-tauri/src/db_connector/mod.rs contains pub struct DbDataApplyPreviewRequest
    - src-tauri/src/db_connector/mod.rs contains target_snapshot_hash
    - npm run check exits 0
    - cargo check --manifest-path src-tauri/Cargo.toml exits 0
  </acceptance_criteria>
  <done>Cross-layer compare/apply contracts are explicit and ready for backend implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire host API and desktop bridge methods with fail-closed capability gate</name>
  <files>client/src/extensions/host-api.ts, client/src/extensions/host-api-runtime.ts, client/src/lib/desktop-bridge.ts, src-tauri/src/builtin_extensions/mod.rs</files>
  <read_first>
    - client/src/extensions/host-api.ts (ConnectionsApi signature patterns)
    - client/src/extensions/host-api-runtime.ts (requireCap usage and ALL_CAPABILITIES list)
    - client/src/lib/desktop-bridge.ts (invoke command naming style and return typing)
    - src-tauri/src/builtin_extensions/mod.rs (db connector capability declaration format)
  </read_first>
  <action>
Expose sync APIs only through dedicated capability-gated methods.

1. Add methods to ConnectionsApi in host-api.ts:
   - previewDataDiff(request)
   - fetchDataDiffDetail(request)
   - previewDataApply(request)
   - executeDataApply(request)
   - fetchDataApplyJobDetail(request)
2. In host-api-runtime.ts:
   - add db.data.sync to capability catalog.
   - gate each method with requireCap(granted, "db.data.sync").
3. In desktop-bridge.ts:
   - add invoke wrappers for db_data_diff_preview, db_data_diff_detail, db_data_apply_preview, db_data_apply_execute, db_data_apply_job_detail.
4. In builtin_extensions/mod.rs:
   - add db.data.sync to db-connector capability array.
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - client/src/extensions/host-api.ts contains previewDataDiff(
    - client/src/extensions/host-api.ts contains executeDataApply(
    - client/src/extensions/host-api-runtime.ts contains "db.data.sync"
    - client/src/extensions/host-api-runtime.ts contains requireCap(granted, "db.data.sync")
    - client/src/lib/desktop-bridge.ts contains "db_data_diff_preview"
    - client/src/lib/desktop-bridge.ts contains "db_data_apply_execute"
    - src-tauri/src/builtin_extensions/mod.rs contains "db.data.sync"
    - npm run check exits 0
    - cargo check --manifest-path src-tauri/Cargo.toml exits 0
  </acceptance_criteria>
  <done>Runtime APIs expose sync operations only behind explicit capability checks.</done>
</task>

<task type="auto">
  <name>Task 3: Register Phase-18 command entrypoints in Tauri command routing</name>
  <files>src-tauri/src/db_connector/commands.rs, src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/db_connector/commands.rs (current command handler style for db connector operations)
    - src-tauri/src/lib.rs (tauri::generate_handler list and module registration)
    - .planning/phases/18-live-data-compare-sync/18-CONTEXT.md (recommended command surface and guard expectations)
  </read_first>
  <action>
Make all data-sync commands reachable from frontend invoke path.

1. In src-tauri/src/db_connector/commands.rs, define public command handlers:
   - db_data_diff_preview
   - db_data_diff_detail
   - db_data_apply_preview
   - db_data_apply_execute
   - db_data_apply_job_detail
2. Ensure handler signatures accept typed request payloads and return typed response payloads from db_connector::mod contracts.
3. In src-tauri/src/lib.rs, add each handler to tauri::generate_handler! list so desktop bridge invocations resolve without runtime "unknown command" errors.
  </action>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
    <automated>rg -n "db_data_diff_preview|db_data_diff_detail|db_data_apply_preview|db_data_apply_execute|db_data_apply_job_detail" src-tauri/src/db_connector/commands.rs src-tauri/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/db_connector/commands.rs contains pub async fn db_data_diff_preview
    - src-tauri/src/db_connector/commands.rs contains pub async fn db_data_apply_preview
    - src-tauri/src/db_connector/commands.rs contains pub async fn db_data_apply_execute
    - src-tauri/src/lib.rs contains db_connector::commands::db_data_diff_preview
    - src-tauri/src/lib.rs contains db_connector::commands::db_data_apply_job_detail
    - cargo check --manifest-path src-tauri/Cargo.toml exits 0
    - rg -n "db_data_diff_preview|db_data_apply_execute" src-tauri/src/db_connector/commands.rs src-tauri/src/lib.rs returns matches for both files
  </acceptance_criteria>
  <done>Phase-18 sync command entrypoints are registered and callable through the existing bridge chain.</done>
</task>

</tasks>

<verification>
- npm run check
- cargo check --manifest-path src-tauri/Cargo.toml
- rg -n "DbDataDiffPreviewRequest|DbDataApplyPreviewRequest|db.data.sync|db_data_apply_execute" shared/schema.ts client/src/extensions/host-api.ts client/src/extensions/host-api-runtime.ts client/src/lib/desktop-bridge.ts src-tauri/src/db_connector/mod.rs src-tauri/src/db_connector/commands.rs src-tauri/src/lib.rs src-tauri/src/builtin_extensions/mod.rs
</verification>

<success_criteria>
- SYNC-01 contract readiness: key policy and diff vocabulary are represented in explicit request/response types
- SYNC-02 contract readiness: compareId and snapshot-hash guard fields are mandatory in apply preview/execute contracts
- SYNC-03 contract readiness: apply job detail command is present in API surface and command registry
</success_criteria>

<output>
After completion, create .planning/phases/18-live-data-compare-sync/18-01-SUMMARY.md
</output>
