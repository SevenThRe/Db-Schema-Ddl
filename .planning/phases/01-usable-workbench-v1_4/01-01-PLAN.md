---
phase: 01-usable-workbench-v1_4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - src-tauri/src/db_connector/mod.rs
  - src-tauri/src/db_connector/query.rs
  - src-tauri/src/db_connector/explain.rs
  - src-tauri/Cargo.toml
  - src-tauri/src/lib.rs
  - test/client/db-connection-config.test.ts
autonomous: true
requirements:
  - CONN-01
  - CONN-03
  - EXEC-02
  - SAFE-01
  - PLAN-01
  - PLAN-02

must_haves:
  truths:
    - "DbConnectionConfig has environment, readonly, colorTag, defaultSchema fields in both TS and Rust"
    - "15+ new query/explain/export type interfaces exist in shared/schema.ts"
    - "QueryExecutionRequest includes confirmed field for server-side dangerous SQL enforcement"
    - "Rust query.rs compiles with db_query_execute, db_query_cancel, db_query_fetch_more stubs"
    - "db_query_execute rejects dangerous SQL when confirmed=false (server-side safety enforcement)"
    - "Rust explain.rs compiles with db_query_explain stub and PlanNode normalization logic"
    - "lib.rs registers DbPoolRegistry, CancellationRegistry as managed state and all new commands"
    - "Dangerous SQL detection correctly classifies all 6 categories via sqlparser AST"
    - "EXPLAIN normalization produces FULL_TABLE_SCAN and LARGE_ROWS_ESTIMATE warnings"
  artifacts:
    - path: "shared/schema.ts"
      provides: "All Phase 1 type definitions"
      contains: "DbEnvironment"
    - path: "src-tauri/src/db_connector/query.rs"
      provides: "Query execution + cancel + fetch-more + dangerous SQL detection"
      contains: "db_query_execute"
    - path: "src-tauri/src/db_connector/explain.rs"
      provides: "EXPLAIN normalization"
      contains: "db_query_explain"
    - path: "src-tauri/src/lib.rs"
      provides: "Managed state registration + command handler"
      contains: "DbPoolRegistry"
    - path: "test/client/db-connection-config.test.ts"
      provides: "Wave 0 test stub for CONN-01 type roundtrip"
  key_links:
    - from: "shared/schema.ts"
      to: "src-tauri/src/db_connector/mod.rs"
      via: "DbConnectionConfig field parity (environment, readonly, colorTag, defaultSchema)"
      pattern: "environment.*readonly.*color_tag.*default_schema"
    - from: "src-tauri/src/db_connector/query.rs"
      to: "src-tauri/src/lib.rs"
      via: "DbPoolRegistry + CancellationRegistry managed state access"
      pattern: "State.*DbPoolRegistry"
---

<objective>
Build the type foundation, Rust backend stubs, and managed state infrastructure for the DB workbench (build sequence steps 1-3).

Purpose: All downstream plans (IPC bridge, UI components) depend on these types and Rust commands existing and compiling. This is the foundation layer. The canonical execution contract is defined here — backend owns statement segmentation, safety enforcement, and pagination.
Output: Extended shared/schema.ts with 15+ types, new query.rs and explain.rs Rust modules with full business logic (dangerous SQL detection with server-side enforcement, EXPLAIN normalization, readonly enforcement, fetch-more pagination), and lib.rs with DbPoolRegistry + CancellationRegistry managed state.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-usable-workbench-v1_4/01-CONTEXT.md
@.planning/phases/01-usable-workbench-v1_4/01-RESEARCH.md
@.planning/phases/01-usable-workbench-v1_4/01-VALIDATION.md

@shared/schema.ts
@src-tauri/src/db_connector/mod.rs
@src-tauri/src/db_connector/commands.rs
@src-tauri/src/db_connector/introspect.rs
@src-tauri/src/lib.rs
@src-tauri/Cargo.toml
@src-tauri/src/ddl_import.rs

<interfaces>
<!-- Existing types the executor must extend — read these first -->

From shared/schema.ts (existing DbConnectionConfig-related types):
```typescript
// DbDriver is used via z.enum pattern elsewhere; confirm actual definition before extending
export type DbDriver = "mysql" | "postgres";
export type DbConnectionConfig = { id: string; name: string; driver: DbDriver; host: string; port: number; database: string; username: string; password: string; };
```

From src-tauri/src/db_connector/mod.rs (existing Rust structs):
```rust
pub enum DbDriver { Mysql, Postgres }
pub struct DbConnectionConfig { pub id: String, pub name: String, pub driver: DbDriver, pub host: String, pub port: u16, pub database: String, pub username: String, pub password: String }
```

From src-tauri/src/lib.rs (managed state pattern):
```rust
let ext_manager = Arc::new(extensions::process::ProcessManager::new(&data_dir));
app.manage(ext_manager);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 test stubs</name>
  <files>test/client/db-connection-config.test.ts</files>
  <read_first>
    - test/client/extension-boundaries.test.ts (existing test pattern — imports, describe/it structure, assert usage)
    - shared/schema.ts (current DbConnectionConfig definition to understand what fields exist)
  </read_first>
  <action>
Create `test/client/db-connection-config.test.ts` using Node.js built-in test runner (same pattern as `extension-boundaries.test.ts`).

Tests to write:
1. `DbConnectionConfig with environment field` — create a config object with `environment: "prod"`, serialize to JSON, parse back, assert `environment === "prod"`
2. `DbConnectionConfig with readonly field` — create config with `readonly: true`, serialize/parse roundtrip, assert `readonly === true`
3. `DbConnectionConfig with colorTag field` — create config with `colorTag: "#ff0000"`, roundtrip, assert match
4. `DbConnectionConfig with defaultSchema field` — create config with `defaultSchema: "public"`, roundtrip, assert match
5. `DbConnectionConfig without new fields` — create config WITHOUT environment/readonly/colorTag/defaultSchema, serialize/parse, assert fields are undefined (backward compat)

Import path: `import type { DbConnectionConfig } from "../../shared/schema.ts";`
Use `import { describe, it } from "node:test";` and `import assert from "node:assert/strict";`

These tests will FAIL until Task 1 adds the new fields — this is intentional (Wave 0 = RED).
  </action>
  <verify>
    <automated>node --test --experimental-strip-types test/client/db-connection-config.test.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - test/client/db-connection-config.test.ts exists and contains `describe("DbConnectionConfig"`
    - File contains at least 5 `it(` test cases
    - File imports from `../../shared/schema.ts`
  </acceptance_criteria>
  <done>Wave 0 test file exists with 5 failing tests covering CONN-01 type roundtrip</done>
</task>

<task type="auto">
  <name>Task 1: Type foundation — shared/schema.ts + Rust mod.rs (Build Step 1)</name>
  <files>shared/schema.ts, src-tauri/src/db_connector/mod.rs</files>
  <read_first>
    - shared/schema.ts (full file — understand existing types, Zod patterns, import structure)
    - src-tauri/src/db_connector/mod.rs (full file — understand existing structs, serde annotations)
    - docs/db-workbench-extension-design.md (section 5.1 for environment model, 6.5 for type list)
    - .planning/phases/01-usable-workbench-v1_4/01-RESEARCH.md (section "New Type Definitions Required in shared/schema.ts" — copy exact type definitions)
  </read_first>
  <action>
**shared/schema.ts additions** (per D-01, CONN-01, CONN-03):

1. Add `DbEnvironment` type:
```typescript
export type DbEnvironment = "dev" | "test" | "prod";
```

2. Extend existing `DbConnectionConfig` type — add these fields AFTER the existing `password` field:
```typescript
environment?: DbEnvironment;
readonly?: boolean;
colorTag?: string;
defaultSchema?: string;
```

3. Add all Phase 1 query/execution types. Copy EXACTLY from RESEARCH.md "New Type Definitions Required" section:
- `QueryExecutionRequest` — fields: connectionId, sql, requestId, limit?, offset?, continueOnError?, **confirmed?: boolean** (REVIEW FIX: server-side safety enforcement — when dangerous SQL is detected and confirmed is false/absent, db_query_execute rejects the request)
- `DbQueryColumn` — fields: name, dataType
- `DbQueryRow` — fields: values: (string | number | boolean | null)[]
- `DbQueryBatchResult` — fields: sql, columns, rows, totalRows, elapsedMs, affectedRows?, error?
- `QueryExecutionResponse` — fields: batches, requestId
- `FetchMoreRequest` — fields: requestId, batchIndex, sql, connectionId, offset, limit (REVIEW FIX: explicit pagination contract for "Load more" — `db_query_fetch_more` command takes this request and returns a single `DbQueryBatchResult` with the next page of rows)
- `PlanNode` — fields: id, label, nodeType, relationName?, cost?, rows?, children: PlanNode[], warnings: string[]
- `DbExplainPlan` — fields: dialect: DbDriver, root: PlanNode, rawJson: string
- `DangerousSqlPreview` — fields: dangers: DangerClass[], sql, connectionName, environment: DbEnvironment, database
- `DangerClass` type — union: "DROP" | "TRUNCATE" | "ALTER_TABLE" | "ALTER_DATABASE" | "DELETE_WITHOUT_WHERE" | "UPDATE_WITHOUT_WHERE"
- `ExportRowsRequest` — fields: rows, columns, format: "json" | "csv" | "markdown" | "sql-insert", tableName?
- `ExplainRequest` — fields: connectionId, sql

**src-tauri/src/db_connector/mod.rs additions**:

1. Add `pub mod query;` and `pub mod explain;` declarations after existing `mod introspect;`

2. Add `DbEnvironment` enum:
```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
#[serde(rename_all = "lowercase")]
pub enum DbEnvironment {
    Dev,
    Test,
    Prod,
}
```

3. Extend `DbConnectionConfig` struct — add after `password` field:
```rust
#[serde(skip_serializing_if = "Option::is_none")]
pub environment: Option<DbEnvironment>,
#[serde(default)]
pub readonly: bool,
#[serde(skip_serializing_if = "Option::is_none")]
pub color_tag: Option<String>,
#[serde(skip_serializing_if = "Option::is_none")]
pub default_schema: Option<String>,
```

4. Add query/execution structs to mod.rs (or a separate types section):
- `QueryExecutionRequest` with fields: connection_id, sql, request_id, limit (Option<u32> default 1000), offset (Option<u32>), continue_on_error (bool default false), **confirmed (bool default false)** (REVIEW FIX: server-side safety gate)
- `FetchMoreRequest` with fields: request_id, batch_index (u32), sql, connection_id, offset (u32), limit (u32 default 1000) (REVIEW FIX: dedicated fetch-more contract)
- `DbQueryColumn` with fields: name, data_type
- `DbQueryRow` with fields: values: Vec<serde_json::Value>
- `DbQueryBatchResult` with fields: sql, columns, rows, total_rows, elapsed_ms, affected_rows (Option), error (Option)
- `QueryExecutionResponse` with fields: batches: Vec<DbQueryBatchResult>, request_id
- `PlanNode` with fields: id, label, node_type, relation_name (Option), cost (Option<f64>), rows (Option<u64>), children: Vec<PlanNode>, warnings: Vec<String>
- `DbExplainPlan` with fields: dialect: DbDriver, root: PlanNode, raw_json: String
- `ExplainRequest` with fields: connection_id, sql
- `DangerClass` enum: Drop, Truncate, AlterTable, AlterDatabase, DeleteWithoutWhere, UpdateWithoutWhere (serde rename_all = "SCREAMING_SNAKE_CASE")
- `DangerousSqlPreview` with fields: dangers: Vec<DangerClass>, sql, connection_name, environment: DbEnvironment, database
- `ExportRowsRequest` with fields: rows, columns, format (String), table_name (Option)
- `AnyPool` enum: Mysql(MySqlPool), Postgres(PgPool) — for DbPoolRegistry
- `DbPoolRegistry` struct: pools: Mutex<HashMap<String, Arc<AnyPool>>>
- `CancellationRegistry` struct: tokens: Mutex<HashMap<String, CancellationToken>>

All structs must have `#[derive(Debug, Clone, Serialize, Deserialize)]` and `#[serde(rename_all = "camelCase")]`.

Note: `readonly` in Rust uses `#[serde(default)]` so it defaults to `false` for backward compatibility with existing saved connections that lack this field.
  </action>
  <verify>
    <automated>cd "C:/Users/ISI202502/Downloads/Db-Schema-Ddl" && node --test --experimental-strip-types test/client/db-connection-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - shared/schema.ts contains `export type DbEnvironment = "dev" | "test" | "prod"`
    - shared/schema.ts contains `environment?: DbEnvironment` inside DbConnectionConfig
    - shared/schema.ts contains `readonly?: boolean` inside DbConnectionConfig
    - shared/schema.ts contains `export interface QueryExecutionRequest`
    - shared/schema.ts contains `confirmed?: boolean` inside QueryExecutionRequest
    - shared/schema.ts contains `export interface FetchMoreRequest`
    - shared/schema.ts contains `export interface PlanNode`
    - shared/schema.ts contains `export type DangerClass`
    - shared/schema.ts contains `export interface ExplainRequest`
    - src-tauri/src/db_connector/mod.rs contains `pub mod query;`
    - src-tauri/src/db_connector/mod.rs contains `pub mod explain;`
    - src-tauri/src/db_connector/mod.rs contains `pub environment: Option<DbEnvironment>`
    - src-tauri/src/db_connector/mod.rs contains `pub readonly: bool`
    - src-tauri/src/db_connector/mod.rs contains `pub confirmed: bool`
    - src-tauri/src/db_connector/mod.rs contains `pub struct FetchMoreRequest`
    - src-tauri/src/db_connector/mod.rs contains `pub struct DbPoolRegistry`
    - src-tauri/src/db_connector/mod.rs contains `pub struct CancellationRegistry`
    - Wave 0 tests pass: `node --test --experimental-strip-types test/client/db-connection-config.test.ts` exits 0
  </acceptance_criteria>
  <done>Both TS and Rust type definitions are in sync with all 15+ new types including confirmed field and FetchMoreRequest; Wave 0 tests go GREEN</done>
</task>

<task type="auto">
  <name>Task 2: Rust command stubs + managed state + registration (Build Steps 2-3)</name>
  <files>src-tauri/src/db_connector/query.rs, src-tauri/src/db_connector/explain.rs, src-tauri/Cargo.toml, src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/db_connector/mod.rs (just updated — get new type definitions)
    - src-tauri/src/db_connector/commands.rs (existing command pattern — #[tauri::command] signature, State usage, error handling)
    - src-tauri/src/db_connector/introspect.rs (pool creation pattern — mysql_opts, pg_opts builders, sqlx::MySqlPool::connect_with)
    - src-tauri/src/lib.rs (existing setup() and invoke_handler)
    - src-tauri/Cargo.toml (existing dependencies)
    - src-tauri/src/ddl_import.rs (sqlparser usage pattern — Parser::parse_sql, MySqlDialect, statement matching)
    - .planning/phases/01-usable-workbench-v1_4/01-RESEARCH.md (Pattern 1: DbPoolRegistry, Pattern 4: sqlparser AST, Pattern 5: EXPLAIN normalization, Pattern 8: CancellationToken, Pitfall 2: Mutex deadlock avoidance)
  </read_first>
  <action>
**Cargo.toml**: Add `tokio-util = { version = "0.7", features = ["rt"] }` to [dependencies] section.

**query.rs** — Create `src-tauri/src/db_connector/query.rs` with:

1. Import types from parent module: `use super::{DbConnectionConfig, DbDriver, DbEnvironment, DbPoolRegistry, CancellationRegistry, AnyPool, QueryExecutionRequest, QueryExecutionResponse, DbQueryBatchResult, DbQueryColumn, DbQueryRow, DangerClass, DangerousSqlPreview, FetchMoreRequest};`

2. `detect_dangerous_sql(sql: &str, driver: &DbDriver) -> Vec<DangerClass>` function:
   - Use `sqlparser::parser::Parser::parse_sql` with appropriate dialect (MySqlDialect for Mysql, PostgreSqlDialect for Postgres)
   - Walk each `Statement` in parsed result:
     - `Statement::Drop { .. }` -> DangerClass::Drop
     - `Statement::Truncate { .. }` -> DangerClass::Truncate
     - `Statement::AlterTable { .. }` -> DangerClass::AlterTable
     - `Statement::Delete(del)` where `del.selection.is_none()` -> DangerClass::DeleteWithoutWhere
     - `Statement::Update { selection, .. }` where `selection.is_none()` -> DangerClass::UpdateWithoutWhere
     - For AlterDatabase: grep sqlparser 0.53 source for exact variant name first. If not found, skip for now and leave a comment.
   - On parse error: return empty vec (don't block execution)

3. `get_or_create_pool(registry: &DbPoolRegistry, config: &DbConnectionConfig) -> Result<Arc<AnyPool>, String>` helper:
   - Lock mutex, check if pool exists for config.id, clone Arc if yes
   - If not: release lock, create pool (mysql_opts/pg_opts pattern from introspect.rs), lock again, insert, return
   - CRITICAL: Never hold std::sync::Mutex lock across .await — clone Arc handle then release

4. `split_sql_statements(sql: &str) -> Vec<String>` helper:
   - Split on semicolons, respecting single-quoted and double-quoted strings
   - Filter out empty/whitespace-only segments
   - Keep it simple — no need for full parser, just quote-aware semicolon split
   - NOTE (review context): This is the CANONICAL statement splitter. The frontend does NOT independently split statements. Frontend sends cursor position + full SQL; backend uses this function for segmentation.

5. `#[tauri::command] pub async fn db_query_execute(app: AppHandle, pool_registry: State<'_, Arc<DbPoolRegistry>>, cancel_registry: State<'_, Arc<CancellationRegistry>>, request: QueryExecutionRequest) -> Result<QueryExecutionResponse, String>`:
   - Load connection config from storage (reuse pattern from commands.rs)
   - Check `config.readonly` — if true and any statement is DML/DDL, return Err("Connection is read-only") (per CONN-03)
   - **(REVIEW FIX: Server-side safety enforcement)** Run `detect_dangerous_sql` on the full SQL. If dangers are found AND `request.confirmed != true`, return `Err("Dangerous SQL detected. Set confirmed=true to proceed.")`. This ensures the safety model is enforced at the Rust layer, not just via frontend dialog.
   - Create CancellationToken, register in cancel_registry with request.request_id
   - Split SQL into statements via split_sql_statements
   - For each statement, execute with tokio::select! { result = sqlx_query, _ = token.cancelled() => Err("Query cancelled") }
   - **(REVIEW FIX: Cancel semantics for multi-statement)** On cancel, stop the currently executing statement and do NOT start the next one. Already-completed statements' results are included in the response. Set an `error: Some("Cancelled")` on the cancelled statement's batch result.
   - Collect results into Vec<DbQueryBatchResult> with elapsed time per statement
   - If continueOnError is false, stop on first error (per D-05)
   - On completion or cancellation, remove token from cancel_registry
   - Apply limit (default 1000) to row fetching

6. `#[tauri::command] pub async fn db_query_cancel(cancel_registry: State<'_, Arc<CancellationRegistry>>, request_id: String) -> Result<(), String>`:
   - Look up token in registry by request_id
   - Call token.cancel()
   - Remove from registry

7. **(REVIEW FIX: Dedicated fetch-more command)** `#[tauri::command] pub async fn db_query_fetch_more(pool_registry: State<'_, Arc<DbPoolRegistry>>, request: FetchMoreRequest) -> Result<DbQueryBatchResult, String>`:
   - Load connection config
   - Get or create pool
   - Re-execute `request.sql` with `LIMIT {request.limit} OFFSET {request.offset}` appended (for SELECT statements)
   - Return a single `DbQueryBatchResult` with the next page of rows
   - This is the canonical pagination contract — frontend calls this with offset = current rows.length

8. `#[tauri::command] pub async fn db_preview_dangerous_sql(request: DangerousSqlPreviewRequest) -> Result<DangerousSqlPreview, String>`:
   - Takes connectionId + sql
   - Loads connection config, calls detect_dangerous_sql
   - Returns DangerousSqlPreview with dangers, sql, connectionName, environment, database

9. Add `#[cfg(test)] mod tests` at bottom with:
   - `test_detect_drop` — `detect_dangerous_sql("DROP TABLE users", &DbDriver::Mysql)` contains DangerClass::Drop
   - `test_detect_truncate` — "TRUNCATE TABLE users" contains DangerClass::Truncate
   - `test_detect_delete_without_where` — "DELETE FROM users" contains DangerClass::DeleteWithoutWhere
   - `test_detect_update_without_where` — "UPDATE users SET active = false" contains DangerClass::UpdateWithoutWhere
   - `test_detect_alter_table` — "ALTER TABLE users ADD COLUMN age INT" contains DangerClass::AlterTable
   - `test_safe_select` — "SELECT * FROM users WHERE id = 1" returns empty vec
   - `test_safe_delete_with_where` — "DELETE FROM users WHERE id = 1" returns empty vec
   - `test_cancel_token_register_unregister` — create CancellationRegistry, insert token, cancel, verify removed
   - **(REVIEW FIX)** `test_split_statements_respects_quotes` — `split_sql_statements("SELECT 'a;b'; SELECT 1")` returns 2 statements, not 3
   - **(REVIEW FIX)** `test_split_statements_respects_comments` — `split_sql_statements("-- comment with ;\nSELECT 1")` returns 1 statement

**explain.rs** — Create `src-tauri/src/db_connector/explain.rs` with:

1. Import: `use super::{DbDriver, PlanNode, DbExplainPlan, DbPoolRegistry, ExplainRequest};`

2. `normalize_mysql_explain(json: &serde_json::Value) -> PlanNode`:
   - Recursively walk the MySQL EXPLAIN FORMAT=JSON structure
   - Extract `access_type`, `rows_examined_per_scan` or `rows_produced_per_join`
   - Map `access_type == "ALL"` to warnings `["FULL_TABLE_SCAN"]`
   - Map rows > 10_000 to warnings push `"LARGE_ROWS_ESTIMATE"`
   - Handle nested `query_block.table`, `query_block.ordering_operation`, `query_block.nested_loop` recursively

3. `normalize_pg_explain(json: &serde_json::Value) -> PlanNode`:
   - Walk PostgreSQL EXPLAIN FORMAT JSON structure (array of Plan objects)
   - Extract `Node Type`, `Relation Name`, `Plan Rows`, `Total Cost`
   - Map `Node Type == "Seq Scan"` to warnings `["FULL_TABLE_SCAN"]`
   - Map Plan Rows > 10_000 to warnings push `"LARGE_ROWS_ESTIMATE"`
   - Recurse into `Plans` array for children

4. `#[tauri::command] pub async fn db_query_explain(pool_registry: State<'_, Arc<DbPoolRegistry>>, request: ExplainRequest) -> Result<DbExplainPlan, String>`:
   - Load connection config
   - Execute `EXPLAIN FORMAT=JSON {sql}` (MySQL) or `EXPLAIN (FORMAT JSON) {sql}` (PostgreSQL)
   - Parse JSON, call appropriate normalize function
   - Return DbExplainPlan with dialect, root PlanNode, raw JSON string

5. Add `#[cfg(test)] mod tests` with:
   - `test_normalize_mysql_full_scan` — mock MySQL EXPLAIN JSON with access_type "ALL", verify warning contains "FULL_TABLE_SCAN"
   - `test_normalize_pg_seq_scan` — mock PG EXPLAIN JSON with Node Type "Seq Scan", verify warning
   - `test_large_rows_warning` — mock JSON with rows > 10_000, verify "LARGE_ROWS_ESTIMATE" warning
   - `test_safe_index_scan` — mock JSON with access_type "ref" / Node Type "Index Scan", verify no warnings

**lib.rs** modifications:

1. In `setup()` closure, after `app.manage(ext_manager);`, add:
```rust
use std::collections::HashMap;
use std::sync::Mutex;
let pool_registry = Arc::new(db_connector::DbPoolRegistry { pools: Mutex::new(HashMap::new()) });
let cancel_registry = Arc::new(db_connector::CancellationRegistry { tokens: Mutex::new(HashMap::new()) });
app.manage(pool_registry);
app.manage(cancel_registry);
```

2. In `invoke_handler`, after the existing `db_connector::commands::db_diff` line, add:
```rust
db_connector::query::db_query_execute,
db_connector::query::db_query_cancel,
db_connector::query::db_query_fetch_more,
db_connector::query::db_preview_dangerous_sql,
db_connector::explain::db_query_explain,
```
  </action>
  <verify>
    <automated>cd "C:/Users/ISI202502/Downloads/Db-Schema-Ddl/src-tauri" && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` in src-tauri exits 0 (no compile errors)
    - src-tauri/Cargo.toml contains `tokio-util`
    - src-tauri/src/db_connector/query.rs contains `pub async fn db_query_execute`
    - src-tauri/src/db_connector/query.rs contains `pub async fn db_query_cancel`
    - src-tauri/src/db_connector/query.rs contains `pub async fn db_query_fetch_more`
    - src-tauri/src/db_connector/query.rs contains `fn detect_dangerous_sql`
    - src-tauri/src/db_connector/query.rs contains `request.confirmed` (server-side safety check)
    - src-tauri/src/db_connector/query.rs contains `fn split_sql_statements`
    - src-tauri/src/db_connector/query.rs contains `mod tests`
    - src-tauri/src/db_connector/explain.rs contains `pub async fn db_query_explain`
    - src-tauri/src/db_connector/explain.rs contains `fn normalize_mysql_explain`
    - src-tauri/src/db_connector/explain.rs contains `fn normalize_pg_explain`
    - src-tauri/src/db_connector/explain.rs contains `mod tests`
    - src-tauri/src/lib.rs contains `DbPoolRegistry`
    - src-tauri/src/lib.rs contains `CancellationRegistry`
    - src-tauri/src/lib.rs contains `db_query_execute`
    - src-tauri/src/lib.rs contains `db_query_fetch_more`
    - `cargo test -p db-schema-ddl-tauri db_connector::query::tests` exits 0
    - `cargo test -p db-schema-ddl-tauri db_connector::explain::tests` exits 0
  </acceptance_criteria>
  <done>Rust backend compiles with all new commands registered including db_query_fetch_more, managed state initialized, dangerous SQL detection with server-side enforcement passes all test cases including quote-aware splitting, EXPLAIN normalization passes all 4 test cases</done>
</task>

</tasks>

<verification>
1. `node --test --experimental-strip-types test/client/db-connection-config.test.ts` exits 0
2. `cargo check -p db-schema-ddl-tauri` exits 0
3. `cargo test -p db-schema-ddl-tauri db_connector` exits 0
4. `node --test --experimental-strip-types test/client/extension-boundaries.test.ts` exits 0 (regression)
</verification>

<success_criteria>
- shared/schema.ts has 15+ new type exports including DbEnvironment, QueryExecutionRequest (with confirmed field), FetchMoreRequest, PlanNode, DangerClass
- DbConnectionConfig is extended in both TS and Rust with environment, readonly, colorTag, defaultSchema
- query.rs enforces safety at Rust layer: rejects dangerous SQL when confirmed=false
- query.rs includes db_query_fetch_more command for explicit pagination
- split_sql_statements is quote-aware and handles edge cases
- query.rs and explain.rs compile and have passing unit tests
- lib.rs registers DbPoolRegistry + CancellationRegistry managed state
- All new Tauri commands registered in invoke_handler (including db_query_fetch_more)
- Existing extension-boundaries tests still pass (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/01-usable-workbench-v1_4/01-01-SUMMARY.md`
</output>
