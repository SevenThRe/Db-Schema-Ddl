---
phase: 02-github-delivery-and-lifecycle
plan: 04
type: execute
wave: 4
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - client/src/components/extensions/ExtensionInstallDialog.tsx
  - client/src/components/extensions/ExtensionStatusDialog.tsx
  - client/src/components/Sidebar.tsx
  - client/src/pages/Dashboard.tsx
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-04
user_setup: []
must_haves:
  truths:
    - "The absent-state install prompt becomes a compact detail panel with version, size, compatibility, and summary metadata"
    - "Download, verify, and install progress are shown inside the same panel rather than a separate floating card"
    - "The sidebar module entry and settings management surface stay consistent with current lifecycle state and update availability"
  artifacts:
    - path: "client/src/components/extensions/ExtensionInstallDialog.tsx"
      provides: "In-panel detail, progress, and lifecycle messaging UI"
      min_lines: 60
    - path: "client/src/components/extensions/ExtensionStatusDialog.tsx"
      provides: "Update- and failure-aware recovery dialog states"
      min_lines: 40
    - path: "client/src/components/Sidebar.tsx"
      provides: "Module entry with update-aware state affordances"
      contains: "DB"
    - path: "client/src/pages/Dashboard.tsx"
      provides: "DB management module entry orchestration for delivery states"
      contains: "extension"
  key_links:
    - from: "client/src/components/extensions/ExtensionInstallDialog.tsx"
      to: "client/src/hooks/use-extensions.ts"
      via: "catalog metadata and lifecycle progress data"
      pattern: "useExtension"
    - from: "client/src/pages/Dashboard.tsx"
      to: "electron/preload.ts"
      via: "delivery lifecycle actions"
      pattern: "extension"
---

<objective>
Finish the user-facing delivery UX for official extension install, update, and failure handling.

Purpose: Phase 2 is only complete when users can discover the extension, understand the metadata, watch lifecycle progress in a single panel, recover from failures clearly, and move into Settings for full management after install.
Output: Detail/progress install panel, richer status dialog, and sidebar/dashboard integration for update-aware extension lifecycle behavior.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-github-delivery-and-lifecycle/02-CONTEXT.md
@.planning/phases/02-github-delivery-and-lifecycle/02-VALIDATION.md
@client/src/components/extensions/ExtensionInstallDialog.tsx
@client/src/components/extensions/ExtensionStatusDialog.tsx
@client/src/components/Sidebar.tsx
@client/src/pages/Dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn the install prompt into a detail-and-progress panel</name>
  <files>client/src/components/extensions/ExtensionInstallDialog.tsx, client/src/pages/Dashboard.tsx</files>
  <action>Replace the bare confirm dialog with the compact detail panel defined in context. It must show version, size, compatibility, and summary before download, then transition in place into a progress view that covers download, verification, and install stages.</action>
  <verify>Run `npm run check` and manually inspect the panel states in Electron mode.</verify>
  <done>The install flow becomes a compact but informative panel rather than a bare confirmation modal.</done>
</task>

<task type="auto">
  <name>Task 2: Add failure- and update-aware recovery states</name>
  <files>client/src/components/extensions/ExtensionStatusDialog.tsx, client/src/components/Sidebar.tsx</files>
  <action>Expand status handling so the UI distinguishes network failure, verification failure, incompatibility, install failure, disabled state, and update-available state. Each state must explain the next action clearly without exposing raw logs.</action>
  <verify>Inspect recovery copy and state transitions, then run `npm run check` plus the relevant UI tests from Wave 0.</verify>
  <done>Lifecycle failures and update prompts are understandable and action-oriented for users.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve additive module navigation during delivery states</name>
  <files>client/src/pages/Dashboard.tsx, client/src/components/Sidebar.tsx</files>
  <action>Keep delivery UX additive to the base app: the sidebar entry should continue to expose DB 管理 status, route users into the install/detail flow when needed, and send them to Settings for full management after install. Existing Excel/DDl workflows must remain uninterrupted.</action>
  <verify>Check that current dashboard workflows still work when the extension is absent, downloading, installed, and update-available.</verify>
  <done>Delivery UX fits the module-entry model without hijacking the base product experience.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] Install metadata and lifecycle progress render in one compact panel
- [ ] Failure and update states explain the next action clearly
- [ ] Existing dashboard workflows remain usable throughout delivery states
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can discover, download, monitor, recover, and hand off to Settings management without leaving the official host UX
</success_criteria>

<output>
After completion, create `.planning/phases/02-github-delivery-and-lifecycle/02-04-SUMMARY.md`
</output>
