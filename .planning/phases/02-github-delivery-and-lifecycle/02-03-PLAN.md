---
phase: 02-github-delivery-and-lifecycle
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - server/routes/extensions-routes.ts
  - server/lib/extensions/registry.ts
  - client/src/hooks/use-extensions.ts
  - client/src/pages/Settings.tsx
  - client/src/components/settings/ExtensionManagementSection.tsx
autonomous: true
requirements:
  - DIST-01
  - DIST-04
user_setup: []
must_haves:
  truths:
    - "The app exposes typed APIs for catalog details, lifecycle status, and management actions backed by the Electron delivery service"
    - "Settings contains a dedicated extension-management surface for enable, disable, update, and uninstall"
    - "Lifecycle status and update availability remain consistent between the sidebar module entry and Settings"
  artifacts:
    - path: "server/routes/extensions-routes.ts"
      provides: "Typed extension delivery and management endpoints"
      contains: "extensions"
    - path: "server/lib/extensions/registry.ts"
      provides: "Registry resolution that includes installed, staged, and update-available states"
      contains: "extension"
    - path: "client/src/hooks/use-extensions.ts"
      provides: "Catalog, progress, and management hooks"
      contains: "useExtension"
    - path: "client/src/components/settings/ExtensionManagementSection.tsx"
      provides: "Settings-based extension lifecycle controls"
      contains: "Extension"
  key_links:
    - from: "server/routes/extensions-routes.ts"
      to: "electron/extensions.ts"
      via: "delivery status and lifecycle commands"
      pattern: "extension"
    - from: "client/src/components/settings/ExtensionManagementSection.tsx"
      to: "client/src/hooks/use-extensions.ts"
      via: "typed lifecycle data and actions"
      pattern: "useExtension"
---

<objective>
Bridge the Electron delivery state machine into typed app APIs and a Settings-based management surface.

Purpose: Users need a stable place to manage the extension after installation, and the rest of the UI needs a typed data layer for catalog metadata, current version, update availability, and lifecycle actions.
Output: Server routes, registry/state resolution, client hooks, and a dedicated Settings management section for the official extension lifecycle.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-github-delivery-and-lifecycle/02-CONTEXT.md
@.planning/phases/02-github-delivery-and-lifecycle/02-VALIDATION.md
@server/routes/extensions-routes.ts
@server/lib/extensions/registry.ts
@client/src/hooks/use-extensions.ts
@client/src/pages/Settings.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend registry and route coverage for delivery states</name>
  <files>server/lib/extensions/registry.ts, server/routes/extensions-routes.ts</files>
  <action>Expand the registry and route layer to include catalog metadata, staged download/install status, current version, update availability, and lifecycle actions such as update and uninstall. Keep response shapes aligned with the shared contract and official-only trust model.</action>
  <verify>Inspect route coverage for metadata and management actions and run `npm run check`.</verify>
  <done>Typed app APIs can describe the full extension lifecycle beyond the Phase 1 host status subset.</done>
</task>

<task type="auto">
  <name>Task 2: Add client data hooks for delivery and management</name>
  <files>client/src/hooks/use-extensions.ts</files>
  <action>Grow the extension hooks into a dedicated lifecycle data layer that covers catalog fetch, progress polling, update availability, enable/disable, update, and uninstall actions. Reuse the existing fetch + parse pattern so both module entry UI and Settings can consume the same source.</action>
  <verify>Check hook coverage and `npm run check` success.</verify>
  <done>The renderer has a focused extension-lifecycle data layer for both discovery and management surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Add Settings-based extension management</name>
  <files>client/src/pages/Settings.tsx, client/src/components/settings/ExtensionManagementSection.tsx</files>
  <action>Create the `扩展管理` section inside Settings with the official extension status, current version, update availability, and lifecycle actions. Keep the sidebar entry as the module/status entry point and reserve full controls for Settings, as locked in context.</action>
  <verify>Inspect Settings IA and run `npm run check` plus the relevant UI tests from Wave 0.</verify>
  <done>Users have a clear administrative surface for managing the official extension lifecycle.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] Settings exposes a dedicated extension-management surface
- [ ] Sidebar and Settings resolve extension state from the same typed data layer
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can manage installed extension lifecycle from Settings without duplicating module navigation
</success_criteria>

<output>
After completion, create `.planning/phases/02-github-delivery-and-lifecycle/02-03-SUMMARY.md`
</output>
