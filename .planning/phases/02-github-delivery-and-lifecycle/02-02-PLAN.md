---
phase: 02-github-delivery-and-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - electron/extensions.ts
  - electron/main.ts
  - electron/preload.ts
  - electron/github-release.ts
autonomous: true
requirements:
  - DIST-02
  - DIST-03
  - DIST-04
user_setup: []
must_haves:
  truths:
    - "The Electron host can fetch official extension release metadata from GitHub and download the selected package"
    - "Downloaded packages are verified before install and unpacked into versioned install roots"
    - "Enable, update, and uninstall flow through a controlled lifecycle path that preserves rollback information"
  artifacts:
    - path: "electron/extensions.ts"
      provides: "Download, verify, install, enable, update, and uninstall orchestration"
      contains: "install"
    - path: "electron/github-release.ts"
      provides: "Official GitHub catalog and asset fetch logic"
      contains: "github"
    - path: "electron/main.ts"
      provides: "Lifecycle IPC handler registration"
      contains: "extensions:"
    - path: "electron/preload.ts"
      provides: "Renderer-safe delivery bridge"
      contains: "extensions"
  key_links:
    - from: "electron/preload.ts"
      to: "electron/main.ts"
      via: "action-specific delivery IPC"
      pattern: "extensions:"
    - from: "electron/main.ts"
      to: "electron/extensions.ts"
      via: "main-process lifecycle orchestration"
      pattern: "extension"
---

<objective>
Implement the Electron-side GitHub delivery and install state machine for the official DB management extension.

Purpose: The host must be able to fetch release metadata, download an official asset, verify it, unpack it into a versioned install root, and perform lifecycle actions such as update and uninstall without reusing the app-updater path.
Output: Electron-side delivery orchestration, dedicated GitHub fetch helpers, and preload/main-process bridges for lifecycle actions and progress polling.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-github-delivery-and-lifecycle/02-CONTEXT.md
@.planning/phases/02-github-delivery-and-lifecycle/02-VALIDATION.md
@electron/extensions.ts
@electron/main.ts
@electron/preload.ts
@electron/updater.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add official GitHub catalog and asset retrieval</name>
  <files>electron/github-release.ts, electron/extensions.ts</files>
  <action>Create focused helpers for reading the official release metadata, selecting the correct platform asset, and surfacing version, size, compatibility, and summary data to the host. Keep the source pinned to the trusted GitHub repository.</action>
  <verify>Inspect the release-selection logic and run `npm run check`.</verify>
  <done>The host can read authoritative extension metadata from GitHub before download begins.</done>
</task>

<task type="auto">
  <name>Task 2: Build download, verification, and install staging</name>
  <files>electron/extensions.ts, electron/main.ts</files>
  <action>Implement the delivery state machine for download, checksum verification, unpack, versioned install directory creation, active-version switching, and staged rollback information. Keep lifecycle progress granular enough to drive the single-panel progress UI decided in context.</action>
  <verify>Trace lifecycle stage transitions and run `npm run check` plus the relevant lifecycle tests from Wave 0.</verify>
  <done>The Electron host can safely move from catalog selection to a verified installed extension version.</done>
</task>

<task type="auto">
  <name>Task 3: Expose lifecycle-safe preload actions</name>
  <files>electron/preload.ts, electron/main.ts</files>
  <action>Add narrow preload methods for catalog fetch, download start, progress/status polling, update, enable/disable, and uninstall. Keep the API intent-based and separate from both raw file-system access and app-update IPC.</action>
  <verify>Confirm the renderer bridge contains only action-specific extension delivery methods and `npm run check` passes.</verify>
  <done>The renderer can drive the official extension lifecycle without privileged direct access.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] Lifecycle handlers are specific to extension delivery and do not piggyback on app-update channels
- [ ] Verified install roots are versioned and rollback-aware
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The official extension can be downloaded and verified through Electron-owned lifecycle code
</success_criteria>

<output>
After completion, create `.planning/phases/02-github-delivery-and-lifecycle/02-02-SUMMARY.md`
</output>
