---
phase: 02-github-delivery-and-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - shared/routes.ts
  - server/storage.ts
  - electron/extensions.ts
autonomous: true
requirements:
  - DIST-01
  - DIST-03
  - DIST-04
user_setup: []
must_haves:
  truths:
    - "The host has a typed official-extension delivery contract for catalog metadata, install state, verification results, and lifecycle actions"
    - "Delivery state survives restarts so interrupted downloads or staged installs can be resumed or explained"
    - "The official DB management extension remains the only trusted distribution target in v1"
  artifacts:
    - path: "shared/schema.ts"
      provides: "Catalog, manifest, install-state, and lifecycle validation schemas"
      contains: "extension"
    - path: "shared/routes.ts"
      provides: "Typed delivery and lifecycle API contract"
      contains: "extensions"
    - path: "server/storage.ts"
      provides: "Persistence methods for lifecycle jobs, staged installs, and active version state"
      contains: "extension"
    - path: "electron/extensions.ts"
      provides: "Delivery-state-aware host service shape"
      contains: "install"
  key_links:
    - from: "shared/routes.ts"
      to: "shared/schema.ts"
      via: "catalog and lifecycle request/response schemas"
      pattern: "extension"
    - from: "server/storage.ts"
      to: "shared/schema.ts"
      via: "persisted lifecycle state"
      pattern: "extension"
---

<objective>
Define the delivery contract and persistence layer for official GitHub-hosted extension lifecycle work.

Purpose: Every later download, verify, install, enable, update, and uninstall step needs a stable typed model and durable host state before UI and Electron behavior can build on top of it.
Output: Shared schemas, route contracts, and storage methods that describe catalog metadata, delivery states, verification outcomes, and versioned install records for the official DB management extension.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-github-delivery-and-lifecycle/02-CONTEXT.md
@.planning/phases/02-github-delivery-and-lifecycle/02-VALIDATION.md
@shared/schema.ts
@shared/routes.ts
@server/storage.ts
@electron/extensions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model catalog and manifest metadata</name>
  <files>shared/schema.ts, shared/routes.ts</files>
  <action>Add typed schemas for official extension catalog entries, release summaries, manifest/checksum metadata, lifecycle stages, and compatibility results. Keep the catalog official-only for v1 and align it with the detail panel metadata locked in context.</action>
  <verify>Confirm the new schemas parse example catalog payloads and `npm run check` passes.</verify>
  <done>Shared contracts exist for version, size, compatibility, summary, and verification metadata before any download starts.</done>
</task>

<task type="auto">
  <name>Task 2: Persist versioned lifecycle state</name>
  <files>server/storage.ts, shared/schema.ts</files>
  <action>Extend host persistence so the app can remember staged downloads, verification results, installed versions, update availability, and active/inactive versions across restarts. Treat lifecycle state as first-class data rather than transient UI-only flags.</action>
  <verify>Inspect storage interfaces and mappings for complete lifecycle coverage and run `npm run check`.</verify>
  <done>The host can durably describe what version is installed, what version is staged, and why a lifecycle action succeeded or failed.</done>
</task>

<task type="auto">
  <name>Task 3: Shape the host lifecycle API surface</name>
  <files>shared/routes.ts, electron/extensions.ts</files>
  <action>Define the action-specific host API surface that later plans will implement: catalog read, download start, progress/status read, verify/install completion, enable/disable, update, and uninstall. Keep updater concerns separate from extension delivery and preserve the official-only trust boundary.</action>
  <verify>Trace the planned route and host method names to ensure delivery logic stays distinct from the app updater.</verify>
  <done>Delivery and lifecycle capabilities have a clear, typed contract for downstream Electron, server, and UI work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] Shared contracts cover DIST-01 and DIST-03 metadata end to end
- [ ] Storage exposes durable lifecycle state rather than ephemeral install flags only
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Delivery metadata and lifecycle state are defined before GitHub fetch/unpack code is added
</success_criteria>

<output>
After completion, create `.planning/phases/02-github-delivery-and-lifecycle/02-01-SUMMARY.md`
</output>
