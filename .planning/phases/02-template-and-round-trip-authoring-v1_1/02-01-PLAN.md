---
phase: 02-template-and-round-trip-authoring-v1_1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - shared/routes.ts
  - client/src/hooks/use-ddl.ts
  - test/server/template-phase2.test.ts
  - test/client/template-phase2-ui.test.tsx
autonomous: true
requirements:
  - TPL-01
  - TPL-02
  - TPL-03
user_setup: []
must_haves:
  truths:
    - "Phase 2 has an explicit first-party template contract rather than hidden workbook assumptions."
    - "The two parser-supported layout variants are modeled distinctly and remain parser-compatible."
    - "Template creation returns round-trip trust information instead of assuming success."
  artifacts:
    - path: "shared/schema.ts"
      provides: "Typed template variants, create request/response, and round-trip validation contracts"
      contains: "template"
    - path: "shared/routes.ts"
      provides: "Typed API routes for template creation and validation"
      contains: "template"
    - path: "client/src/hooks/use-ddl.ts"
      provides: "Typed client seams for template-led workbook creation"
      contains: "use"
  key_links:
    - from: "shared/routes.ts"
      to: "shared/schema.ts"
      via: "template request/response contracts"
      pattern: "template|round"
---

<objective>
Define the shared contract for first-party workbook templates and their trust model before generation and UI work begin.

Purpose: Phase 2 needs one explicit language for template variants, round-trip validation results, and successful file registration. This plan creates that language first so backend and UI work do not invent incompatible local states.
Output: Shared schemas, route contracts, client hook seams, and Wave 0 test scaffolds for Phase 2.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-template-and-round-trip-authoring-v1_1/02-CONTEXT.md
@.planning/phases/02-template-and-round-trip-authoring-v1_1/02-VALIDATION.md
@shared/schema.ts
@shared/routes.ts
@client/src/hooks/use-ddl.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed template and round-trip contracts plus Wave 0 tests</name>
  <files>shared/schema.ts, test/server/template-phase2.test.ts, test/client/template-phase2-ui.test.tsx</files>
  <behavior>
    - Test 1: exactly two first-party template variants are represented explicitly
    - Test 2: template creation result carries round-trip validation state and mismatch detail
    - Test 3: a built-in template cannot be marked successful when round-trip validation fails
  </behavior>
  <action>Add Phase 2 schemas for template variants, create-template request/response payloads, round-trip validation summaries, and activation metadata. Create focused Wave 0 server/client tests that lock these expectations before implementation spreads.</action>
  <verify>
    <automated>node --test --import tsx test/server/template-phase2.test.ts</automated>
  </verify>
  <done>Phase 2 starts from a stable typed contract for template-led authoring and trust signaling.</done>
</task>

<task type="auto">
  <name>Task 2: Extend typed routes and hooks for template creation</name>
  <files>shared/routes.ts, client/src/hooks/use-ddl.ts</files>
  <action>Expose typed routes and client hooks for listing template variants, creating a workbook from a template, and receiving round-trip validation feedback through the same shared contract family.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Later backend and sidebar work can rely on typed template hooks instead of manual request wiring.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/server/template-phase2.test.ts`
- [ ] `node --test --import tsx test/client/template-phase2-ui.test.tsx`
- [ ] `npm run check`
- [ ] built-in template success requires round-trip success
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 starts from one explicit template/trust contract instead of implicit workbook assumptions
</success_criteria>

<output>
After completion, create `.planning/phases/02-template-and-round-trip-authoring-v1_1/02-01-SUMMARY.md`
</output>
