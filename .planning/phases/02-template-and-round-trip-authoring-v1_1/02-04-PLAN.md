---
phase: 02-template-and-round-trip-authoring-v1_1
plan: 04
type: execute
wave: 4
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - client/src/pages/Dashboard.tsx
  - client/src/components/templates/TemplateCreateDialog.tsx
  - client/src/components/Sidebar.tsx
  - test/client/template-phase2-ui.test.tsx
autonomous: true
requirements:
  - TPL-01
  - TPL-03
user_setup: []
must_haves:
  truths:
    - "A successfully created workbook immediately becomes the active file in the normal workspace."
    - "Round-trip trust is visible to the user rather than hidden behind a silent backend call."
    - "Blocking validation failures are explained clearly and do not leave the app pretending a broken workbook was created."
  artifacts:
    - path: "client/src/pages/Dashboard.tsx"
      provides: "post-create file activation and handoff into normal workflow"
      contains: "selectedFileId"
    - path: "client/src/components/templates/TemplateCreateDialog.tsx"
      provides: "trust/failure messaging for template creation"
      contains: "validation"
    - path: "test/client/template-phase2-ui.test.tsx"
      provides: "coverage for success activation and blocked failure UX"
      contains: "template"
---

<objective>
Finish the trust and handoff experience for template-led workbook creation.

Purpose: Phase 2 only feels complete if successful template creation immediately drops the user into the normal file workflow and blocked validation failures are clearly explained. This plan closes that loop.
Output: Post-create activation flow, trust messaging, and blocked-failure UX for templates.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-template-and-round-trip-authoring-v1_1/02-CONTEXT.md
@.planning/phases/02-template-and-round-trip-authoring-v1_1/02-VALIDATION.md
@client/src/pages/Dashboard.tsx
@client/src/components/Sidebar.tsx
@client/src/components/templates/TemplateCreateDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auto-select newly created template workbooks in the existing file flow</name>
  <files>client/src/pages/Dashboard.tsx, client/src/components/Sidebar.tsx, test/client/template-phase2-ui.test.tsx</files>
  <behavior>
    - Test 1: successful template creation adds a workbook to the file list
    - Test 2: the newly created workbook becomes the active file automatically
    - Test 3: the user lands back in the standard workspace without manual re-import steps
  </behavior>
  <action>Wire the successful create-template response into dashboard file selection so the created workbook appears and becomes active immediately inside the existing workflow.</action>
  <verify>
    <automated>node --test --import tsx test/client/template-phase2-ui.test.tsx</automated>
  </verify>
  <done>The template flow feels like true in-app file creation instead of a detached export-and-reimport loop.</done>
</task>

<task type="auto">
  <name>Task 2: Surface trust and failure states clearly for built-in templates</name>
  <files>client/src/components/templates/TemplateCreateDialog.tsx, client/src/pages/Dashboard.tsx</files>
  <action>Show users when built-in template validation succeeds and explain blocking failures with actionable mismatch messaging. Do not leave behind a fake success state if round-trip validation failed.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Phase 2 delivers "trusted templates" as a product promise rather than an invisible backend behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/client/template-phase2-ui.test.tsx`
- [ ] `npm run check`
- [ ] successful create auto-selects the new workbook
- [ ] blocked validation failure does not present a false success state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Template-led creation ends in a trusted workbook handoff into the normal file workflow
</success_criteria>

<output>
After completion, create `.planning/phases/02-template-and-round-trip-authoring-v1_1/02-04-SUMMARY.md`
</output>
