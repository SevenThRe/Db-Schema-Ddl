---
phase: 02-template-and-round-trip-authoring-v1_1
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - server/lib/excel.ts
  - server/lib/excel-writeback.ts
  - server/routes/files-routes.ts
  - server/storage.ts
  - test/server/template-phase2.test.ts
autonomous: true
requirements:
  - TPL-01
  - TPL-02
  - TPL-03
user_setup: []
must_haves:
  truths:
    - "Built-in templates come from parser-proven workbook skeletons rather than ad hoc workbook shapes."
    - "Template creation produces a real `.xlsx` file and immediately round-trips it through the parser."
    - "Successful template creation enters the normal file inventory path without a manual re-import loop."
  artifacts:
    - path: "server/routes/files-routes.ts"
      provides: "template creation route and file registration behavior"
      contains: "template"
    - path: "server/storage.ts"
      provides: "uploaded-file registration for generated template workbooks"
      contains: "file"
    - path: "server/lib/excel.ts"
      provides: "round-trip validation source of truth"
      contains: "parse"
  key_links:
    - from: "server/routes/files-routes.ts"
      to: "server/storage.ts"
      via: "generated workbook is registered like a normal file"
      pattern: "create|file"
---

<objective>
Implement server-side first-party template creation and blocking round-trip validation.

Purpose: Phase 2 only works if template output is genuinely parser-compatible and becomes a first-class workbook in the existing file lifecycle. This plan builds the backend generation, validation, and file registration path.
Output: Server-side template builder, parser-backed round-trip validation, and generated workbook registration.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-template-and-round-trip-authoring-v1_1/02-CONTEXT.md
@.planning/phases/02-template-and-round-trip-authoring-v1_1/02-VALIDATION.md
@server/lib/excel.ts
@server/lib/excel-writeback.ts
@server/routes/files-routes.ts
@server/storage.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build template generation and parser-backed round-trip validation</name>
  <files>server/routes/files-routes.ts, server/lib/excel.ts, test/server/template-phase2.test.ts</files>
  <behavior>
    - Test 1: each official template variant can be generated as a real `.xlsx`
    - Test 2: the generated workbook reopens through the existing parser as the intended supported layout
    - Test 3: round-trip mismatch blocks the response from claiming success
  </behavior>
  <action>Implement a server-side template creation pipeline that starts from parser-proven workbook skeletons, blanks them into valid first-party templates, and immediately re-parses the result to verify trust before returning success.</action>
  <verify>
    <automated>node --test --import tsx test/server/template-phase2.test.ts</automated>
  </verify>
  <done>Built-in templates are self-validating rather than trusted by assumption.</done>
</task>

<task type="auto">
  <name>Task 2: Register created workbooks into the normal file inventory</name>
  <files>server/routes/files-routes.ts, server/storage.ts, server/lib/excel-writeback.ts</files>
  <action>Persist each successfully generated workbook as a real file and register it through the same uploaded-files lifecycle used by normal workbook input so the UI can select it immediately without manual upload/download churn.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>A successful template create call yields a trusted workbook that is already part of the app's normal file list.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/server/template-phase2.test.ts`
- [ ] `npm run check`
- [ ] generated workbook is a real `.xlsx`
- [ ] built-in template validation failure blocks success
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 backend creates parser-compatible workbooks that immediately join the normal file lifecycle
</success_criteria>

<output>
After completion, create `.planning/phases/02-template-and-round-trip-authoring-v1_1/02-02-SUMMARY.md`
</output>
