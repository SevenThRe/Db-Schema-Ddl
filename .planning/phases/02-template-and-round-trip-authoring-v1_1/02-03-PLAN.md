---
phase: 02-template-and-round-trip-authoring-v1_1
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - client/src/components/Sidebar.tsx
  - client/src/hooks/use-ddl.ts
  - client/src/components/templates/TemplateCreateDialog.tsx
  - test/client/template-phase2-ui.test.tsx
autonomous: true
requirements:
  - TPL-01
  - TPL-02
user_setup: []
must_haves:
  truths:
    - "Template creation is discoverable from the existing upload entry point."
    - "The upload affordance becomes a lightweight action menu rather than a second sidebar module."
    - "Template variant selection happens in a small chooser panel, not through multiple top-level buttons."
  artifacts:
    - path: "client/src/components/Sidebar.tsx"
      provides: "upload action menu with template entry point"
      contains: "template"
    - path: "client/src/components/templates/TemplateCreateDialog.tsx"
      provides: "template chooser UI with two official variants"
      contains: "template"
    - path: "test/client/template-phase2-ui.test.tsx"
      provides: "structural coverage for menu and chooser behavior"
      contains: "template"
---

<objective>
Add the template-led creation entrypoint to the existing file workflow.

Purpose: The user wants this to feel like "new file" rather than a detached export utility. This plan transforms the upload affordance into a menu and adds the small chooser flow for selecting between the two official workbook variants.
Output: Upload action menu and template chooser dialog wired into the current sidebar flow.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-template-and-round-trip-authoring-v1_1/02-CONTEXT.md
@.planning/phases/02-template-and-round-trip-authoring-v1_1/02-VALIDATION.md
@client/src/components/Sidebar.tsx
@client/src/pages/Dashboard.tsx
@client/src/hooks/use-ddl.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert the upload affordance into an action menu with template entry</name>
  <files>client/src/components/Sidebar.tsx, test/client/template-phase2-ui.test.tsx</files>
  <behavior>
    - Test 1: upload is still available from the same sidebar area
    - Test 2: `从模板创建` is available from the new action menu
    - Test 3: selecting `从模板创建` opens a chooser instead of immediately forcing a variant
  </behavior>
  <action>Refactor the sidebar upload entry into a small action menu that preserves upload behavior while adding a template creation path. Keep the control lightweight and close to the existing file-centric mental model.</action>
  <verify>
    <automated>node --test --import tsx test/client/template-phase2-ui.test.tsx</automated>
  </verify>
  <done>The template flow becomes discoverable without creating a second file-creation surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add a small chooser dialog for the two official template variants</name>
  <files>client/src/components/templates/TemplateCreateDialog.tsx, client/src/hooks/use-ddl.ts, client/src/components/Sidebar.tsx</files>
  <action>Create a small chooser dialog that explains the two official template variants and launches typed template creation. Keep the UI explicit, low-learning-cost, and ready for future template descriptions.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Users can choose the correct workbook layout variant without cluttering the sidebar with multiple top-level template buttons.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/client/template-phase2-ui.test.tsx`
- [ ] `npm run check`
- [ ] upload remains available
- [ ] template selection happens in a chooser, not the top-level menu
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Template creation is introduced as a natural extension of the file upload flow
</success_criteria>

<output>
After completion, create `.planning/phases/02-template-and-round-trip-authoring-v1_1/02-03-SUMMARY.md`
</output>
