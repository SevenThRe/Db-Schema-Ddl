---
phase: 40-ship-er-diagram-relation-canvas-and-graph-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/extensions/db-workbench/workbench-session.ts
  - client/src/components/extensions/db-workbench/relation-graph.ts
  - client/src/components/extensions/db-workbench/RelationCanvasPane.tsx
  - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
  - client/src/components/extensions/db-workbench/ObjectInspectionPane.tsx
  - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  - docs/db-workbench-feature-checklist.md
  - test/client/db-workbench-relation-canvas-phase40.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The relation canvas is derived from the current introspected schema snapshot and foreign-key metadata, not from disconnected mock data."
    - "Canvas selection, sidebar selection, and object inspection stay linked so graph navigation does not fork the operator context."
    - "Large schema graphs remain usable through focus, filtering, and hide-isolated controls instead of rendering every table as an unreadable wall."
  artifacts:
    - path: "client/src/components/extensions/db-workbench/RelationCanvasPane.tsx"
      provides: "first-class ER and relation graph surface"
      contains: "ReactFlow"
    - path: "client/src/components/extensions/db-workbench/workbench-session.ts"
      provides: "relation canvas session tab persistence"
      contains: "\"relations\""
    - path: "test/client/db-workbench-relation-canvas-phase40.test.ts"
      provides: "focused relation canvas regression coverage"
      contains: "RelationCanvasPane"
  key_links:
    - from: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      to: "client/src/components/extensions/db-workbench/RelationCanvasPane.tsx"
      via: "selectedTableName"
      pattern: "relations"
---

<objective>
Ship a read-only ER and relation canvas that turns the already introspected table, index, and foreign-key metadata into a navigable graph inside the canonical DB workbench.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/db-workbench-extension-design.md
@docs/db-workbench-feature-checklist.md
@client/src/components/extensions/db-workbench/workbench-session.ts
@client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
@client/src/components/extensions/db-workbench/ObjectInspectionPane.tsx
@client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
@client/src/components/extensions/db-workbench/ExplainPlanPane.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add relation-graph shaping utilities and a dedicated workbench result surface</name>
  <read_first>
    - client/src/components/extensions/db-workbench/workbench-session.ts
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
  </read_first>
  <files>
    - client/src/components/extensions/db-workbench/workbench-session.ts
    - client/src/components/extensions/db-workbench/relation-graph.ts
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  </files>
  <action>
Add a `relations` workbench result tab, create a `relation-graph.ts` helper that converts the active `DbSchemaSnapshot` tables and foreign keys into graph nodes and edges, and preserve the last-opened relation canvas state in the workbench session model.
  </action>
  <acceptance_criteria>
    - `workbench-session.ts` contains `"relations"`
    - `client/src/components/extensions/db-workbench/relation-graph.ts` exists
    - `relation-graph.ts` contains `foreignKeys`
    - `WorkbenchLayout.tsx` can switch into a relation-canvas result surface
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The workbench now has a stable graph model and reachable relation-canvas route.</done>
</task>

<task type="auto">
  <name>Task 2: Build an interactive relation canvas linked to sidebar and inspection context</name>
  <read_first>
    - client/src/components/extensions/db-workbench/ExplainPlanPane.tsx
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
    - client/src/components/extensions/db-workbench/ObjectInspectionPane.tsx
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  </read_first>
  <files>
    - client/src/components/extensions/db-workbench/RelationCanvasPane.tsx
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
    - client/src/components/extensions/db-workbench/ObjectInspectionPane.tsx
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  </files>
  <action>
Create `RelationCanvasPane.tsx` on top of `@xyflow/react` and `elkjs`, with graph search, fit view, hide-isolated tables, and neighbor-focus actions, then wire sidebar and inspection affordances so operators can open and focus tables directly in the graph.
  </action>
  <acceptance_criteria>
    - `RelationCanvasPane.tsx` exists
    - `RelationCanvasPane.tsx` contains `ReactFlow`
    - `RelationCanvasPane.tsx` contains `Hide isolated`
    - `ConnectionSidebar.tsx` or `ObjectInspectionPane.tsx` contains `Open graph`
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Operators can move between tree navigation, inspection, and graph navigation without losing table context.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused canvas regression coverage and update the shipped-feature inventory</name>
  <read_first>
    - docs/db-workbench-feature-checklist.md
    - client/src/components/extensions/db-workbench/RelationCanvasPane.tsx
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  </read_first>
  <files>
    - docs/db-workbench-feature-checklist.md
    - test/client/db-workbench-relation-canvas-phase40.test.ts
  </files>
  <action>
Add a client regression test that asserts the new relation canvas surface is reachable and linked to the existing navigation surfaces, then update the feature checklist so ER and relation graph support no longer remains a gap after implementation.
  </action>
  <acceptance_criteria>
    - `test/client/db-workbench-relation-canvas-phase40.test.ts` exists
    - the test asserts `RelationCanvasPane`
    - the test asserts `Open graph`
    - `docs/db-workbench-feature-checklist.md` mentions `relation canvas`
  </acceptance_criteria>
  <verify>
    <automated>node --import=tsx --test test/client/db-workbench-relation-canvas-phase40.test.ts</automated>
  </verify>
  <done>The shipped relation-canvas surface is locked down by a focused regression and reflected in the product checklist.</done>
</task>

</tasks>

<verification>
- npm run check
- node --import=tsx --test test/client/db-workbench-relation-canvas-phase40.test.ts
</verification>

<success_criteria>
- The canonical workbench can render and navigate a usable relation graph from live schema snapshots without leaving the operator workflow
</success_criteria>

<output>
After completion, create `.planning/phases/40-ship-er-diagram-relation-canvas-and-graph-navigation/40-01-SUMMARY.md`
</output>
