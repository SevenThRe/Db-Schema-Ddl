---
phase: 04-ddl-import-and-extension-management-v1_4
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified: []
autonomous: false
requirements: [IMP-01, IMP-02, IMP-03, EXTUI-01, EXTUI-02, EXTUI-03, EXTUI-04, EXTUI-05]
must_haves:
  truths:
    - "All 5 success criteria from ROADMAP Phase 4 are visually verified"
    - "App restart preserves disabled extension state"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of all Phase 4 deliverables.

Purpose: Confirm the phase is complete before marking done.
Output: Human-verified confirmation of all 5 success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ddl-import-and-extension-management-v1_4/04-CONTEXT.md
@.planning/phases/04-ddl-import-and-extension-management-v1_4/04-01-SUMMARY.md
@.planning/phases/04-ddl-import-and-extension-management-v1_4/04-02-SUMMARY.md
@.planning/phases/04-ddl-import-and-extension-management-v1_4/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build validation</name>
  <files></files>
  <read_first>
    - .planning/phases/04-ddl-import-and-extension-management-v1_4/04-VALIDATION.md (phase gate criteria)
  </read_first>
  <action>
Run `npm run build` to confirm the complete application compiles without errors after all Phase 4 plans.
  </action>
  <verify>
    <automated>cd "C:\Users\ISI202502\Downloads\Db-Schema-Ddl\.claude\worktrees\gracious-saha" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - No TypeScript type errors in output
  </acceptance_criteria>
  <done>Full build passes with all Phase 4 changes integrated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 4 complete: DDL 导入 sidebar entry + live-DB execution, Extension Management full-page, sidebar cleanup.
  </what-built>
  <how-to-verify>
**Start the app** with `npm run dev` (or `cargo tauri dev` for desktop).

**Success Criterion 1 (IMP-01):** Click "DDL 导入" in the sidebar. Verify the workspace opens. Switch to "导入到数据库" tab. Select a connection, load a .sql file, click "导入到数据库". Verify per-statement success/error with line number reference.

**Success Criterion 2 (IMP-02):** Prepare a .sql file containing a DROP TABLE statement. Use a prod connection. Verify: (a) DangerousSqlDialog appears before executing the DROP statement, (b) the dialog requires typing the database name to confirm.

**Success Criterion 3 (EXTUI-01):** Check the primary sidebar. Verify: 数据库 does NOT appear as a direct entry (only as dynamic extNavItem when DB 工作台 is enabled). DDL→Excel and Enum生成 are NOT visible.

**Success Criterion 4 (EXTUI-02 + EXTUI-03 + EXTUI-04):** Click "扩展功能" in the sidebar footer. Verify: full-page Extension Management opens (not a dialog). DB 工作台 card shows name, version, description, capability badges, enabled toggle, 打开 button, 卸载 button.

**Success Criterion 5 (EXTUI-05):** Toggle DB 工作台 OFF in Extension Management. Verify: sidebar DB 工作台 entry disappears immediately. Toggle back ON. Verify: entry reappears. Close and restart the app. Toggle OFF, restart — verify DB 工作台 stays hidden after restart.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 criteria pass, or describe specific issues.</resume-signal>
</task>

</tasks>

<verification>
All 5 ROADMAP Phase 4 success criteria verified by human.
</verification>

<success_criteria>
- Full build passes
- Human approves all 5 success criteria
</success_criteria>

<output>
After completion, create `.planning/phases/04-ddl-import-and-extension-management-v1_4/04-04-SUMMARY.md`
</output>
