---
phase: 47-build-extension-install-activation-and-persisted-enablement-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/extensions/commands.rs
  - src-tauri/src/extensions/lifecycle.rs
  - client/src/hooks/use-extensions.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Installing an extension leaves it enabled by default unless the operator explicitly disables it later."
    - "Uninstalling an extension clears persisted disabled-state residue so reinstall is recoverable."
    - "Frontend mutations have one truthful path for install, uninstall, and enablement invalidation."
  artifacts:
    - path: "src-tauri/src/extensions/commands.rs"
      provides: "install/uninstall/enable persistence truth"
      contains: "ext_set_enabled"
    - path: "client/src/hooks/use-extensions.ts"
      provides: "canonical extension lifecycle mutations for the UI"
      contains: "setEnabled"
---

<objective>
Fix the lifecycle truth underneath Phase 47 so install, enable, disable, reinstall, and uninstall produce recoverable persisted state instead of leaving stale disabled markers behind.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-build-extension-install-activation-and-persisted-enablement-flow/47-CONTEXT.md
@src-tauri/src/extensions/commands.rs
@src-tauri/src/extensions/lifecycle.rs
@src-tauri/src/extensions/registry.rs
@client/src/hooks/use-extensions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize persisted enablement across install and uninstall</name>
  <read_first>
    - src-tauri/src/extensions/commands.rs
    - .planning/phases/47-build-extension-install-activation-and-persisted-enablement-flow/47-CONTEXT.md
  </read_first>
  <files>
    - src-tauri/src/extensions/commands.rs
  </files>
  <action>
Ensure install clears any stale disabled marker for the extension, uninstall clears persisted disabled state after removal, and the backend remains the single source of truth for enabled/disabled persistence.
  </action>
  <acceptance_criteria>
    - `ext_install` returns an installed extension that is visible as enabled through the next `ext_list_all` read
    - `ext_uninstall` removes the extension from the disabled-state file if present
    - no client-only workaround is required to make reinstall visible again
  </acceptance_criteria>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -j 1</automated>
  </verify>
  <done>Persisted enablement no longer gets stranded across install/uninstall cycles.</done>
</task>

<task type="auto">
  <name>Task 2: Expose enable/disable as a canonical frontend mutation</name>
  <read_first>
    - client/src/hooks/use-extensions.ts
    - src-tauri/src/extensions/commands.rs
  </read_first>
  <files>
    - client/src/hooks/use-extensions.ts
  </files>
  <action>
Add a first-class enable/disable mutation to the shared hook and make all lifecycle mutations invalidate both installed-list and resolved-shell queries consistently.
  </action>
  <acceptance_criteria>
    - `useExtensions()` exposes `setEnabled(id, enabled)`
    - install, uninstall, and enable/disable all invalidate both `extensions/list` and `extensions/all`
    - UI callers no longer need to hand-roll `invoke(\"ext_set_enabled\")`
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The frontend has one truthful lifecycle API for extension state changes.</done>
</task>

</tasks>

<verification>
- npm run check
- cargo check --manifest-path src-tauri/Cargo.toml -j 1
</verification>

<output>
After completion, create `.planning/phases/47-build-extension-install-activation-and-persisted-enablement-flow/47-01-SUMMARY.md`
</output>
