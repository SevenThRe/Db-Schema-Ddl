---
phase: 33-persist-edit-drafts-and-recovery-across-reruns-and-restart
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/extensions/db-workbench/workbench-session.ts
  - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  - client/src/components/extensions/db-workbench/ResultGridPane.tsx
  - client/src/components/extensions/db-workbench/GridEditCommitDialog.tsx
  - client/src/components/extensions/db-workbench/grid-edit-summary.ts
  - test/client/db-workbench-draft-recovery-phase33.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Insert, update, and delete drafts survive reload and restart only when they still match the same connection, schema, table, and result signature."
    - "Recovered drafts are explicit operator-facing state, not silent background mutation."
    - "Stale drafts cannot drift into commit preview without a visible recovery or discard decision."
  artifacts:
    - path: "client/src/components/extensions/db-workbench/workbench-session.ts"
      provides: "durable grid draft session contract"
      contains: "gridDrafts"
    - path: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      provides: "draft restore and invalidation wiring"
      contains: "restoredGridDraft"
    - path: "test/client/db-workbench-draft-recovery-phase33.test.ts"
      provides: "focused draft persistence regression coverage"
      contains: "gridDrafts"
  key_links:
    - from: "client/src/components/extensions/db-workbench/workbench-session.ts"
      to: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      via: "gridDrafts"
      pattern: "resultSignature"
---

<objective>
Persist grid edit drafts across reruns and app restart without letting recovered state silently apply to the wrong result set or table context.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/components/extensions/db-workbench/workbench-session.ts
@client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
@client/src/components/extensions/db-workbench/ResultGridPane.tsx
@client/src/components/extensions/db-workbench/GridEditCommitDialog.tsx
@client/src/components/extensions/db-workbench/grid-edit-summary.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a durable grid-draft session model keyed by result scope</name>
  <read_first>
    - client/src/components/extensions/db-workbench/workbench-session.ts
    - client/src/components/extensions/db-workbench/grid-edit-summary.ts
  </read_first>
  <files>
    - client/src/components/extensions/db-workbench/workbench-session.ts
    - client/src/components/extensions/db-workbench/grid-edit-summary.ts
  </files>
  <action>
Extend `WorkbenchSessionState` with a `gridDrafts` collection that stores `tableName`, `schema`, `primaryKeyColumns`, `resultSignature`, `patchCells`, `deletedRows`, `insertedRows`, and `updatedAt`, and bump the session storage version so old session payloads migrate safely.
  </action>
  <acceptance_criteria>
    - `workbench-session.ts` contains `gridDrafts`
    - `workbench-session.ts` contains `resultSignature`
    - the session storage key version changes from `v2` to `v3`
    - saved drafts include insert, update, and delete payloads in one shape
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Recovered drafts now have one typed durable storage shape instead of transient component-only state.</done>
</task>

<task type="auto">
  <name>Task 2: Restore compatible drafts and force explicit stale-draft resolution</name>
  <read_first>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx
    - client/src/components/extensions/db-workbench/GridEditCommitDialog.tsx
  </read_first>
  <files>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
    - client/src/components/extensions/db-workbench/ResultGridPane.tsx
    - client/src/components/extensions/db-workbench/GridEditCommitDialog.tsx
  </files>
  <action>
Restore drafts only when the active result matches the saved `resultSignature`, surface a visible `Recovered draft` state in the grid and commit dialog, and require explicit discard when the saved draft no longer matches the current table, schema, or primary-key scope.
  </action>
  <acceptance_criteria>
    - `WorkbenchLayout.tsx` contains `restoredGridDraft`
    - `ResultGridPane.tsx` contains `Recovered draft`
    - incompatible drafts are not merged automatically into the active edit set
    - recovered drafts can be discarded without clearing unrelated session state
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Draft recovery is explicit, scoped, and safe against stale replay.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused draft recovery regression coverage</name>
  <read_first>
    - client/src/components/extensions/db-workbench/workbench-session.ts
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  </read_first>
  <files>
    - test/client/db-workbench-draft-recovery-phase33.test.ts
  </files>
  <action>
Create a Phase 33 client test file that asserts the `gridDrafts` session model exists, compatible drafts restore into the workbench, and stale drafts are flagged instead of replayed silently.
  </action>
  <acceptance_criteria>
    - `test/client/db-workbench-draft-recovery-phase33.test.ts` exists
    - the test asserts `gridDrafts`
    - the test asserts `Recovered draft`
    - the test asserts stale drafts require discard or recovery handling
  </acceptance_criteria>
  <verify>
    <automated>node --import=tsx --test test/client/db-workbench-draft-recovery-phase33.test.ts</automated>
  </verify>
  <done>The durable-draft behavior is pinned down by source-level regression tests.</done>
</task>

</tasks>

<verification>
- npm run check
- node --import=tsx --test test/client/db-workbench-draft-recovery-phase33.test.ts
</verification>

<success_criteria>
- Insert, update, and delete drafts can survive restart without drifting into the wrong result context
</success_criteria>

<output>
After completion, create `.planning/phases/33-persist-edit-drafts-and-recovery-across-reruns-and-restart/33-01-SUMMARY.md`
</output>
