---
phase: 45-support-installable-frontend-extension-bundles-and-runtime-ui-mounting
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - test/client/extension-runtime-mount-phase45.test.ts
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 45 has regression evidence for the new bundle contract and runtime mount shell path."
    - "ROADMAP and STATE reflect that runtime bundle loading became the new current anchor after Phase 44."
    - "The shell-level failure semantics stay guarded as the extension platform evolves."
  artifacts:
    - path: "test/client/extension-runtime-mount-phase45.test.ts"
      provides: "Static regression guard for runtime bundle contract and shell mount path"
      contains: "uiBundle"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 45 plan/progress accounting"
      contains: "Phase 45"
    - path: ".planning/STATE.md"
      provides: "Current milestone state after Phase 45 execution"
      contains: "Phase 45"
  key_links:
    - from: "test/client/extension-runtime-mount-phase45.test.ts"
      to: "client/src/extensions/ExtensionWorkspaceHost.tsx"
      via: "runtime mount contract is guarded by explicit text assertions"
      pattern: "iframe"
    - from: ".planning/ROADMAP.md"
      to: ".planning/STATE.md"
      via: "phase completion updates the current focus and completed-plan counts"
      pattern: "Phase 45"
---

<objective>
Add regression coverage and planning-state updates so Phase 45 becomes a stable handoff point for the later DB Workbench extraction work.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-support-installable-frontend-extension-bundles-and-runtime-ui-mounting/45-CONTEXT.md
@client/src/extensions/ExtensionWorkspaceHost.tsx
@client/src/extensions/shell/ExtensionSecondarySidebar.tsx
@src-tauri/src/extensions/commands.rs
@shared/extension-schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a focused static regression test for runtime bundle mounting</name>
  <read_first>
    - shared/extension-schema.ts
    - client/src/extensions/ExtensionWorkspaceHost.tsx
    - client/src/extensions/shell/ExtensionSecondarySidebar.tsx
    - src-tauri/src/extensions/commands.rs
  </read_first>
  <files>
    - test/client/extension-runtime-mount-phase45.test.ts
  </files>
  <action>
Create `test/client/extension-runtime-mount-phase45.test.ts` that asserts the shared schema contains the new bundle contract, backend state exposes explicit bundle readiness, and the host render path contains the runtime iframe fallback plus explicit placeholder semantics.
  </action>
  <acceptance_criteria>
    - `test/client/extension-runtime-mount-phase45.test.ts` exists
    - the test asserts `shared/extension-schema.ts` contains `uiBundle`
    - the test asserts `src-tauri/src/extensions/commands.rs` contains bundle `status`
    - the test asserts `client/src/extensions/ExtensionWorkspaceHost.tsx` contains `iframe`
    - the test asserts `client/src/extensions/shell/ExtensionSecondarySidebar.tsx` contains explicit non-ready bundle placeholder text
  </acceptance_criteria>
  <verify>
    <automated>NODE_OPTIONS=--import=tsx node --test --experimental-strip-types test/client/extension-runtime-mount-phase45.test.ts</automated>
  </verify>
  <done>The runtime mount contract and shell fallbacks are guarded against regression.</done>
</task>

<task type="auto">
  <name>Task 2: Update roadmap/state tracking to reflect Phase 45 execution</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/45-support-installable-frontend-extension-bundles-and-runtime-ui-mounting/45-CONTEXT.md
  </read_first>
  <files>
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </files>
  <action>
Update roadmap and state tracking so Phase 45's plans, completion state, and downstream focus are visible to the next autonomous resume point. Keep the next extension-platform anchor pointed at Phase 46 once verification passes.
  </action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` records Phase 45 plans and completion state
    - `.planning/STATE.md` advances the current focus beyond Phase 45 after execution completes
    - `.planning/STATE.md` references the latest Phase 45 verification artifact for resume continuity
  </acceptance_criteria>
  <verify>
    <automated>node "C:/Users/ASUS/.codex/get-shit-done/bin/gsd-tools.cjs" runtime status</automated>
  </verify>
  <done>The GSD planning state reflects Phase 45 as the new completion anchor.</done>
</task>

</tasks>

<verification>
- npm run check
- cargo check --manifest-path src-tauri/Cargo.toml -j 1
- NODE_OPTIONS=--import=tsx node --test --experimental-strip-types test/client/extension-runtime-mount-phase45.test.ts
</verification>

<success_criteria>
- Phase 45 truth 1: regression coverage exists for bundle contract and runtime mount behavior
- Phase 45 truth 2: GSD roadmap/state advance cleanly after execution
- Phase 45 truth 3: Phase 46 can start from a stable runtime-bundle foundation
</success_criteria>

<output>
After completion, create `.planning/phases/45-support-installable-frontend-extension-bundles-and-runtime-ui-mounting/45-03-SUMMARY.md`
</output>
