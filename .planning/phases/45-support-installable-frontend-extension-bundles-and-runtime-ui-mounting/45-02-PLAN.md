---
phase: 45-support-installable-frontend-extension-bundles-and-runtime-ui-mounting
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src-tauri/tauri.conf.json
  - client/src/extensions/contribution-resolver.ts
  - client/src/extensions/host-context.tsx
  - client/src/extensions/ExtensionWorkspaceHost.tsx
  - client/src/extensions/shell/ExtensionSecondarySidebar.tsx
  - client/src/extensions/panel-registry.ts
  - client/src/extensions/sidebar-view-registry.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The host can mount extension-provided sidebar and workbench UI at runtime without recompiling the app."
    - "Builtin surfaces still resolve through local registries while external surfaces use an explicit runtime mount path."
    - "The shell shows explicit failure states for non-ready bundles instead of a blank content area."
  artifacts:
    - path: "src-tauri/tauri.conf.json"
      provides: "Asset-protocol access for installed extension UI bundles"
      contains: "assetProtocol"
    - path: "client/src/extensions/ExtensionWorkspaceHost.tsx"
      provides: "Runtime workbench mount fallback for external bundle views"
      contains: "iframe"
    - path: "client/src/extensions/shell/ExtensionSecondarySidebar.tsx"
      provides: "Runtime sidebar mount fallback for external bundle views"
      contains: "runtime"
    - path: "client/src/extensions/contribution-resolver.ts"
      provides: "Resolved runtime-view metadata on sidebar and workbench surfaces"
      contains: "runtimeView"
  key_links:
    - from: "src-tauri/tauri.conf.json"
      to: "client/src/extensions/ExtensionWorkspaceHost.tsx"
      via: "asset protocol enables local bundle URLs to load inside the shell"
      pattern: "assetProtocol"
    - from: "client/src/extensions/contribution-resolver.ts"
      to: "client/src/extensions/shell/ExtensionSecondarySidebar.tsx"
      via: "resolved runtime-view metadata drives sidebar iframe mounting"
      pattern: "runtimeView"
---

<objective>
Add the host-side runtime loader. External sidebar and workbench surfaces should mount from installed bundle assets, while builtin surfaces continue to render local React components.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-support-installable-frontend-extension-bundles-and-runtime-ui-mounting/45-CONTEXT.md
@shared/extension-schema.ts
@src-tauri/tauri.conf.json
@client/src/extensions/contribution-resolver.ts
@client/src/extensions/host-context.tsx
@client/src/extensions/ExtensionWorkspaceHost.tsx
@client/src/extensions/shell/ExtensionSecondarySidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable an asset-protocol path for installed extension UI bundles</name>
  <read_first>
    - src-tauri/tauri.conf.json
    - .planning/phases/45-support-installable-frontend-extension-bundles-and-runtime-ui-mounting/45-CONTEXT.md
  </read_first>
  <files>
    - src-tauri/tauri.conf.json
  </files>
  <action>
Enable Tauri asset-protocol access for installed extension files so the frontend can turn validated bundle entry paths into webview-loadable URLs. Keep the scope narrow to the installed extension directory under app data rather than widening local-file access globally.
  </action>
  <acceptance_criteria>
    - `src-tauri/tauri.conf.json` contains `assetProtocol`
    - the configured scope targets the installed extension asset path instead of opening unrestricted file access
  </acceptance_criteria>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -j 1</automated>
  </verify>
  <done>The runtime has a supported path for loading installed extension bundle assets.</done>
</task>

<task type="auto">
  <name>Task 2: Teach the resolver and host state to carry runtime-view metadata</name>
  <read_first>
    - client/src/extensions/contribution-resolver.ts
    - client/src/extensions/host-context.tsx
    - shared/extension-schema.ts
  </read_first>
  <files>
    - client/src/extensions/contribution-resolver.ts
    - client/src/extensions/host-context.tsx
  </files>
  <action>
Update resolved sidebar and workbench view shapes so they carry explicit runtime-view metadata from the manifest. Preserve current builtin behavior, but make sure the frontend host can tell when a contributed surface should resolve through a runtime bundle instead of only a local registry component.
  </action>
  <acceptance_criteria>
    - `client/src/extensions/contribution-resolver.ts` carries the runtime-view field on resolved sidebar views
    - `client/src/extensions/contribution-resolver.ts` carries the runtime-view field on resolved workbench views
    - `client/src/extensions/host-context.tsx` continues to expose resolved extension state with bundle readiness available to consumers
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The shell has the metadata it needs to decide between builtin and external runtime surface mounting.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime-mount fallbacks for sidebar and workbench surfaces with explicit placeholders</name>
  <read_first>
    - client/src/extensions/ExtensionWorkspaceHost.tsx
    - client/src/extensions/shell/ExtensionSecondarySidebar.tsx
    - client/src/extensions/panel-registry.ts
    - client/src/extensions/sidebar-view-registry.ts
    - client/src/extensions/host-api.ts
  </read_first>
  <files>
    - client/src/extensions/ExtensionWorkspaceHost.tsx
    - client/src/extensions/shell/ExtensionSecondarySidebar.tsx
  </files>
  <action>
Add a runtime-mount path that renders a sandboxed iframe when a contributed sidebar or workbench view resolves through an external runtime bundle instead of a local registry component. Use the resolved bundle status to show explicit missing, invalid, or incompatible placeholders rather than silently failing. Keep builtin registry rendering untouched when `component` resolves locally.
  </action>
  <acceptance_criteria>
    - `client/src/extensions/ExtensionWorkspaceHost.tsx` can render runtime-mounted extension workbench UI
    - `client/src/extensions/shell/ExtensionSecondarySidebar.tsx` can render runtime-mounted extension sidebar UI
    - the runtime path uses `iframe` with an explicit sandbox
    - non-ready bundle states render explicit placeholder content
    - builtin component rendering still works when a local registry component exists
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The shell can mount external runtime UI safely without breaking builtin shell paths.</done>
</task>

</tasks>

<verification>
- npm run check
- cargo check --manifest-path src-tauri/Cargo.toml -j 1
</verification>

<success_criteria>
- Phase 45 truth 1: runtime loader exists for extension-provided sidebar and workbench UI
- Phase 45 truth 2: builtin and external mount paths are explicit and separate
- Phase 45 truth 3: shell error states are visible for non-ready bundles
</success_criteria>

<output>
After completion, create `.planning/phases/45-support-installable-frontend-extension-bundles-and-runtime-ui-mounting/45-02-SUMMARY.md`
</output>
