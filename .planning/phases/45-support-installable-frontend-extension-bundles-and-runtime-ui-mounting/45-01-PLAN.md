---
phase: 45-support-installable-frontend-extension-bundles-and-runtime-ui-mounting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/extension-schema.ts
  - src-tauri/src/builtin_extensions/mod.rs
  - src-tauri/src/extensions/manifest.rs
  - src-tauri/src/extensions/lifecycle.rs
  - src-tauri/src/extensions/commands.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "External extensions can declare frontend bundle metadata and runtime-view identifiers without breaking builtin extension manifests."
    - "Install/list flows validate declared bundle entries and expose explicit bundle readiness to the frontend."
    - "Builtin component registration remains compatible while external surfaces gain a separate runtime mount path."
  artifacts:
    - path: "shared/extension-schema.ts"
      provides: "Canonical manifest and resolved-extension schema for runtime UI bundles"
      contains: "uiBundle"
    - path: "src-tauri/src/extensions/manifest.rs"
      provides: "External manifest parsing for declared frontend bundles"
      contains: "pub ui_bundle"
    - path: "src-tauri/src/extensions/commands.rs"
      provides: "Resolved extension state with explicit bundle status"
      contains: "ui_mount"
    - path: "src-tauri/src/extensions/lifecycle.rs"
      provides: "Install-time validation for declared frontend bundle entry files"
      contains: "ui bundle"
  key_links:
    - from: "shared/extension-schema.ts"
      to: "src-tauri/src/extensions/manifest.rs"
      via: "TypeScript and Rust parse the same external bundle fields"
      pattern: "uiBundle"
    - from: "src-tauri/src/extensions/lifecycle.rs"
      to: "src-tauri/src/extensions/commands.rs"
      via: "validated install assets become explicit runtime bundle state"
      pattern: "entryPath"
---

<objective>
Extend the extension contract so external packages can declare a frontend bundle and contributed runtime views. The backend must validate those bundle assets and surface explicit readiness information to the shell.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-support-installable-frontend-extension-bundles-and-runtime-ui-mounting/45-CONTEXT.md
@shared/extension-schema.ts
@src-tauri/src/builtin_extensions/mod.rs
@src-tauri/src/extensions/manifest.rs
@src-tauri/src/extensions/lifecycle.rs
@src-tauri/src/extensions/commands.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontend-bundle and runtime-view fields to the shared extension schema</name>
  <read_first>
    - shared/extension-schema.ts
    - .planning/phases/45-support-installable-frontend-extension-bundles-and-runtime-ui-mounting/45-CONTEXT.md
  </read_first>
  <files>
    - shared/extension-schema.ts
  </files>
  <action>
Add an optional manifest-level bundle declaration such as `uiBundle` to `shared/extension-schema.ts`, with at least an entry file path and mount mode for Phase 45. Extend `sidebarViewSchema` and `workbenchViewSchema` with an explicit runtime view identifier for externally mounted surfaces. Add a resolved runtime-bundle state object on `resolvedExtensionSchema` so the frontend can tell whether an external bundle is ready, missing, invalid, or incompatible.
  </action>
  <acceptance_criteria>
    - `shared/extension-schema.ts` contains `uiBundle`
    - `shared/extension-schema.ts` contains a runtime view field on `sidebarViewSchema`
    - `shared/extension-schema.ts` contains a runtime view field on `workbenchViewSchema`
    - `shared/extension-schema.ts` contains a resolved bundle state with `status`
    - `shared/extension-schema.ts` contains `ready`, `missing`, `invalid`, and `incompatible`
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The shared schema can express installable frontend bundles and explicit runtime mount status.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the bundle contract in Rust manifest and builtin models</name>
  <read_first>
    - src-tauri/src/builtin_extensions/mod.rs
    - src-tauri/src/extensions/manifest.rs
    - shared/extension-schema.ts
  </read_first>
  <files>
    - src-tauri/src/builtin_extensions/mod.rs
    - src-tauri/src/extensions/manifest.rs
  </files>
  <action>
Update the Rust-side manifest types so builtin and external extensions share the same optional frontend-bundle and runtime-view fields. Builtin manifests should keep working without providing a bundle, while external manifest parsing must deserialize the new fields with camelCase parity.
  </action>
  <acceptance_criteria>
    - `src-tauri/src/builtin_extensions/mod.rs` contains an optional frontend bundle field
    - `src-tauri/src/builtin_extensions/mod.rs` contains the runtime view field on contributed sidebar or workbench views
    - `src-tauri/src/extensions/manifest.rs` contains `pub ui_bundle`
    - serde naming keeps camelCase parity for the new fields
  </acceptance_criteria>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -j 1</automated>
  </verify>
  <done>Rust manifest parsing and builtin definitions stay aligned with the new runtime bundle contract.</done>
</task>

<task type="auto">
  <name>Task 3: Validate bundle assets and expose explicit runtime bundle state from ext_list_all</name>
  <read_first>
    - src-tauri/src/extensions/lifecycle.rs
    - src-tauri/src/extensions/commands.rs
    - src-tauri/src/extensions/registry.rs
    - .planning/phases/45-support-installable-frontend-extension-bundles-and-runtime-ui-mounting/45-CONTEXT.md
  </read_first>
  <files>
    - src-tauri/src/extensions/lifecycle.rs
    - src-tauri/src/extensions/commands.rs
  </files>
  <action>
During install, validate that a declared frontend bundle entry exists under the extracted extension root. In `ext_list_all`, compute a resolved runtime bundle state that includes status, entry path, and explicit error text so the frontend does not have to infer missing or invalid bundle conditions from absent registry components.
  </action>
  <acceptance_criteria>
    - `src-tauri/src/extensions/lifecycle.rs` validates the declared bundle entry path when `uiBundle` is present
    - `src-tauri/src/extensions/commands.rs` contains a resolved bundle state on `ResolvedExtensionState`
    - `src-tauri/src/extensions/commands.rs` returns bundle `status`
    - `src-tauri/src/extensions/commands.rs` returns bundle `entryPath`
    - the bundle state distinguishes non-ready conditions instead of silently omitting bundle metadata
  </acceptance_criteria>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -j 1</automated>
  </verify>
  <done>The backend can prove whether an external UI bundle is mountable before the shell tries to render it.</done>
</task>

</tasks>

<verification>
- npm run check
- cargo check --manifest-path src-tauri/Cargo.toml -j 1
</verification>

<success_criteria>
- Phase 45 truth 1: external bundles are declared explicitly in the shared contract
- Phase 45 truth 2: backend validation and `ext_list_all` expose explicit mount readiness
- Phase 45 truth 3: builtin manifests remain compatible while external runtime views gain a real contract
</success_criteria>

<output>
After completion, create `.planning/phases/45-support-installable-frontend-extension-bundles-and-runtime-ui-mounting/45-01-SUMMARY.md`
</output>
