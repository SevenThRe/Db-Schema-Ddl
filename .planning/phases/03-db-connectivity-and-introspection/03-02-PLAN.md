---
phase: 03-db-connectivity-and-introspection
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - package.json
  - electron/main.ts
  - electron/preload.ts
  - server/routes/db-management-routes.ts
  - server/lib/extensions/db-management/credential-vault.ts
  - server/lib/extensions/db-management/connection-service.ts
autonomous: true
requirements:
  - DBCO-01
  - DBCO-02
user_setup: []
must_haves:
  truths:
    - "Remembered MySQL passwords are encrypted with Electron safeStorage and are never exposed as routine plain-text renderer or storage state"
    - "Users can create, edit, delete, and test MySQL server connections and list accessible databases through typed extension APIs"
    - "The server and Electron boundary stays narrow: privileged secret handling remains outside the renderer while the module still gets quick reconnect behavior"
  artifacts:
    - path: "server/lib/extensions/db-management/credential-vault.ts"
      provides: "safeStorage-backed credential encryption and decryption"
      contains: "encrypt"
    - path: "server/lib/extensions/db-management/connection-service.ts"
      provides: "Saved-connection CRUD, connection test, and database listing"
      contains: "connection"
    - path: "server/routes/db-management-routes.ts"
      provides: "Typed DB connection and database-selection endpoints"
      contains: "db"
    - path: "electron/preload.ts"
      provides: "Renderer-safe bridge for privileged DB credential flows if needed"
      contains: "extensions"
  key_links:
    - from: "server/lib/extensions/db-management/connection-service.ts"
      to: "server/lib/extensions/db-management/credential-vault.ts"
      via: "encrypted secret persistence and test-time decryption"
      pattern: "credential"
    - from: "server/routes/db-management-routes.ts"
      to: "shared/routes.ts"
      via: "typed connection and database list endpoints"
      pattern: "db"
---

<objective>
Implement protected credential handling and the MySQL connection-management service layer for the extension.

Purpose: Phase 3 is not useful until users can save a MySQL server connection, remember the password safely, test the connection, and browse accessible databases before schema introspection begins.
Output: MySQL connection service, safeStorage-backed credential vault, typed routes, and any narrow preload or Electron glue needed to preserve the privileged secret boundary.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-db-connectivity-and-introspection/03-CONTEXT.md
@.planning/phases/03-db-connectivity-and-introspection/03-RESEARCH.md
@.planning/phases/03-db-connectivity-and-introspection/03-VALIDATION.md
@shared/schema.ts
@shared/routes.ts
@electron/main.ts
@electron/preload.ts
@server/storage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MySQL client dependency and config normalization</name>
  <files>package.json, server/lib/extensions/db-management/connection-service.ts</files>
  <action>Add the MySQL client dependency chosen in research and centralize connection-option normalization for host, port, username, SSL mode, timeouts, and selected-database handling. Keep this layer focused on MySQL-only behavior for Phase 3.</action>
  <verify>Confirm the dependency and service compile cleanly and run `npm run check`.</verify>
  <done>The codebase has a standard MySQL connectivity layer ready for connection test and database discovery.</done>
</task>

<task type="auto">
  <name>Task 2: Build the safeStorage-backed credential vault</name>
  <files>server/lib/extensions/db-management/credential-vault.ts, electron/main.ts, electron/preload.ts</files>
  <action>Implement the remembered-password vault with Electron `safeStorage`, making sure encryption and decryption happen inside privileged code paths only. Persist encrypted blobs plus non-secret metadata, and avoid leaking secrets to logs, query caches, or renderer-visible state.</action>
  <verify>Inspect the secret flow boundary and run `npm run check` plus the Wave 0 credential-vault tests.</verify>
  <done>The extension can remember MySQL passwords securely while preserving the quick reconnect behavior the user asked for.</done>
</task>

<task type="auto">
  <name>Task 3: Expose connection CRUD, test, and database listing APIs</name>
  <files>server/lib/extensions/db-management/connection-service.ts, server/routes/db-management-routes.ts</files>
  <action>Implement saved-connection create or update, delete, test connection, and list databases from `INFORMATION_SCHEMA.SCHEMATA`. Keep the saved connection separate from the current database selection so the module can switch databases later without duplicating connection records.</action>
  <verify>Exercise the route/service coverage against DBCO-01 and run `npm run check` plus the relevant storage and route tests from Wave 0.</verify>
  <done>The extension has a working service/API layer for managing MySQL server connections before introspection starts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] Remembered passwords are encrypted and not stored or returned in plain text
- [ ] Connection APIs support create, edit, delete, test, and list databases
- [ ] Saved connections remain server-level records rather than one-record-per-database
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can safely save and test MySQL connections with remembered credentials
</success_criteria>

<output>
After completion, create `.planning/phases/03-db-connectivity-and-introspection/03-02-SUMMARY.md`
</output>
