---
phase: 03-db-connectivity-and-introspection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - shared/routes.ts
  - server/storage.ts
  - server/constants/db-init.ts
  - server/constants/db-migrations.ts
  - server/init-db.ts
autonomous: true
requirements:
  - DBCO-01
  - DBCO-02
  - DBCO-03
user_setup: []
must_haves:
  truths:
    - "Phase 3 has a canonical live-schema contract that represents MySQL databases, tables, columns, PKs, FKs, indexes or uniques, and comments without overloading the Excel-oriented TableInfo model"
    - "Saved DB connections, protected credential metadata, selected-database state, and live-schema snapshots persist in local SQLite as extension-owned records"
    - "Typed request and response contracts exist for connection CRUD, connection test, database listing, and schema introspection entry points"
  artifacts:
    - path: "shared/schema.ts"
      provides: "Canonical live-schema types plus saved-connection and snapshot schemas"
      contains: "db"
    - path: "shared/routes.ts"
      provides: "Typed DB management extension API contract"
      contains: "db"
    - path: "server/storage.ts"
      provides: "Persistence methods for saved connections, database selection, and live-schema snapshots"
      contains: "connection"
    - path: "server/constants/db-init.ts"
      provides: "SQLite table creation for extension-owned DB connectivity records"
      contains: "db_"
  key_links:
    - from: "shared/routes.ts"
      to: "shared/schema.ts"
      via: "connection and introspection request/response schemas"
      pattern: "db"
    - from: "server/storage.ts"
      to: "shared/schema.ts"
      via: "typed persistence mappings for canonical live-schema and connection records"
      pattern: "db"
---

<objective>
Define the data contract and persistence foundation for MySQL connectivity and canonical live-schema ingestion.

Purpose: Every later connection, credential, introspection, snapshot, and UI workflow in Phase 3 needs a stable shared model before service code and module screens can build on top of it.
Output: Shared canonical schema types, saved-connection and snapshot persistence tables, and typed route contracts for MySQL connection management and introspection workflows.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-db-connectivity-and-introspection/03-CONTEXT.md
@.planning/phases/03-db-connectivity-and-introspection/03-RESEARCH.md
@.planning/phases/03-db-connectivity-and-introspection/03-VALIDATION.md
@shared/schema.ts
@shared/routes.ts
@server/storage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical live-schema and connection contracts</name>
  <files>shared/schema.ts, shared/routes.ts</files>
  <action>Introduce a dedicated canonical live-schema model for MySQL introspection, plus typed saved-connection, selected-database, connection-test, database-list, and introspection request/response schemas. Keep this model separate from `TableInfo` and from file-oriented diff payloads.</action>
  <verify>Inspect the new contracts for complete coverage of tables, columns, PKs, FKs, indexes or uniques, and comments, then run `npm run check`.</verify>
  <done>Phase 3 has a stable type-safe contract for live DB connectivity and canonical schema data.</done>
</task>

<task type="auto">
  <name>Task 2: Persist extension-owned DB records in SQLite</name>
  <files>shared/schema.ts, server/storage.ts, server/constants/db-init.ts, server/constants/db-migrations.ts, server/init-db.ts</files>
  <action>Add SQLite tables and storage methods for saved DB connections, encrypted credential blobs and metadata, selected-database state, and live-schema snapshots. Use clear extension-owned naming so future phases can evolve these records without colliding with existing file-diff storage.</action>
  <verify>Trace migration coverage and storage mappings, then run `npm run check`.</verify>
  <done>The host can durably remember MySQL connection definitions and captured live-schema data across restarts.</done>
</task>

<task type="auto">
  <name>Task 3: Shape API boundaries for later service work</name>
  <files>shared/routes.ts, server/storage.ts</files>
  <action>Define the typed API surface and storage seams that later plans will implement: connection create or update, delete, test, database list, current-database selection, introspection execution, and snapshot persistence. Keep the route surface minimal and aligned with the MySQL-first boundary decided in context.</action>
  <verify>Confirm the new API surface covers DBCO-01 through DBCO-03 without leaking later diff or deploy concerns.</verify>
  <done>Downstream service and UI plans have a clear, typed contract to implement instead of inventing ad hoc payloads.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] Canonical live-schema contracts are separate from `TableInfo`
- [ ] SQLite persistence covers saved connections, selected database state, encrypted credential metadata, and snapshots
- [ ] Typed APIs cover connection CRUD, test, database list, and introspection entry points
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 has a durable shared model and persistence base before MySQL service code is added
</success_criteria>

<output>
After completion, create `.planning/phases/03-db-connectivity-and-introspection/03-01-SUMMARY.md`
</output>
