---
phase: 03-db-connectivity-and-introspection
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - server/lib/extensions/db-management/mysql-introspection.ts
  - server/lib/extensions/db-management/schema-normalizer.ts
  - server/lib/extensions/db-management/snapshot-service.ts
  - server/routes/db-management-routes.ts
  - server/storage.ts
autonomous: true
requirements:
  - DBCO-03
  - DBCO-01
user_setup: []
must_haves:
  truths:
    - "The extension can introspect the selected MySQL database using INFORMATION_SCHEMA rather than ad hoc SQL text parsing"
    - "Raw MySQL metadata is normalized into the canonical live-schema model with tables, columns, PKs, FKs, indexes or uniques, and comments"
    - "Captured live schema can be hashed and persisted as snapshots for later diff and deploy phases"
  artifacts:
    - path: "server/lib/extensions/db-management/mysql-introspection.ts"
      provides: "Scoped MySQL INFORMATION_SCHEMA queries for database introspection"
      contains: "INFORMATION_SCHEMA"
    - path: "server/lib/extensions/db-management/schema-normalizer.ts"
      provides: "Canonical live-schema normalization from raw MySQL metadata"
      contains: "schema"
    - path: "server/lib/extensions/db-management/snapshot-service.ts"
      provides: "Snapshot hashing and persistence for live DB schemas"
      contains: "snapshot"
    - path: "server/routes/db-management-routes.ts"
      provides: "Introspection and snapshot endpoints"
      contains: "introspect"
  key_links:
    - from: "server/lib/extensions/db-management/mysql-introspection.ts"
      to: "server/lib/extensions/db-management/schema-normalizer.ts"
      via: "raw metadata rows flowing into canonical schema output"
      pattern: "schema"
    - from: "server/lib/extensions/db-management/snapshot-service.ts"
      to: "server/storage.ts"
      via: "persisted live-schema snapshots"
      pattern: "snapshot"
---

<objective>
Implement MySQL database introspection, canonical normalization, and live-schema snapshot persistence.

Purpose: Phase 3 only reaches its schema-ingestion goal when the extension can read a selected MySQL database, normalize the results into a stable canonical model, and save enough snapshot state for later diff work.
Output: MySQL INFORMATION_SCHEMA adapter, canonical normalization layer, snapshot persistence service, and typed introspection endpoints.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-db-connectivity-and-introspection/03-CONTEXT.md
@.planning/phases/03-db-connectivity-and-introspection/03-RESEARCH.md
@.planning/phases/03-db-connectivity-and-introspection/03-VALIDATION.md
@shared/schema.ts
@shared/routes.ts
@server/storage.ts
@server/lib/schema-diff.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Query MySQL metadata for the selected database</name>
  <files>server/lib/extensions/db-management/mysql-introspection.ts</files>
  <action>Implement the minimum metadata query set from research using `INFORMATION_SCHEMA` for schemas, tables, columns, constraints, foreign-key rules, and indexes. Scope all reads to the selected database and `BASE TABLE` objects so Phase 3 stays focused and performant.</action>
  <verify>Inspect query coverage against the researched INFORMATION_SCHEMA tables and run `npm run check`.</verify>
  <done>The extension can gather the raw MySQL metadata needed for canonical schema assembly.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize MySQL metadata into the canonical live-schema model</name>
  <files>server/lib/extensions/db-management/schema-normalizer.ts, server/routes/db-management-routes.ts</files>
  <action>Translate raw metadata rows into the canonical live-schema contract introduced in Plan 01. Preserve table comments, column nullability/default/comment/auto-increment signals, PK composition, FK targets with update/delete rules, and index or unique structure in a diff-friendly shape.</action>
  <verify>Run `npm run check` plus the Wave 0 normalization tests and confirm the output is independent from the old Excel parsing model.</verify>
  <done>Live MySQL schema data is emitted in a stable, later-phase-friendly canonical format.</done>
</task>

<task type="auto">
  <name>Task 3: Hash and persist live-schema snapshots</name>
  <files>server/lib/extensions/db-management/snapshot-service.ts, server/storage.ts, server/routes/db-management-routes.ts</files>
  <action>Implement snapshot hashing and persistence for introspected MySQL schemas, including enough metadata to reconnect later Phase 4 and Phase 5 work to a connection and selected database. Keep snapshot persistence additive and avoid coupling it prematurely to file-vs-DB diff output.</action>
  <verify>Trace snapshot creation and storage coverage, then run `npm run check` plus the relevant snapshot and introspection tests from Wave 0.</verify>
  <done>The extension can persist stable live-schema snapshots immediately after introspection.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] Introspection uses INFORMATION_SCHEMA instead of `SHOW CREATE TABLE` parsing
- [ ] Canonical output includes tables, columns, PKs, FKs, indexes or uniques, and comments
- [ ] Snapshots persist enough metadata for later diff and deployment phases
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The extension can read and persist canonical MySQL schema state for a selected database
</success_criteria>

<output>
After completion, create `.planning/phases/03-db-connectivity-and-introspection/03-03-SUMMARY.md`
</output>
