---
phase: 03-db-connectivity-and-introspection
plan: 04
type: execute
wave: 4
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - client/src/pages/Dashboard.tsx
  - client/src/hooks/use-db-management.ts
  - client/src/components/db-management/ConnectionManager.tsx
  - client/src/components/db-management/DatabaseSelector.tsx
  - client/src/components/db-management/SchemaIntrospectionPanel.tsx
autonomous: true
requirements:
  - DBCO-01
  - DBCO-02
  - DBCO-03
user_setup: []
must_haves:
  truths:
    - "The installed extension exposes MySQL connection CRUD, connection test, database switching, and schema introspection from inside the DB 管理 module"
    - "Remembered-password behavior feels fast and integrated without exposing secret values in routine UI"
    - "The selected database and latest introspection state are obvious to the user before Phase 4 diff features exist"
  artifacts:
    - path: "client/src/hooks/use-db-management.ts"
      provides: "Typed client data layer for connection, database selection, and introspection workflows"
      contains: "use"
    - path: "client/src/components/db-management/ConnectionManager.tsx"
      provides: "Saved MySQL connection creation, editing, delete, and test UI"
      contains: "Connection"
    - path: "client/src/components/db-management/DatabaseSelector.tsx"
      provides: "In-module database switching UI"
      contains: "Database"
    - path: "client/src/components/db-management/SchemaIntrospectionPanel.tsx"
      provides: "Selected-database introspection controls and result summary"
      contains: "Schema"
  key_links:
    - from: "client/src/components/db-management/ConnectionManager.tsx"
      to: "client/src/hooks/use-db-management.ts"
      via: "connection CRUD and test actions"
      pattern: "useDb"
    - from: "client/src/components/db-management/SchemaIntrospectionPanel.tsx"
      to: "server/routes/db-management-routes.ts"
      via: "database-selection and introspection actions"
      pattern: "db"
---

<objective>
Expose the MySQL connection and introspection workflow inside the DB 管理 module UI.

Purpose: Phase 3 is only user-complete when the installed extension gives people one place to save a MySQL server, test it, switch databases, and run introspection without leaving the module they were promised.
Output: Module-local connection management UI, database selector, introspection panel, and a typed client hook layer wired into the DB 管理 workspace.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-db-connectivity-and-introspection/03-CONTEXT.md
@.planning/phases/03-db-connectivity-and-introspection/03-RESEARCH.md
@.planning/phases/03-db-connectivity-and-introspection/03-VALIDATION.md
@client/src/pages/Dashboard.tsx
@client/src/hooks/use-extensions.ts
@client/src/components/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a typed DB-management client data layer</name>
  <files>client/src/hooks/use-db-management.ts, client/src/pages/Dashboard.tsx</files>
  <action>Create focused React Query hooks for saved connections, connection create or update, delete, test, database list, database selection, and schema introspection. Keep this layer aligned with the Phase 3 API contracts and separate from extension lifecycle hooks.</action>
  <verify>Inspect hook coverage and run `npm run check`.</verify>
  <done>The module has a dedicated data layer for MySQL connectivity and introspection workflows.</done>
</task>

<task type="auto">
  <name>Task 2: Build saved-connection and database-selection UI</name>
  <files>client/src/components/db-management/ConnectionManager.tsx, client/src/components/db-management/DatabaseSelector.tsx</files>
  <action>Create the UI for saved MySQL connection CRUD, connection test, remembered-password behavior, and in-module database switching. Make the selected database explicit in the UI and avoid displaying secret values after save.</action>
  <verify>Run `npm run check` plus the Wave 0 connection-management UI tests, then manually inspect the saved-connection and database-switch flow.</verify>
  <done>Users can manage and reuse MySQL server connections from inside DB 管理 without confusion about the current database target.</done>
</task>

<task type="auto">
  <name>Task 3: Surface introspection controls and latest schema state</name>
  <files>client/src/components/db-management/SchemaIntrospectionPanel.tsx, client/src/pages/Dashboard.tsx</files>
  <action>Add module-local introspection controls and result summaries so users can trigger a fresh schema read for the selected database and understand what was captured. Keep the UX intentionally pre-diff: show current connection, selected database, last introspection status, and snapshot summary without drifting into Phase 4 compare UI.</action>
  <verify>Check the module flow end to end and run `npm run check` plus the relevant UI tests from Wave 0.</verify>
  <done>The DB 管理 module delivers a complete MySQL connection and introspection workflow for Phase 3.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] DB 管理 contains connection CRUD, test, database switch, and introspection controls
- [ ] Remembered-password UX avoids exposing secrets in routine UI
- [ ] The selected database and latest introspection state are obvious before diff features arrive
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The installed extension exposes a self-contained MySQL connection and schema-ingestion workflow inside DB 管理
</success_criteria>

<output>
After completion, create `.planning/phases/03-db-connectivity-and-introspection/03-04-SUMMARY.md`
</output>
