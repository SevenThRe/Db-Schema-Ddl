---
phase: 19-trusted-query-continuity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/db_connector/query.rs
  - client/src/components/extensions/db-workbench/ResultGridPane.tsx
  - test/client/db-workbench-runtime-phase19.test.ts
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - COR-01
  - COR-02
  - MEM-01
must_haves:
  truths:
    - "Unsupported paging never means the runtime silently skips a supported result-returning statement."
    - "The result footer makes it obvious that rows were returned even when load-more is unavailable."
    - "Recent query continuity remains connection-scoped and survives reconnect/restart through the existing session store."
  artifacts:
    - path: "src-tauri/src/db_connector/query.rs"
      provides: "Supported non-pageable statements execute and emit explicit paging semantics"
      contains: "paging_mode"
    - path: "client/src/components/extensions/db-workbench/ResultGridPane.tsx"
      provides: "Operator-visible unsupported paging copy with loaded-row evidence"
      contains: "Load more unavailable for this result."
    - path: "test/client/db-workbench-runtime-phase19.test.ts"
      provides: "Phase-19 regression coverage for continuity and operator-visible trust signals"
      contains: "recent query context"
  key_links:
    - from: "src-tauri/src/db_connector/query.rs"
      to: "client/src/components/extensions/db-workbench/ResultGridPane.tsx"
      via: "pagingMode/hasMore contract determines visible footer state"
      pattern: "unsupported"
    - from: "client/src/components/extensions/db-workbench/workbench-session.ts"
      to: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      via: "appendRecentQuery -> hydrateConnectionSession -> recentQueries UI"
      pattern: "recentQueries"
---

<objective>
Finish Phase 19 by closing the operator-trust gap between the page-first runtime and the workbench continuity surface.

Purpose: The current runtime is honest about unsupported paging only by returning empty batches, which breaks trust for supported statements like `SHOW` or script-mode `EXPLAIN`. At the same time, recent query persistence already exists and should be treated as a continuity contract that this phase locks rather than reinvents.
Output: Supported non-pageable statements return real rows with explicit unsupported-paging metadata, the result footer communicates that limitation clearly, and phase-specific regression coverage proves recent query continuity still survives reconnect.
</objective>

<execution_context>
C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.7-phases/19-trusted-query-continuity/19-CONTEXT.md
@src-tauri/src/db_connector/query.rs
@client/src/components/extensions/db-workbench/ResultGridPane.tsx
@client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
@client/src/components/extensions/db-workbench/workbench-session.ts
@test/client/db-workbench-session-phase16.test.ts
@test/client/db-workbench-flow-phase16.test.tsx
@test/client/db-workbench-runtime-phase15.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute supported non-pageable result shapes instead of returning empty unsupported batches</name>
  <files>src-tauri/src/db_connector/query.rs</files>
  <action>
Rework the runtime statement classification and result shaping so "unsupported paging" only disables load-more. Keep the bounded wrapper for safe pageable reads, but return real rows for supported non-pageable cases such as `SHOW`, script-mode `EXPLAIN`, and multi-statement result batches where paging is disallowed by policy. Use `pagingMode="none"` for non-query statements, and preserve explicit `pagingReason` for unsupported load-more cases.
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml query -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - supported non-pageable result statements no longer route through an empty `unsupported_paging_batch(...)`
    - `DbQueryPagingMode::None` is used for non-query statements
    - Rust tests lock statement classification and unsupported paging batch shaping
  </acceptance_criteria>
  <done>Runtime query execution is trustworthy for supported result shapes even when load-more is unavailable.</done>
</task>

<task type="auto">
  <name>Task 2: Make the result footer communicate unsupported paging without hiding returned rows</name>
  <files>client/src/components/extensions/db-workbench/ResultGridPane.tsx</files>
  <action>
Change the footer rendering so the load-more button appears only for `pagingMode="offset"` plus `hasMore=true`. For `pagingMode="unsupported"`, show loaded-row evidence together with the unsupported-paging copy so operators can see that the statement ran and why paging stopped.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `ResultGridPane.tsx` derives a `canLoadMore` gate from `pagingMode === "offset"`
    - unsupported paging state still renders row counts or loaded-row evidence
    - existing unsupported copy remains present
  </acceptance_criteria>
  <done>Operator-facing result state is explicit: returned rows are visible and load-more unavailability is explained.</done>
</task>

<task type="auto">
  <name>Task 3: Lock recent query continuity and phase-19 trust behavior with regression coverage</name>
  <files>test/client/db-workbench-runtime-phase19.test.ts</files>
  <action>
Add a narrow phase-19 regression file that proves the reconnect-time recent query continuity path stays wired and that the workbench footer/runtime source contains the new trust signals. Reuse existing phase-16 session primitives instead of duplicating persistence logic.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--import=tsx node --test --experimental-strip-types test/client/db-workbench-runtime-phase19.test.ts test/client/db-workbench-session-phase16.test.ts test/client/db-workbench-flow-phase16.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - new phase-19 test covers recent query continuity or reconnect path explicitly
    - targeted node tests pass without live DB fixtures
  </acceptance_criteria>
  <done>Phase 19 has explicit regression evidence for runtime trust and recent query continuity.</done>
</task>

</tasks>

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml query -- --nocapture`
- `npm run check`
- `NODE_OPTIONS=--import=tsx node --test --experimental-strip-types test/client/db-workbench-runtime-phase19.test.ts test/client/db-workbench-session-phase16.test.ts test/client/db-workbench-flow-phase16.test.tsx`
</verification>

<success_criteria>
- COR-01: supported result-returning statements still execute when load-more is unavailable
- COR-02: unsupported paging is communicated as a limitation, not as an empty or ambiguous result
- MEM-01: recent query context still restores for the reopened connection through the existing session path
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.7-phases/19-trusted-query-continuity/19-01-SUMMARY.md`
</output>
