## 31-01 Plan

Objective: make data sync compare/apply use truthful source/target table context and operator-configurable stable-key input inside the canonical workbench route.

Required work:

1. Add sync-side schema snapshots keyed by source/target connection so sync table options are no longer derived only from the active workbench connection.
2. Add table-level sync configuration state with runtime-aligned defaults for key columns, compare columns, and optional row filters.
3. Send configured key/compare/filter values in `previewDataDiff` requests and show the resolved key/compare columns in the compare summary.
4. Keep sync selection/config state safe when source or target connection changes by dropping invalid tables and preserving valid per-table settings.
5. Add targeted regression coverage or type-check verification for the new sync configuration helpers and request shaping.
