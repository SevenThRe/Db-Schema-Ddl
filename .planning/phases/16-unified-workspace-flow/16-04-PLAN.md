---
phase: 16-unified-workspace-flow
plan: 04
type: execute
wave: 3
depends_on:
  - "16-02"
  - "16-03"
files_modified:
  - client/src/components/extensions/db-workbench/SqlEditorPane.tsx
  - client/src/components/extensions/db-workbench/sql-autocomplete.ts
  - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  - test/client/db-workbench-autocomplete-phase16.test.tsx
  - test/client/db-workbench-flow-phase16.test.tsx
autonomous: true
requirements:
  - NAV-03
  - FLOW-02
  - FLOW-03
must_haves:
  truths:
    - "Monaco autocomplete uses active schema snapshot metadata, not hardcoded static SQL keywords only"
    - "Alias-aware column completion resolves table aliases in FROM/JOIN clauses"
    - "Phase-16 flow behavior is locked by regression tests for session restore + autocomplete scopes"
  artifacts:
    - path: "client/src/components/extensions/db-workbench/sql-autocomplete.ts"
      provides: "Metadata + alias resolver used by Monaco completion provider"
      contains: "resolveTableAlias"
    - path: "client/src/components/extensions/db-workbench/SqlEditorPane.tsx"
      provides: "Monaco completion provider registration for schema-aware suggestions"
      contains: "registerCompletionItemProvider"
    - path: "test/client/db-workbench-autocomplete-phase16.test.tsx"
      provides: "Autocomplete behavior tests for schema scope and alias resolution"
      contains: "alias"
    - path: "test/client/db-workbench-flow-phase16.test.tsx"
      provides: "End-to-end-ish flow regression for connection session isolation"
      contains: "recent sql"
  key_links:
    - from: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      to: "client/src/components/extensions/db-workbench/sql-autocomplete.ts"
      via: "schema snapshot -> completion metadata conversion"
      pattern: "buildAutocompleteContext"
---

<objective>
Deliver schema-aware, alias-resolved autocomplete and lock Phase 16 behavior with tests.

Purpose: Daily SQL work requires fast context-aware completion. This plan adds metadata-backed Monaco suggestions and verifies that connection isolation + recent/snippet flows remain stable.
Output: Autocomplete responds to active schema objects/aliases and regression tests protect Flow + Nav guarantees.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/components/extensions/db-workbench/SqlEditorPane.tsx
@client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
@client/src/components/extensions/db-workbench/ConnectionSidebar.tsx

<interfaces>
Autocomplete requirements:
- suggestion sources: schema name, table name, view name, column name
- scope: active connection + active schema only
- alias resolution examples:
  - `SELECT u.` after `FROM users u`
  - `SELECT o.` after `JOIN orders o ON ...`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement schema-aware autocomplete context and alias resolver</name>
  <files>client/src/components/extensions/db-workbench/sql-autocomplete.ts, client/src/components/extensions/db-workbench/WorkbenchLayout.tsx, client/src/components/extensions/db-workbench/SqlEditorPane.tsx</files>
  <read_first>
    - client/src/components/extensions/db-workbench/SqlEditorPane.tsx (Monaco mount and action registration)
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx (schemaSnapshot + activeSchema state)
    - shared/schema.ts (DbSchemaSnapshot shape)
  </read_first>
  <action>
Add metadata-driven completion utilities and wire them into Monaco.

1. Create `client/src/components/extensions/db-workbench/sql-autocomplete.ts` with exports:
   - `buildAutocompleteContext(snapshot, activeSchema)`
   - `resolveTableAlias(sqlText, cursorOffset)`
   - `buildCompletionItems(context, aliasHint)`
2. Alias resolver must support concrete patterns:
   - `FROM <table> <alias>`
   - `JOIN <table> <alias>`
   - `FROM <schema>.<table> <alias>`
3. In `WorkbenchLayout.tsx`, derive autocomplete context from current `schemaSnapshot` and `runtimeSchema`, then pass it into `SqlEditorPane`.
4. In `SqlEditorPane.tsx`, register Monaco completion provider with `registerCompletionItemProvider` and use passed context to provide table/column/schema suggestions.
5. Ensure provider cleanup on component unmount/connection switch to avoid duplicate providers.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/extensions/db-workbench/sql-autocomplete.ts exists
    - client/src/components/extensions/db-workbench/sql-autocomplete.ts contains `resolveTableAlias`
    - client/src/components/extensions/db-workbench/sql-autocomplete.ts contains `FROM <table> <alias>` or equivalent parser rule comments/tests
    - client/src/components/extensions/db-workbench/SqlEditorPane.tsx contains `registerCompletionItemProvider`
    - client/src/components/extensions/db-workbench/SqlEditorPane.tsx contains `autocompleteContext`
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx contains `buildAutocompleteContext`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Autocomplete is backed by live schema metadata and alias-aware resolution rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for autocomplete scope and phase-16 workflow continuity</name>
  <files>test/client/db-workbench-autocomplete-phase16.test.tsx, test/client/db-workbench-flow-phase16.test.tsx</files>
  <read_first>
    - test/client/db-workbench-runtime-phase15.test.tsx (existing db-workbench test patterns)
    - test/client/db-connection-config.test.ts (node --test style)
    - client/src/components/extensions/db-workbench/sql-autocomplete.ts (new helpers)
  </read_first>
  <action>
Add focused phase-16 regression tests.

1. Create `test/client/db-workbench-autocomplete-phase16.test.tsx` to verify:
   - active-schema table suggestions are present
   - non-active schema entries are excluded when scope is set
   - alias `u` resolves columns from `users` in `SELECT u.` context
   - alias `o` resolves columns from `orders` in `JOIN orders o` context
2. Create `test/client/db-workbench-flow-phase16.test.tsx` to verify:
   - switching connection restores isolated tabs and draft sql
   - recent sql list is per-connection
   - snippet list is per-connection
3. Use `node --import tsx --test --experimental-strip-types` style for `.tsx` node-test verification commands (no new runner).
  </action>
  <verify>
    <automated>node --import tsx --test --experimental-strip-types test/client/db-workbench-autocomplete-phase16.test.tsx test/client/db-workbench-flow-phase16.test.tsx</automated>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - test/client/db-workbench-autocomplete-phase16.test.tsx exists
    - test/client/db-workbench-autocomplete-phase16.test.tsx contains `SELECT u.`
    - test/client/db-workbench-autocomplete-phase16.test.tsx contains `JOIN orders o`
    - test/client/db-workbench-flow-phase16.test.tsx exists
    - test/client/db-workbench-flow-phase16.test.tsx contains `recent sql`
    - test/client/db-workbench-flow-phase16.test.tsx contains `snippet`
    - `node --import tsx --test --experimental-strip-types test/client/db-workbench-autocomplete-phase16.test.tsx test/client/db-workbench-flow-phase16.test.tsx` exits 0
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Phase-16 flow and autocomplete semantics are protected against regressions.</done>
</task>

</tasks>

<verification>
- `node --import tsx --test --experimental-strip-types test/client/db-workbench-autocomplete-phase16.test.tsx test/client/db-workbench-flow-phase16.test.tsx`
- `npm run check`
- Manual smoke: type alias-based SQL and confirm completion candidates match active schema metadata
Note: .tsx node tests require node --import tsx --test --experimental-strip-types.
</verification>

<success_criteria>
- NAV-03 is achieved with active-schema, alias-aware completion behavior
- FLOW-02/FLOW-03 regressions are caught by dedicated tests before phase closeout
</success_criteria>

<output>
After completion, create `.planning/phases/16-unified-workspace-flow/16-04-SUMMARY.md`
</output>
