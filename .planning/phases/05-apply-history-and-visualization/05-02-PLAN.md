---
phase: 05-apply-history-and-visualization
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - server/lib/extensions/db-management/snapshot-service.ts
  - server/lib/extensions/db-management/db-diff-service.ts
  - server/lib/extensions/db-management/history-service.ts
  - server/lib/extensions/db-management/apply-service.ts
  - server/lib/extensions/db-management/graph-service.ts
  - server/routes/db-management-routes.ts
  - test/server/db-history-phase5.test.ts
  - test/server/db-apply-phase5.test.ts
autonomous: true
requirements:
  - DIFF-02
  - DEPL-03
  - DEPL-04
  - VIZ-02
user_setup: []
must_haves:
  truths:
    - "Live refreshes create scan history without duplicating unchanged snapshots, and users can compare live DB state with any stored snapshot or between stored snapshots."
    - "Safe apply executes only the approved low-risk subset, supports partial table selection, and records an auditable job with per-table and per-statement results."
    - "Users can open a full-database graph with changed-table highlight metadata derived from the same canonical schema used by history and apply flows."
  artifacts:
    - path: "server/lib/extensions/db-management/history-service.ts"
      provides: "History list/detail and snapshot/live compare orchestration over canonical schemas"
      contains: "history"
    - path: "server/lib/extensions/db-management/apply-service.ts"
      provides: "Preview-hash-gated safe apply execution and deploy-job persistence"
      contains: "apply"
    - path: "server/lib/extensions/db-management/graph-service.ts"
      provides: "Canonical schema to ER-style graph DTO transformation"
      contains: "graph"
    - path: "server/routes/db-management-routes.ts"
      provides: "Typed Phase 5 history/apply/graph endpoints"
      contains: "db-management"
  key_links:
    - from: "server/lib/extensions/db-management/history-service.ts"
      to: "server/lib/extensions/db-management/db-diff-service.ts"
      via: "shared canonical compare logic for live/snapshot and snapshot/snapshot"
      pattern: "preview|compare"
    - from: "server/lib/extensions/db-management/apply-service.ts"
      to: "server/lib/extensions/db-management/snapshot-service.ts"
      via: "post-apply rescan and immutable snapshot/event recording"
      pattern: "snapshot"
    - from: "server/lib/extensions/db-management/graph-service.ts"
      to: "shared/schema.ts"
      via: "graph DTO derived from canonical schema and changed entity keys"
      pattern: "graph"
---

<objective>
Implement the backend operational core for Phase 5: immutable scan history, safe apply execution, and graph DTO generation.

Purpose: Phase 5’s value depends on one backend pipeline, not three separate subsystems. The same canonical schema and diff artifacts must drive history compare, safe apply, and graph highlighting.
Output: Changed-only scan history, safe apply job executor, graph service, and typed route wiring for DB 管理.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-apply-history-and-visualization/05-CONTEXT.md
@.planning/phases/05-apply-history-and-visualization/05-RESEARCH.md
@.planning/phases/05-apply-history-and-visualization/05-VALIDATION.md
@shared/schema.ts
@shared/routes.ts
@server/storage.ts
@server/lib/extensions/db-management/snapshot-service.ts
@server/lib/extensions/db-management/db-diff-service.ts
@server/routes/db-management-routes.ts
@.planning/phases/04-file-vs-db-diff-and-deploy-preview/04-03-SUMMARY.md

<interfaces>
From shared/schema.ts:
\`\`\`typescript
export const dbDiffPreviewResponseSchema = z.object({
  context: dbDiffContextSchema,
  tableChanges: z.array(dbDiffTableChangeSchema).default([]),
  blockers: z.array(dbDiffBlockerSchema).default([]),
  canPreview: z.boolean().default(false),
});

export const dbSqlPreviewArtifactSchema = z.object({
  artifactName: z.string().min(1),
  tableName: z.string().optional(),
  sql: z.string().min(1),
  statements: z.array(dbSqlPreviewStatementSchema).default([]),
});
\`\`\`

From server/lib/extensions/db-management/db-diff-service.ts:
\`\`\`typescript
export async function previewDbDiff(connectionId: number, compare: DbDiffPreviewRequest): Promise<DbDiffPreviewResponse>;
export async function confirmDbDiffRenames(connectionId: number, request: DbDiffConfirmRenamesRequest): Promise<DbDiffPreviewResponse>;
export async function previewDbSql(connectionId: number, request: DbSqlPreviewRequest): Promise<DbSqlPreviewResponse>;
export async function previewDbDryRun(connectionId: number, request: DbDryRunRequest): Promise<DbDryRunResponse>;
\`\`\`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend snapshot persistence into changed-only scan history and reusable compare sources</name>
  <files>server/lib/extensions/db-management/snapshot-service.ts, server/lib/extensions/db-management/history-service.ts, server/lib/extensions/db-management/db-diff-service.ts, test/server/db-history-phase5.test.ts</files>
  <behavior>
    - Test 1: a live refresh with an unchanged schema records a scan event but does not create a duplicate snapshot version
    - Test 2: history compare defaults to previous changed snapshot, but users can compare live vs any snapshot and snapshot vs snapshot
    - Test 3: compare logic reuses the same canonical diff rules rather than maintaining a second history-only comparer
  </behavior>
  <action>Create a dedicated history service that resolves \`file\`, \`live\`, and \`snapshot\` compare sources over canonical \`DbSchemaCatalog\` data. Extend snapshot persistence so every scan writes an event row, only changed schemas create new snapshot versions, and history views can answer both “what changed since the previous meaningful scan?” and “how does snapshot A differ from snapshot B?” Do not hide history inside snapshot rows, and do not model baseline as deploy-only state.</action>
  <verify>
    <automated>node --test --import tsx test/server/db-history-phase5.test.ts</automated>
  </verify>
  <done>Phase 5 has durable changed-only scan history plus reusable snapshot/live compare capabilities that honor the user’s baseline workflow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement preview-hash-gated safe apply jobs with auditable results</name>
  <files>server/lib/extensions/db-management/apply-service.ts, server/lib/extensions/db-management/db-diff-service.ts, server/lib/extensions/db-management/snapshot-service.ts, test/server/db-apply-phase5.test.ts</files>
  <behavior>
    - Test 1: apply rejects stale \`compareHash\` or \`planHash\` values when the live snapshot changed after preview
    - Test 2: blocked tables stay non-executable even if the request includes them in \`selectedEntityKeys\`
    - Test 3: a successful or failed job persists per-table and per-statement results and triggers a post-job rescan
  </behavior>
  <action>Build \`apply-service.ts\` around the exact preview statement list, not regenerated SQL. Validate the approved plan hash and live snapshot hash, filter execution to the user-selected safe tables, and run statements sequentially on one leased \`mysql2\` connection. Start with the narrow safe set from research: \`CREATE TABLE\`, additive \`ADD COLUMN\` when server-valid without backfill, and clearly widening-only modifications already proven safe by blocker rules. Keep rename, PK, FK, drop, shrink, and nullability-tightening work preview-only. Persist summary-first job records plus per-table and per-SQL details, stop on first failure, and always record the resulting live state via post-apply rescan metadata.</action>
  <verify>
    <automated>node --test --import tsx test/server/db-apply-phase5.test.ts</automated>
  </verify>
  <done>Users can safely execute only the conservative approved subset and later inspect a traceable deploy job instead of a toast-level result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire typed history/apply/graph routes and finish backend DTO coverage</name>
  <files>server/lib/extensions/db-management/graph-service.ts, server/routes/db-management-routes.ts, test/server/db-history-phase5.test.ts, test/server/db-apply-phase5.test.ts</files>
  <behavior>
    - Test 1: route handlers expose history list/detail, compare, apply submit/detail, and graph DTO endpoints through the DB management route group
    - Test 2: graph DTOs describe the full selected database plus changed-table highlight metadata
    - Test 3: route responses keep summary-first apply results and job drilldown data separate but linked
  </behavior>
  <action>Expose the new history, apply, and graph services through \`server/routes/db-management-routes.ts\`. Add a graph DTO service that derives nodes and FK edges from canonical schema data and annotates changed tables/relationships for the client without inventing separate persistence. Keep route semantics explicit: summary-first apply responses, separate job-detail fetch, and graph data scoped to the selected connection plus database. Avoid bundling any direct \`DB <=> DB\` live comparison endpoints because that scope is deferred.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The server presents one typed Phase 5 API surface for history compare, safe apply execution, deploy history, and graph visualization data.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] \`node --test --import tsx test/server/db-history-phase5.test.ts\`
- [ ] \`node --test --import tsx test/server/db-apply-phase5.test.ts\`
- [ ] \`npm run check\`
- [ ] No route or service treats risky changes as executable merely because the user clicked confirm
- [ ] Every scan/apply persists event history separately from immutable snapshots
- [ ] Graph DTOs represent the full selected database, not changed tables only
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The backend can compare history, apply safe changes, persist job records, and feed graph mode from one canonical schema model
</success_criteria>

<output>
After completion, create \`.planning/phases/05-apply-history-and-visualization/05-02-SUMMARY.md\`
</output>
