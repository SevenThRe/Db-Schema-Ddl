---
phase: 05-apply-history-and-visualization
plan: 04
type: execute
wave: 4
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
files_modified:
  - client/src/components/db-management/DbSchemaGraph.tsx
  - client/src/components/db-management/DbManagementWorkspace.tsx
  - test/client/db-management-phase5-ui.test.tsx
autonomous: true
requirements:
  - VIZ-02
user_setup: []
must_haves:
  truths:
    - "Users can open an ER-style full-database graph and still see changed tables highlighted inside that complete view."
    - "Large schemas remain usable through filter modes such as full DB and changed-plus-neighbors rather than changed-only as the sole visualization."
    - "The app remembers the user’s last active DB 管理 view after the initial history-first default."
  artifacts:
    - path: "client/src/components/db-management/DbSchemaGraph.tsx"
      provides: "Interactive ER-style graph mode powered by the Phase 5 graph DTO"
      contains: "ReactFlow"
    - path: "client/src/components/db-management/DbManagementWorkspace.tsx"
      provides: "Mode toggle and remembered-view persistence for diff/history versus graph"
      contains: "DB 管理"
    - path: "test/client/db-management-phase5-ui.test.tsx"
      provides: "Structural coverage for graph mode labels, filters, and remembered view state"
      contains: "graph"
  key_links:
    - from: "client/src/components/db-management/DbSchemaGraph.tsx"
      to: "client/src/hooks/use-db-management.ts"
      via: "typed graph DTO loading and changed-table highlight state"
      pattern: "graph"
    - from: "client/src/components/db-management/DbManagementWorkspace.tsx"
      to: "client/src/components/db-management/DbSchemaGraph.tsx"
      via: "mode toggle that preserves connection/database/file context"
      pattern: "DbSchemaGraph"
---

<objective>
Add the Phase 5 ER-style schema graph and remember the user’s preferred DB 管理 view.

Purpose: The graph mode should complement the operational list/history workflow, not replace it. This plan adds the richer visualization requested by the user while preserving the existing default and making the graph practical on real schemas.
Output: Interactive graph mode, filterable full-db visualization with changed highlights, remembered workspace mode, and UI coverage for graph behavior.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-apply-history-and-visualization/05-CONTEXT.md
@.planning/phases/05-apply-history-and-visualization/05-RESEARCH.md
@.planning/phases/05-apply-history-and-visualization/05-VALIDATION.md
@client/src/components/db-management/DbManagementWorkspace.tsx
@test/client/db-management-phase5-ui.test.tsx

<interfaces>
Use the Phase 5 graph DTO from \`shared/schema.ts\` and the history/apply shell already integrated in \`DbManagementWorkspace.tsx\`.

Render against the current package choice locked by research:
\`\`\`typescript
import { ReactFlow, Background, Controls, MiniMap } from "@xyflow/react";
\`\`\`

Keep layout externalized to \`elkjs\`; do not implement custom graph layout math inside the component.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the ER-style graph mode for the full selected database</name>
  <files>client/src/components/db-management/DbSchemaGraph.tsx, test/client/db-management-phase5-ui.test.tsx</files>
  <behavior>
    - Test 1: graph mode renders the whole selected database rather than only changed tables
    - Test 2: changed tables are visually highlighted inside that full graph
    - Test 3: filter controls support a focused changed-plus-neighbors slice without making it the only visualization
  </behavior>
  <action>Create \`DbSchemaGraph.tsx\` using \`@xyflow/react\` plus \`elkjs\`. Render the full selected database graph from the backend DTO, highlight changed tables and relationships, and support at least two filter modes: full database and changed tables plus neighboring relations. Optimize for schema/filter changes only; do not rerun layout on every hover, click, or pan event. Avoid a custom SVG canvas and do not degrade the graph into a changed-only view.</action>
  <verify>
    <automated>node --test --import tsx test/client/db-management-phase5-ui.test.tsx</automated>
  </verify>
  <done>The product has an ER-style graph that honors the user’s preference for a full-database view with change highlighting and usable filters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate graph mode into DB 管理 with remembered last-view behavior</name>
  <files>client/src/components/db-management/DbManagementWorkspace.tsx, test/client/db-management-phase5-ui.test.tsx</files>
  <behavior>
    - Test 1: first entry still lands on diff/history
    - Test 2: after the user switches to graph mode, the workspace remembers that preference on the next visit
    - Test 3: switching modes preserves the active connection/database/file context instead of resetting the workflow
  </behavior>
  <action>Add a clear mode toggle between the existing diff/history workspace and the new graph view. The initial default must remain the list/history experience, but after that the workspace should remember the user’s last active view using the project’s existing local-state/persistence style. Preserve all current connection/database/file context when toggling modes and keep graph mode clearly complementary to the operational panels.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Users can move between operational diff/history work and graph visualization without losing context, and the app remembers their preferred view after first use.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] \`node --test --import tsx test/client/db-management-phase5-ui.test.tsx\`
- [ ] \`npm run check\`
- [ ] First entry still defaults to diff/history
- [ ] Graph mode shows the full database with changed-table highlighting
- [ ] Filter mode includes changed-plus-neighbors without replacing full-db mode
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 includes the requested ER-style graph while preserving the operational diff/history default
</success_criteria>

<output>
After completion, create \`.planning/phases/05-apply-history-and-visualization/05-04-SUMMARY.md\`
</output>
