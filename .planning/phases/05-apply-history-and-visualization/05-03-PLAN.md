---
phase: 05-apply-history-and-visualization
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - client/src/components/db-management/DbManagementWorkspace.tsx
  - client/src/components/db-management/DbDiffWorkspace.tsx
  - client/src/components/db-management/DbHistoryPanel.tsx
  - client/src/components/db-management/DbApplyPanel.tsx
  - client/src/hooks/use-db-management.ts
  - test/client/db-management-phase5-ui.test.tsx
autonomous: true
requirements:
  - DIFF-02
  - DEPL-03
  - DEPL-04
user_setup: []
must_haves:
  truths:
    - "DB 管理 stays list/history-first by default while exposing snapshot history and compare-target switching inside the same operational workspace."
    - "Users can choose only safe tables for apply, see blocked tables disabled with reasons, and get a summary-first job record with drilldown."
    - "History and apply flows stay traceable to the selected file revision, live snapshot, and stored job records."
  artifacts:
    - path: "client/src/components/db-management/DbHistoryPanel.tsx"
      provides: "Snapshot/deploy history list with previous-changed default comparison and alternate target selection"
      contains: "History"
    - path: "client/src/components/db-management/DbApplyPanel.tsx"
      provides: "Selectable safe-apply UI with blocked-table explanations and summary/drilldown results"
      contains: "Apply"
    - path: "client/src/components/db-management/DbManagementWorkspace.tsx"
      provides: "Default DB management shell that keeps diff/history as the primary operating mode"
      contains: "DB 管理"
  key_links:
    - from: "client/src/components/db-management/DbHistoryPanel.tsx"
      to: "client/src/hooks/use-db-management.ts"
      via: "typed history and compare-source queries"
      pattern: "useDb"
    - from: "client/src/components/db-management/DbApplyPanel.tsx"
      to: "client/src/hooks/use-db-management.ts"
      via: "safe apply mutation and deploy-job detail retrieval"
      pattern: "useDb"
    - from: "client/src/components/db-management/DbManagementWorkspace.tsx"
      to: "client/src/components/db-management/DbDiffWorkspace.tsx"
      via: "default diff/history workspace integration"
      pattern: "DbDiffWorkspace"
---

<objective>
Expose the default Phase 5 operational experience inside \`DB 管理\`: history-first compare plus conservative safe apply.

Purpose: The backend is not enough. Users need one trustworthy workspace that keeps the existing diff/list workflow intact, lets them compare DB history against selected snapshots or live state, and executes only the safe subset with job-style traceability.
Output: DB history panel, safe apply panel, updated DB 管理 shell, and UI tests for the Phase 5 list/apply experience.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-apply-history-and-visualization/05-CONTEXT.md
@.planning/phases/05-apply-history-and-visualization/05-RESEARCH.md
@.planning/phases/05-apply-history-and-visualization/05-VALIDATION.md
@client/src/components/db-management/DbManagementWorkspace.tsx
@client/src/components/db-management/DbDiffWorkspace.tsx
@client/src/hooks/use-db-management.ts
@.planning/phases/04-file-vs-db-diff-and-deploy-preview/04-04-SUMMARY.md

<interfaces>
From client/src/components/db-management/DbManagementWorkspace.tsx:
\`\`\`typescript
interface DbManagementWorkspaceProps {
  onBack: () => void;
  selectedFileId: number | null;
  selectedSheet: string | null;
  selectedFileName?: string | null;
}
\`\`\`

From client/src/components/db-management/DbDiffWorkspace.tsx:
\`\`\`typescript
interface DbDiffWorkspaceProps {
  selectedConnection: DbConnectionSummary | null;
  selectedFileId: number | null;
  selectedFileName?: string | null;
  selectedSheet: string | null;
}
\`\`\`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a dedicated history panel and compare-target workflow to the default DB diff view</name>
  <files>client/src/components/db-management/DbHistoryPanel.tsx, client/src/components/db-management/DbDiffWorkspace.tsx, client/src/components/db-management/DbManagementWorkspace.tsx, test/client/db-management-phase5-ui.test.tsx</files>
  <behavior>
    - Test 1: the default DB 管理 experience still lands on diff/history rather than graph mode
    - Test 2: history defaults to “compare with previous changed snapshot” but supports live-vs-snapshot and snapshot-vs-snapshot switching
    - Test 3: the selected file revision remains visible so users can judge whether the document or DB is newer
  </behavior>
  <action>Create a dedicated history panel inside the current \`DbDiffWorkspace\` flow instead of hiding snapshots behind background storage. The panel should list scan and deploy history for the selected connection plus database, show what changed relative to the previous changed snapshot by default, and let users switch comparison targets to live DB or any stored snapshot. Keep the operational landing mode list-oriented, preserve the Phase 4 file/sheet context, and avoid replacing the current diff review workflow with the graph view.</action>
  <verify>
    <automated>node --test --import tsx test/client/db-management-phase5-ui.test.tsx</automated>
  </verify>
  <done>Users can inspect meaningful DB history and launch the required compare modes without leaving the default Phase 4 workspace shell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add conservative safe-apply selection plus summary-first job drilldown</name>
  <files>client/src/components/db-management/DbApplyPanel.tsx, client/src/components/db-management/DbDiffWorkspace.tsx, client/src/hooks/use-db-management.ts, test/client/db-management-phase5-ui.test.tsx</files>
  <behavior>
    - Test 1: only safe tables are selectable for apply and blocked tables remain visibly disabled with explicit reasons
    - Test 2: users can apply only a subset of safe tables from the current diff result
    - Test 3: after apply, the first screen is a job summary with drilldown to per-table and per-SQL details
  </behavior>
  <action>Implement the safe apply panel as an extension of the current diff review flow. Reuse the server’s blocker and statement metadata so the client never invents its own safety logic. Allow partial table selection, disable blocked entries with immediately readable explanations, and show summary-first execution results with navigation into per-table and per-SQL detail. Keep risky changes visible in diff/history and SQL review, but never offer them as executable in Phase 5.</action>
  <verify>
    <automated>node --test --import tsx test/client/db-management-phase5-ui.test.tsx</automated>
  </verify>
  <done>The DB 管理 workspace now gives users a conservative path from approved diff to apply job record without weakening the existing safety boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Wire cache invalidation and state transitions across history and apply flows</name>
  <files>client/src/components/db-management/DbManagementWorkspace.tsx, client/src/components/db-management/DbDiffWorkspace.tsx, client/src/hooks/use-db-management.ts, test/client/db-management-phase5-ui.test.tsx</files>
  <action>Finish the UI integration work so history refreshes, compare-source switches, and apply completion all invalidate and repopulate the right queries. Preserve the currently selected connection/database/file context across these transitions, and ensure apply completion naturally refreshes the history panel and detail pane instead of forcing a full manual reset.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The default Phase 5 workspace behaves like one coherent operational surface rather than several disconnected panels.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] \`node --test --import tsx test/client/db-management-phase5-ui.test.tsx\`
- [ ] \`npm run check\`
- [ ] Default landing mode remains diff/history, not graph
- [ ] Blocked tables are disabled with explicit reasons
- [ ] Apply completion surfaces summary first, then drilldown
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can compare DB history and execute the safe subset from the default DB 管理 workspace with durable job visibility
</success_criteria>

<output>
After completion, create \`.planning/phases/05-apply-history-and-visualization/05-03-SUMMARY.md\`
</output>
