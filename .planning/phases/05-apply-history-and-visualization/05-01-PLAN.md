---
phase: 05-apply-history-and-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - shared/schema.ts
  - shared/routes.ts
  - server/storage.ts
  - client/src/hooks/use-db-management.ts
  - test/server/db-phase5-fixtures.ts
  - test/server/db-history-phase5.test.ts
  - test/server/db-apply-phase5.test.ts
  - test/client/db-management-phase5-ui.test.tsx
autonomous: true
requirements:
  - DIFF-02
  - DEPL-03
  - DEPL-04
  - VIZ-02
user_setup: []
must_haves:
  truths:
    - "Phase 5 users will see history compare, safe apply, deploy records, and graph data described consistently across DB 管理 surfaces."
    - "Phase 5 can preserve DB scan history and deployment history as separate visible concepts instead of collapsing everything into 'last successful apply'."
    - "Phase 5 verifiers can check history, apply, and graph behavior against stable expected outcomes instead of rediscovering the rules during execution."
  artifacts:
    - path: "shared/schema.ts"
      provides: "Typed schemas for history compare sources, scan/deploy records, apply requests/results, and graph DTOs"
      contains: "db"
    - path: "shared/routes.ts"
      provides: "Typed DB management endpoints for history, apply, job detail, and graph mode"
      contains: "dbManagement"
    - path: "server/storage.ts"
      provides: "Persistence methods for snapshot events, deploy jobs, and statement-level results"
      contains: "Db"
    - path: "test/server/db-phase5-fixtures.ts"
      provides: "Canonical schema, snapshot, and deploy-job fixtures reused by Phase 5 tests"
      contains: "schema"
  key_links:
    - from: "shared/routes.ts"
      to: "shared/schema.ts"
      via: "history/apply/graph request and response contracts"
      pattern: "history|apply|graph"
    - from: "server/storage.ts"
      to: "shared/schema.ts"
      via: "typed persistence of snapshot events and deploy jobs"
      pattern: "Snapshot|Deploy"
    - from: "client/src/hooks/use-db-management.ts"
      to: "shared/routes.ts"
      via: "React Query hooks for history, apply, and graph flows"
      pattern: "dbManagement"
---

<objective>
Define the Phase 5 contracts, persistence primitives, dependency setup, and Wave 0 tests.

Purpose: Apply, history, and graph work all depend on one shared language for compare sources, immutable history records, safe-apply job payloads, and graph DTOs. This plan creates that language first so later plans can implement against stable interfaces instead of exploring the codebase.
Output: Phase 5 test scaffolds, shared schemas/routes, storage methods, graph dependencies, and client hook seams ready for backend and UI implementation.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-apply-history-and-visualization/05-CONTEXT.md
@.planning/phases/05-apply-history-and-visualization/05-RESEARCH.md
@.planning/phases/05-apply-history-and-visualization/05-VALIDATION.md
@shared/schema.ts
@shared/routes.ts
@server/storage.ts
@client/src/hooks/use-db-management.ts
@.planning/phases/04-file-vs-db-diff-and-deploy-preview/04-03-SUMMARY.md

<interfaces>
From shared/schema.ts:
\`\`\`typescript
export const dbSchemaSnapshotSchema = z.object({
  id: z.number().int().positive(),
  connectionId: z.number().int().positive(),
  databaseName: z.string().min(1),
  snapshotHash: z.string().min(8),
  tableCount: z.number().int().min(0),
  schemaJson: z.string(),
});

export const dbDiffPreviewResponseSchema = z.object({
  context: dbDiffContextSchema,
  summary: dbDiffSummarySchema,
  tableChanges: z.array(dbDiffTableChangeSchema).default([]),
  blockers: z.array(dbDiffBlockerSchema).default([]),
  canPreview: z.boolean().default(false),
});

export const dbSqlPreviewStatementSchema = z.object({
  id: z.string().min(1),
  kind: dbSqlStatementKindSchema,
  tableName: z.string().optional(),
  sql: z.string().min(1),
  relatedEntityKeys: z.array(z.string()).default([]),
  blocked: z.boolean().default(false),
  blockerCodes: z.array(dbDiffBlockerCodeSchema).default([]),
});
\`\`\`

From server/storage.ts:
\`\`\`typescript
listDbSchemaSnapshots(connectionId: number, databaseName?: string): Promise<DbSchemaSnapshot[]>;
getLatestDbSchemaSnapshot(connectionId: number, databaseName: string): Promise<DbSchemaSnapshot | undefined>;
getDbSchemaSnapshotByHash(connectionId: number, databaseName: string, snapshotHash: string): Promise<DbSchemaSnapshot | undefined>;
createDbSchemaSnapshot(snapshot: Omit<DbSchemaSnapshot, "id" | "capturedAt" | "updatedAt">): Promise<DbSchemaSnapshot>;
\`\`\`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 Phase 5 fixtures and executable test scaffolds</name>
  <files>test/server/db-phase5-fixtures.ts, test/server/db-history-phase5.test.ts, test/server/db-apply-phase5.test.ts, test/client/db-management-phase5-ui.test.tsx</files>
  <behavior>
    - Test 1: changed-only scan history can distinguish "new scan event" from "new snapshot version"
    - Test 2: safe apply requests reject stale compare hashes or blocked entity selections
    - Test 3: the DB management UI exposes history/apply/graph labels and remembers the active mode
  </behavior>
  <action>Create the shared Phase 5 fixture module and the three missing test files listed in \`05-VALIDATION.md\`. Seed them with canonical schema catalogs, snapshot event/job fixtures, and concrete assertions for DIFF-02, DEPL-03, DEPL-04, and VIZ-02. Keep the tests structural and deterministic; do not use brittle full-markup snapshots or real MySQL side effects in the initial scaffold.</action>
  <verify>
    <automated>node --test --import tsx test/server/db-history-phase5.test.ts</automated>
  </verify>
  <done>The missing Phase 5 test files exist, compile, and encode the required behaviors the later plans must satisfy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add shared schemas, route contracts, and persistence records for history/apply/graph flows</name>
  <files>shared/schema.ts, shared/routes.ts, server/storage.ts</files>
  <behavior>
    - Test 1: compare sources can represent \`file\`, \`live\`, and \`snapshot\` inputs without overloading the existing Phase 4 request shape
    - Test 2: persistence records can capture scan events, deploy jobs, and statement-level results while keeping snapshots immutable
    - Test 3: new route contracts cover history list/detail, history compare, apply submit/detail, and graph DTO retrieval
  </behavior>
  <action>Extend the shared contract and storage layers with Phase 5 types only once. Add compare-source unions, history response shapes, deploy-job and statement-result schemas, graph node/edge DTOs, and the storage methods/tables needed to persist scan events and deploy jobs per connection plus schema. Honor the user’s locked decision that baseline is not only “last successful deployment”; history APIs must support live-vs-snapshot and snapshot-vs-snapshot. Do not add any \`DB <=> DB\` live-to-live comparison contracts because that idea is explicitly deferred.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Downstream services can implement Phase 5 against stable typed contracts and storage primitives without revisiting scope decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Add graph dependencies and client hook seams for Phase 5</name>
  <files>package.json, package-lock.json, client/src/hooks/use-db-management.ts</files>
  <action>Install \`@xyflow/react\` and \`elkjs\`, update the lockfile, and add focused React Query hooks for history queries, snapshot compare, safe apply submission, deploy job detail, and graph DTO loading. Preserve the existing DB management query-key style and keep the hook surface typed off \`shared/routes.ts\` instead of manual fetch shapes.</action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The backend and renderer have dependency-ready, typed client seams for the rest of Phase 5.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] \`node --test --import tsx test/server/db-history-phase5.test.ts test/server/db-apply-phase5.test.ts test/client/db-management-phase5-ui.test.tsx\`
- [ ] \`npm run check\`
- [ ] No contract models baseline as only the last successful apply
- [ ] New storage primitives separate immutable snapshots from scan/deploy event history
- [ ] Graph dependencies are added without introducing custom layout code
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 implementation can start from stable contracts, tests, and storage seams instead of exploratory edits
</success_criteria>

<output>
After completion, create \`.planning/phases/05-apply-history-and-visualization/05-01-SUMMARY.md\`
</output>
