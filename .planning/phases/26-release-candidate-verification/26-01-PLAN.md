---
phase: 26-release-candidate-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - client/src/extensions/host-api.ts
  - client/src/extensions/host-api-runtime.ts
  - client/src/lib/desktop-bridge.ts
  - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  - src-tauri/src/db_connector/mod.rs
  - src-tauri/src/db_connector/query.rs
  - test/client/db-workbench-runtime-phase26.test.ts
autonomous: true
requirements:
  - QUAL-01
must_haves:
  truths:
    - "Ctrl+Enter statement execution is truthful: the runtime receives a cursor offset and executes only the statement under the cursor when no text is selected."
    - "Cancelled query or export work cannot mutate the UI or trigger downloads after the active request has been cleared or replaced."
    - "Release verification can rely on current runtime semantics instead of documenting misleading behavior."
  artifacts:
    - path: "shared/schema.ts"
      provides: "cursor-offset request contract for query execution and dangerous-SQL preview"
      contains: "cursorOffset"
    - path: "src-tauri/src/db_connector/query.rs"
      provides: "backend statement-at-cursor selection and cancel-safe runtime behavior"
      contains: "cursor_offset"
    - path: "client/src/components/extensions/db-workbench/WorkbenchLayout.tsx"
      provides: "stale-response guards for query/export flows"
      contains: "activeQueryRequestIdRef"
  key_links:
    - from: "client/src/components/extensions/db-workbench/SqlEditorPane.tsx"
      to: "src-tauri/src/db_connector/query.rs"
      via: "cursorOffset"
      pattern: "Execute Selection / Statement"
---

<objective>
Fix the runtime-semantics blockers discovered while preparing release verification so Phase 26 evidence reflects truthful current behavior rather than known UI/runtime mismatches.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-release-candidate-verification/26-CONTEXT.md
@client/src/components/extensions/db-workbench/SqlEditorPane.tsx
@client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
@shared/schema.ts
@src-tauri/src/db_connector/query.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carry cursor-offset execution through the full request pipeline</name>
  <read_first>
    - client/src/components/extensions/db-workbench/SqlEditorPane.tsx
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
    - shared/schema.ts
    - src-tauri/src/db_connector/query.rs
  </read_first>
  <files>
    - shared/schema.ts
    - client/src/extensions/host-api.ts
    - client/src/extensions/host-api-runtime.ts
    - client/src/lib/desktop-bridge.ts
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
    - src-tauri/src/db_connector/mod.rs
    - src-tauri/src/db_connector/query.rs
  </files>
  <action>
Add an optional `cursorOffset` contract to query execution and dangerous-SQL preview, pass it from the editor/workbench, and make the Rust query path execute only the statement under the cursor when no selection text is provided.
  </action>
  <acceptance_criteria>
    - `shared/schema.ts` includes `cursorOffset?: number` on the relevant request contracts
    - `WorkbenchLayout.tsx` stops discarding `cursorOffset`
    - `query.rs` resolves a single target statement from the full SQL when `cursor_offset` is provided
    - dangerous-SQL preview and execution use the same target statement semantics
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -j 1</automated>
  </verify>
  <done>Statement-under-cursor execution is truthful end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Prevent cancelled query and export work from mutating state after cancellation</name>
  <read_first>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
    - src-tauri/src/db_connector/query.rs
  </read_first>
  <files>
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
  </files>
  <action>
Guard query and export result handling so late responses are ignored when the active request id has been cancelled or superseded. Suppress stale UI updates and stale file downloads after cancellation.
  </action>
  <acceptance_criteria>
    - `WorkbenchLayout.tsx` tracks active query/export request identity in a way that survives async response races
    - late query responses no longer overwrite newer state after cancel or rerun
    - late export responses no longer trigger file download after cancel
    - cancel still clears visible executing/exporting state immediately
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Cancel semantics are trustworthy enough to verify in Phase 26.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the release-verification runtime fixes with focused guard tests</name>
  <read_first>
    - client/src/components/extensions/db-workbench/SqlEditorPane.tsx
    - client/src/components/extensions/db-workbench/WorkbenchLayout.tsx
    - src-tauri/src/db_connector/query.rs
  </read_first>
  <files>
    - test/client/db-workbench-runtime-phase26.test.ts
  </files>
  <action>
Add a focused Phase 26 regression test file that asserts cursor-offset execution is wired and stale-response guards exist in the workbench runtime path.
  </action>
  <acceptance_criteria>
    - `test/client/db-workbench-runtime-phase26.test.ts` exists
    - it asserts `cursorOffset` is present in the request contract/runtime path
    - it asserts stale query/export responses are gated on active request identity
  </acceptance_criteria>
  <verify>
    <automated>$env:NODE_OPTIONS='--import=tsx'; node --test --experimental-strip-types test/client/db-workbench-runtime-phase26.test.ts</automated>
  </verify>
  <done>Phase 26 runtime-truth fixes are guarded against regression.</done>
</task>

</tasks>

<verification>
- npm run check
- $env:NODE_OPTIONS='--import=tsx'; node --test --experimental-strip-types test/client/db-workbench-runtime-phase26.test.ts
- cargo check --manifest-path src-tauri/Cargo.toml -j 1
</verification>

<success_criteria>
- QUAL-01 evidence can trust statement-execution and cancel semantics
</success_criteria>

<output>
After completion, create `.planning/phases/26-release-candidate-verification/26-01-SUMMARY.md`
</output>
