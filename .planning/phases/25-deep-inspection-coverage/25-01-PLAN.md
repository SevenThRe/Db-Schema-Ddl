---
phase: 25-deep-inspection-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/extensions/db-workbench/ObjectInspectionPane.tsx
  - test/client/db-workbench-inspection-phase25.test.ts
autonomous: true
requirements:
  - INSP-01
  - INSP-02
  - INSP-03
must_haves:
  truths:
    - "The canonical explorer exposes credible coverage for tables, views, indexes, foreign keys, routines, triggers, and PostgreSQL sequences."
    - "Supported objects open definition/DDL inspection directly from the explorer."
    - "Coverage messaging stays honest about supported object families and driver-specific limits."
  artifacts:
    - path: "client/src/components/extensions/db-workbench/ObjectInspectionPane.tsx"
      provides: "Operator-visible inspection coverage copy"
      contains: "functions/procedures"
    - path: "test/client/db-workbench-inspection-phase25.test.ts"
      provides: "Guard coverage for frontend explorer and backend inspection support matrix"
      contains: "pg_get_functiondef"
  key_links:
    - from: "client/src/components/extensions/db-workbench/ConnectionSidebar.tsx"
      to: "src-tauri/src/db_connector/object_inspect.rs"
      via: "supported explorer object families map to backend inspection dispatch"
      pattern: "DbObjectKind::Sequence"
---

<objective>
Formalize and harden the already-implemented deep inspection coverage so Phase 25 is verifiably complete. The goal is not to invent a new inspection subsystem, but to make the support matrix explicit, honest, and guarded against regression.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-deep-inspection-coverage/25-CONTEXT.md
@.specify/specs/002-db-object-ddl-inspection/spec.md
@.specify/specs/004-db-object-deep-inspection/spec.md
@.specify/specs/005-db-index-fk-inspection/spec.md
@client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
@client/src/components/extensions/db-workbench/ObjectInspectionPane.tsx
@src-tauri/src/db_connector/object_inspect.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten operator-visible inspection coverage copy</name>
  <read_first>
    - client/src/components/extensions/db-workbench/ObjectInspectionPane.tsx
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
    - .planning/phases/25-deep-inspection-coverage/25-CONTEXT.md
  </read_first>
  <files>
    - client/src/components/extensions/db-workbench/ObjectInspectionPane.tsx
  </files>
  <action>
Update inspection empty-state and coverage messaging so the supported object family list is explicit and release-grade. Prefer wording that calls out `functions/procedures` directly instead of only saying `routines`, while preserving the current honest unsupported messaging behavior.
  </action>
  <acceptance_criteria>
    - `ObjectInspectionPane.tsx` mentions `functions/procedures` in its supported coverage copy
    - inspection coverage copy still includes tables, views, triggers, PostgreSQL sequences, indexes, and foreign keys
    - no copy change removes the existing honest unsupported-state behavior
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Operators can see the real supported inspection matrix in the UI copy itself.</done>
</task>

<task type="auto">
  <name>Task 2: Add a focused Phase 25 inspection coverage guard test</name>
  <read_first>
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
    - client/src/components/extensions/db-workbench/ObjectInspectionPane.tsx
    - src-tauri/src/db_connector/object_inspect.rs
  </read_first>
  <files>
    - test/client/db-workbench-inspection-phase25.test.ts
  </files>
  <action>
Create a focused Phase 25 test file that asserts the explorer and inspection support matrix through source-level guard checks. Cover frontend explorer sections and inspect actions for views, routines, triggers, sequences, indexes, and foreign keys, and verify backend inspection dispatch includes table/view/index/foreign-key/function/procedure/trigger/sequence plus the PostgreSQL/MySQL definition fetch helpers (`pg_get_functiondef`, `pg_get_triggerdef`, `SHOW CREATE FUNCTION`, `SHOW CREATE PROCEDURE`, `SHOW CREATE TRIGGER`, and PostgreSQL sequence metadata).
  </action>
  <acceptance_criteria>
    - `test/client/db-workbench-inspection-phase25.test.ts` exists
    - the test asserts `ConnectionSidebar.tsx` contains sections for views, routines, triggers, sequences, indexes, and foreign keys
    - the test asserts `ObjectInspectionPane.tsx` contains the supported inspection coverage copy
    - the test asserts `object_inspect.rs` contains backend coverage for `DbObjectKind::Table`, `View`, `Index`, `ForeignKey`, `Function`, `Procedure`, `Trigger`, and `Sequence`
    - the test asserts `object_inspect.rs` contains `pg_get_functiondef`, `pg_get_triggerdef`, `SHOW CREATE FUNCTION`, `SHOW CREATE PROCEDURE`, and `SHOW CREATE TRIGGER`
  </acceptance_criteria>
  <verify>
    <automated>$env:NODE_OPTIONS='--import=tsx'; node --test --experimental-strip-types test/client/db-workbench-inspection-phase25.test.ts</automated>
  </verify>
  <done>The deep inspection support matrix is locked against future regression.</done>
</task>

</tasks>

<verification>
- npm run check
- $env:NODE_OPTIONS='--import=tsx'; node --test --experimental-strip-types test/client/db-workbench-inspection-phase25.test.ts
- cargo check --manifest-path src-tauri/Cargo.toml -j 1
</verification>

<success_criteria>
- INSP-01: explorer coverage includes tables, views, indexes, and foreign keys in a usable canonical explorer
- INSP-02: supported routines, triggers, functions, and procedures remain discoverable when driver introspection exists
- INSP-03: supported objects can open definition/DDL inspection directly from the explorer
</success_criteria>

<output>
After completion, create `.planning/phases/25-deep-inspection-coverage/25-01-SUMMARY.md`
</output>
