---
phase: 04-file-vs-db-diff-and-deploy-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - shared/routes.ts
  - server/storage.ts
  - client/src/hooks/use-db-management.ts
autonomous: true
requirements:
  - DIFF-01
  - DIFF-03
  - DEPL-01
  - DEPL-02
  - DIFF-02
user_setup: []
must_haves:
  truths:
    - "Phase 4 has a typed file-vs-live-DB compare contract that represents compare scope, table and column changes, rename candidates, blockers, and preview readiness."
    - "The compare API surface lives with DB management rather than overloading the legacy file-history diff contract."
    - "The compare model is baseline-ready for Phase 5 without forcing a half-finished baseline-vs-live workflow into Phase 4."
  artifacts:
    - path: "shared/schema.ts"
      provides: "Typed DB compare request/response schemas, blocker types, rename candidate contracts, and dry-run summary payloads"
      contains: "db"
    - path: "shared/routes.ts"
      provides: "Typed DB diff and preview APIs for the DB management module"
      contains: "dbManagement"
    - path: "client/src/hooks/use-db-management.ts"
      provides: "Client hook seams for compare, rename review, SQL preview, and dry-run summary"
      contains: "use"
  key_links:
    - from: "shared/routes.ts"
      to: "shared/schema.ts"
      via: "DB diff request/response contracts"
      pattern: "diff"
    - from: "client/src/hooks/use-db-management.ts"
      to: "shared/routes.ts"
      via: "typed DB compare hook coverage"
      pattern: "dbManagement"
---

<objective>
Define the typed Phase 4 compare and preview contract before backend and UI work begins.

Purpose: The new DB-oriented diff workspace needs a shared language for compare scope, rename review, blockers, SQL preview readiness, and dry-run summaries before the server and renderer can build consistent behavior.
Output: Shared compare/dry-run schemas, DB management route contracts, and client hook seams ready for file-vs-live-DB workflows.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-file-vs-db-diff-and-deploy-preview/04-CONTEXT.md
@.planning/phases/04-file-vs-db-diff-and-deploy-preview/04-VALIDATION.md
@shared/schema.ts
@shared/routes.ts
@client/src/hooks/use-db-management.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed DB-oriented compare and blocker schemas</name>
  <files>shared/schema.ts</files>
  <action>Introduce shared schemas for file-vs-live-DB compare scope, object-tree payloads, rename suggestions, blocker classifications, SQL preview artifacts, and dry-run summaries. Keep the contracts separate from the legacy file-history diff payloads and make sure they can support the sheet-first, table-drilldown UI chosen in context.</action>
  <verify>Inspect the new schemas for table/column changes, rename review, blocker reasons, and preview readiness, then run `npm run check`.</verify>
  <done>Phase 4 has a stable typed contract for DB-oriented compare and preview workflows.</done>
</task>

<task type="auto">
  <name>Task 2: Shape DB management API routes for compare, rename review, and dry-run</name>
  <files>shared/routes.ts</files>
  <action>Extend the DB management route contract with endpoints for compare preview, rename confirmation, SQL preview generation, and dry-run summaries. Keep these APIs inside the DB management group rather than bolting them onto the legacy diff route surface.</action>
  <verify>Confirm the routes cover DIFF-01, DIFF-03, DEPL-01, and DEPL-02 without leaking Phase 5 apply/history concerns.</verify>
  <done>The backend and renderer have a single typed API surface for Phase 4 compare work.</done>
</task>

<task type="auto">
  <name>Task 3: Prepare client hook seams for the new workspace</name>
  <files>client/src/hooks/use-db-management.ts</files>
  <action>Add or reserve focused React Query hooks for compare execution, rename review submission, SQL preview generation, and dry-run summary retrieval. Preserve the existing Phase 3 connection/introspection hooks and make the new hooks feel like one coherent DB management data layer.</action>
  <verify>Run `npm run check` and confirm the hook surface aligns with the new route contracts.</verify>
  <done>The UI plan can build directly on typed DB diff hooks instead of ad hoc fetch calls.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] DB-oriented compare contracts are separate from the legacy SchemaDiffPanel payloads
- [ ] Route contracts cover compare, rename review, SQL preview, and dry-run summary
- [ ] Contracts leave room for future baseline-vs-live support without forcing that workflow into Phase 4
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 4 has a durable typed contract before server-side compare logic is implemented
</success_criteria>

<output>
After completion, create `.planning/phases/04-file-vs-db-diff-and-deploy-preview/04-01-SUMMARY.md`
</output>
