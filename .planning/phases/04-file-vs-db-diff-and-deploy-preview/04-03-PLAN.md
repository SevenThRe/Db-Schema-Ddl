---
phase: 04-file-vs-db-diff-and-deploy-preview
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - server/lib/ddl.ts
  - server/lib/ddl-validation.ts
  - server/lib/extensions/db-management/
  - server/routes/db-management-routes.ts
autonomous: true
requirements:
  - DEPL-01
  - DEPL-02
  - DIFF-03
user_setup: []
must_haves:
  truths:
    - "The extension can generate SQL preview artifacts from confirmed file-vs-live-DB compare results."
    - "Dry-run output includes a risk summary and blocked reasons rather than pretending all changes are executable."
    - "SQL preview remains gated by server-side blocker logic and never bypasses unresolved rename or destructive-risk checks."
  artifacts:
    - path: "server/lib/extensions/db-management/"
      provides: "DB diff to SQL preview and dry-run summary generation"
      contains: "preview"
    - path: "server/lib/ddl.ts"
      provides: "Reusable DDL or ALTER generation primitives leveraged by Phase 4 preview"
      contains: "generate"
    - path: "server/routes/db-management-routes.ts"
      provides: "Preview and dry-run endpoints returning SQL artifacts and summary metadata"
      contains: "preview"
  key_links:
    - from: "server/lib/extensions/db-management/"
      to: "server/lib/ddl.ts"
      via: "DB diff result translated into SQL preview artifacts"
      pattern: "DDL"
    - from: "server/routes/db-management-routes.ts"
      to: "shared/routes.ts"
      via: "typed SQL preview and dry-run summary responses"
      pattern: "preview"
---

<objective>
Generate gated SQL previews and dry-run summaries from confirmed compare results.

Purpose: Once compare and rename review work, Phase 4 still needs to turn those results into safe SQL previews and dry-run summaries that explain what will happen and what is still blocked.
Output: Preview-generation services, dry-run summary logic, and typed DB management endpoints for SQL preview and non-applying deployment review.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-file-vs-db-diff-and-deploy-preview/04-CONTEXT.md
@.planning/phases/04-file-vs-db-diff-and-deploy-preview/04-VALIDATION.md
@shared/schema.ts
@shared/routes.ts
@server/lib/ddl.ts
@server/lib/ddl-validation.ts
@server/routes/db-management-routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate confirmed compare results into SQL preview artifacts</name>
  <files>server/lib/extensions/db-management/, server/lib/ddl.ts</files>
  <action>Implement SQL preview generation from the confirmed file-vs-live-DB diff result, reusing existing DDL/ALTER helpers where helpful but shaping the output around the new DB compare model. Ensure the preview can map back to table and field changes so the UI can highlight relevant fragments later.</action>
  <verify>Run `npm run check` and Wave 0 preview tests, then inspect artifacts for table/field traceability and dialect correctness.</verify>
  <done>The backend can return SQL preview artifacts tied to the selected compare result.</done>
</task>

<task type="auto">
  <name>Task 2: Build dry-run summaries with risk and blocker reporting</name>
  <files>server/lib/extensions/db-management/, server/routes/db-management-routes.ts</files>
  <action>Implement a dry-run summary endpoint that reports intended adds/modifies, blocked items, and risk counts without applying anything. The summary should clearly distinguish “preview generated” from “preview blocked” and reuse the same blocker taxonomy as compare.</action>
  <verify>Run `npm test` with Wave 0 dry-run coverage and confirm the summary reports blocked reasons for every required Phase 4 safety rule.</verify>
  <done>The extension has a safe, non-applying dry-run view that explains impact before execution exists in Phase 5.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce preview gating and response consistency</name>
  <files>server/routes/db-management-routes.ts, server/lib/extensions/db-management/</files>
  <action>Wire the preview and dry-run services through typed DB management routes with consistent response semantics for success, blocked preview, and invalid compare state. Keep the route layer strict so the UI never has to infer whether it is safe to show executable SQL.</action>
  <verify>Run `npm run check` and confirm route responses keep success, blocked, and error states unambiguous.</verify>
  <done>The preview pipeline is safe, typed, and ready for the new DB diff workspace to consume.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] SQL preview is generated only from confirmed compare state
- [ ] Dry-run summaries distinguish actionable preview from blocked preview
- [ ] Preview and dry-run endpoints never bypass required blocker rules
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 4 can produce safe SQL previews and dry-run summaries before execution exists
</success_criteria>

<output>
After completion, create `.planning/phases/04-file-vs-db-diff-and-deploy-preview/04-03-SUMMARY.md`
</output>
