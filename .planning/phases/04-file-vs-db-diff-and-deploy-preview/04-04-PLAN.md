---
phase: 04-file-vs-db-diff-and-deploy-preview
plan: 04
type: execute
wave: 4
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
files_modified:
  - client/src/components/db-management/
  - client/src/hooks/use-db-management.ts
  - client/src/pages/Dashboard.tsx
autonomous: true
requirements:
  - DIFF-01
  - DIFF-03
  - DEPL-01
  - DEPL-02
user_setup: []
must_haves:
  truths:
    - "DB 管理 exposes a dedicated three-column DB-oriented diff workspace instead of reusing the legacy SchemaDiffPanel."
    - "Users can start at sheet level, drill into table-level differences, review rename suggestions in bulk, and inspect field-level details."
    - "The SQL pane highlights fragments related to the selected diff item and clearly explains blocked preview states."
  artifacts:
    - path: "client/src/components/db-management/"
      provides: "DB-oriented diff workspace, object tree, rename review, and SQL preview UI"
      contains: "Db"
    - path: "client/src/hooks/use-db-management.ts"
      provides: "Hook wiring for compare, rename, preview, and dry-run flows"
      contains: "use"
    - path: "client/src/pages/Dashboard.tsx"
      provides: "Workspace integration under DB 管理"
      contains: "DbManagementWorkspace"
  key_links:
    - from: "client/src/components/db-management/"
      to: "client/src/hooks/use-db-management.ts"
      via: "typed compare, rename, and preview data flows"
      pattern: "useDb"
    - from: "client/src/components/db-management/"
      to: "server/routes/db-management-routes.ts"
      via: "DB compare workspace reads typed server responses"
      pattern: "db"
---

<objective>
Expose the DB-oriented compare, rename review, and SQL preview workflow inside the DB 管理 UI.

Purpose: Phase 4 only feels complete when users can move through the new compare flow in one place: choose scope, inspect object-tree differences, confirm rename suggestions, and read SQL with meaningful highlighting and blocker explanations.
Output: A dedicated three-column diff workspace inside DB 管理, wired to the new compare/preview hooks and intentionally separate from the legacy SchemaDiffPanel.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-file-vs-db-diff-and-deploy-preview/04-CONTEXT.md
@.planning/phases/04-file-vs-db-diff-and-deploy-preview/04-VALIDATION.md
@client/src/components/db-management/DbManagementWorkspace.tsx
@client/src/hooks/use-db-management.ts
@client/src/pages/Dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the three-column DB diff workspace shell</name>
  <files>client/src/components/db-management/, client/src/pages/Dashboard.tsx</files>
  <action>Create the dedicated DB-oriented diff workspace inside `DB 管理`, with top compare context, object tree on the left, diff detail in the middle, and SQL/risk pane on the right. Preserve the current connection/introspection capabilities while adding a clear entry into the new compare flow.</action>
  <verify>Run `npm run check` and the Wave 0 workspace UI tests, then inspect the module layout for sheet-level entry and table drilldown clarity.</verify>
  <done>The extension has a new live-DB compare shell that is clearly separate from the old SchemaDiffPanel.</done>
</task>

<task type="auto">
  <name>Task 2: Add bulk rename review and detail-pane drilldown</name>
  <files>client/src/components/db-management/, client/src/hooks/use-db-management.ts</files>
  <action>Implement the bulk rename review step as the default path for unresolved high-confidence rename suggestions, while still allowing users to inspect or override individual candidates from the detail pane. Keep rename confirmation tightly linked to preview gating so users understand why unresolved rename blocks SQL preview.</action>
  <verify>Run `npm test` with Wave 0 rename/workspace coverage and confirm unresolved rename states remain visually obvious.</verify>
  <done>Users can confidently review rename candidates before preview rather than guessing what the system will do.</done>
</task>

<task type="auto">
  <name>Task 3: Wire SQL highlighting and blocked preview messaging</name>
  <files>client/src/components/db-management/, client/src/hooks/use-db-management.ts</files>
  <action>Render SQL preview artifacts with highlighting tied to the selected table or field diff, and show explicit risk or blocker messages when preview cannot proceed. Favor readability and safety explanations over overly flashy presentation, since diagram-style visualization is deferred to a later phase.</action>
  <verify>Run `npm run check` plus the relevant UI tests, then manually inspect that selected diffs map to SQL fragments and blocked states explain the next action clearly.</verify>
  <done>The DB diff workspace gives users a trustworthy path from selected diff item to SQL preview or blocker explanation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] DB 管理 contains a dedicated three-column diff workspace
- [ ] Rename review defaults to bulk confirmation but supports per-item drilldown
- [ ] SQL pane highlights selected diff context and explains blocked preview states clearly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The new DB-oriented compare and preview workflow is user-complete inside DB 管理
</success_criteria>

<output>
After completion, create `.planning/phases/04-file-vs-db-diff-and-deploy-preview/04-04-SUMMARY.md`
</output>
