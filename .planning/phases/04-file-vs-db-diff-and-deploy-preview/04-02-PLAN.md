---
phase: 04-file-vs-db-diff-and-deploy-preview
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - server/lib/schema-diff.ts
  - server/lib/extensions/db-management/
  - server/routes/db-management-routes.ts
autonomous: true
requirements:
  - DIFF-01
  - DIFF-03
  - DIFF-02
user_setup: []
must_haves:
  truths:
    - "The extension can compare a selected file or sheet against the selected live MySQL database and emit table/column changes for a DB-oriented object tree."
    - "Rename suggestions are high-confidence only and are separated from already-confirmed structural changes."
    - "Blocking conditions are computed server-side so the UI cannot accidentally preview unsafe SQL."
  artifacts:
    - path: "server/lib/schema-diff.ts"
      provides: "Reusable file-schema normalization and comparison helpers"
      contains: "diff"
    - path: "server/lib/extensions/db-management/"
      provides: "DB compare and rename-suggestion services for live-schema workflows"
      contains: "db"
    - path: "server/routes/db-management-routes.ts"
      provides: "Compare preview and rename confirmation endpoints"
      contains: "compare"
  key_links:
    - from: "server/lib/extensions/db-management/"
      to: "server/lib/schema-diff.ts"
      via: "file-schema to live-schema comparison reuse"
      pattern: "diff"
    - from: "server/routes/db-management-routes.ts"
      to: "shared/routes.ts"
      via: "typed compare and rename review endpoints"
      pattern: "dbManagement"
---

<objective>
Implement the file-vs-live-DB compare engine and rename-review workflow.

Purpose: Phase 4’s main value appears when the extension can actually compare the selected sheet or table against a live MySQL database, compute safe change buckets, and surface rename candidates that must be reviewed before preview.
Output: Server-side compare service, high-confidence rename suggestion flow, blocker classification, and typed compare endpoints in the DB management route group.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-file-vs-db-diff-and-deploy-preview/04-CONTEXT.md
@.planning/phases/04-file-vs-db-diff-and-deploy-preview/04-VALIDATION.md
@shared/schema.ts
@shared/routes.ts
@server/lib/schema-diff.ts
@server/routes/db-management-routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compare file-derived schema against live DB snapshots</name>
  <files>server/lib/schema-diff.ts, server/lib/extensions/db-management/</files>
  <action>Build the Phase 4 compare pipeline that turns the selected file/sheet/table into a comparable schema view, matches it against the selected live DB snapshot, and emits table/column change sets for the new DB-oriented workspace. Keep the user-facing workflow focused on file-vs-live DB while leaving baseline-compatible seams for Phase 5.</action>
  <verify>Run `npm run check` and the Wave 0 file-vs-db compare tests, then inspect the output shape for sheet-level entry and table-level drilldown support.</verify>
  <done>The backend can generate a DB-oriented compare result for the currently selected file scope versus a live MySQL database.</done>
</task>

<task type="auto">
  <name>Task 2: Add high-confidence rename suggestion and confirmation flow</name>
  <files>server/lib/extensions/db-management/, server/routes/db-management-routes.ts</files>
  <action>Implement server-side rename suggestion scoring that emits only high-confidence candidates and keeps them pending until the user confirms or rejects them. The compare result should clearly separate confirmed mappings from blockers caused by unresolved rename decisions.</action>
  <verify>Run `npm test` with Wave 0 rename coverage and confirm unresolved rename candidates block downstream preview generation.</verify>
  <done>Rename review becomes an explicit safety step rather than an implicit guess inside SQL preview.</done>
</task>

<task type="auto">
  <name>Task 3: Classify blockers and preview-readiness on the server</name>
  <files>server/lib/extensions/db-management/, server/routes/db-management-routes.ts</files>
  <action>Compute preview blockers on the server for dangerous type shrink, unresolved rename, drop table, drop column, and `NULL -> NOT NULL` without fill/default strategy. Return a clear preview-readiness summary alongside compare results so the UI can explain blocked states without duplicating rules.</action>
  <verify>Run `npm run check` plus blocker-focused tests and confirm the server returns deterministic blocker reasons for all required Phase 4 safety cases.</verify>
  <done>The compare result fully describes whether SQL preview may proceed and why it is blocked when unsafe changes remain.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] File-vs-live-DB compare works at sheet entry and table drilldown levels
- [ ] Rename suggestions are high-confidence only and require confirmation
- [ ] Drop, shrink, unresolved rename, and unsafe nullability changes are surfaced as blockers
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 4 has a reliable backend compare engine and rename-review flow before SQL preview UI work begins
</success_criteria>

<output>
After completion, create `.planning/phases/04-file-vs-db-diff-and-deploy-preview/04-02-SUMMARY.md`
</output>
