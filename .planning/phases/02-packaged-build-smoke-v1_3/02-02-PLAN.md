---
phase: 02-packaged-build-smoke-v1_3
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - package.json
  - electron/main.ts
  - script/desktop-packaged-smoke.ts
  - test/electron/packaged-smoke-phase2.test.ts
autonomous: true
requirements:
  - STAB-05
  - STAB-06
user_setup: []
must_haves:
  truths:
    - "win-unpacked smoke is the primary fast feedback path for packaged confidence."
    - "Packaged readiness must be checkpoint-driven instead of sleep-driven."
    - "The runner and app can cooperate through lightweight smoke-mode signals without building a heavy E2E lab."
  artifacts:
    - path: "script/desktop-packaged-smoke.ts"
      provides: "win-unpacked launch, readiness, evidence collection, and shutdown orchestration"
      contains: "win-unpacked"
    - path: "electron/main.ts"
      provides: "app-assisted smoke mode hooks or evidence capture seams"
      contains: "checkpoint"
    - path: "package.json"
      provides: "packaged smoke script entrypoints"
      contains: "smoke"
---

<objective>
Create a repeatable `win-unpacked` packaged smoke seam that can launch the real packaged app, wait for readiness, collect evidence, and close cleanly.

Purpose: This is the main fast-feedback packaged surface for the phase.
Output: App-assisted smoke-mode seam, a `win-unpacked` smoke runner, and focused tests.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-CONTEXT.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-RESEARCH.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-VALIDATION.md
@package.json
@electron/main.ts
@script/desktop-smoke.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a lightweight packaged smoke runner for `win-unpacked`</name>
  <read_first>
    - package.json
    - electron/main.ts
    - script/desktop-smoke.ts
    - .planning/phases/02-packaged-build-smoke-v1_3/02-RESEARCH.md
  </read_first>
  <files>package.json, electron/main.ts, script/desktop-packaged-smoke.ts, test/electron/packaged-smoke-phase2.test.ts</files>
  <behavior>
    - Test 1: runner resolves the packaged executable path under `dist-electron/win-unpacked`
    - Test 2: runner waits on checkpoint/log evidence rather than fixed sleep only
    - Test 3: runner can emit blocker findings when launch/readiness/shutdown fails
  </behavior>
  <action>Create `script/desktop-packaged-smoke.ts` as the primary `win-unpacked` runner. It must launch `dist-electron/win-unpacked/DBSchemaExcel2DDL.exe`, watch for concrete readiness evidence such as bootstrap checkpoints (`server_bootstrap_ready`, `browser_window_loaded`), collect executable/log metadata, and write a packaged smoke artifact. Add a lightweight app-assisted smoke-mode seam in `electron/main.ts` only where needed to support reliable checkpoint or screenshot evidence capture. Add npm script entrypoints in `package.json` so the packaged smoke runner can be invoked repeatably without ad-hoc shell commands.</action>
  <acceptance_criteria>
    - `package.json` contains `smoke:packaged`
    - `script/desktop-packaged-smoke.ts` contains `win-unpacked`
    - `script/desktop-packaged-smoke.ts` contains `browser_window_loaded`
    - `script/desktop-packaged-smoke.ts` contains `server_bootstrap_ready`
    - `node --test --import tsx test/electron/packaged-smoke-phase2.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/electron/packaged-smoke-phase2.test.ts</automated>
  </verify>
  <done>The repo has a repeatable packaged `win-unpacked` smoke seam instead of ad-hoc manual launch steps.</done>
</task>

<task type="auto">
  <name>Task 2: Keep packaged build flows and Node test flows safely separated</name>
  <read_first>
    - package.json
    - script/desktop-preflight.ts
    - .planning/phases/02-packaged-build-smoke-v1_3/02-RESEARCH.md
  </read_first>
  <files>package.json, script/desktop-preflight.ts</files>
  <action>Extend the existing desktop preflight or script guard layer so packaged smoke scripts clearly use Electron-native rebuild assumptions and do not silently regress the Node/Electron native-module separation solved in Phase 1. Keep the checks narrow and explicit rather than inventing a general CI framework.</action>
  <acceptance_criteria>
    - `package.json` contains a packaged smoke script that does not bypass Electron-native rebuild expectations
    - `script/desktop-preflight.ts` contains a packaged smoke or packaged build guard check
    - `npm run build:electron` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build:electron</automated>
  </verify>
  <done>Packaged smoke can iterate safely without reintroducing native-module drift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/electron/packaged-smoke-phase2.test.ts`
- [ ] `npm run build:electron`
- [ ] packaged runner uses checkpoint/log evidence, not only fixed sleeps
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `win-unpacked` packaged smoke becomes repeatable and release-relevant
</success_criteria>

<output>
After completion, create `.planning/phases/02-packaged-build-smoke-v1_3/02-02-SUMMARY.md`
</output>
