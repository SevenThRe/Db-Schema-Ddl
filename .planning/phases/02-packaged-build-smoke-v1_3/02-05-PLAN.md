---
phase: 02-packaged-build-smoke-v1_3
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - script/desktop-packaged-smoke.ts
  - electron/main.ts
  - client/src/pages/Dashboard.tsx
  - test/electron/packaged-smoke-phase2.test.ts
  - artifacts/desktop-smoke/
autonomous: true
requirements:
  - STAB-05
  - STAB-06
gap_closure: true
must_haves:
  truths:
    - "Timestamp-prefixed packaged bootstrap logs no longer trigger false `PACKAGED_READINESS_FAILED` or `PACKAGED_SHUTDOWN_FAILED` results."
    - "The latest `win-unpacked` packaged artifact includes screenshot and log references directly in JSON and Markdown."
    - "The packaged smoke artifact records explicit outcomes for `startup`, `sqlite-init`, `extension-entry`, and `shutdown`, while keeping live MySQL optional."
    - "The regression test fixture uses the same timestamp-prefixed checkpoint shape written by the real packaged app."
  artifacts:
    - path: "script/desktop-packaged-smoke.ts"
      provides: "timestamp-tolerant checkpoint parsing plus win-unpacked artifact generation"
      contains: "extractPackagedCheckpointNames"
    - path: "electron/main.ts"
      provides: "smoke-only packaged checkpoints for screenshot, sqlite/migration, and DB 管理 proof"
      contains: "formatDesktopCheckpoint"
    - path: "client/src/pages/Dashboard.tsx"
      provides: "smoke-only DB 管理 entry hook using the real UI path"
      contains: "db-management"
    - path: "test/electron/packaged-smoke-phase2.test.ts"
      provides: "production-shaped packaged smoke regression coverage"
      contains: "server_bootstrap_ready"
    - path: "artifacts/desktop-smoke/"
      provides: "fresh win-unpacked JSON/Markdown/log/screenshot evidence"
      contains: "desktop-smoke-packaged-win-unpacked"
  key_links:
    - from: "script/desktop-packaged-smoke.ts"
      to: "artifacts/desktop-smoke/*.bootstrap.log"
      via: "timestamp-tolerant checkpoint extraction"
      pattern: "\\[checkpoint:"
    - from: "electron/main.ts"
      to: "script/desktop-packaged-smoke.ts"
      via: "smoke-mode checkpoints for sqlite/migration, DB 管理 entry, screenshot, and shutdown"
      pattern: "formatDesktopCheckpoint"
    - from: "client/src/pages/Dashboard.tsx"
      to: "script/desktop-packaged-smoke.ts"
      via: "smoke-only DB 管理 activation using the existing UI entry flow"
      pattern: "setActiveModule\\(\"db-management\"\\)"
---

<objective>
Close the `win-unpacked` packaged-smoke regression by fixing the real log parser, restoring trustworthy readiness/shutdown results, and regenerating evidence that proves the packaged app reaches SQLite/migration and `DB 管理`.

Purpose: Phase 2 remains blocked until the main packaged smoke path stops producing false negatives and incomplete proof artifacts.
Output: A corrected packaged smoke runner, production-shaped regression tests, and a fresh `win-unpacked` artifact set with explicit step outcomes.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-CONTEXT.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-RESEARCH.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-VALIDATION.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-VERIFICATION.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-02-SUMMARY.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-04-SUMMARY.md
@script/desktop-smoke.ts
@script/desktop-packaged-smoke.ts
@electron/main.ts
@client/src/pages/Dashboard.tsx
@test/electron/packaged-smoke-phase2.test.ts

<interfaces>
From `script/desktop-smoke.ts`:
```ts
export const DESKTOP_SMOKE_STEP_IDS = {
  startup: "startup",
  sqliteInit: "sqlite-init",
  extensionEntry: "extension-entry",
  dbManagementMysqlRead: "db-management-mysql-read",
  shutdown: "shutdown",
} as const;
```

From `script/desktop-packaged-smoke.ts`:
```ts
export const PACKAGED_READY_CHECKPOINTS = [
  "server_bootstrap_ready",
  "browser_window_loaded",
] as const;

export function extractPackagedCheckpointNames(logContents: string): string[] {
  const matches = logContents.matchAll(/^\[checkpoint:([^\]]+)\]/gm);
  // current bug: this misses timestamp-prefixed lines
}
```

From the latest packaged artifact:
```text
[2026-03-18T15:15:47.771Z] [checkpoint:server_bootstrap_ready] {"port":5000}
[2026-03-18T15:15:48.289Z] [checkpoint:browser_window_loaded] {"port":5000}
[2026-03-18T15:15:48.743Z] [checkpoint:smoke_screenshot_written] {"path":"...png"}
[2026-03-18T15:15:50.266Z] [checkpoint:server_shutdown_complete]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix timestamp-prefixed checkpoint parsing and lock the regression test to production log shape</name>
  <read_first>
    - .planning/phases/02-packaged-build-smoke-v1_3/02-VERIFICATION.md
    - script/desktop-packaged-smoke.ts
    - test/electron/packaged-smoke-phase2.test.ts
    - shared/desktop-runtime.ts
  </read_first>
  <files>script/desktop-packaged-smoke.ts, test/electron/packaged-smoke-phase2.test.ts</files>
  <behavior>
    - Test 1: `extractPackagedCheckpointNames()` returns checkpoint names from log lines shaped like `[timestamp] [checkpoint:name] {...}`.
    - Test 2: readiness polling succeeds when the fixture contains the timestamp-prefixed production format instead of raw `[checkpoint:...]` lines.
    - Test 3: false-negative readiness/shutdown blocker findings stop originating from the parser mismatch itself.
  </behavior>
  <action>Update `script/desktop-packaged-smoke.ts` so checkpoint extraction matches the packaged bootstrap log format actually emitted by the app, including timestamp-prefixed lines and later checkpoint tokens on the same line. Remove the brittle line-start anchor assumption instead of papering over the failure with longer timeouts. Rewrite the packaged-smoke unit fixture in `test/electron/packaged-smoke-phase2.test.ts` to use the production timestamp shape from the latest artifact so this regression cannot escape again.</action>
  <acceptance_criteria>
    - `script/desktop-packaged-smoke.ts` no longer relies on `/^\\[checkpoint:/`
    - `test/electron/packaged-smoke-phase2.test.ts` contains timestamp-prefixed fixture lines matching `\\[\\d{4}-\\d{2}-\\d{2}T`
    - `node --test --import tsx test/electron/packaged-smoke-phase2.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/electron/packaged-smoke-phase2.test.ts</automated>
  </verify>
  <done>The packaged runner reads the same checkpoint format the real app writes, and the test suite now proves that exact contract.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate win-unpacked evidence with explicit sqlite and DB 管理 outcomes</name>
  <read_first>
    - .planning/phases/02-packaged-build-smoke-v1_3/02-CONTEXT.md
    - .planning/phases/02-packaged-build-smoke-v1_3/02-VALIDATION.md
    - .planning/phases/02-packaged-build-smoke-v1_3/02-VERIFICATION.md
    - electron/main.ts
    - client/src/pages/Dashboard.tsx
    - script/desktop-packaged-smoke.ts
    - docs/desktop-packaged-smoke.md
  </read_first>
  <files>electron/main.ts, client/src/pages/Dashboard.tsx, script/desktop-packaged-smoke.ts, test/electron/packaged-smoke-phase2.test.ts, artifacts/desktop-smoke/</files>
  <action>Keep the fix operationally narrow: use the existing packaged smoke runner and real `DB 管理` UI entry path, adding only the smallest smoke-only hooks needed to prove `sqlite-init`, extension/catalog readiness, and `DB 管理` entry before auto-close. Persist screenshot references from `smoke_screenshot_written`, persist the bootstrap log excerpt in JSON/Markdown, and update step classification so `startup`, `sqlite-init`, `extension-entry`, and `shutdown` are explicit pass/fail outcomes instead of staying skipped. Do not make live MySQL a blocker here; if no real MySQL read is exercised, leave `db-management-mysql-read` as warning/skipped with an explicit note per the locked phase decision.</action>
  <acceptance_criteria>
    - `electron/main.ts` emits smoke checkpoints for packaged sqlite/migration readiness and `DB 管理` proof
    - `client/src/pages/Dashboard.tsx` contains a smoke-only path that uses the real `handleDbManagementEntryClick` flow to reach `db-management`
    - `script/desktop-packaged-smoke.ts` records screenshot paths from packaged evidence instead of leaving `screenshotPaths` empty
    - `npm run smoke:packaged` exits 0
    - the latest `artifacts/desktop-smoke/desktop-smoke-packaged-win-unpacked-*.json` contains a non-empty `screenshotPaths`
    - the latest `artifacts/desktop-smoke/desktop-smoke-packaged-win-unpacked-*.json` marks `startup`, `sqlite-init`, `extension-entry`, and `shutdown` as `passed`
    - the latest `artifacts/desktop-smoke/desktop-smoke-packaged-win-unpacked-*.json` does not contain `PACKAGED_READINESS_FAILED` or `PACKAGED_SHUTDOWN_FAILED`
    - the latest `artifacts/desktop-smoke/desktop-smoke-packaged-win-unpacked-*.json` leaves `db-management-mysql-read` explicit as `warning` or `skipped` if no live MySQL proof is attached
  </acceptance_criteria>
  <verify>
    <automated>npm run smoke:packaged</automated>
    <automated>powershell -ExecutionPolicy Bypass -Command "$ErrorActionPreference='Stop'; $latest = Get-ChildItem 'artifacts/desktop-smoke/desktop-smoke-packaged-win-unpacked-*.json' | Sort-Object LastWriteTimeUtc -Descending | Select-Object -First 1; if ($null -eq $latest) { throw 'No win-unpacked artifact found.' }; $json = Get-Content $latest.FullName -Raw | ConvertFrom-Json; if ($json.screenshotPaths.Count -lt 1) { throw 'Missing screenshotPaths evidence.' }; foreach ($required in 'startup','sqlite-init','extension-entry','shutdown') { if (-not ($json.steps | Where-Object { $_.id -eq $required -and $_.status -eq 'passed' })) { throw \"Missing passed step: $required\" } }; $codes = @($json.blockerFindings | ForEach-Object { $_.code }); if ($codes -contains 'PACKAGED_READINESS_FAILED' -or $codes -contains 'PACKAGED_SHUTDOWN_FAILED') { throw 'False-negative packaged blocker still present.' }"</automated>
  </verify>
  <done>The `win-unpacked` packaged path becomes trustworthy again, and its latest artifact pair is sufficient to re-verify the main packaged smoke truths.</done>
</task>

</tasks>

<verification>
- [ ] `node --test --import tsx test/electron/packaged-smoke-phase2.test.ts`
- [ ] `npm run smoke:packaged`
- [ ] Latest `win-unpacked` JSON/Markdown/log/screenshot artifacts agree on pass status and evidence refs
</verification>

<success_criteria>
- The false-negative parser regression is closed in code and tests.
- A fresh `win-unpacked` artifact proves startup, sqlite/migration, `DB 管理` entry, and shutdown.
- Screenshot and log references are present in the latest JSON and Markdown artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-packaged-build-smoke-v1_3/02-05-SUMMARY.md`
</output>
