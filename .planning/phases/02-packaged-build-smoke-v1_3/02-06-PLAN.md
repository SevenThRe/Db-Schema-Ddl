---
phase: 02-packaged-build-smoke-v1_3
plan: 06
type: execute
wave: 2
depends_on:
  - 05
files_modified:
  - script/desktop-packaged-smoke-installer.ps1
  - docs/desktop-packaged-smoke.md
  - test/electron/packaged-smoke-phase2.test.ts
  - artifacts/desktop-smoke/
autonomous: false
requirements:
  - STAB-05
  - STAB-06
user_setup:
  - "On the Windows execution machine, complete one NSIS install -> first launch -> `DB 管理` -> close path and capture: installer UI screenshot, first-launch screenshot, and one packaged log excerpt path."
gap_closure: true
must_haves:
  truths:
    - "NSIS installer coverage is not treated as complete unless install, first-run, `DB 管理`, and close outcomes are explicit in the artifact."
    - "Semi-manual installer proof remains allowed, but missing screenshots or log refs are represented as incomplete/blocking evidence instead of placeholder success."
    - "The latest NSIS JSON and Markdown artifacts contain screenshot/log references and install -> first run -> close pass/fail evidence that a reviewer can follow."
  artifacts:
    - path: "script/desktop-packaged-smoke-installer.ps1"
      provides: "installer evidence capture with explicit screenshot/log args and step outcomes"
      contains: "SemiManual"
    - path: "docs/desktop-packaged-smoke.md"
      provides: "NSIS review checklist with required screenshot/log attachments and blocker policy"
      contains: "NSIS installer"
    - path: "test/electron/packaged-smoke-phase2.test.ts"
      provides: "installer artifact contract coverage"
      contains: "first-launch"
    - path: "artifacts/desktop-smoke/"
      provides: "fresh NSIS JSON/Markdown evidence with screenshot/log references"
      contains: "desktop-smoke-packaged-nsis"
  key_links:
    - from: "script/desktop-packaged-smoke-installer.ps1"
      to: "artifacts/desktop-smoke/*.json"
      via: "structured evidenceRefs plus per-step install/first-run/close status fields"
      pattern: "evidenceRefs"
    - from: "docs/desktop-packaged-smoke.md"
      to: "script/desktop-packaged-smoke-installer.ps1"
      via: "operator command and attachment checklist"
      pattern: "installer UI screenshot"
    - from: "artifacts/desktop-smoke/*.md"
      to: "artifacts/desktop-smoke/*.json"
      via: "same screenshot/log references and pass/fail outcomes"
      pattern: "first-launch"
---

<objective>
Replace the placeholder NSIS proof with a structured installer artifact that either proves install -> first run -> `DB 管理` -> close, or records a concrete blocker when that proof is missing.

Purpose: Phase 2 cannot stay "complete" while the real installer surface has only metadata and manual notes.
Output: A hardened NSIS helper/doc/test contract plus one fresh installer artifact pair with explicit screenshot/log/pass-fail evidence.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-CONTEXT.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-RESEARCH.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-VALIDATION.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-VERIFICATION.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-03-SUMMARY.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-04-SUMMARY.md
@script/desktop-packaged-smoke-installer.ps1
@docs/desktop-packaged-smoke.md
@test/electron/packaged-smoke-phase2.test.ts

<interfaces>
From the current NSIS artifact:
```json
{
  "semiManual": true,
  "evidenceRefs": [
    { "kind": "installer", "path": "...Setup-1.1.4.exe" },
    { "kind": "install-directory", "path": "C:\\Program Files\\DBSchemaExcel2DDL" },
    { "kind": "installed-executable", "path": "C:\\Program Files\\DBSchemaExcel2DDL\\DBSchemaExcel2DDL.exe" },
    { "kind": "manual-step", "path": "...desktop-smoke-packaged-nsis-....md" }
  ],
  "blockerFindings": []
}
```

Missing today:
```text
- installer UI screenshot ref
- first-launch screenshot ref
- packaged log ref
- explicit install / first-run / DB 管理 / close pass-fail outcomes
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden the NSIS helper so missing proof becomes explicit artifact state</name>
  <read_first>
    - .planning/phases/02-packaged-build-smoke-v1_3/02-VERIFICATION.md
    - script/desktop-packaged-smoke-installer.ps1
    - docs/desktop-packaged-smoke.md
    - test/electron/packaged-smoke-phase2.test.ts
  </read_first>
  <files>script/desktop-packaged-smoke-installer.ps1, docs/desktop-packaged-smoke.md, test/electron/packaged-smoke-phase2.test.ts</files>
  <behavior>
    - Test 1: the helper accepts concrete evidence refs for installer UI screenshot, first-launch screenshot, and packaged log attachment.
    - Test 2: the artifact records explicit step outcomes for install, first launch, `DB 管理` entry, and close.
    - Test 3: when semi-manual proof is incomplete, the helper emits blocker/warning evidence instead of a blank success artifact.
  </behavior>
  <action>Extend `script/desktop-packaged-smoke-installer.ps1` with explicit inputs for screenshot/log attachments and per-step status fields, then mirror those fields into both JSON and Markdown output. Add blocker or warning findings when required proof is missing, failed, or still awaiting operator confirmation. Update `docs/desktop-packaged-smoke.md` so the NSIS checklist names the exact command shape and required attachments. Extend `test/electron/packaged-smoke-phase2.test.ts` so the contract checks these exact parameter names and output fields.</action>
  <acceptance_criteria>
    - `script/desktop-packaged-smoke-installer.ps1` contains `InstallerScreenshotPath`
    - `script/desktop-packaged-smoke-installer.ps1` contains `FirstLaunchScreenshotPath`
    - `script/desktop-packaged-smoke-installer.ps1` contains `PackagedLogPath`
    - `script/desktop-packaged-smoke-installer.ps1` contains explicit install/first-run/db-entry/close status fields
    - `docs/desktop-packaged-smoke.md` contains `installer UI screenshot`
    - `docs/desktop-packaged-smoke.md` contains `first-launch screenshot`
    - `docs/desktop-packaged-smoke.md` contains `packaged log excerpt`
    - `node --test --import tsx test/electron/packaged-smoke-phase2.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/electron/packaged-smoke-phase2.test.ts</automated>
  </verify>
  <done>The installer helper can no longer produce an artifact that looks complete while lacking the proof Phase 2 actually requires.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture one real NSIS install -> first run -> `DB 管理` -> close artifact pair</name>
  <read_first>
    - docs/desktop-packaged-smoke.md
    - script/desktop-packaged-smoke-installer.ps1
    - .planning/phases/02-packaged-build-smoke-v1_3/02-VALIDATION.md
    - .planning/phases/02-packaged-build-smoke-v1_3/02-VERIFICATION.md
  </read_first>
  <files>artifacts/desktop-smoke/</files>
  <action>Run `npm run build:electron`, complete one real NSIS install on the execution machine, launch the installed app, enter `DB 管理`, and close it cleanly. Capture an installer UI screenshot, a first-launch screenshot after the main window is interactive, and one packaged log excerpt path from that same run. Then execute the updated helper with those attachment paths and explicit step statuses so it writes the final JSON/Markdown evidence pair. If any step fails or proof cannot be captured, keep the artifact explicit by marking the failed step and blocker code instead of recording a placeholder pass.</action>
  <acceptance_criteria>
    - the latest `artifacts/desktop-smoke/desktop-smoke-packaged-nsis-*.json` contains evidence refs for installer UI screenshot, first-launch screenshot, and packaged log
    - the latest `artifacts/desktop-smoke/desktop-smoke-packaged-nsis-*.json` contains explicit pass/fail outcomes for install, first launch, `DB 管理`, and close
    - the latest `artifacts/desktop-smoke/desktop-smoke-packaged-nsis-*.md` mirrors those same screenshot/log refs and per-step outcomes
    - if any proof is missing or failed, the latest NSIS artifact contains a matching blocker or warning finding instead of blank success state
  </acceptance_criteria>
  <how-to-verify>
    1. Run `npm run build:electron`.
    2. Launch the NSIS installer and complete the install UI on the local Windows machine.
    3. Open the installed app, enter `DB 管理`, then close the app cleanly.
    4. Save the installer UI screenshot and first-launch screenshot to absolute paths.
    5. Re-run `script/desktop-packaged-smoke-installer.ps1` with the new attachment/status arguments so it emits the final artifact pair.
    6. Review the newest NSIS JSON/Markdown files under `artifacts/desktop-smoke/` and confirm the evidence refs and step outcomes are present.
  </how-to-verify>
  <resume-signal>Type `approved` after the generated NSIS artifact pair contains the required screenshot/log refs and explicit step outcomes, or report the blocker you observed.</resume-signal>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -Command "$ErrorActionPreference='Stop'; $latest = Get-ChildItem 'artifacts/desktop-smoke/desktop-smoke-packaged-nsis-*.json' | Sort-Object LastWriteTimeUtc -Descending | Select-Object -First 1; if ($null -eq $latest) { throw 'No NSIS artifact found.' }; $json = Get-Content $latest.FullName -Raw | ConvertFrom-Json; $kinds = @($json.evidenceRefs | ForEach-Object { $_.kind }); foreach ($required in 'installer-ui-screenshot','first-launch-screenshot','packaged-log') { if ($kinds -notcontains $required) { throw \"Missing evidence ref: $required\" } }; foreach ($requiredStep in 'install','first-launch','db-entry','close') { if (-not $json.stepResults.PSObject.Properties.Name.Contains($requiredStep)) { throw \"Missing step result: $requiredStep\" } }"</automated>
  </verify>
  <done>The installer path now has one reviewable evidence pair that either proves the packaged run or records an explicit blocker at the exact missing step.</done>
</task>

</tasks>

<verification>
- [ ] `node --test --import tsx test/electron/packaged-smoke-phase2.test.ts`
- [ ] One fresh `desktop-smoke-packaged-nsis-*.json` / `.md` pair exists under `artifacts/desktop-smoke/`
- [ ] The artifact pair includes screenshot/log refs plus install, first-run, `DB 管理`, and close outcomes
</verification>

<success_criteria>
- The NSIS helper cannot emit placeholder success metadata without evidence.
- One latest NSIS artifact pair is reviewable and traceable by a human or future automation.
- Any remaining installer limitation is encoded as a blocker or warning in the artifact rather than hidden in chat.
</success_criteria>

<output>
After completion, create `.planning/phases/02-packaged-build-smoke-v1_3/02-06-SUMMARY.md`
</output>
