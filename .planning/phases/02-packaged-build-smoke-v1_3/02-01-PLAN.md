---
phase: 02-packaged-build-smoke-v1_3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - script/desktop-smoke.ts
  - test/electron/packaged-smoke-phase2.test.ts
autonomous: true
requirements:
  - STAB-05
  - STAB-06
user_setup: []
must_haves:
  truths:
    - "Packaged smoke extends the existing smoke artifact family instead of inventing a parallel report format."
    - "Packaged runs carry stable mode, executable path, screenshot refs, log refs, and blocker findings that are machine-usable."
    - "Markdown and JSON evidence derive from the same packaged smoke artifact."
  artifacts:
    - path: "shared/schema.ts"
      provides: "packaged smoke schemas, blocker findings, and evidence reference fields"
      contains: "desktopSmoke"
    - path: "script/desktop-smoke.ts"
      provides: "artifact generation helpers reused by packaged smoke"
      contains: "smoke"
    - path: "test/electron/packaged-smoke-phase2.test.ts"
      provides: "wave-0 contract coverage for packaged smoke artifacts"
      contains: "packaged"
---

<objective>
Define the shared packaged-smoke artifact model before runner scripts and release-gate logic diverge into ad-hoc JSON, screenshots, and log snippets.

Purpose: Phase 2 needs one stable vocabulary for packaged run mode, evidence refs, blocker findings, and report generation.
Output: Shared schemas plus focused contract tests for packaged smoke evidence.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-CONTEXT.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-RESEARCH.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-VALIDATION.md
@shared/schema.ts
@script/desktop-smoke.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend desktop smoke artifacts for packaged Windows runs</name>
  <read_first>
    - shared/schema.ts
    - script/desktop-smoke.ts
    - .planning/phases/02-packaged-build-smoke-v1_3/02-CONTEXT.md
    - .planning/phases/02-packaged-build-smoke-v1_3/02-RESEARCH.md
  </read_first>
  <files>shared/schema.ts, script/desktop-smoke.ts, test/electron/packaged-smoke-phase2.test.ts</files>
  <behavior>
    - Test 1: packaged smoke artifact exposes `runMode`, `executablePath`, `screenshotPaths`, `logExcerpt`, and structured blocker findings
    - Test 2: `win-unpacked` and `packaged-nsis` are first-class artifact modes
    - Test 3: Markdown and JSON packaged evidence still derive from one artifact
  </behavior>
  <action>Add packaged smoke fields to the existing desktop smoke artifact family in `shared/schema.ts`, including concrete support for `packaged-win-unpacked` and `packaged-nsis` run modes, executable path metadata, screenshot references, log excerpt fields, and blocker findings with exact `code`, `blocker`, `severity`, and `message` fields. Extend `script/desktop-smoke.ts` so report rendering can consume those exact fields. Create focused tests that assert the packaged artifact remains MCP-friendly and report-safe.</action>
  <acceptance_criteria>
    - `shared/schema.ts` contains a packaged run mode schema including `packaged-win-unpacked`
    - `shared/schema.ts` contains a packaged run mode schema including `packaged-nsis`
    - `shared/schema.ts` contains `blockerFindings`
    - `script/desktop-smoke.ts` contains `screenshotPaths`
    - `node --test --import tsx test/electron/packaged-smoke-phase2.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/electron/packaged-smoke-phase2.test.ts</automated>
  </verify>
  <done>Packaged smoke starts from one stable artifact model instead of ad-hoc evidence blobs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/electron/packaged-smoke-phase2.test.ts`
- [ ] packaged smoke artifact remains schema-backed and AI/MCP-friendly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Packaged smoke has one stable machine-usable evidence model
</success_criteria>

<output>
After completion, create `.planning/phases/02-packaged-build-smoke-v1_3/02-01-SUMMARY.md`
</output>
