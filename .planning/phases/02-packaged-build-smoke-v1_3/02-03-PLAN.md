---
phase: 02-packaged-build-smoke-v1_3
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - build/installer.nsh
  - script/desktop-packaged-smoke-installer.ps1
  - docs/desktop-packaged-smoke.md
  - test/electron/packaged-smoke-phase2.test.ts
autonomous: true
requirements:
  - STAB-05
  - STAB-06
user_setup:
  - "Allow one semi-manual NSIS installer proof path if elevation or local policy blocks full automation."
must_haves:
  truths:
    - "Installer smoke is part of release confidence, but it may stay semi-manual."
    - "Release blockers must be encoded in evidence, not left as tribal knowledge."
    - "Installer runs must account for the repo's existing NSIS customizations and sticky install-state behavior."
  artifacts:
    - path: "script/desktop-packaged-smoke-installer.ps1"
      provides: "installer-run orchestration and evidence capture seam"
      contains: "nsis"
    - path: "docs/desktop-packaged-smoke.md"
      provides: "reviewable packaged smoke checklist and blocker policy"
      contains: "blocker"
---

<objective>
Add an installer-path smoke seam and encode packaged release blockers as first-class evidence and review policy.

Purpose: `win-unpacked` alone is not enough because the real delivered installer has its own failure modes.
Output: NSIS smoke helper/documentation plus explicit packaged blocker policy.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-CONTEXT.md
@.planning/phases/02-packaged-build-smoke-v1_3/02-RESEARCH.md
@build/installer.nsh
@docs/desktop-smoke.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an NSIS installer smoke helper and evidence seam</name>
  <read_first>
    - build/installer.nsh
    - package.json
    - docs/desktop-smoke.md
    - .planning/phases/02-packaged-build-smoke-v1_3/02-RESEARCH.md
  </read_first>
  <files>script/desktop-packaged-smoke-installer.ps1, docs/desktop-packaged-smoke.md, test/electron/packaged-smoke-phase2.test.ts</files>
  <behavior>
    - Test 1: installer helper records installer path, install directory, timestamps, and evidence refs
    - Test 2: blocker policy explicitly classifies startup/native/migration/close/catalog/DB-entry failures
    - Test 3: installer flow can be marked semi-manual without breaking artifact/report structure
  </behavior>
  <action>Create `script/desktop-packaged-smoke-installer.ps1` to orchestrate the NSIS smoke path at a semi-automated level: locate the installer artifact, launch it, capture install/start evidence metadata, and hand off to the packaged smoke artifact model. Add `docs/desktop-packaged-smoke.md` to document the packaged smoke checklist, manual evidence steps, and the exact blocker vs warning policy for packaged runs.</action>
  <acceptance_criteria>
    - `script/desktop-packaged-smoke-installer.ps1` contains `NSIS`
    - `script/desktop-packaged-smoke-installer.ps1` contains `blocker`
    - `docs/desktop-packaged-smoke.md` contains `win-unpacked`
    - `docs/desktop-packaged-smoke.md` contains `NSIS installer`
    - `docs/desktop-packaged-smoke.md` contains `release blocker`
    - `node --test --import tsx test/electron/packaged-smoke-phase2.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/electron/packaged-smoke-phase2.test.ts</automated>
  </verify>
  <done>Installer-path packaged smoke is explicit, reviewable, and no longer tribal knowledge.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/electron/packaged-smoke-phase2.test.ts`
- [ ] packaged blocker policy is explicit in docs and artifacts
- [ ] installer path can be semi-manual without breaking structured evidence
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- installer smoke is covered by a real seam instead of a TODO
</success_criteria>

<output>
After completion, create `.planning/phases/02-packaged-build-smoke-v1_3/02-03-SUMMARY.md`
</output>
