---
phase: 37-scale-connection-governance-for-large-saved-connection-catalogs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - src-tauri/src/storage.rs
  - client/src/components/extensions/DbConnectorWorkspace.tsx
  - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
  - test/client/db-connection-governance-phase37.test.ts
  - test/server/db-connection-governance-phase37.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Connection governance metadata becomes usable at scale instead of stopping at favorite/group/notes."
    - "Bulk governance actions operate on persisted connection metadata without breaking the canonical workbench flow."
    - "The quick-switch sidebar reflects richer governance state without turning into decorative noise."
  artifacts:
    - path: "shared/schema.ts"
      provides: "large-scale connection governance contract"
      contains: "tags?: string[]"
    - path: "client/src/components/extensions/DbConnectorWorkspace.tsx"
      provides: "bulk governance UI"
      contains: "selectedConnectionIds"
    - path: "src-tauri/src/storage.rs"
      provides: "persisted catalog metadata storage"
      contains: "last_verified_at"
  key_links:
    - from: "client/src/components/extensions/DbConnectorWorkspace.tsx"
      to: "client/src/components/extensions/db-workbench/ConnectionSidebar.tsx"
      via: "tags"
      pattern: "archived"
---

<objective>
Make large saved-connection inventories governable through richer metadata, bulk actions, and dense filtering without weakening the canonical workbench connection flow.
</objective>

<execution_context>
C:/Users/ASUS/.codex/get-shit-done/workflows/execute-plan.md
C:/Users/ASUS/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/db-workbench-feature-checklist.md
@shared/schema.ts
@src-tauri/src/storage.rs
@client/src/components/extensions/DbConnectorWorkspace.tsx
@client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend connection config with scale-oriented governance metadata</name>
  <read_first>
    - shared/schema.ts
    - src-tauri/src/storage.rs
  </read_first>
  <files>
    - shared/schema.ts
    - src-tauri/src/storage.rs
  </files>
  <action>
Extend `DbConnectionConfig` with `tags`, `archived`, `lastUsedAt`, and `lastVerifiedAt`, persist those fields in storage, and keep older saved connections compatible when the richer governance metadata is missing.
  </action>
  <acceptance_criteria>
    - `shared/schema.ts` contains `tags?: string[]`
    - `shared/schema.ts` contains `archived?: boolean`
    - `shared/schema.ts` contains `lastUsedAt?: string`
    - `shared/schema.ts` contains `lastVerifiedAt?: string`
    - `storage.rs` persists the new governance fields without breaking older rows
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -j 1</automated>
  </verify>
  <done>The connection model can now support large-catalog governance instead of only cosmetic metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add dense filters and bulk governance actions in the connection center</name>
  <read_first>
    - client/src/components/extensions/DbConnectorWorkspace.tsx
  </read_first>
  <files>
    - client/src/components/extensions/DbConnectorWorkspace.tsx
  </files>
  <action>
Add multi-select connection management with a `selectedConnectionIds` state, then support bulk update actions for `environment`, `readonly`, `groupName`, `tags`, and `archived` while keeping existing search, favorite, and quick-connect behavior intact.
  </action>
  <acceptance_criteria>
    - `DbConnectorWorkspace.tsx` contains `selectedConnectionIds`
    - the connection center can filter by `tags` and `archived`
    - bulk actions can update `environment`, `readonly`, `groupName`, and `tags`
    - archived connections no longer dominate the default active catalog view
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Operators can govern large connection catalogs without one-by-one edits.</done>
</task>

<task type="auto">
  <name>Task 3: Reflect governance state in quick-switch UI and lock it with tests</name>
  <read_first>
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
    - client/src/components/extensions/DbConnectorWorkspace.tsx
  </read_first>
  <files>
    - client/src/components/extensions/db-workbench/ConnectionSidebar.tsx
    - test/client/db-connection-governance-phase37.test.ts
    - test/server/db-connection-governance-phase37.test.ts
  </files>
  <action>
Mirror the richer governance metadata into the connection sidebar and quick-switch surfaces, then add focused client and server tests that assert tags, archived state, and last verification timestamps stay wired through UI and storage.
  </action>
  <acceptance_criteria>
    - `ConnectionSidebar.tsx` surfaces `tags` or `lastVerifiedAt`
    - `test/client/db-connection-governance-phase37.test.ts` exists
    - `test/server/db-connection-governance-phase37.test.ts` exists
    - the tests assert `archived` and `tags`
  </acceptance_criteria>
  <verify>
    <automated>node --import=tsx --test test/client/db-connection-governance-phase37.test.ts test/server/db-connection-governance-phase37.test.ts</automated>
  </verify>
  <done>The richer governance model is visible and regression-covered across storage and quick-switch UI.</done>
</task>

</tasks>

<verification>
- npm run check
- cargo check --manifest-path src-tauri/Cargo.toml -j 1
- node --import=tsx --test test/client/db-connection-governance-phase37.test.ts test/server/db-connection-governance-phase37.test.ts
</verification>

<success_criteria>
- Large saved-connection catalogs become governable through real metadata and bulk actions
</success_criteria>

<output>
After completion, create `.planning/phases/37-scale-connection-governance-for-large-saved-connection-catalogs/37-01-SUMMARY.md`
</output>
