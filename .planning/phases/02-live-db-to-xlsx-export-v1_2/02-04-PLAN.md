---
phase: 02-live-db-to-xlsx-export-v1_2
plan: 04
type: execute
wave: 4
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - client/src/components/db-management/DbManagementWorkspace.tsx
  - client/src/components/db-management/DbLiveExportWorkspace.tsx
  - client/src/hooks/use-db-management.ts
  - client/src/pages/Dashboard.tsx
  - test/client/db-live-export-phase2-ui.test.tsx
autonomous: true
requirements:
  - DBXLSX-01
  - DBXLSX-02
  - DBXLSX-03
user_setup: []
must_haves:
  truths:
    - "Live DB to XLSX is a dedicated DB management workspace, not a hidden action."
    - "The screen follows the approved UI-SPEC three-column review pattern."
    - "Export success hands the user back into the standard file workflow immediately."
  artifacts:
    - path: "client/src/components/db-management/DbLiveExportWorkspace.tsx"
      provides: "dedicated Phase 2 UI for source selection, issue review, and workbook export"
      contains: "export"
    - path: "client/src/components/db-management/DbManagementWorkspace.tsx"
      provides: "main-view integration for the new export workspace"
      contains: "DB 工作区"
    - path: "client/src/hooks/use-db-management.ts"
      provides: "typed preview/export hooks consumed by the workspace"
      contains: "usePreviewLiveDbWorkbookExport"
  key_links:
    - from: "client/src/components/db-management/DbLiveExportWorkspace.tsx"
      to: ".planning/phases/02-live-db-to-xlsx-export-v1_2/02-UI-SPEC.md"
      via: "three-column layout, copy contract, and trust-gate hierarchy"
      pattern: "导出准备|使用最近 snapshot|生成 XLSX 工作簿"
---

<objective>
Deliver the dedicated `live DB -> XLSX` workspace in `DB 管理`, aligned with the approved UI contract.

Purpose: Phase 2 needs a user-facing workflow that makes freshness, full-catalog visibility, issue review, and export trust obvious. This plan integrates the backend seams into a dedicated UI and returns successful exports to the file-first flow.
Output: A new DB-management main view for live export, with freshness controls, table filtering, issue review, and file activation after success.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-live-db-to-xlsx-export-v1_2/02-CONTEXT.md
@.planning/phases/02-live-db-to-xlsx-export-v1_2/02-UI-SPEC.md
@.planning/phases/02-live-db-to-xlsx-export-v1_2/02-VALIDATION.md
@client/src/components/db-management/DbManagementWorkspace.tsx
@client/src/components/ddl-import/DdlImportWorkspace.tsx
@client/src/components/db-management/DbSnapshotCompareWorkspace.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the dedicated live-export workspace UI</name>
  <read_first>
    - client/src/components/db-management/DbManagementWorkspace.tsx
    - client/src/components/ddl-import/DdlImportWorkspace.tsx
    - client/src/components/db-management/DbSnapshotCompareWorkspace.tsx
    - .planning/phases/02-live-db-to-xlsx-export-v1_2/02-UI-SPEC.md
  </read_first>
  <files>client/src/components/db-management/DbLiveExportWorkspace.tsx, test/client/db-live-export-phase2-ui.test.tsx</files>
  <behavior>
    - Test 1: workspace exposes the explicit freshness controls `使用最近 snapshot` and `导出前刷新 live`
    - Test 2: workspace loads whole-database scope first, then lets the user filter and select tables
    - Test 3: right-hand trust panel surfaces blocker/confirm/info counts, template choice, and export CTA
  </behavior>
  <action>Create `client/src/components/db-management/DbLiveExportWorkspace.tsx` as a three-column workspace that matches `02-UI-SPEC.md`. Left column: source and freshness controls. Middle column: whole-database table review with selection/filtering. Right column: issue summary, template picker, lossy confirmation, and the primary CTA `生成 XLSX 工作簿`. Use the exact copy contract from `02-UI-SPEC.md` for empty, error, and refresh-confirmation messaging.</action>
  <acceptance_criteria>
    - `client/src/components/db-management/DbLiveExportWorkspace.tsx` exists
    - `client/src/components/db-management/DbLiveExportWorkspace.tsx` contains `使用最近 snapshot`
    - `client/src/components/db-management/DbLiveExportWorkspace.tsx` contains `导出前刷新 live`
    - `client/src/components/db-management/DbLiveExportWorkspace.tsx` contains `生成 XLSX 工作簿`
    - `node --test --import tsx test/client/db-live-export-phase2-ui.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/client/db-live-export-phase2-ui.test.tsx</automated>
  </verify>
  <done>Users can review and export live DB catalogs inside a dedicated, trust-first workspace.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate the workspace into DB 管理 and activate exported files</name>
  <read_first>
    - client/src/components/db-management/DbManagementWorkspace.tsx
    - client/src/pages/Dashboard.tsx
    - client/src/hooks/use-db-management.ts
    - .planning/phases/02-live-db-to-xlsx-export-v1_2/02-UI-SPEC.md
  </read_first>
  <files>client/src/components/db-management/DbManagementWorkspace.tsx, client/src/hooks/use-db-management.ts, client/src/pages/Dashboard.tsx</files>
  <action>Add a new `live-export` main view to `DB 管理`, wire it to the typed preview/export hooks, preserve the existing remembered-view behavior, and make successful export select the newly created workbook in the standard file list so the user lands back in the file-driven workflow immediately.</action>
  <acceptance_criteria>
    - `client/src/components/db-management/DbManagementWorkspace.tsx` contains `live-export`
    - `client/src/components/db-management/DbManagementWorkspace.tsx` contains `DbLiveExportWorkspace`
    - `client/src/hooks/use-db-management.ts` contains `useExecuteLiveDbWorkbookExport`
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The dedicated workspace is a first-class DB-management view and successful exports re-enter the file flow cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/client/db-live-export-phase2-ui.test.tsx`
- [ ] `npm run check`
- [ ] `npm test`
- [ ] exported workbooks activate inside the standard file list on success
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 users can export live DB catalogs through a dedicated, trust-first UI
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-db-to-xlsx-export-v1_2/02-04-SUMMARY.md`
</output>
