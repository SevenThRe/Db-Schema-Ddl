---
phase: 02-live-db-to-xlsx-export-v1_2
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - server/lib/extensions/db-management/history-service.ts
  - server/lib/extensions/db-management/schema-normalizer.ts
  - server/lib/extensions/db-management/live-export-service.ts
  - server/lib/ddl-import/issues.ts
  - test/server/db-live-export-phase2.test.ts
autonomous: true
requirements:
  - DBXLSX-01
  - DBXLSX-02
  - DBXLSX-03
user_setup: []
must_haves:
  truths:
    - "Freshness remains explicit: recent snapshot by default, optional force refresh."
    - "Export starts from whole-database visibility and only then narrows to selected tables."
    - "Blocker / confirm / info issue severity is reused from existing workbook trust rules."
  artifacts:
    - path: "server/lib/extensions/db-management/live-export-service.ts"
      provides: "preview pipeline for source resolution, catalog loading, and issue classification"
      contains: "export"
    - path: "server/lib/extensions/db-management/history-service.ts"
      provides: "live versus snapshot freshness resolution patterns reused by export preview"
      contains: "snapshot"
  key_links:
    - from: "server/lib/extensions/db-management/live-export-service.ts"
      to: "server/lib/ddl-import/issues.ts"
      via: "shared blocker/confirm/info trust semantics"
      pattern: "issue|blocking|confirm|info"
---

<objective>
Implement the backend preview pipeline that resolves live freshness, loads the whole catalog, and classifies export issues before any workbook is written.

Purpose: Users must be able to review the exact source, selection scope, and trust posture before export. This plan builds that preview layer so workbook generation only happens once the export is review-ready.
Output: A live-export backend service that resolves source freshness, loads the full database catalog, applies the trust model, and returns the preview artifact.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-live-db-to-xlsx-export-v1_2/02-CONTEXT.md
@.planning/phases/02-live-db-to-xlsx-export-v1_2/02-UI-SPEC.md
@.planning/phases/02-live-db-to-xlsx-export-v1_2/02-VALIDATION.md
@server/lib/extensions/db-management/history-service.ts
@server/lib/extensions/db-management/schema-normalizer.ts
@server/lib/ddl-import/issues.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build live-export preview service with explicit freshness and whole-catalog loading</name>
  <read_first>
    - server/lib/extensions/db-management/history-service.ts
    - server/lib/extensions/db-management/schema-normalizer.ts
    - shared/schema.ts
    - .planning/phases/02-live-db-to-xlsx-export-v1_2/02-CONTEXT.md
  </read_first>
  <files>server/lib/extensions/db-management/live-export-service.ts, server/lib/extensions/db-management/history-service.ts, test/server/db-live-export-phase2.test.ts</files>
  <behavior>
    - Test 1: preview supports both `use-recent-snapshot` and `refresh-live-before-export`
    - Test 2: preview loads the full selected database catalog before table filtering
    - Test 3: preview returns the resolved snapshot hash or fresh-live provenance used for export
  </behavior>
  <action>Create `server/lib/extensions/db-management/live-export-service.ts` with a preview function that accepts `connectionId`, `databaseName`, and explicit freshness mode. Reuse `history-service.ts` patterns so `使用最近 snapshot` resolves through existing snapshot state and `导出前刷新 live` triggers the same safe refresh path used by introspection. Return the entire catalog plus selection defaults instead of asking the caller for an entry-time table subset.</action>
  <acceptance_criteria>
    - `server/lib/extensions/db-management/live-export-service.ts` exists
    - `server/lib/extensions/db-management/live-export-service.ts` contains `previewLiveDbWorkbookExport`
    - `server/lib/extensions/db-management/live-export-service.ts` contains `resolvedSnapshotHash`
    - `node --test --import tsx test/server/db-live-export-phase2.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/server/db-live-export-phase2.test.ts</automated>
  </verify>
  <done>Preview can explain exactly which live/snapshot source produced the exportable catalog.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse workbook trust rules for live DB export issue classification</name>
  <read_first>
    - server/lib/ddl-import/issues.ts
    - server/lib/extensions/db-management/schema-normalizer.ts
    - .planning/phases/02-live-db-to-xlsx-export-v1_2/02-CONTEXT.md
  </read_first>
  <files>server/lib/extensions/db-management/live-export-service.ts, server/lib/ddl-import/issues.ts, test/server/db-live-export-phase2.test.ts</files>
  <action>Project live DB catalogs through the same blocker / confirm / info trust model used by workbook export. Explicitly classify generated columns, check constraints, partitioning, and any inexpressible structures as blocking; classify comments, defaults, unique intent, secondary indexes, foreign keys, and metadata loss as confirm or info according to the locked decisions in `02-CONTEXT.md`.</action>
  <acceptance_criteria>
    - `server/lib/extensions/db-management/live-export-service.ts` contains `blocking`
    - `server/lib/extensions/db-management/live-export-service.ts` contains `confirm`
    - `server/lib/extensions/db-management/live-export-service.ts` contains `info`
    - `test/server/db-live-export-phase2.test.ts` contains `generated`
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Phase 2 preview uses the same conservative trust vocabulary as the rest of the workbook pipeline.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/server/db-live-export-phase2.test.ts`
- [ ] `npm run check`
- [ ] freshness mode is explicit in the preview artifact
- [ ] whole-database-first catalog loading is preserved
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Live DB export preview is trustworthy before workbook generation starts
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-db-to-xlsx-export-v1_2/02-02-SUMMARY.md`
</output>

