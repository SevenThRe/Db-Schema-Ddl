---
phase: 02-live-db-to-xlsx-export-v1_2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - shared/routes.ts
  - client/src/hooks/use-db-management.ts
  - test/server/db-live-export-phase2.test.ts
  - test/client/db-live-export-phase2-ui.test.tsx
autonomous: true
requirements:
  - DBXLSX-01
  - DBXLSX-02
  - DBXLSX-03
user_setup: []
must_haves:
  truths:
    - "Live DB to XLSX starts from one stable preview/export artifact rather than UI-only state."
    - "Freshness mode, selected tables, template choice, issue summary, and output file metadata are all machine-usable."
    - "The dedicated DB export workflow reuses existing trust vocabulary instead of inventing a parallel severity model."
  artifacts:
    - path: "shared/schema.ts"
      provides: "typed preview/export artifacts, issue summaries, and workspace request/response schemas"
      contains: "dbLiveExport"
    - path: "shared/routes.ts"
      provides: "typed db-management endpoints for preview and export"
      contains: "dbManagement"
    - path: "client/src/hooks/use-db-management.ts"
      provides: "React Query hooks for previewing and executing live DB workbook export"
      contains: "useDb"
  key_links:
    - from: "shared/routes.ts"
      to: "shared/schema.ts"
      via: "preview and export request/response contracts"
      pattern: "export|preview|template|snapshot"
---

<objective>
Define the shared contract for live DB workbook export before backend and UI implementation diverge.

Purpose: Phase 2 needs one stable artifact that can drive review UI, export execution, file registration, and later MCP automation. This plan creates the typed contract and wave-0 tests first so downstream plans build on one language.
Output: Shared schemas, route seams, hook typing, and focused phase tests for live DB workbook export.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-live-db-to-xlsx-export-v1_2/02-CONTEXT.md
@.planning/phases/02-live-db-to-xlsx-export-v1_2/02-UI-SPEC.md
@.planning/phases/02-live-db-to-xlsx-export-v1_2/02-VALIDATION.md
@shared/schema.ts
@shared/routes.ts
@client/src/hooks/use-db-management.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add live-export preview and export artifacts to shared schema</name>
  <read_first>
    - shared/schema.ts
    - shared/routes.ts
    - .planning/phases/02-live-db-to-xlsx-export-v1_2/02-CONTEXT.md
    - .planning/phases/02-live-db-to-xlsx-export-v1_2/02-UI-SPEC.md
  </read_first>
  <files>shared/schema.ts, test/server/db-live-export-phase2.test.ts, test/client/db-live-export-phase2-ui.test.tsx</files>
  <behavior>
    - Test 1: preview artifact captures connection, database, freshness mode, resolved snapshot hash, selected tables, template id, issue summary, and safe export readiness
    - Test 2: export result carries output file metadata plus the artifact identity used to create it
    - Test 3: artifact keys are stable enough for future MCP / automation references
  </behavior>
  <action>Add `dbLiveExportPreviewRequest`, `dbLiveExportPreviewResponse`, `dbLiveExportExecuteRequest`, `dbLiveExportExecuteResponse`, and supporting issue/selection/template schemas to `shared/schema.ts`. The preview artifact must include concrete fields for `connectionId`, `databaseName`, `freshnessMode`, `resolvedSnapshotHash`, `catalog`, `selectedTableNames`, `templateId`, `issueSummary`, `issues`, and `canExport`. Create focused phase tests that assert these exact fields exist and remain machine-friendly.</action>
  <acceptance_criteria>
    - `shared/schema.ts` contains `dbLiveExportPreviewRequestSchema`
    - `shared/schema.ts` contains `dbLiveExportPreviewResponseSchema`
    - `shared/schema.ts` contains `dbLiveExportExecuteRequestSchema`
    - `shared/schema.ts` contains `dbLiveExportExecuteResponseSchema`
    - `node --test --import tsx test/server/db-live-export-phase2.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/server/db-live-export-phase2.test.ts</automated>
  </verify>
  <done>Live DB export has one stable typed artifact for UI, backend, and automation.</done>
</task>

<task type="auto">
  <name>Task 2: Expose typed route and hook seams for preview/export</name>
  <read_first>
    - shared/routes.ts
    - client/src/hooks/use-db-management.ts
    - shared/schema.ts
  </read_first>
  <files>shared/routes.ts, client/src/hooks/use-db-management.ts</files>
  <action>Add typed DB-management routes for previewing live DB workbook export and executing export into the file list. Extend `client/src/hooks/use-db-management.ts` with React Query hooks named for preview and export so the later workspace can call them without manual fetch payloads.</action>
  <acceptance_criteria>
    - `shared/routes.ts` contains `dbManagementPreviewLiveExport`
    - `shared/routes.ts` contains `dbManagementExecuteLiveExport`
    - `client/src/hooks/use-db-management.ts` contains `usePreviewLiveDbWorkbookExport`
    - `client/src/hooks/use-db-management.ts` contains `useExecuteLiveDbWorkbookExport`
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Backend and UI can implement the export workflow on typed seams instead of manual request shapes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/server/db-live-export-phase2.test.ts`
- [ ] `npm run check`
- [ ] preview/export artifacts remain schema-backed and MCP-friendly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 starts from one stable contract instead of separate backend/UI shapes
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-db-to-xlsx-export-v1_2/02-01-SUMMARY.md`
</output>

