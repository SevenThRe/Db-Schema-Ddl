---
phase: 02-live-db-to-xlsx-export-v1_2
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - server/lib/extensions/db-management/live-export-service.ts
  - server/lib/ddl-import/export-service.ts
  - server/lib/workbook-templates.ts
  - server/routes/db-management-routes.ts
  - server/routes/files-routes.ts
  - test/server/db-live-export-phase2.test.ts
autonomous: true
requirements:
  - DBXLSX-01
  - DBXLSX-02
  - DBXLSX-03
user_setup: []
must_haves:
  truths:
    - "Exports only target the two official workbook template families."
    - "Selected-table export is allowed, but it starts from a full catalog preview."
    - "Parser-backed round-trip validation remains the hard trust gate before file-list registration."
  artifacts:
    - path: "server/lib/ddl-import/export-service.ts"
      provides: "canonical catalog to workbook export pipeline"
      contains: "export"
    - path: "server/lib/workbook-templates.ts"
      provides: "official template metadata and trust helpers"
      contains: "template"
    - path: "server/routes/db-management-routes.ts"
      provides: "preview/export HTTP routes for the DB management workspace"
      contains: "db"
  key_links:
    - from: "server/lib/extensions/db-management/live-export-service.ts"
      to: "server/lib/ddl-import/export-service.ts"
      via: "catalog-to-workbook export handoff"
      pattern: "catalog|template|round-trip"
---

<objective>
Generate trusted workbooks from the reviewed live catalog and register them into the normal file list.

Purpose: Preview alone does not satisfy Phase 2. This plan turns the selected live catalog into a real `.xlsx`, validates it by reopening through the parser, and registers it through the same file lifecycle as uploads and template creation.
Output: Export execution service, db-management export routes, round-trip validation, and file-list registration.
</objective>

<execution_context>
@C:/Users/ISI202502/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ISI202502/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-live-db-to-xlsx-export-v1_2/02-CONTEXT.md
@.planning/phases/02-live-db-to-xlsx-export-v1_2/02-UI-SPEC.md
@.planning/phases/02-live-db-to-xlsx-export-v1_2/02-VALIDATION.md
@server/lib/extensions/db-management/live-export-service.ts
@server/lib/ddl-import/export-service.ts
@server/lib/workbook-templates.ts
@server/routes/files-routes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert reviewed live catalogs into official workbook templates</name>
  <read_first>
    - server/lib/ddl-import/export-service.ts
    - server/lib/workbook-templates.ts
    - server/lib/extensions/db-management/live-export-service.ts
    - .planning/phases/02-live-db-to-xlsx-export-v1_2/02-CONTEXT.md
  </read_first>
  <files>server/lib/extensions/db-management/live-export-service.ts, server/lib/ddl-import/export-service.ts, test/server/db-live-export-phase2.test.ts</files>
  <behavior>
    - Test 1: export can target either official workbook template family
    - Test 2: selected-table export includes only requested tables from the reviewed catalog
    - Test 3: blocking issues or round-trip failure prevent success responses
  </behavior>
  <action>Extend the live-export service so it passes the reviewed catalog into the existing workbook export pipeline. Reuse the exact official template ids already shipped by Phase 2 of `v1.1`, support both whole-catalog and selected-table export, remember the chosen template, and refuse to continue when blockers remain or parser-backed round-trip fails.</action>
  <acceptance_criteria>
    - `server/lib/extensions/db-management/live-export-service.ts` contains `executeLiveDbWorkbookExport`
    - `server/lib/extensions/db-management/live-export-service.ts` contains `selectedTableNames`
    - `server/lib/extensions/db-management/live-export-service.ts` contains `templateId`
    - `node --test --import tsx test/server/db-live-export-phase2.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test --import tsx test/server/db-live-export-phase2.test.ts</automated>
  </verify>
  <done>Reviewed live catalogs can become trusted workbooks through the same official-template path as existing exports.</done>
</task>

<task type="auto">
  <name>Task 2: Register successful workbooks through the normal file lifecycle and route layer</name>
  <read_first>
    - server/routes/db-management-routes.ts
    - server/routes/files-routes.ts
    - shared/routes.ts
    - server/lib/workbook-templates.ts
  </read_first>
  <files>server/routes/db-management-routes.ts, server/routes/files-routes.ts, server/lib/workbook-templates.ts</files>
  <action>Add DB-management preview and execute endpoints that return the stable export artifact and successful file metadata. Register successful workbooks through the same file inventory path used by uploads and template creation so the generated workbook appears immediately in the normal file list without a manual import loop.</action>
  <acceptance_criteria>
    - `server/routes/db-management-routes.ts` contains `previewLiveDbWorkbookExport`
    - `server/routes/db-management-routes.ts` contains `executeLiveDbWorkbookExport`
    - `server/routes/db-management-routes.ts` contains `file`
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Successful exports enter the standard file-driven workflow immediately after passing trust validation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test --import tsx test/server/db-live-export-phase2.test.ts`
- [ ] `npm run check`
- [ ] export only targets official template families
- [ ] parser-backed validation blocks registration on mismatch
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Live DB catalogs export into trusted workbooks that immediately join the normal file lifecycle
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-db-to-xlsx-export-v1_2/02-03-SUMMARY.md`
</output>

