# Tasks: Data Sync Apply Execution

- [ ] Add the 010 data sync apply execution spec, plan, and tasks.
- [ ] Implement guarded backend execution for selected insert/update/delete apply actions.
- [ ] Persist apply job results and expose them through existing job detail loading.
- [ ] Enable real execute behavior in the unified Sync tab UI.
- [ ] Re-run TypeScript and Rust static checks.
