# Tasks: Schema Diff In Unified Workbench

- [x] Add the 010 schema diff in workbench spec, plan, and tasks.
- [x] Extract a shared schema diff viewer from the legacy outer diff implementation.
- [x] Add schema diff target selection and compare flow to `WorkbenchLayout`.
- [x] Keep the legacy outer diff route rendering through the shared viewer.
- [x] Re-run TypeScript and Rust static checks.
