# Tasks: Real Data Sync Apply

- [x] Add the 010 real data sync apply spec, plan, and tasks.
- [x] Implement real SQL preview generation and executability evaluation in `data_apply.rs`.
- [x] Implement transactional apply execution and persisted apply job/result records.
- [x] Remove the hard-coded preview-only gate in `WorkbenchLayout` and wire the real execute flow.
- [x] Re-run TypeScript and Rust static checks.
