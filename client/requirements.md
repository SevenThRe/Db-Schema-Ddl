## Packages
framer-motion | Smooth animations for layout changes and panel transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes

## Notes
The app interacts with a Python/Node backend to parse Excel files.
We need to handle file uploads via FormData.
The layout should be a responsive dashboard with a sidebar for file navigation.
